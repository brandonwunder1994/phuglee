# Copy to .env and fill locally (never commit real secrets)

# --- Phuglee auth (required in production) ---
# Explicit HMAC secret for session cookies. Prefer this over deriving from bootstrap.
PHUGLEE_SESSION_SECRET=
# Admin / brad bootstrap password (used when users.json has no password yet)
PHUGLEE_BOOTSTRAP_ADMIN_PASSWORD=
# Local only: skip login. Entrypoint forces auth ON in production unless PHUGLEE_AUTH_OPEN=1.
# PHUGLEE_AUTH_DISABLED=1
# Temporary open staging (production): PHUGLEE_AUTH_OPEN=1
# Registration: open | invite | closed (production default: invite)
PHUGLEE_REGISTRATION_MODE=invite
PHUGLEE_INVITE_CODE=
# Shared analyzer service token (shell proxy injects; do not expose to browser)
PDA_AUTH_TOKEN=
# Direct :3456 debug only — injects token into analyzer HTML (keep off in prod)
# PDA_EXPOSE_TOKEN=1

# GoHighLevel Private Integration (Wunder Real Estate / DTS)
GHL_API_KEY=
GHL_LOCATION_ID=
GHL_API_BASE=https://services.leadconnectorhq.com
GHL_API_VERSION=2021-07-28

# SignNow (AOC / JV / Amendment sends from Contract Tracker)
SIGNNOW_ACCESS_TOKEN=
SIGNNOW_API_BASE=https://api.signnow.com
SIGNNOW_FROM_EMAIL=brandon@wunderhausgroup.com

# Giphy (Internal team chat GIF picker — Discord-style, rating=r)
# Free key: https://developers.giphy.com/dashboard/  (Create App → API Key)
# Without this key, GIF search is disabled; paste media.giphy.com / tenor URLs still works.
GIPHY_API_KEY=

# RealEstateAPI (Vault Comping — server-only backend/secret key)
# Set in Railway Variables on service `phuglee`. Never commit or put in frontend.
REALESTATE_API_KEY=
REALESTATE_API_BASE=https://api.realestateapi.com

# Railway (Operating Costs)
# Prefer an Account token (railway.com/account/tokens → "No workspace") for live invoice $.
# Project tokens also work: they estimate $ from CPU/RAM/egress/disk usage.
RAILWAY_API_TOKEN=
# Required for Workspace-scoped Bearer tokens (optional for Account tokens)
RAILWAY_WORKSPACE_ID=
