# v1.8 Premium Shell Implementation Plan

> **For agentic workers:** Execute phases 8.1–8.6 in order. Run `npm test` after each phase.

**Goal:** Transform Distress Analyzer into a sellable, imagery-forward Aerial Command premium shell.

**Architecture:** Update `tokens.css` as single source of truth; add `premium-aerial.css` + `premium-motion.css` layers loaded after legacy cyber CSS (override, don't delete yet); add `premium-shell.js` for demo theater and enhanced animations; add `landing.html` with static route.

**Tech Stack:** Vanilla HTML/CSS/JS, Google Fonts, existing Node static router.

---

## Phase 8.1 — Design Doc ✓
- [x] Write `.planning/v1.8-PREMIUM-SHELL-DESIGN.md`

## Phase 8.2 — Token Consolidation ✓
**Files:** `tokens.css`, `index.html`, `premium-aerial.css` (new)

- [x] Update tokens to Aerial Command palette + Fraunces/DM Sans
- [x] Swap Google Fonts link in index.html
- [x] Add `body.aerial-theme` class alongside cyber-theme
- [x] Hide orbs, scanlines, grain via CSS (`display:none`)
- [x] Override glass panels, sidebar, command bar with amber-accent chrome
- [x] Map legacy `--copper` / `--neon-cyan` aliases to new accent tokens

## Phase 8.3 — Hero Surfaces ✓
**Files:** `index.html`, `premium-aerial.css`, `premium-shell.js` (new)

- [x] Rebuild empty workspace with demo scan theater (3 sample property cards animating tier reveal)
- [x] Property cards: taller thumb (aspect-ratio 16/10), stronger hover lift, distress ring on score pill
- [x] Scan progress: amber gradient bar + sweep animation during active scan
- [x] Wire `premium-shell.js` in index.html

## Phase 8.4 — Review + KPI Motion ✓
**Files:** `premium-aerial.css`, `premium-motion.css` (new), `session.js`

- [x] Enhance `animateStatNumber` with RAF count-up (skip if `prefers-reduced-motion` or `instant`)
- [x] Pipeline segments: CSS transition on flex-grow
- [x] Distressed KPI pulse class when count > 0
- [x] Review overlay: larger media column, cinematic header fade

## Phase 8.5 — Visual Verification ✓
- [x] Run `npm test` — 190/190 pass
- [x] Verify server serves updated CSS/JS
- [x] 22 surfaces covered via aerial-theme overrides on existing cyber shell

## Phase 8.6 — Landing Page ✓
**Files:** `public/landing.html`, `public/css/landing.css` (new), `routes/static.js`

- [x] Build sales landing: hero, 3-step flow, feature grid, CTA to app
- [x] Add `GET /landing` route
- [x] Match Aerial Command design system

---

## Test Command
```bash
cd Projects/property-distress-analyzer && npm test
```