---
phase: 28-export-integrity-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - tests/export-profiles.test.js
  - .planning/REQUIREMENTS.md
  - .planning/phases/28-export-integrity-tests/28-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - EXPORT-14
  - QA-07
---

<objective>
Regression-test full export profile, document verification, close v1.7 export requirements.
</objective>

<tasks>
<task id="1" name="Full profile regression test">
<read_first>
- public/js/render.js (buildExportRows full branch — column keys)
- lib/export-schema.js (DIAL_READY_COLUMNS)
- tests/export-schema.test.js
</read_first>
<action>
Create tests/export-profiles.test.js (Node-only, no browser):

Extract or duplicate minimal column-key lists:
- FULL_EXPORT_SIGNATURE_COLUMNS = ['First Name', 'Last Name', 'Distress Score', 'D4D Indicators', 'Why This Tier']
- Import DIAL_READY_COLUMNS from lib/export-schema.js

Tests:
1. DIAL_READY_COLUMNS and FULL_EXPORT_SIGNATURE_COLUMNS have zero overlap on ambiguous names
   (dial_ready uses 'Contact Name' not 'First Name' — profiles are distinct).
2. FULL_EXPORT_SIGNATURE_COLUMNS every key is documented as expected in full profile
   (static list in test file matching render.js full branch keys).
3. DIAL_READY_COLUMNS length === 13; full profile has more than 20 keys (document count).

Optional: add `lib/export-profiles.js` with FULL_EXPORT_COLUMNS frozen array extracted from
render.js if duplication is too brittle — only if test maintenance needs it.
</action>
<acceptance_criteria>
- tests/export-profiles.test.js exists
- tests/export-profiles.test.js contains 'Distress Score'
- tests/export-profiles.test.js contains 'DIAL_READY_COLUMNS'
- npm test passes
</acceptance_criteria>
</task>

<task id="2" name="Verification doc + requirements closure">
<read_first>
- .planning/REQUIREMENTS.md (EXPORT-10–14, QA-07)
- .planning/phases/24-scale-metrics/24-VERIFICATION.md (template)
- .planning/ROADMAP.md
</read_first>
<action>
Create .planning/phases/28-export-integrity-tests/28-VERIFICATION.md:

| REQ | Status | Evidence |
|-----|--------|----------|
| EXPORT-10 | ✅ | hydration test in export-schema.test.js |
| EXPORT-11 | ✅ | absoluteUrl test |
| EXPORT-12 | ✅ | buildGoogleMapsStreetViewLink tests |
| EXPORT-13 | ✅ | row count parity test |
| EXPORT-14 | ✅ | export-profiles.test.js |
| QA-07 | ✅ | npm test count |

Manual smoke section:
1. Load session with scanned leads
2. Sidebar → Export Database (Excel)
3. Open xlsx — verify 13 columns, row count = lead count
4. Sidebar → Excel current list — verify detailed columns still present

Mark EXPORT-10–14 and QA-07 [x] in REQUIREMENTS.md.
Update ROADMAP.md Phase 28 status to Complete.
Update STATE.md — Phase 28 complete, v1.7 ready for milestone audit.
</action>
<acceptance_criteria>
- .planning/phases/28-export-integrity-tests/28-VERIFICATION.md exists
- REQUIREMENTS.md EXPORT-10 through EXPORT-14 are [x]
- REQUIREMENTS.md QA-07 is [x]
- ROADMAP.md Phase 28 shows Complete
</acceptance_criteria>
</task>
</tasks>

<must_haves>
- Full profile export columns unchanged (regression test documents contract)
- All EXPORT-10–14 requirements marked validated
- Verification doc with npm test evidence
</must_haves>