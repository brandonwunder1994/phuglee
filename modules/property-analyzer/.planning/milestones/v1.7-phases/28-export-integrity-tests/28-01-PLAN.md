---
phase: 28-export-integrity-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/export-schema.test.js
  - lib/export-schema.js
autonomous: true
requirements:
  - EXPORT-10
  - EXPORT-11
  - EXPORT-12
  - EXPORT-13
---

<objective>
Prove dial-ready export data integrity: hydration populates Cache Date, row count matches input, URL columns correct under realistic deps.
</objective>

<tasks>
<task id="1" name="Hydration + row-count tests">
<read_first>
- lib/export-schema.js (buildDialReadyRow, buildDialReadyRows)
- tests/export-schema.test.js
- public/js/review.js (resolveImageryForResult, mergeImageryIntoResult patterns)
</read_first>
<action>
Extend tests/export-schema.test.js:

1. **Index hydration simulation** — record with no inline imagery; deps.resolveImageryForResult merges
   `{ streetView: { cachedAt: 1719792000000, status: 'ok', url: '/api/cached-imagery/streetview/x.jpg' } }`
   from index; assert Cache Date === '2024-07-01' and Street View Image URL is absolute.

2. **Satellite cachedAt fallback** — record with only `imagery.satellite.cachedAt` (no streetView);
   assert Cache Date still populated.

3. **Row count parity** — buildDialReadyRows on array of 5 records (mixed tiers); assert
   `rows.length === 5` and every row has exactly DIAL_READY_COLUMNS keys in order.

4. **No-score-sort** — pass records in known order [A,B,C]; assert output row Street Addresses
   match input order (dial_ready must not re-sort like full profile).

5. **Empty imagery** — record with no imagery/viewMeta; assert Cache Date and Street View Image URL
   are '' but Google Maps link falls back to address search when street/city present.
</action>
<acceptance_criteria>
- tests/export-schema.test.js contains 'row count parity' or 'buildDialReadyRows on array of 5'
- tests/export-schema.test.js contains 'Satellite cachedAt fallback'
- tests/export-schema.test.js contains 'Index hydration' or 'resolveImageryForResult merges'
- npm test passes with at least 12 tests in export-schema describe block
</acceptance_criteria>
</task>

<task id="2" name="URL edge-case hardening">
<read_first>
- lib/export-schema.js (absoluteUrl, buildGoogleMapsStreetViewLink)
- .planning/research/PITFALLS.md (relative URL pitfall)
</read_first>
<action>
Add tests for:
- absoluteUrl with empty origin leaves relative path unchanged (document limitation)
- buildGoogleMapsStreetViewLink with targetLat/targetLng when panoId absent
- buildGoogleMapsStreetViewLink returns '' when record has no address fields

If buildDialReadyRow should prefer streetView cachedAt over satellite, add one-line comment in
export-schema.js (no behavior change unless test reveals bug).
</action>
<acceptance_criteria>
- tests/export-schema.test.js contains test for targetLat/targetLng Maps link
- tests/export-schema.test.js contains test for empty address Maps link returns ''
- npm test passes
</acceptance_criteria>
</task>
</tasks>

<must_haves>
- EXPORT-10: Cache Date populated after simulated hydration
- EXPORT-11: Absolute URL test covers cached imagery path
- EXPORT-12: Maps link test covers panoId, lat/lng, address fallback
- EXPORT-13: Row count === input length, no reorder
</must_haves>