---
phase: 13-workflow-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - public/css/app.css
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - FLOW-05
  - QA-01
  - QA-04
must_haves:
  truths:
    - "Empty workspace uses flat --card surface without dashed copper border"
    - "Scan progress panel is slim (~120px) with 6px accent progress bar, no HUD animations by default"
    - "Summary hero row is 3-column grid at ≥1024px; breakdown hidden via CSS class"
    - "Tier alert stack repositioned bottom-right as slim single toast"
    - "Log panel max-height 200px when expanded"
  artifacts:
    - path: public/css/app.css
      provides: Calm empty workspace styles
      contains: "empty-workspace"
    - path: public/css/app.css
      provides: Slim scan progress styles
      contains: "scan-progress-slim"
    - path: public/css/app.css
      provides: Summary hero grid + toast reposition
      contains: "summary-hero-row"
  key_links:
    - from: public/css/app.css
      to: public/css/tokens.css
      via: var(--card), var(--accent), var(--border)
      pattern: "var(--card)"
---

<objective>
Restyle workflow surfaces in `app.css`: calm empty state, slim scan bar, hero KPI grid, toast repositioning, log panel expand styles.

Purpose: Make Phase 13 visible — replace HUD workflow chrome with calm surfaces (FLOW-01–05).
Output: Updated `public/css/app.css` workflow sections.
</objective>

<execution_context>
@C:\Users\brand\.codex\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workflow-surfaces/13-UI-SPEC.md
@.planning/phases/13-workflow-surfaces/13-RESEARCH.md
@public/css/tokens.css
@public/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: FLOW-01 — Calm empty workspace styles</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 5260-5286, 6419-6424)
    - .planning/phases/13-workflow-surfaces/13-UI-SPEC.md (FLOW-01, Color section)
  </read_first>
  <action>
**Update `.empty-workspace` rules (both occurrences ~5260 and ~6419):**

```css
.empty-workspace {
  display: block;
  text-align: center;
  padding: var(--space-2xl) var(--space-lg);
  margin-bottom: var(--space-md);
  border-radius: var(--radius);
  background: var(--card);
  border: 1px solid var(--border);
}
.empty-workspace h2 {
  font-family: var(--font-display);
  font-size: 28px;
  font-weight: 600;
  line-height: 1.2;
  margin-bottom: var(--space-sm);
}
.empty-workspace p {
  color: var(--muted-foreground);
  max-width: 420px;
  margin: 0 auto var(--space-md);
  font-size: 14px;
}
.empty-workspace kbd {
  font-family: var(--font-mono);
  font-size: 12px;
  padding: 2px 6px;
  border: 1px solid var(--border);
  border-radius: 4px;
  background: var(--secondary);
}
.empty-workspace-hint {
  font-size: 12px;
  color: var(--muted-foreground);
  margin-top: var(--space-sm);
}
.empty-workspace-actions {
  display: flex;
  gap: var(--space-sm);
  justify-content: center;
  flex-wrap: wrap;
}
```

- Remove `border: 1px dashed rgba(201, 169, 98, 0.25)` everywhere
- Remove any `.empty-workspace.glass` backdrop styles if present
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'empty-workspace-hint' -Quiet; !(Select-String -Path public/css/app.css -Pattern 'dashed rgba\(201, 169, 98' -Quiet)</automated>
  </verify>
  <acceptance_criteria>
    - app.css `.empty-workspace` uses `border: 1px solid var(--border)` (not dashed copper)
    - app.css `.empty-workspace` uses `background: var(--card)`
    - app.css contains `.empty-workspace-hint` rule with `font-size: 12px`
    - app.css `.empty-workspace h2` font-size is `28px`
    - app.css does NOT contain `dashed rgba(201, 169, 98` in empty-workspace rules
  </acceptance_criteria>
  <done>Empty workspace renders as calm flat card</done>
</task>

<task type="auto">
  <name>Task 2: FLOW-02 + FLOW-05 — Slim scan progress + log panel styles</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 3816-4047)
    - .planning/phases/13-workflow-surfaces/13-UI-SPEC.md (FLOW-02, FLOW-05, Motion)
  </read_first>
  <action>
**Add new `.scan-progress-slim` block (after existing command-hud-scan section or override):**

```css
.scan-progress-slim {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--space-md);
  margin-bottom: var(--space-md);
}
.scan-progress-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-sm);
}
.scan-progress-header h2 {
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 600;
  margin: 0;
}
.scan-progress-slim .progress-pct {
  font-family: var(--font-mono);
  font-size: 14px;
  color: var(--muted-foreground);
}
.scan-progress-bar-row .progress-bar-wrap {
  height: 6px;
  background: var(--secondary);
  border-radius: 3px;
  overflow: hidden;
}
.scan-progress-slim .progress-bar {
  height: 100%;
  background: var(--accent);
  transition: width 300ms ease;
  border-radius: 3px;
}
.scan-progress-metrics {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: var(--space-sm);
  font-size: 14px;
  color: var(--muted-foreground);
  margin-top: var(--space-sm);
}
.scan-progress-metrics strong {
  font-family: var(--font-mono);
  color: var(--foreground);
}
.scan-metric-sep { opacity: 0.5; }
.scan-log-toggle {
  margin-top: var(--space-sm);
  min-height: 44px;
  font-size: 14px;
  color: var(--muted-foreground);
  background: transparent;
  border: none;
  cursor: pointer;
  text-decoration: underline;
  text-underline-offset: 3px;
}
.scan-progress-slim .log-panel:not([hidden]) {
  display: block;
  max-height: 200px;
  overflow-y: auto;
  margin-top: var(--space-sm);
  padding: var(--space-xs) var(--space-sm);
  background: var(--secondary);
  font-family: var(--font-mono);
  font-size: 12px;
  border-radius: var(--radius-sm);
}
```

**Disable HUD scan theater by default** — wrap existing animations under `body.legacy-hud` only, OR add overrides:
```css
.scan-progress-slim .hud-scan-live-tag,
.scan-progress-slim .hud-scan-pct-ring,
.scan-progress-slim .hud-scan-bar-frame::after { display: none !important; }
```

**Reduced motion:**
```css
@media (prefers-reduced-motion: reduce) {
  .scan-progress-slim .progress-bar { transition: none; }
}
```
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'scan-progress-slim' -Quiet; Select-String -Path public/css/app.css -Pattern 'scan-log-toggle' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.scan-progress-slim` with `background: var(--card)`
    - app.css `.scan-progress-slim .progress-bar` uses `background: var(--accent)`
    - app.css progress bar height rule sets `height: 6px` on bar wrap
    - app.css contains `.scan-log-toggle` with `min-height: 44px`
    - app.css log panel open state sets `max-height: 200px`
    - app.css contains `prefers-reduced-motion` rule disabling progress bar transition
  </acceptance_criteria>
  <done>Slim scan bar and log panel styles applied</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-03 + FLOW-04 — Summary hero grid + toast reposition</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 138-268, 3725-3800)
    - .planning/phases/13-workflow-surfaces/13-UI-SPEC.md (FLOW-03, FLOW-04)
  </read_first>
  <action>
**Summary hero/breakdown styles:**

```css
.summary-section {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--space-lg);
}
.summary-hero-row {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-md);
  margin-bottom: var(--space-md);
}
@media (min-width: 1024px) {
  .summary-hero-row { grid-template-columns: repeat(3, 1fr); }
}
.summary-hero-kpi .summary-val {
  font-family: var(--font-display);
  font-size: 28px;
  font-weight: 600;
}
.summary-breakdown-toggle {
  display: flex;
  align-items: center;
  gap: var(--space-xs);
  min-height: 44px;
  width: 100%;
  padding: var(--space-sm) var(--space-md);
  background: var(--muted);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  margin-bottom: var(--space-sm);
}
#summaryBreakdown[hidden] { display: none !important; }
#summaryBreakdown:not([hidden]) {
  animation: breakdown-expand 150ms ease;
}
@keyframes breakdown-expand {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

**Flatten KPI card hover glow** — remove or scope `box-shadow` glow on `.summary-kpi-card.clickable:hover` to `body.legacy-hud` only.

**Tier toast reposition (`.live-tier-alert-stack` ~line 138):**
```css
.live-tier-alert-stack {
  position: fixed;
  bottom: 5rem;
  right: 1.5rem;
  top: auto;
  z-index: 10050;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 0;
  max-width: min(360px, 48vw);
  pointer-events: none;
}
.live-tier-alert-card {
  padding: 0.5rem 0.85rem;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  background: var(--card);
  box-shadow: var(--shadow-sm);
  animation: toast-fade-in 150ms ease both;
  max-height: none;
}
.live-tier-alert-card.fade-out {
  animation: toast-fade-out 200ms ease forwards;
}
@keyframes toast-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes toast-fade-out {
  from { opacity: 1; }
  to { opacity: 0; }
}
/* Hide theater elements in toast */
.live-tier-alert-head,
.live-tier-alert-dot,
.live-tier-alert-label { display: none; }
.live-tier-alert-addr {
  font-size: 14px;
  color: var(--foreground);
  padding-left: 0;
}
```
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'summary-hero-row' -Quiet; Select-String -Path public/css/app.css -Pattern 'bottom: 5rem' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.summary-hero-row` with `grid-template-columns: repeat(3, 1fr)` inside min-width 1024px media query
    - app.css contains `.summary-breakdown-toggle` with `min-height: 44px`
    - app.css `.live-tier-alert-stack` uses `bottom: 5rem` and `top: auto`
    - app.css `.live-tier-alert-stack` has `pointer-events: none`
    - app.css contains `toast-fade-in` keyframes at 150ms
    - app.css `.live-tier-alert-head` set to `display: none`
  </acceptance_criteria>
  <done>Summary hero grid and bottom-right toast styles applied</done>
</task>

</tasks>

<verification>
```powershell
Select-String -Path public/css/app.css -Pattern 'scan-progress-slim|summary-hero-row|bottom: 5rem'
```
All three patterns must match.
</verification>

<success_criteria>
- [ ] Empty workspace: flat card, no dashed border
- [ ] Scan: 6px accent bar, log toggle styles, HUD animations scoped to legacy-hud
- [ ] Summary: 3-column hero grid, breakdown toggle styles
- [ ] Toasts: bottom-right, slim pill, fade animations
</success_criteria>

## PLANNING COMPLETE