---
phase: 13-workflow-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-05
  - QA-04
must_haves:
  truths:
    - "Empty workspace shows 2 visible buttons with calm copy and ⌘K hint"
    - "Scan section uses slim 3-row layout with hidden log panel and scan log toggle"
    - "Summary shows 3 hero KPIs with collapsible breakdown containing secondary cards and pipeline"
    - "All preserved DOM IDs from 13-UI-SPEC still present in index.html"
  artifacts:
    - path: public/index.html
      provides: Calm empty state DOM
      contains: "Upload spreadsheet"
    - path: public/index.html
      provides: Slim scan structure
      contains: "scanLogToggle"
    - path: public/index.html
      provides: Summary hero/breakdown split
      contains: "summaryBreakdownToggle"
  key_links:
    - from: public/index.html
      to: public/js/config.js
      via: preserved element IDs
      pattern: "emptyUploadBtn"
    - from: public/index.html
      to: public/js/session.js
      via: "#summaryBreakdown"
      pattern: "sumDistressedKpiCard"
---

<objective>
Restructure workflow surface DOM in `index.html`: calm empty state (2 CTAs), slim scan progress layout, summary hero/breakdown split, hidden log panel with toggle.

Purpose: Establish correct DOM topology for Phase 13 workflow surfaces (FLOW-01, FLOW-02, FLOW-03, FLOW-05) before CSS/JS wiring.
Output: Updated `public/index.html` with new toggle controls and reorganized KPI layout.
</objective>

<execution_context>
@C:\Users\brand\.codex\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/13-workflow-surfaces/13-UI-SPEC.md
@.planning/phases/13-workflow-surfaces/13-RESEARCH.md
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: FLOW-01 — Calm empty workspace (2 visible CTAs)</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 140-151)
    - .planning/phases/13-workflow-surfaces/13-UI-SPEC.md (FLOW-01 section)
  </read_first>
  <action>
**Section `#emptyWorkspace` (lines 140-151):**

1. Remove `glass` class from section; keep `empty-workspace` class only.

2. Replace body copy:
```html
<p>Excel with address columns. Press <kbd>⌘K</kbd> for backup, settings, and more.</p>
```
Remove all `<code>` elements referencing desktop shortcut and localhost.

3. Update primary button:
- `#emptyUploadBtn` label: **Upload spreadsheet** (was "Choose Excel File")
- Keep `class="btn-primary"`

4. Keep `#emptyRestoreBackupBtn` visible with `class="btn-secondary"` (change from `btn-primary` if needed per UI-SPEC).

5. Hide demoted buttons with `hidden` attribute (preserve IDs):
- `#emptySettingsBtn`
- `#emptySaveBackupBtn`
- `#emptyLoadBackupBtn`
- `#emptyResetDataBtn`

6. Add hint paragraph after actions:
```html
<p class="empty-workspace-hint" id="emptyWorkspaceHint" hidden>API keys required before scanning — open Settings to configure.</p>
```
(Use `hidden` default; JS may reveal when keys missing — do not add new key-check logic in this task.)

7. Remove emoji from `#emptySaveBackupBtn` title if element kept in DOM.
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'Upload spreadsheet' -Quiet; Select-String -Path public/index.html -Pattern 'id="emptySettingsBtn"[^>]*hidden' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains button text `Upload spreadsheet` on `#emptyUploadBtn`
    - index.html contains `Restore my last scan` on `#emptyRestoreBackupBtn` without `hidden`
    - index.html `#emptySettingsBtn`, `#emptySaveBackupBtn`, `#emptyLoadBackupBtn`, `#emptyResetDataBtn` each have `hidden` attribute
    - index.html empty body does NOT contain `localhost:3456` or `Property Distress Analyzer` desktop reference
    - index.html contains `class="empty-workspace-hint"`
    - index.html still contains all 6 empty button IDs listed in 13-UI-SPEC DOM preservation block
  </acceptance_criteria>
  <done>Empty state DOM shows 2 visible CTAs with calm copy</done>
</task>

<task type="auto">
  <name>Task 2: FLOW-02 + FLOW-05 — Slim scan progress + log toggle</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 158-191)
    - .planning/phases/13-workflow-surfaces/13-UI-SPEC.md (FLOW-02, FLOW-05 sections)
  </read_first>
  <action>
**Replace `#progressSection` inner structure (lines 158-191) with slim layout:**

```html
<section class="progress-section scan-progress-slim" id="progressSection">
  <div class="scan-progress-header">
    <h2>Scanning</h2>
    <span class="progress-pct" id="progressPct">0%</span>
  </div>
  <div class="scan-progress-bar-row">
    <div class="progress-bar-wrap">
      <div class="progress-bar" id="progressBar"></div>
    </div>
  </div>
  <div class="scan-progress-metrics" aria-live="polite">
    <span>Done <strong id="statDone">0</strong></span>
    <span class="scan-metric-sep" aria-hidden="true">·</span>
    <span>Remaining <strong id="statRemaining">0</strong></span>
    <span class="scan-metric-sep" aria-hidden="true">·</span>
    <span>Workers <strong><span id="statWorkersActive">0</span>/<span id="statWorkersSet">0</span></strong></span>
  </div>
  <span id="statTotal" hidden aria-hidden="true">0</span>
  <span id="statBatch" hidden aria-hidden="true">0</span>
  <span id="statSuccess" hidden aria-hidden="true">0</span>
  <span id="statSkipped" hidden aria-hidden="true">0</span>
  <span id="statAvg" hidden aria-hidden="true">—</span>
  <div class="fail-stats" id="failStats" hidden>
    <div class="fail-stat sv"><span class="num" id="failSvCount">0</span>Failed at Street View</div>
    <div class="fail-stat gem"><span class="num" id="failGemCount">0</span>Failed at Gemini</div>
  </div>
  <button type="button" class="scan-log-toggle" id="scanLogToggle"
    aria-expanded="false" aria-controls="logPanel">Show scan log</button>
  <div class="log-panel" id="logPanel" hidden></div>
</section>
```

- Remove classes: `glass`, `command-hud-scan`, all `hud-scan-*` elements
- Keep `#statWorkersMetric` as hidden shim if referenced: `<span id="statWorkersMetric" hidden></span>` after metrics row
- `#logPanel` must have `hidden` attribute by default
- Remove `command-hud-scan` from section — CSS plan adds calm styles to `.scan-progress-slim`
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'id="scanLogToggle"' -Quiet; Select-String -Path public/index.html -Pattern 'id="logPanel"[^>]*hidden' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html `#progressSection` contains class `scan-progress-slim`
    - index.html does NOT contain `hud-scan-live-tag` or `Active Scan` text
    - index.html contains `id="scanLogToggle"` with text `Show scan log`
    - index.html `#logPanel` has `hidden` attribute
    - index.html preserves IDs: progressBar, progressPct, statDone, statRemaining, statWorkersActive, statWorkersSet, statTotal, failStats, failSvCount, failGemCount
    - index.html scan heading text is `Scanning` (not "Scanning Properties")
  </acceptance_criteria>
  <done>Slim scan DOM with hidden log panel and toggle button</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-03 — Summary hero KPIs + collapsible breakdown</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 207-279)
    - .planning/phases/13-workflow-surfaces/13-UI-SPEC.md (FLOW-03 section)
  </read_first>
  <action>
**Restructure `#summarySection` (lines 207-279):**

1. Remove/hide `#command-dash-tag` Intelligence Brief span.

2. Update intro copy in `#summaryIntro`:
```html
<p class="summary-intro" id="summaryIntro">Lead counts by tier — click a card to filter.</p>
```

3. Add hero row BEFORE pipeline/breakdown — extract and reorder 3 cards:
```html
<div class="summary-hero-row" id="summaryHeroRow">
  <!-- #sumDistressedKpiCard (move from current grid) -->
  <!-- #sumReviewCard (move from current grid) -->
  <div class="summary-kpi-card summary-hero-kpi" id="sumScannedHeroCard">
    <div class="summary-val" id="sumScannedHero">0</div>
    <div class="summary-lbl">Scanned</div>
    <div class="summary-hint">Properties analyzed this session</div>
  </div>
</div>
```

4. Add breakdown toggle + container wrapping secondary content:
```html
<button type="button" class="summary-breakdown-toggle" id="summaryBreakdownToggle"
  aria-expanded="false" aria-controls="summaryBreakdown">
  Full breakdown <span class="sidebar-chevron" aria-hidden="true">▾</span>
</button>
<div id="summaryBreakdown" hidden>
  <div class="summary-pipeline" id="summaryPipeline" ...>...</div>
  <div class="summary-grid summary-kpi-grid" id="summarySecondaryGrid">
    <!-- #sumWellMaintainedCard, #sumVacantCard, #sumBlurredCard -->
  </div>
</div>
```

5. Move `#summaryPipeline` inside `#summaryBreakdown` (hidden by default).

6. Keep all existing card IDs and inner structure (`sumDistressedKpi`, `sumReview`, etc.) — only reorder parent containers.

7. Remove `glass`, `hud-panel`, `command-dashboard` classes from `#summarySection`; keep `summary-section`.
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'id="summaryBreakdownToggle"' -Quiet; Select-String -Path public/index.html -Pattern 'id="sumScannedHeroCard"' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="summaryHeroRow"` with `#sumDistressedKpiCard`, `#sumReviewCard`, `#sumScannedHeroCard` as children
    - index.html contains `id="summaryBreakdownToggle"` with text `Full breakdown`
    - index.html `#summaryBreakdown` has `hidden` attribute
    - index.html `#summaryPipeline` is descendant of `#summaryBreakdown`
    - index.html does NOT contain visible text `Intelligence Brief`
    - index.html `#summaryIntro` contains `Lead counts by tier`
    - index.html preserves all summary KPI IDs from 13-UI-SPEC DOM preservation block
  </acceptance_criteria>
  <done>Summary DOM split into 3 hero KPIs + collapsed breakdown</done>
</task>

</tasks>

<verification>
After all tasks:
```powershell
Select-String -Path public/index.html -Pattern 'emptyUploadBtn|scanLogToggle|summaryBreakdownToggle|sumScannedHeroCard'
```
All four IDs must match.
</verification>

<success_criteria>
- [ ] Empty state: 2 visible buttons, calm copy, 4 hidden admin buttons
- [ ] Scan: slim structure, no HUD live tag, log hidden with toggle
- [ ] Summary: 3 hero KPIs, breakdown collapsed, pipeline inside breakdown
- [ ] All 13-UI-SPEC preserved IDs present in index.html
</success_criteria>

## PLANNING COMPLETE