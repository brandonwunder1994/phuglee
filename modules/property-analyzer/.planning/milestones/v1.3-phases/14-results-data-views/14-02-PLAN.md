---
phase: 14-results-data-views
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - public/css/app.css
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - QA-01
  - QA-04
must_haves:
  truths:
    - "Filter segment fits one row at 1280px with nowrap"
    - "Search input is visually dominant over sort label"
    - "Prop cards use flat calm styling without gradient/glow hover"
    - "Bulk edit bar uses flat muted surface when visible"
    - "Table view uses calm borders matching card tier badges"
  artifacts:
    - path: public/css/app.css
      provides: Segmented filter styles
      contains: "filter-segmented"
    - path: public/css/app.css
      provides: Calm card layout classes
      contains: "card-body-calm"
    - path: public/css/app.css
      provides: Calm bulk bar
      contains: "results-edit-toggle"
  key_links:
    - from: public/css/app.css
      to: public/css/tokens.css
      via: var(--card), var(--border), var(--accent)
      pattern: "var(--card)"
---

<objective>
Restyle results chrome in `app.css`: segmented filters, prominent search, calm cards/table, flat bulk bar.

Purpose: Visible calm data-view transformation (DATA-01–05).
Output: Updated `public/css/app.css` results section.
</objective>

<execution_context>
@C:\Users\brand\.codex\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/14-results-data-views/14-UI-SPEC.md
@.planning/phases/14-results-data-views/14-RESEARCH.md
@public/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: DATA-01 + DATA-02 — Results header and segmented filters</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 4403-4540, 1983-2018)
    - .planning/phases/14-results-data-views/14-UI-SPEC.md
  </read_first>
  <action>
**Add Phase 14 results chrome block (end of file, before reduced-motion):**

```css
/* ── Phase 14: Results data views ── */
.results-header {
  padding: var(--space-md) var(--space-lg);
  border-bottom: 1px solid var(--border);
  background: var(--card);
}
.results-title-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-md);
  margin-bottom: var(--space-sm);
}
.results-title {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 600;
  margin: 0;
}
.results-header-actions {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
}
.results-search-row {
  display: flex;
  align-items: center;
  gap: var(--space-md);
  flex-wrap: nowrap;
}
.results-search-primary {
  flex: 1;
  min-width: 0;
  min-height: 44px;
  padding: var(--space-sm) var(--space-md);
  font-size: 14px;
  font-family: var(--font-body);
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  color: var(--foreground);
}
.results-search-primary:focus {
  outline: none;
  border-color: var(--ring);
  box-shadow: 0 0 0 2px color-mix(in srgb, var(--ring) 25%, transparent);
}
.results-sort-label {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--muted-foreground);
  white-space: nowrap;
  flex-shrink: 0;
  border: none;
  background: transparent;
  padding: 0;
}
.results-filter-bar {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-sm) var(--space-lg);
  border-bottom: 1px solid var(--border);
  background: var(--card);
  flex-wrap: nowrap;
}
.filter-segmented {
  display: inline-flex;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  overflow: hidden;
}
.filter-segment-btn {
  border-radius: 0;
  border: none;
  border-right: 1px solid var(--border);
  padding: var(--space-sm) var(--space-md);
  min-height: 44px;
  font-size: 14px;
  background: var(--card);
  color: var(--muted-foreground);
}
.filter-segment-btn:last-child { border-right: none; }
.filter-segment-btn.active {
  background: var(--secondary);
  color: var(--foreground);
  box-shadow: none;
}
.filter-overflow-toggle {
  min-height: 44px;
  padding: var(--space-sm) var(--space-md);
  font-size: 14px;
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  color: var(--muted-foreground);
  cursor: pointer;
}
.filter-overflow-toggle.active {
  background: var(--secondary);
  color: var(--foreground);
  border-color: var(--accent);
}
.filter-overflow-menu {
  position: absolute;
  margin-top: var(--space-xs);
  padding: var(--space-sm);
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-md);
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
  z-index: 100;
  min-width: 200px;
}
.filter-overflow-menu[hidden] { display: none !important; }
.filter-overflow-group { position: relative; }
.filter-overflow-btn {
  width: 100%;
  text-align: left;
  border-radius: var(--radius-sm);
}
.results-edit-toggle {
  min-height: 44px;
  padding: var(--space-sm) var(--space-md);
  font-size: 14px;
  font-weight: 600;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--card);
  color: var(--foreground);
  cursor: pointer;
}
.results-edit-toggle.active {
  background: var(--accent);
  color: var(--accent-foreground, var(--card));
  border-color: var(--accent);
}
```

**Override legacy filter pill glow** — scope to legacy-hud:
```css
.filter-segment-btn.active.distressed-filter,
.filter-segment-btn.active.review-filter,
.filter-overflow-btn.active {
  box-shadow: none;
}
body.legacy-hud .filter-btn.active.distressed-filter {
  box-shadow: 0 0 14px var(--tier-distressed-glow);
}
```

@media (max-width: 1280px) { allow wrap on filter bar only below 1024px }
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'filter-segmented' -Quiet; Select-String -Path public/css/app.css -Pattern 'results-search-primary' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.filter-segmented` with segment button styles
    - app.css `.results-search-primary` has `min-height: 44px` and `flex: 1`
    - app.css `.results-sort-label` has `background: transparent` and no border
    - app.css `.results-filter-bar` has `flex-wrap: nowrap`
    - app.css `.filter-segment-btn.active` sets `box-shadow: none`
    - app.css contains `.results-edit-toggle.active` using `var(--accent)`
  </acceptance_criteria>
  <done>Segmented filter and search prominence styles applied</done>
</task>

<task type="auto">
  <name>Task 2: DATA-03 — Calm lead card layout styles</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 4557-4700)
    - .planning/phases/14-results-data-views/14-UI-SPEC.md (DATA-03)
  </read_first>
  <action>
**Add calm card classes:**

```css
.prop-card.card-calm {
  background: var(--card);
  border: 1px solid var(--border);
  box-shadow: none;
  display: flex;
  flex-direction: column;
}
.prop-card.card-calm .card-body-calm {
  padding: var(--space-md);
  order: 1;
}
.prop-card.card-calm .card-address {
  font-size: 14px;
  font-weight: 600;
  color: var(--foreground);
  line-height: 1.4;
}
.prop-card.card-calm .card-badges-calm {
  margin-top: var(--space-xs);
  display: flex;
  gap: var(--space-xs);
}
.prop-card.card-calm .card-thumb {
  order: 2;
  aspect-ratio: 16/9;
}
.prop-card.card-calm .card-meta-calm {
  order: 3;
  padding: var(--space-sm) var(--space-md) var(--space-md);
  border-top: 1px solid var(--border);
}
.prop-card.card-calm .card-name {
  font-size: 12px;
  color: var(--muted-foreground);
}
.prop-card.card-calm .card-meta-row {
  font-size: 12px;
  font-family: var(--font-mono);
  color: var(--muted-foreground);
}
.prop-card.card-calm .card-score-float,
.prop-card.card-calm .card-rank,
.prop-card.card-calm .card-review-badge {
  display: none;
}
.prop-card.card-calm.heat-distressed,
.prop-card.card-calm.heat-critical,
.prop-card.card-calm.heat-high {
  background: var(--card);
  border-left: 3px solid var(--tier-distressed-border);
}
.prop-card.card-calm:hover {
  transform: none;
  border-color: var(--border);
  box-shadow: var(--shadow-sm);
}
body.legacy-hud .prop-card.card-calm:hover {
  transform: translateY(-4px);
}
```

Scope existing `.prop-card:hover` lift to `body.legacy-hud .prop-card:not(.card-calm):hover` or add `.card-calm` override as above.

**cards-grid:** background `var(--card)` not `var(--void-deep)`.
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'card-body-calm' -Quiet; Select-String -Path public/css/app.css -Pattern 'card-calm' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.card-body-calm` padding rule
    - app.css `.prop-card.card-calm .card-score-float` set to `display: none`
    - app.css `.prop-card.card-calm:hover` sets `transform: none`
    - app.css distressed calm cards use `border-left: 3px solid var(--tier-distressed-border)` without gradient background
    - app.css `.card-address` in calm context uses `font-weight: 600`
  </acceptance_criteria>
  <done>Calm card hierarchy styles defined</done>
</task>

<task type="auto">
  <name>Task 3: DATA-04 + DATA-05 — Bulk bar and table calm styles</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 2579-2660)
    - .planning/phases/14-results-data-views/14-UI-SPEC.md (DATA-04, DATA-05)
  </read_first>
  <action>
**Bulk edit bar calm override:**
```css
.bulk-edit-bar {
  background: var(--muted);
  border-bottom: 1px solid var(--border);
}
.bulk-edit-bar.visible { display: flex; }
.bulk-edit-count { color: var(--muted-foreground); }
```

Remove gradient from `.bulk-edit-bar` (replace existing rule).

**Table calm styles:**
```css
.table-view table {
  border-collapse: collapse;
  width: 100%;
}
.table-view thead th {
  background: var(--card);
  border-bottom: 1px solid var(--border);
  font-size: 12px;
  font-weight: 600;
  color: var(--muted-foreground);
  padding: var(--space-sm) var(--space-md);
}
.table-view tbody td {
  border-bottom: 1px solid var(--border);
  padding: var(--space-sm) var(--space-md);
  font-size: 14px;
}
.table-view tbody tr:hover {
  background: var(--muted);
}
.table-view .tier-badge {
  font-size: 12px;
}
```

**results-wrap:** `background: var(--card); border: 1px solid var(--border); border-radius: var(--radius);`
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'bulk-edit-bar' -Quiet; Select-String -Path public/css/app.css -Pattern '\.table-view thead th' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css `.bulk-edit-bar` uses `background: var(--muted)` (not linear-gradient)
    - app.css contains `.table-view thead th` with `background: var(--card)`
    - app.css `.table-view tbody tr:hover` uses `background: var(--muted)`
    - app.css `.bulk-edit-bar.visible` still sets `display: flex`
  </acceptance_criteria>
  <done>Bulk bar and table calm styles applied</done>
</task>

</tasks>

<verification>
```powershell
Select-String -Path public/css/app.css -Pattern 'filter-segmented|card-body-calm|table-view thead'
```
</verification>

<success_criteria>
- [ ] Segmented filters + search styles
- [ ] Calm card classes
- [ ] Flat bulk bar + calm table
</success_criteria>

## PLANNING COMPLETE