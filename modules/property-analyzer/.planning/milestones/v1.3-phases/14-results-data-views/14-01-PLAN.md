---
phase: 14-results-data-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
  - QA-04
must_haves:
  truths:
    - "Search row is first results chrome row with prominent #resultSearch"
    - "Primary segment shows All, Distressed, Needs Review, More toggle"
    - "Secondary filters and leadTypeFilter live in overflow menu"
    - "Bulk toggle label is Edit; all 6 filter-btn data-filter values preserved"
  artifacts:
    - path: public/index.html
      provides: Segmented filter DOM
      contains: "filterSegmented"
    - path: public/index.html
      provides: Filter overflow menu
      contains: "filterOverflowMenu"
    - path: public/index.html
      provides: Search-first header
      contains: "results-header"
  key_links:
    - from: public/index.html
      to: public/js/review.js
      via: .filter-btn data-filter
      pattern: 'data-filter="distressed"'
    - from: public/index.html
      to: public/js/imagery.js
      via: bulkSelectToggleBtn
      pattern: 'id="bulkSelectToggleBtn"'
---

<objective>
Restructure results chrome DOM in `index.html`: search-first header, segmented filters with overflow, Edit bulk toggle.

Purpose: Establish calm data-view topology (DATA-01, DATA-02, DATA-04) before CSS/JS wiring.
Output: Updated results section in `public/index.html`.
</objective>

<execution_context>
@C:\Users\brand\.codex\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/14-results-data-views/14-UI-SPEC.md
@.planning/phases/14-results-data-views/14-RESEARCH.md
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: DATA-02 — Search-first results header</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 294-340)
    - .planning/phases/14-results-data-views/14-UI-SPEC.md (DATA-02)
  </read_first>
  <action>
**Replace `.results-toolbar` + `.results-search-row` block with:**

```html
<div class="results-header" id="resultsHeader">
  <div class="results-title-row">
    <h3 class="results-title">Distress Rankings <span class="result-count" id="resultCount"></span></h3>
    <div class="results-header-actions">
      <div class="view-toggle">
        <!-- keep existing view-btn cards/table SVG buttons unchanged -->
      </div>
      <button type="button" class="results-edit-toggle" id="bulkSelectToggleBtn" disabled title="Select multiple leads and change tier or category">Edit</button>
    </div>
  </div>
  <div class="results-search-row" id="resultsSearchRow">
    <input type="search" class="results-search results-search-primary" id="resultSearch"
      placeholder="Search name, address, phone, email… (press /)" autocomplete="off">
    <span class="results-sort-label" id="resultSortLabel" title="Sort order">Newest first</span>
  </div>
</div>
```

- Remove inline `style` from h3 — use `.results-title` class instead
- Move `#bulkSelectToggleBtn` from filter-group to header actions
- Change button label from `☑ Bulk edit` to **Edit**
- Remove `export-toolbar-btn` class from bulk toggle; use `results-edit-toggle`
- Delete old `.results-search-row` duplicate below toolbar
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'results-search-primary' -Quiet; Select-String -Path public/index.html -Pattern 'id="resultsSearchRow"' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `class="results-search-primary"` on `#resultSearch`
    - index.html `#resultSearch` appears before `#filterSegmented` in document order
    - index.html `#bulkSelectToggleBtn` button text is `Edit` (not "Bulk edit")
    - index.html contains exactly one `#resultSearch` and one `#resultSortLabel`
    - index.html still contains `id="resultCount"`
  </acceptance_criteria>
  <done>Search row is primary header; Edit toggle relocated</done>
</task>

<task type="auto">
  <name>Task 2: DATA-01 — Segmented filters + overflow menu</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (filter-group section)
    - .planning/phases/14-results-data-views/14-UI-SPEC.md (DATA-01)
  </read_first>
  <action>
**Add filter bar after results header:**

```html
<div class="results-filter-bar" id="resultsFilterBar">
  <div class="filter-segmented" id="filterSegmented" role="group" aria-label="Filter leads by tier">
    <button class="filter-btn filter-segment-btn active" data-filter="all" type="button">All</button>
    <button class="filter-btn filter-segment-btn distressed-filter" data-filter="distressed" type="button">Distressed</button>
    <button class="filter-btn filter-segment-btn review-filter" data-filter="review" type="button">Needs Review</button>
  </div>
  <div class="filter-overflow-group" id="filterOverflowGroup">
    <button type="button" class="filter-overflow-toggle" id="filterOverflowToggle"
      aria-expanded="false" aria-controls="filterOverflowMenu" aria-label="More filters">
      More <span class="sidebar-chevron" aria-hidden="true">▾</span>
    </button>
    <div class="filter-overflow-menu" id="filterOverflowMenu" hidden>
      <button class="filter-btn filter-overflow-btn well-maintained-filter" data-filter="well_maintained" type="button">Well Maintained</button>
      <button class="filter-btn filter-overflow-btn vacant-filter" data-filter="vacant" type="button">Vacant Lot/Land</button>
      <button class="filter-btn filter-overflow-btn blurred-filter" data-filter="blurred" type="button">Blurred Imagery</button>
      <select id="leadTypeFilter" class="lead-type-filter" title="Filter by lead source type" aria-label="Filter by lead type">
        <option value="all">All lead types</option>
      </select>
    </div>
  </div>
</div>
```

- Remove old `.filter-group` with 6 pills + inline bulk button + lead type
- All 6 `data-filter` values must exist: `all`, `distressed`, `review`, `well_maintained`, `vacant`, `blurred`
- Well Maintained button text: **Well Maintained** (no ✨ emoji)
- Keep `#leadTypeFilter` ID and options structure
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'id="filterOverflowMenu"' -Quiet; (Select-String -Path public/index.html -Pattern 'data-filter=' -AllMatches).Matches.Count -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="filterSegmented"` with 3 visible segment buttons
    - index.html contains `id="filterOverflowMenu"` with 3 overflow filter buttons
    - index.html has 6 elements with `class="filter-btn"` and `data-filter` attribute
    - index.html `#leadTypeFilter` is inside `#filterOverflowMenu`
    - index.html does NOT contain old `.filter-group` wrapper with 6 pills in toolbar
    - index.html well-maintained button does NOT contain ✨ character
  </acceptance_criteria>
  <done>Segmented filters with overflow menu; all filter IDs preserved</done>
</task>

<task type="auto">
  <name>Task 3: DATA-04 + QA-04 — Bulk bar position + results wrap cleanup</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (bulk-edit-bar, results-wrap)
    - .planning/phases/14-results-data-views/14-UI-SPEC.md (DATA-04, DOM preservation)
  </read_first>
  <action>
**`#resultsWrap` structure order:**
1. `.results-header` (from Task 1)
2. `.results-filter-bar` (from Task 2)
3. `#bulkEditBar` — keep all child IDs unchanged; no `visible` class in HTML
4. `#cardsView` / `#tableView` — unchanged IDs

**`#resultsWrap` classes:** Remove `glass hud-panel` from `#resultsWrap`; keep `results-wrap` id.

**`#bulkEditBar`:** Update hint default text to: `Select leads, then mark tier or category`

Preserve all bulk button IDs: `bulkEditCount`, `bulkSelectAllBtn`, `bulkClearBtn`, `bulkTierDistressedBtn`, `bulkTierWellMaintainedBtn`, `bulkCatVacantBtn`, `bulkCatPropertyBtn`, `bulkDoneBtn`, `bulkEditHint`
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'id="bulkEditBar"' -Quiet; Select-String -Path public/index.html -Pattern 'id="bulkTierDistressedBtn"' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html `#bulkEditBar` appears after `#resultsFilterBar` and before `#cardsView`
    - index.html `#bulkEditBar` does NOT have class `visible` in static HTML
    - index.html preserves all bulk edit element IDs from 14-UI-SPEC
    - index.html `#resultsWrap` still has `id="resultsWrap"`
    - index.html `#cardsGrid` and `#resultsBody` unchanged
  </acceptance_criteria>
  <done>Results wrap structure ordered; bulk bar preserved</done>
</task>

</tasks>

<verification>
```powershell
Select-String -Path public/index.html -Pattern 'filterSegmented|filterOverflowMenu|results-search-primary|bulkSelectToggleBtn'
```
</verification>

<success_criteria>
- [ ] Search-first header with prominent search input
- [ ] Segmented filters + overflow with 6 data-filter buttons
- [ ] Edit toggle; bulk bar IDs preserved
- [ ] resultsWrap DOM order correct
</success_criteria>

## PLANNING COMPLETE