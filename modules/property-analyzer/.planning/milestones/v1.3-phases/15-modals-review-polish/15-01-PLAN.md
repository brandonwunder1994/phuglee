---
phase: 15-modals-review-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true
requirements:
  - MODAL-01
  - MODAL-02
  - MODAL-03
  - MODAL-04
  - QA-04
must_haves:
  truths:
    - "Tool modals use calm-dialog class instead of glass hud-panel"
    - "Property modal grid has inspector-calm class for imagery-first layout"
    - "Review action buttons have emoji-free static labels in HTML"
    - "Score edit dialog uses calm-dialog; all modal IDs preserved"
  artifacts:
    - path: public/index.html
      provides: Calm tool modals
      contains: "tool-modal-dialog calm-dialog"
    - path: public/index.html
      provides: Imagery-first inspector grid
      contains: "inspector-calm"
    - path: public/index.html
      provides: Calm review overlay DOM
      contains: "reviewKeepBtn"
  key_links:
    - from: public/index.html
      to: public/js/state.js
      via: openToolModal
      pattern: 'id="settingsModal"'
    - from: public/index.html
      to: public/js/session.js
      via: propertyModal
      pattern: 'id="propertyModal"'
---

<objective>
Restructure modal DOM in `index.html`: calm dialog chrome on tool/score modals, imagery-first inspector grid, emoji-free review button labels.

Purpose: Establish calm modal topology (MODAL-01–04) before CSS/JS wiring.
Output: Updated modal section in `public/index.html` (L410–655).
</objective>

<execution_context>
@C:\Users\brand\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/15-modals-review-polish/15-UI-SPEC.md
@.planning/phases/15-modals-review-polish/15-RESEARCH.md
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: MODAL-01 — Calm tool modal dialogs</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 410-510)
    - .planning/phases/15-modals-review-polish/15-UI-SPEC.md (MODAL-01)
  </read_first>
  <action>
**For `#settingsModal`, `#uploadModal`, `#brainModal` dialog elements:**

Replace:
- `class="tool-modal-dialog glass hud-panel"` → `class="tool-modal-dialog calm-dialog"`
- `class="tool-modal-dialog glass hud-panel wide"` → `class="tool-modal-dialog calm-dialog calm-dialog-wide"`

**Settings modal inline styles:** Remove `style="color:var(--copper-bright);"` from all `<code>` elements in the key-note paragraph (lines ~417). Use default text color.

**Concurrent limit label:** Change `<span id="concurrentLimitVal" style="color:var(--copper-bright);...">` to use class `calm-mono-accent` instead of inline copper color:
```html
<span id="concurrentLimitVal" class="calm-mono-accent">10</span>
```

**Preserve unchanged:** All input IDs (`streetViewKey`, `geminiKey`, `concurrentLimit`, `fileInput`, `importLeadTypeSelect`), all button IDs (`testSvBtn`, `exportLearnedBtn`, `importLearnedBtn`, `importLearnedFile`), diag panel IDs.
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'tool-modal-dialog calm-dialog' | Measure-Object | Select-Object -ExpandProperty Count; (Select-String -Path public/index.html -Pattern 'glass hud-panel').Matches.Count -eq 0 -or (Select-String -Path public/index.html -Pattern 'tool-modal-dialog glass').Matches.Count -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly 3 elements matching `tool-modal-dialog calm-dialog`
    - index.html `#settingsModal` dialog does NOT contain class `glass`
    - index.html `#brainModal` still contains `id="exportLearnedBtn"` and `id="importLearnedBtn"`
    - index.html `#uploadModal` still contains `id="fileInput"` and `id="fileDrop"`
  </acceptance_criteria>
  <done>Tool modals use calm-dialog; brain/upload IDs preserved</done>
</task>

<task type="auto">
  <name>Task 2: MODAL-02 — Property inspector imagery-first DOM</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 512-566)
    - .planning/phases/15-modals-review-polish/15-UI-SPEC.md (MODAL-02)
  </read_first>
  <action>
**Property modal dialog:** Replace `class="property-modal-dialog glass hud-panel"` with `class="property-modal-dialog calm-dialog"`.

**Property modal grid:** Add `inspector-calm` class:
```html
<div class="property-modal-grid inspector-calm">
```

**Restructure media column for imagery-first:**

Move gauge panel OUT of `.preview-layout` into details column wrapper. Target structure:

```html
<div class="property-modal-media">
  <div class="preview-images inspector-images-primary" id="previewImages">
    <!-- keep previewSatWrap and previewWrap unchanged inside -->
  </div>
</div>
<div class="property-modal-details">
  <div class="inspector-gauge-calm" id="inspectorGaugePanel">
    <!-- move gauge-panel SVG here from old preview-layout -->
    <svg class="gauge-svg gauge-svg-calm" viewBox="0 0 100 100">...</svg>
    <div class="gauge-center">
      <div class="gauge-num" id="gaugeNum">—</div>
      <div class="gauge-lbl">Distress</div>
    </div>
  </div>
  <div class="inspector-body empty" id="inspectorBody">Click a property below to view full details</div>
</div>
```

- Remove outer `.preview-layout` wrapper and side-by-side gauge placement
- Keep all image element IDs: `previewSatImg`, `previewImg`, `previewWrap`, `previewSatWrap`, `previewPlaceholder`, `gaugeFill`, `gaugeNum`
- Keep header nav: `prevPropBtn`, `nextPropBtn`, `inspectorPos`, `closePropertyBtn`
- Title: remove `<span class="live-dot idle" id="liveDot"></span>` from `#propertyModalTitle` — keep `liveDot` as hidden element for JS compatibility:
```html
<h2 class="property-modal-title" id="propertyModalTitle">
  <span class="live-dot idle" id="liveDot" hidden aria-hidden="true"></span>
  <span id="previewHeaderTitle">Property</span>
</h2>
```
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'inspector-calm' -Quiet; Select-String -Path public/index.html -Pattern 'inspector-gauge-calm' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `class="property-modal-grid inspector-calm"`
    - index.html contains `id="inspectorGaugePanel"` inside `.property-modal-details`
    - index.html `#gaugeNum` and `#gaugeFill` still present
    - index.html `#inspectorBody` still present
    - index.html property modal dialog does NOT contain class `glass`
  </acceptance_criteria>
  <done>Inspector DOM imagery-first; gauge relocated to details column</done>
</task>

<task type="auto">
  <name>Task 3: MODAL-03 + MODAL-04 — Review overlay and score edit DOM</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 568-655)
    - .planning/phases/15-modals-review-polish/15-UI-SPEC.md (MODAL-03, MODAL-04)
  </read_first>
  <action>
**Score edit modal:** Replace `class="score-edit-dialog glass hud-panel"` with `class="score-edit-dialog calm-dialog"`.

Update hint text (line ~580):
```html
<div class="score-adjust-hint">The system reviews this change automatically — approve proposed rules in AI Brain.</div>
```
(Remove 🧠 emoji)

**Review action bar — emoji-free static labels:**
```html
<button type="button" class="review-action keep" id="reviewKeepBtn" title="AI got it right — advance (1)">Keep <kbd>1</kbd></button>
<button type="button" class="review-action change-tier" id="reviewChangeBtn" title="Change tier (2)">Change <kbd>2</kbd></button>
<button type="button" class="review-action vacant" id="reviewLandBtn" title="Mark as land / vacant lot (3)" hidden>Land <kbd>3</kbd></button>
<button type="button" class="review-action defer-later" id="reviewDeferBtn" title="Can't decide now — send to Needs Review queue (4)">Later <kbd>4</kbd></button>
<button type="button" class="review-action blurred" id="reviewBlurredBtn" title="Move to Blurred list only — not Needs Review (5)">Blurred <kbd>5</kbd></button>
<button type="button" class="review-action undo" id="reviewUndoBtn" title="Undo last change (3)" disabled>Undo <kbd>3</kbd></button>
```

**Review tier pick overlay:** Replace `class="review-tier-pick-dialog glass"` with `class="review-tier-pick-dialog calm-dialog"`.

Update tier pick buttons:
```html
<button type="button" class="review-tier-pick-btn distressed" data-review-tier="distressed">Distressed</button>
<button type="button" class="review-tier-pick-btn well-maintained" data-review-tier="well_maintained">Well Maintained</button>
```

**Bulk bar emoji cleanup (deferred from Phase 14):**
```html
<button type="button" class="bulk-tier-btn" id="bulkTierWellMaintainedBtn" ...>Well Maintained</button>
```

**Preserve:** All review overlay IDs unchanged; `hidden` attributes unchanged on `#reviewLandBtn`.
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'score-edit-dialog calm-dialog' -Quiet; Select-String -Path public/index.html -Pattern 'id="reviewKeepBtn".*Keep' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html `#scoreEditModal` dialog has class `calm-dialog` not `glass`
    - index.html `#reviewKeepBtn` text contains `Keep` and does NOT contain `✓`
    - index.html `#reviewLandBtn` text is `Land` not `🏜️ Land`
    - index.html `#reviewDeferBtn` text is `Later` not `⏳ Later`
    - index.html `#reviewBlurredBtn` text is `Blurred` not `🌫`
    - index.html tier pick buttons do NOT contain ⚠️ or ✨ characters
    - index.html still contains all review element IDs from 15-RESEARCH.md
    - index.html still contains `id="scoreEditTierPicker"`, `id="scoreEditSave"`, `id="scoreEditCancel"`
    - index.html `#bulkTierWellMaintainedBtn` text is `Well Maintained` (no ✨)
  </acceptance_criteria>
  <done>Review and score edit DOM calm; emoji removed from static HTML</done>
</task>

</tasks>

<verification>
```powershell
Select-String -Path public/index.html -Pattern 'calm-dialog|inspector-calm|reviewKeepBtn'
```
</verification>

<success_criteria>
- [ ] Tool modals use calm-dialog
- [ ] Property inspector imagery-first grid
- [ ] Review buttons emoji-free in HTML
- [ ] Score edit calm dialog
- [ ] All modal IDs preserved
</success_criteria>

## PLANNING COMPLETE