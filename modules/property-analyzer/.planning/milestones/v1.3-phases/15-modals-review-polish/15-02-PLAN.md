---
phase: 15-modals-review-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - public/css/app.css
autonomous: true
requirements:
  - MODAL-01
  - MODAL-02
  - MODAL-03
  - MODAL-04
  - QA-04
must_haves:
  truths:
    - "calm-dialog provides flat card surface with var(--card) and var(--border)"
    - "Inspector imagery column dominates; gauge de-emphasized in details"
    - "Review overlay uses calm scrim without neon glow on action buttons"
    - "Review action buttons use flat tier colors not box-shadow glow"
  artifacts:
    - path: public/css/app.css
      provides: Shared calm dialog chrome
      contains: ".calm-dialog"
    - path: public/css/app.css
      provides: Inspector imagery-first layout
      contains: "inspector-calm"
    - path: public/css/app.css
      provides: Calm review overlay
      contains: "review-mode-overlay"
  key_links:
    - from: public/css/app.css
      to: public/css/tokens.css
      via: var(--card), var(--border), var(--muted)
      pattern: "var(--card)"
---

<objective>
Restyle all modal surfaces in `app.css`: shared calm-dialog chrome, imagery-first inspector, calm review scrim and action bar.

Purpose: Visible calm modal transformation (MODAL-01–04).
Output: Phase 15 CSS block appended to `public/css/app.css`.
</objective>

<execution_context>
@C:\Users\brand\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/15-modals-review-polish/15-UI-SPEC.md
@.planning/phases/15-modals-review-polish/15-RESEARCH.md
@public/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: MODAL-01 — Shared calm-dialog + tool modal styles</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 5322-5377 tool-modal section)
    - .planning/phases/15-modals-review-polish/15-UI-SPEC.md (MODAL-01)
  </read_first>
  <action>
**Add Phase 15 block before reduced-motion media queries:**

```css
/* ── Phase 15: Modals & review polish ── */

/* Shared calm dialog chrome */
.calm-dialog {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.24);
}
.calm-dialog-wide {
  width: min(94vw, 860px);
}
.calm-mono-accent {
  color: var(--muted-foreground);
  font-family: var(--font-mono);
}

/* Tool modals */
.tool-modal-backdrop {
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
}
.tool-modal-dialog.calm-dialog {
  width: min(94vw, 720px);
  max-height: 90vh;
  overflow-y: auto;
  padding: var(--space-lg);
}
.tool-modal-head {
  border-bottom: 1px solid var(--border);
  margin-bottom: var(--space-md);
  padding-bottom: var(--space-sm);
}
.tool-modal-head h2 {
  font-family: var(--font-display);
  font-size: 18px;
  font-weight: 600;
  color: var(--foreground);
}
.tool-modal-close {
  border: 1px solid var(--border);
  background: transparent;
  color: var(--muted-foreground);
  border-radius: var(--radius-xs);
}
.tool-modal-close:hover {
  border-color: var(--border);
  color: var(--foreground);
  background: var(--muted);
}
```

**Override legacy glass on calm dialogs:**
```css
.calm-dialog.glass,
.calm-dialog.hud-panel {
  background: var(--card);
  backdrop-filter: none;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.24);
}
```
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern '\.calm-dialog \{' -Quiet; Select-String -Path public/css/app.css -Pattern 'tool-modal-backdrop' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.calm-dialog {` with `background: var(--card)`
    - app.css contains `.tool-modal-backdrop` with `rgba(0, 0, 0, 0.6)`
    - app.css `.tool-modal-close:hover` does NOT set `color: var(--copper-bright)`
    - app.css Phase 15 block comment `Phase 15: Modals` present
  </acceptance_criteria>
  <done>Shared calm-dialog and tool modal styles applied</done>
</task>

<task type="auto">
  <name>Task 2: MODAL-02 + MODAL-04 — Inspector and score edit calm styles</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 1070-1220 property-modal, 2829-2900 score-edit)
    - public/index.html (inspector-calm structure from 15-01)
  </read_first>
  <action>
**Property modal calm styles:**

```css
.property-modal-backdrop {
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
}
.property-modal-dialog.calm-dialog {
  width: min(96vw, 1100px);
  max-height: 92vh;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.property-modal-grid.inspector-calm {
  display: grid;
  grid-template-columns: 1fr minmax(280px, 360px);
  gap: var(--space-md);
  flex: 1;
  min-height: 0;
  overflow: hidden;
}
@media (max-width: 900px) {
  .property-modal-grid.inspector-calm {
    grid-template-columns: 1fr;
    overflow-y: auto;
  }
}
.inspector-images-primary {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  min-height: 280px;
}
.inspector-images-primary .preview-image-wrap {
  border: 1px solid var(--border);
  border-radius: var(--radius-xs);
  background: var(--muted);
}
.inspector-calm .rec-badge,
.inspector-calm .scan-line,
.inspector-calm .target-reticle,
.inspector-calm .preview-reticle {
  display: none !important;
}
.inspector-calm .preview-pane-label {
  font-size: 11px;
  color: var(--muted-foreground);
  font-family: var(--font-mono);
}
.inspector-gauge-calm {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-sm) 0;
  border-bottom: 1px solid var(--border);
  margin-bottom: var(--space-sm);
}
.gauge-svg-calm {
  width: 56px;
  height: 56px;
  flex-shrink: 0;
}
.inspector-gauge-calm .gauge-num {
  font-size: 18px;
  font-weight: 600;
}
.inspector-gauge-calm .gauge-lbl {
  font-size: 11px;
  color: var(--muted-foreground);
}
```

**Score edit modal:**
```css
.score-edit-backdrop {
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
}
.score-edit-dialog.calm-dialog {
  width: min(94vw, 480px);
  padding: var(--space-lg);
}
.score-edit-header h3 {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 600;
}
```
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'inspector-calm' -Quiet; Select-String -Path public/css/app.css -Pattern 'inspector-gauge-calm' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.property-modal-grid.inspector-calm`
    - app.css contains `.inspector-gauge-calm` with `gauge-svg-calm`
    - app.css hides `.inspector-calm .rec-badge` with `display: none`
    - app.css contains `.score-edit-dialog.calm-dialog`
  </acceptance_criteria>
  <done>Inspector imagery-first and score edit calm styles applied</done>
</task>

<task type="auto">
  <name>Task 3: MODAL-03 — Review overlay calm scrim and action bar</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 5493-5800 review-mode section)
    - .planning/phases/15-modals-review-polish/15-UI-SPEC.md (MODAL-03)
  </read_first>
  <action>
**Review overlay calm restyle:**

```css
.review-mode-overlay {
  background: rgba(0, 0, 0, 0.72);
  backdrop-filter: blur(6px);
}
.review-mode-header {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius-xs);
}
.review-mode-badge {
  color: var(--foreground);
  border: 1px solid var(--border);
  background: var(--muted);
  letter-spacing: 0.08em;
}
.review-mode-exit {
  border: 1px solid var(--border);
  background: var(--muted);
  color: var(--foreground);
  border-radius: var(--radius-xs);
}
.review-mode-exit:hover {
  background: var(--accent);
  color: var(--accent-foreground);
}

/* Action bar — flat tier colors, no glow */
.review-action-bar {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius-xs);
  gap: var(--space-sm);
  padding: var(--space-sm);
}
.review-action {
  border: 1px solid var(--border);
  background: var(--muted);
  border-radius: var(--radius-xs);
  font-size: 13px;
  font-weight: 500;
  box-shadow: none;
}
.review-action:hover {
  box-shadow: none;
}
.review-action.keep {
  border-color: var(--tier-distressed-border, rgba(109, 184, 154, 0.4));
  color: var(--tier-distressed-bright, #6db89a);
}
.review-action.keep:hover {
  background: rgba(109, 184, 154, 0.12);
  box-shadow: none;
}
.review-action.change-tier,
.review-action.distressed {
  border-color: rgba(34, 197, 94, 0.35);
  color: #4ade80;
}
.review-action.well-maintained {
  border-color: rgba(96, 165, 250, 0.35);
  color: #60a5fa;
}
.review-action.vacant {
  border-color: var(--tier-land-border);
  color: var(--tier-land-bright);
}
.review-action.defer-later {
  border-color: rgba(196, 92, 106, 0.35);
  color: var(--crimson);
}
.review-action.blurred {
  border-color: var(--border);
  color: var(--muted-foreground);
}
.review-action kbd {
  font-family: var(--font-mono);
  font-size: 11px;
  opacity: 0.8;
}

/* Tier pick overlay */
.review-tier-pick-dialog.calm-dialog {
  padding: var(--space-lg);
  width: min(94vw, 420px);
}
.review-tier-pick-btn {
  border: 1px solid var(--border);
  background: var(--muted);
  border-radius: var(--radius-xs);
  box-shadow: none;
}
.review-tier-pick-btn.distressed:hover {
  background: rgba(34, 197, 94, 0.12);
  border-color: rgba(34, 197, 94, 0.35);
}
.review-tier-pick-btn.well-maintained:hover {
  background: rgba(96, 165, 250, 0.12);
  border-color: rgba(96, 165, 250, 0.35);
}

/* Legacy HUD escape hatch */
body.legacy-hud .review-action.keep:hover {
  box-shadow: 0 0 20px rgba(109, 184, 154, 0.15);
}
```
  </action>
  <verify>
    <automated>Select-String -Path public/css/app.css -Pattern 'review-action-bar' -Quiet; Select-String -Path public/css/app.css -Pattern 'review-mode-overlay' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - app.css `.review-mode-overlay` uses `rgba(0, 0, 0, 0.72)` background
    - app.css `.review-action:hover` sets `box-shadow: none`
    - app.css `.review-action.keep:hover` does NOT contain `0 0 20px` outside `body.legacy-hud` block
    - app.css contains `.review-tier-pick-dialog.calm-dialog`
  </acceptance_criteria>
  <done>Review overlay calm scrim and flat action bar styles applied</done>
</task>

</tasks>

<verification>
```powershell
Select-String -Path public/css/app.css -Pattern 'calm-dialog|inspector-calm|review-action-bar'
```
</verification>

<success_criteria>
- [ ] calm-dialog shared chrome
- [ ] Inspector imagery-first CSS
- [ ] Score edit calm dialog CSS
- [ ] Review overlay calm scrim and flat action buttons
</success_criteria>

## PLANNING COMPLETE