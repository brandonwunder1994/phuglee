---
phase: 12-shell-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-05
  - QA-04
must_haves:
  truths:
    - "HUD bar hidden or removed; #hudStatus lives inside #commandBar"
    - "Sidebar shows Overview, Lead Rankings, Review, More — admin nav demoted to overflow"
    - "Branding reads Distress Analyzer (no DistressOS / Intelligence Suite in shell)"
    - "All preserved DOM IDs from 12-UI-SPEC still present in index.html"
  artifacts:
    - path: public/index.html
      provides: Restructured calm shell DOM
      contains: "sidebarOverflowMenu"
    - path: public/index.html
      provides: Status chip in command bar
      contains: 'id="hudStatus"'
  key_links:
    - from: public/index.html
      to: public/js/config.js
      via: preserved element IDs
      pattern: "sidebarExportExcelBtn"
    - from: public/index.html
      to: public/js/render.js
      via: "#hudStatus"
      pattern: 'id="hudStatus"'
---

<objective>
Restructure `index.html` shell DOM: hide HUD bar, slim sidebar to 4 top-level items with overflow menu, flatten command bar metadata cluster, update branding copy.

Purpose: Establish correct DOM topology for calm shell (SHELL-01, SHELL-02, SHELL-03, SHELL-05) before CSS/JS wiring.
Output: Updated `public/index.html` with overflow container and command bar status chip.
</objective>

<execution_context>
@C:\Users\brand\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/12-shell-simplification/12-UI-SPEC.md
@.planning/phases/12-shell-simplification/12-RESEARCH.md
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide HUD bar and move #hudStatus into command bar</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 73-128)
    - .planning/phases/12-shell-simplification/12-UI-SPEC.md (SHELL-01 section)
  </read_first>
  <action>
**HUD bar (lines 73-77):** Add `hidden` attribute and `style="display:none"` to `.hud-bar` div OR remove the block entirely. If removing, keep a hidden shim for `#hudClock` to avoid JS errors:

```html
<span id="hudClock" hidden aria-hidden="true" style="display:none">--:--:--</span>
```

**Command bar restructure:** Replace `.command-bar-left` contents with a single-row metadata cluster:

```html
<div class="command-bar-left">
  <div class="command-bar-meta">
    <div class="command-title">Distress Analyzer</div>
    <span class="command-meta-sep" aria-hidden="true">·</span>
    <div class="command-file-status" id="commandFileStatus">No file loaded</div>
    <span class="command-meta-sep" aria-hidden="true">·</span>
    <div class="command-save-status" id="commandSaveStatus" hidden></div>
    <span class="command-meta-sep command-save-sep" aria-hidden="true" hidden>·</span>
    <span class="command-status-chip sys-ok" id="hudStatus">Ready</span>
  </div>
  <!-- keep hidden, do not delete IDs -->
  <div class="command-workers-status" id="commandWorkersStatus" hidden>...</div>
  <div class="command-backup-size" id="backupSizeIndicator" hidden>Backup: —</div>
  <span id="heroCount" hidden aria-hidden="true">—</span>
</div>
```

- Remove gradient span from title — plain text `Distress Analyzer` only (no nested `<span>` with accent color).
- `#commandWorkersStatus` and `#backupSizeIndicator` stay in DOM but `hidden`.
- Preserve `#startBtn`, `#stopBtn`, hidden shim buttons block unchanged.
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'id="hudStatus"' -Quiet; Select-String -Path public/index.html -Pattern 'command-bar-meta' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="hudStatus"` inside `#commandBar` (between commandBar tags)
    - index.html contains `class="command-bar-meta"`
    - index.html command title text is `Distress Analyzer` without gradient span child
    - index.html does NOT show visible `.hud-bar` content (hidden or removed)
    - index.html still contains `id="hudClock"` (visible or hidden shim)
    - index.html still contains `id="commandWorkersStatus"` and `id="backupSizeIndicator"`
  </acceptance_criteria>
  <done>Status chip relocated; HUD bar no longer visible in DOM layout</done>
</task>

<task type="auto">
  <name>Task 2: Restructure sidebar — 4 nav items + overflow menu</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lines 15-70)
    - .planning/phases/12-shell-simplification/12-UI-SPEC.md (SHELL-02, DOM ID Preservation)
  </read_first>
  <action>
**Brand block (lines 16-21):**
- Change `.sidebar-title` text to `Distress Analyzer`
- Hide `.sidebar-sub` with `hidden` attribute or delete element

**Review group (line 28):** Change button label from `Review Leads` to `Review` (keep `id="sidebarReviewGroup"` and all review submenu button IDs).

**Remove from always-visible nav:**
- Delete or hide `.sidebar-divider.sidebar-admin-divider`
- Remove `#sidebarAdminNav` wrapper as top-level sibling — its children move to overflow

**Add overflow group after review group:**

```html
<div class="sidebar-nav-group" id="sidebarOverflowGroup">
  <button type="button" class="sidebar-nav-btn sidebar-nav-toggle" id="sidebarOverflowToggle"
    aria-expanded="false" aria-controls="sidebarOverflowMenu" aria-label="More actions" title="More actions">
    <span class="nav-icon">···</span><span>More</span><span class="sidebar-chevron" aria-hidden="true">▾</span>
  </button>
  <div class="sidebar-submenu sidebar-overflow-menu" id="sidebarOverflowMenu" role="menu" hidden>
    <div class="sidebar-overflow-section-label">Settings</div>
    <!-- MOVE entire #sidebarSettingsGroup inner content here (toggle becomes flat section OR keep group collapsed) -->
    <div class="sidebar-overflow-section-label">Data</div>
    <!-- MOVE entire #sidebarManageDataGroup inner content here -->
    <div class="sidebar-overflow-section-label">Export</div>
    <!-- export buttons from manage data menu -->
  </div>
</div>
```

**Implementation approach:** Move the existing `#sidebarSettingsGroup` and `#sidebarManageDataGroup` divs (with ALL child IDs intact) inside `#sidebarOverflowMenu`. Remove the old `#sidebarAdminNav` wrapper. Keep every button ID: `sidebarSettingsSaveBackupBtn`, `sidebarLoadBackupBtn`, `sidebarExportExcelBtn`, `sidebarExportCsvBtn`, `sidebarExportAllBtn`, `sidebarSaveBackupBtn`, `sidebarSettingsToggle`, `sidebarManageDataToggle`, etc.

**Do NOT rename any ID** from the UI-SPEC preservation list.
  </action>
  <verify>
    <automated>Select-String -Path public/index.html -Pattern 'id="sidebarOverflowMenu"' -Quiet; Select-String -Path public/index.html -Pattern 'id="sidebarExportExcelBtn"' -Quiet; Select-String -Path public/index.html -Pattern 'sidebar-admin-nav' -NotMatch</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="sidebarOverflowToggle"` and `id="sidebarOverflowMenu"`
    - index.html contains `id="sidebarSettingsGroup"` and `id="sidebarManageDataGroup"` (inside overflow)
    - index.html contains all export button IDs: sidebarExportExcelBtn, sidebarExportCsvBtn, sidebarExportAllBtn
    - index.html does NOT contain `id="sidebarAdminNav"` as always-visible top-level nav
    - sidebar title text is `Distress Analyzer`
    - sidebar-sub is hidden or removed
    - Review toggle label is `Review` (not `Review Leads`)
  </acceptance_criteria>
  <done>Sidebar DOM matches 4-item + overflow topology with preserved IDs</done>
</task>

<task type="auto">
  <name>Task 3: Verify critical ID preservation</name>
  <files>public/index.html</files>
  <read_first>
    - .planning/phases/12-shell-simplification/12-UI-SPEC.md (DOM ID Preservation section)
  </read_first>
  <action>
Grep `public/index.html` for every ID in the UI-SPEC preservation list. If any missing, restore from git before proceeding.

Required IDs (all must be present):
```
appSidebar, sidebarReviewGroup, sidebarReviewToggle, sidebarReviewMenu,
sidebarReviewDistressedBtn, sidebarReviewWellMaintainedBtn, sidebarReviewLandBtn,
sidebarReviewNeedsReviewBtn, sidebarSettingsGroup, sidebarSettingsToggle,
sidebarSettingsMenu, sidebarSettingsSaveBackupBtn, sidebarManageDataGroup,
sidebarManageDataToggle, sidebarManageDataMenu, sidebarLoadBackupBtn,
sidebarExportExcelBtn, sidebarExportCsvBtn, sidebarExportAllBtn, sidebarSaveBackupBtn,
hudStatus, hudClock, commandBar, commandFileStatus, commandSaveStatus,
commandWorkersStatus, backupSizeIndicator, startBtn, stopBtn,
openSettingsBtn, openBrainBtn, openUploadModalBtn, loadBackupBtn, saveBackupNowBtn
```

New IDs must also exist: `sidebarOverflowToggle`, `sidebarOverflowMenu`, `sidebarOverflowGroup`.

Run `npm test` — must still pass (HTML-only change should not break tests).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - All 33 preserved IDs found in public/index.html (grep count ≥ 33)
    - `sidebarOverflowToggle` and `sidebarOverflowMenu` present
    - `npm test` exit code 0
    - Output contains `pass 78` or `tests 78`
  </acceptance_criteria>
  <done>HTML shell restructure complete with ID integrity verified</done>
</task>

</tasks>

<verification>
- [ ] All preserved DOM IDs present in index.html
- [ ] `#hudStatus` inside `#commandBar`
- [ ] `npm test` passes
</verification>

<success_criteria>
- Shell DOM matches 12-UI-SPEC layout contract
- Branding updated to Distress Analyzer
- Admin actions demoted to overflow container (IDs preserved)
- QA-04 satisfied via ID grep
</success_criteria>