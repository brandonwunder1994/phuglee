---
phase: 16-cyber-design-system-formalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/tokens.css
  - public/css/cyber-ultra.css
autonomous: true
requirements:
  - CYBER-01
  - CYBER-02
  - QA-01
  - QA-06
must_haves:
  truths:
    - "tokens.css defines Orbitron, Outfit, JetBrains as font-display/body/mono"
    - "Cyber palette tokens are documented with header comment"
    - "cyber-ultra.css does not duplicate font-family overrides in body.cyber-theme block"
  artifacts:
    - path: public/css/tokens.css
      provides: Authoritative cyber token + typography source
      contains: "'Orbitron'"
---

<objective>
Consolidate cyber typography and token documentation into `tokens.css` as the single design source (CYBER-01, CYBER-02).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Formalize tokens.css typography and documentation</name>
  <files>public/css/tokens.css</files>
  <read_first>
    - public/css/tokens.css
    - public/css/cyber-ultra.css (lines 1-14)
    - .planning/phases/16-cyber-design-system-formalization/16-UI-SPEC.md
  </read_first>
  <action>
Add file header comment: "Property Distress Analyzer — Cyber Design Tokens (v1.4 authoritative)".

Update typography block:
```css
--font-display: 'Orbitron', system-ui, sans-serif;
--font-body: 'Outfit', system-ui, sans-serif;
--font-mono: 'JetBrains Mono', 'Consolas', monospace;
```

Add `--radius: 12px` and `--radius-sm: 10px` if not already matching UI-SPEC (update from 10px/8px to 12px/10px).

Ensure no calm/stone palette values remain (no `#1c1917`, `#fafaf9`, `stone` references).
  </action>
  <acceptance_criteria>
    - tokens.css contains `--font-display: 'Orbitron'`
    - tokens.css contains `--font-body: 'Outfit'`
    - tokens.css contains `Cyber Design Tokens`
    - tokens.css does NOT contain `stone` or `#1c1917`
    - npm test exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate typography from cyber-ultra.css</name>
  <files>public/css/cyber-ultra.css</files>
  <read_first>
    - public/css/cyber-ultra.css (lines 1-14)
    - public/css/tokens.css
  </read_first>
  <action>
In `body.cyber-theme` block at top of cyber-ultra.css, REMOVE these duplicate lines (now in tokens.css):
```css
--font-display: 'Orbitron', system-ui, sans-serif;
--font-body: 'Outfit', system-ui, sans-serif;
```

KEEP `--sidebar-w`, `--radius`, `--radius-sm`, `letter-spacing` in cyber-ultra if not in tokens.

Add comment: `/* Typography: see tokens.css */`
  </action>
  <acceptance_criteria>
    - cyber-ultra.css body.cyber-theme block does NOT set --font-display or --font-body
    - cyber-ultra.css contains comment "Typography: see tokens.css"
  </acceptance_criteria>
</task>

</tasks>