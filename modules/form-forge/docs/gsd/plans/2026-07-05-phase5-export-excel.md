# Phase 5 — Export to Excel

**Goal:** Export unified registry + submission data back to Excel for reporting.

## Tasks
1. `export_registry_to_rows()` in `portal_registry.py`
2. `scripts/export_portal_registry.py` — writes Desktop xlsx
3. Reverse-map status enums to readable text
4. Tests for export row builder