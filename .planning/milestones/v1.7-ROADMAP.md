# Milestone v1.7: Filter Accuracy & Grouping

**Status:** ✅ SHIPPED 2026-07-10  
**Phases:** 48–50  
**Total Plans:** 4 (48: 2, 49: 1, 50: 1)

## Overview

Fix Train/Filter grouping accuracy after v1.6 Superpower Brain: stack same real-world categories despite timestamps, promote unmapped city categories into type labels, keep signal chips as arrays on the process path, and lock behavior with processUpload regression tests.

**Diagnosis:** `.planning/debug/filter-singleton-no-category.md`

## Phases

### Phase 48: Category Promotion & Signal Shape

**Goal:** Process path yields real city categories on rows and array-shaped signals so Train can label FN/distressed groups and render chips  
**Depends on:** Nothing (v1.6 foundation)  
**Requirements:** MAP-01, MAP-02, MAP-03, SHAPE-01, SHAPE-02  
**Plans:** 2/2

- [x] 48-01: SHAPE — `matchedIndicators` arrays on process rows; join only at export
- [x] 48-02: MAP — pure `bridge-category-promote` + normalizer wire

**Verification:** 48-VERIFICATION.md — 5/5 passed

### Phase 49: Stable Group Keys

**Goal:** Same real-world category stacks into one group; incidental timestamps no longer flood Train with false singletons  
**Depends on:** Phase 48  
**Requirements:** GROUP-01, GROUP-02, GROUP-03, GROUP-04  
**Plans:** 1/1

- [x] 49-01: `bridge-stable-text` + wire `buildReviewGroups`; `isSingleton = count === 1`

**Verification:** 49-VERIFICATION.md — 6/6 passed

### Phase 50: Regression Lock

**Goal:** Accuracy fixes stay locked by automated tests; full suite and live server remain green  
**Depends on:** Phase 49  
**Requirements:** TEST-01, TEST-02, TEST-03  
**Plans:** 1/1

- [x] 50-01: processUpload e2e contracts + TAGGING-RULES note + npm test + verify-live

**Verification:** 50-VERIFICATION.md — 5/5 passed

## Milestone Summary

**Key Decisions:**
- GROUP + MAP + SHAPE only (no Train CSS, phrase mining, Analyzer vision)
- Promote from category-like headers only (never invent from free-text noise)
- Arrays on process path; join with `'; '` only at export
- Stable keys strip incidental dates/times; singleton remains pure count

**Issues Resolved:**
- Distressed singleton flood from timestamped free-text descriptions
- FN “(no type)” / missing category when column unmapped
- Train chips “No matched signals” due to stringified indicators

**Issues Deferred:**
- brain-apply still matches raw type keys without strip (non-blocking)
- Multi-tenant server sessions
- Month-name date stripping unless fixtures appear

**Stats:**
- Git range: `a5a10f5` → `5899e60` (16 commits, +1493 / −121 across 21 files)
- Tests: **380** pass / 0 fail
- Live: `scripts/verify-live.ps1` exit 0 (health + homepage 200)
