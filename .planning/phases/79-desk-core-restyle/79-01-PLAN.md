---
phase: 79-desk-core-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/bridge.css
  - public/bridge.html
autonomous: true
requirements:
  - CARDS-03
must_haves:
  truths:
    - "Operator can scan elevation hierarchy — primary scrub stage reads elevated vs scrap/secondary quieter"
    - "Victory strip is visually featured (ready for Phase 80 theater polish) without changing show/hide behavior"
    - "Hero uses Anton cream display hierarchy; pipeline stays slim progress chrome; city→type→dropzone→process structure frozen"
    - "Import / scrub desk panels use glass elevation roles without equal-card marketing density"
    - "No new unscoped !important block appended at end of bridge.css; no bridge-* ID or data-action renames"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "Elevation role map + restyled hero/pipeline/scrub/dossier/outcome/import chrome"
      contains: "bridge-elev--primary|bridge-scrub-stage|bridge-outcome-drawer|bridge-victory-strip"
    - path: "public/bridge.html"
      provides: "Optional dual-class elevation hooks only; structure order and IDs frozen"
      contains: "id=\"bridge-scrub-stage\""
  key_links:
    - from: ".bridge-scrub-stage / .bridge-panel--desk / .bridge-panel--stage"
      to: "elevated primary work surface"
      via: "glass-fill-elevated + stronger border/shadow tokens"
      pattern: "bridge-scrub-stage|bridge-panel--stage|bridge-elev--primary"
    - from: ".bridge-outcome-drawer / .bridge-attach-panel--scrap / .bridge-results-details"
      to: "quieter scrap/secondary"
      via: "reduced opacity/border/shadow vs primary"
      pattern: "bridge-outcome-drawer|bridge-attach-panel--scrap|bridge-elev--scrap"
    - from: "#bridge-victory-strip / .bridge-victory-strip"
      to: "featured elevation"
      via: "glass-shadow-featured / gold-orange energy (hidden attr still controls visibility)"
      pattern: "bridge-victory-strip|glass-shadow-featured|phuglee-panel-featured"
    - from: "public/bridge.html structure"
      to: "city → type → dropzone → process path"
      via: "unchanged panel order inside #bridge-scrub-stage"
      pattern: "bridge-city-search|bridge-type-chips|bridge-dropzone|id=\"bridge-process\""
---

<objective>
Establish the Filter desk **elevation hierarchy** and restyle high-frequency chrome (hero, pipeline, scrub stage, dossier, outcome scrap, import/dropzone panel shells) so operators can scan primary vs scrap vs featured without reading labels.

Purpose: CARDS-03 — primary scrub elevated, scrap quieter, victory featured — while freezing cinema structure and contracts.

Output: Updated `public/css/bridge.css` (+ optional dual-class hooks in `public/bridge.html`). No JS/lib changes.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/79-desk-core-restyle/79-CONTEXT.md
@public/bridge.html
@public/css/bridge.css
@public/css/tokens.css
@public/css/phuglee-components.css
@AGENTS.md

**Depends on prior phases (assumed shipped when executing):**
- Phase 76: shared Phuglee tokens (no hex islands for status)
- Phase 77: `.phuglee-panel*`, buttons, chips
- Phase 78: cascade order components → bridge → a11y; dual-class form hooks; dropzone state CSS

**Locked decisions (79-CONTEXT — non-negotiable):**
- Desk density ≠ auth-modal roominess
- City → type → dropzone → process structure frozen
- No 4th unscoped `!important` strata at end of bridge.css
- CSS/markup only

**Out of scope for this plan:** kept/inventory table cell polish (plan 02), kill report / Train theater / motion (Phase 80), dialogs already in 78.

<interfaces>
<!-- Contracts — do not rename. Extracted from bridge.html / tests. -->

### Frozen structure (city → type → dropzone → process)

```html
<header class="bridge-hero">…</header>
<div id="bridge-victory-strip" class="bridge-victory-strip" hidden>…</div>
<div class="bridge-desk">
  <div class="bridge-desk-primary">
    <nav class="bridge-pipeline bridge-pipeline--slim">
      <ol id="bridge-pipeline">… data-step location|type|upload|results …</ol>
    </nav>
    <div class="bridge-scrub-stage" id="bridge-scrub-stage">
      <section class="bridge-panel phuglee-panel bridge-panel--desk">… city + dossier + outcome drawer …</section>
      <section id="bridge-type-panel" class="bridge-panel phuglee-panel bridge-panel--meta" hidden>… type chips …</section>
      <section id="bridge-upload-panel" class="bridge-panel phuglee-panel bridge-panel--stage bridge-import-panel" hidden>
        … date chips · paste · dropzone #bridge-dropzone · #bridge-process …
      </section>
    </div>
    … loading / results / lists …
  </div>
</div>
```

### Locked IDs (never rename)

`bridge-scrub-stage`, `bridge-pipeline`, `bridge-city-search`, `bridge-state`, `bridge-city`, `bridge-city-dossier`, `bridge-outcome-drawer`, `bridge-outcome-drawer-toggle`, `bridge-city-outcome`, `bridge-type-panel`, `bridge-upload-panel`, `bridge-dropzone`, `bridge-file-input`, `bridge-process`, `bridge-paste-text`, `bridge-paste-convert`, `bridge-victory-strip`, `bridge-lists-panel`

### Locked data-* (never change values)

`data-step="location|type|upload|results"`, `data-action` on victory download, `name="bridge-upload-type"` values `code_violation` / `water_shut_off`, outcome radio values, `data-bridge-outcome-drawer`

### Elevation role map (implement in CSS)

| Role | Surfaces | Visual intent |
|------|----------|---------------|
| **Primary elevated** | `#bridge-scrub-stage`, `.bridge-panel--desk`, `.bridge-panel--stage` / import | Stronger glass fill/border/shadow (`--glass-fill-elevated`, `--glass-shadow-float` or equivalent) |
| **Scrap / secondary quieter** | `.bridge-outcome-drawer`, `.bridge-attach-panel--scrap`, `.bridge-results-details`, `.bridge-lists-panel` shell | Lower contrast border, softer fill, slightly reduced shadow — still readable |
| **Featured** | `.bridge-victory-strip` | Gold/orange featured energy via tokens (`--glass-shadow-featured`, gold border) — do not remove `[hidden]` control |

### Shared panel DNA (consume, don't re-invent)

From `phuglee-components.css`:
```css
.phuglee-panel { background: var(--glass-fill); border: 1px solid var(--glass-border); box-shadow: var(--glass-shadow); … }
.phuglee-panel-featured { border-color: var(--glass-border-strong); background: var(--glass-fill-elevated); box-shadow: var(--glass-shadow-featured); }
```

Prefer dual-class hooks (`phuglee-panel-featured` on victory card inner if needed) over duplicating glass rules in bridge.css.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elevation role CSS map (primary / scrap / featured)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Define a single **elevation role** section near the existing desk/scrub rules in `bridge.css` (edit in place — do **not** append a new “v3.0 block” at file end).

1. **Primary elevated** — restyle `#bridge-scrub-stage` and/or `.bridge-scrub-stage` so the whole primary work column reads as one elevated stage (subtle stage padding/background or ring using tokens only — no raw hex islands if Phase 76 tokens cover glass). Strengthen `.bridge-panel--desk` and `.bridge-panel--stage` to use elevated glass (`var(--glass-fill-elevated)`, `var(--glass-border-strong)` or existing orange stage accent via `var(--phuglee-orange)` / gold tokens). Keep desk **dense** spacing (current ~0.65rem gaps OK); do not pad like auth modal.

2. **Scrap quieter** — restyle `.bridge-outcome-drawer`, expanded `.bridge-city-outcome`, `.bridge-attach-panel--scrap`, and secondary shells (`.bridge-results-details` summary chrome, `.bridge-lists-panel` outer shell) so they are clearly **below** primary elevation: quieter border (taupe glass border tokens), lower box-shadow, optional slight opacity on non-interactive chrome only (never opacity that hides focus rings or fails contrast on labels).

3. **Featured victory** — restyle `.bridge-victory-strip` to featured elevation using shared featured tokens / optional dual-class `phuglee-panel-featured` on an inner wrapper **only if** you do not break `#bridge-victory-strip` id, `hidden`, `role="status"`, or child button ids (`bridge-victory-download`, `bridge-victory-next`). Visibility still driven solely by JS `hidden` attribute.

4. **Markup hooks (optional, class-only):** You may add utility classes such as `bridge-elev--primary` / `bridge-elev--scrap` / `bridge-elev--featured` on **existing** wrappers. Forbidden: renaming ids, reordering city→type→upload panels, changing `data-step` / radio values / `data-action`.

5. **Stratigraphy ban:** Prefer overriding existing selectors. Delete or merge duplicate glass rules rather than stacking a 4th unscoped `!important` layer. Scoped `!important` only where an existing fight already requires it (do not invent new unscoped ones).

6. Bump cache: `bridge.css?v=44` → `?v=45` (or next integer) in `bridge.html` when CSS ships.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('public/css/bridge.css','utf8'); const h=fs.readFileSync('public/bridge.html','utf8'); const checks=[['scrub stage',/bridge-scrub-stage/],['outcome scrap',/bridge-outcome-drawer/],['victory',/bridge-victory-strip/],['glass token',/--glass-/]]; for (const [n,re] of checks) { if(!re.test(c)) { console.error('FAIL css missing',n); process.exit(1);} } if(!/bridge\.css\?v=\d+/.test(h)) { console.error('FAIL cache bust'); process.exit(1);} if(!/id=\"bridge-scrub-stage\"/.test(h)) { console.error('FAIL scrub stage id'); process.exit(1);} console.log('79-01 elevation map checks OK');"</automated>
  </verify>
  <done>CSS encodes primary elevated / scrap quieter / victory featured; IDs and structure intact; cache-bust bumped; no end-of-file !important strata dump.</done>
</task>

<task type="auto">
  <name>Task 2: Hero, pipeline, dossier, import shell restyle</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Restyle high-frequency chrome **layout-preserving** (no structure rewrite):

1. **Hero** — `.bridge-hero`, `.bridge-step-label`, `.bridge-title` / `.bridge-hero h1`, `.bridge-lead`: keep left-aligned cream Anton display hierarchy; ensure colors use Phuglee tokens (`--phuglee-cream`, `--phuglee-orange`, `--phuglee-taupe*`, `--font-display` / `--font-body`). No gradient-clip regression on H1 (solid cream). Ops lead stays short; do not rewrite copy.

2. **Pipeline** — `.bridge-pipeline--slim` / `.bridge-pipeline-step` (+ `.is-active` / `.is-complete`): slim progress chips, not teaching wall. Active step uses orange/gold token energy; incomplete quieter. Preserve `#bridge-pipeline`, `data-step` values, and span number markup.

3. **City desk + dossier** — within `.bridge-panel--desk`: densify label/type hierarchy for city search label and dossier stamp (`.bridge-city-dossier-stamp`, last-scan rows) using Outfit/Anton tokens. Dossier remains under city selects; do not unhide by CSS (still JS `[hidden]`).

4. **Type panel shell** — `.bridge-panel--meta` / type chips container: align with elevated stage family but slightly quieter than import stage (meta step). Do **not** reimplement chip selected energy if Phase 77 already owns `.bridge-type-chip` / `.phuglee-chip` — only fix shell/gap/heading density if needed. Preserve radio `name`/`value`.

5. **Import panel shell** — `.bridge-import-panel` / `.bridge-panel--stage`, path titles, OR separator, format badges: glass + gold accent using tokens. Dropzone visual polish may refine borders/shadows via tokens but **must not** change multi-file accept list, `#bridge-dropzone` role/tabindex, or file input. Process CTA (`#bridge-process`) already `phuglee-btn-primary` — only layout density around `.bridge-actions--upload` if needed; no JS enablement changes.

6. **Outcome scrap chrome** — `.bridge-outcome-drawer-toggle` and open state: scrap elevation; expanded radiogroup remains only when `.is-open` (do not break existing `:not(.is-open)` hide rules).

7. **Markup:** dual-class only if a shared panel class is missing on a shell (e.g. ensure key panels still have `phuglee-panel`). Never touch `public/js/**` or `lib/**`.

8. After CSS/HTML edits: run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` (restart via `scripts\restart.ps1` if needed) so local server stays live.
  </action>
  <verify>
    <automated>node --test tests/bridge-desk-cinema.test.js tests/bridge-city-dossier.test.js</automated>
  </verify>
  <done>Hero/pipeline/dossier/import/outcome shells restyled with elevation roles; desk cinema + city dossier contract tests green; city→type→dropzone→process DOM order unchanged.</done>
</task>

</tasks>

<verification>
- Grep `public/js` and `lib` git status: no behavior files modified.
- Visual intent: at idle `/bridge`, scrub desk panels read stronger than outcome scrap toggle; victory strip CSS is featured even while `hidden`.
- Structure: `id="bridge-scrub-stage"` still wraps city + type + upload panels in that order.
- No new unscoped `!important` dump at EOF of bridge.css.
</verification>

<success_criteria>
1. CARDS-03 observable: primary scrub elevated, scrap quieter, victory featured.
2. Frozen contracts: all critical bridge ids / data-step / radio values intact (desk cinema + dossier tests pass).
3. CSS/markup only; verify-live healthy after ship.
</success_criteria>

<output>
After completion, create `.planning/phases/79-desk-core-restyle/79-01-SUMMARY.md`
</output>
