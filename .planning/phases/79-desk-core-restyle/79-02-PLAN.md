---
phase: 79-desk-core-restyle
plan: 02
type: execute
wave: 2
depends_on:
  - 79-01
files_modified:
  - public/css/bridge.css
  - public/bridge.html
autonomous: true
requirements:
  - DESK-03
must_haves:
  truths:
    - "Operator can read kept results table on dark glass with sticky header, row hover, and zebra striping"
    - "Operator can read staging inventory table on the same dark-glass table language"
    - "At ~390px width, both table wraps allow usable horizontal scroll (no clipped columns without scroll)"
    - "Table density is ops-desk tight — not auth-modal roomy padding"
    - "Table structure IDs and thead columns unchanged; no JS sort/filter behavior changes"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "Dark-glass table system for kept + inventory"
      contains: "bridge-results-table|bridge-lists-table|sticky|nth-child"
    - path: "public/bridge.html"
      provides: "Optional dual-class table hooks only"
      contains: "id=\"bridge-results-table\"|id=\"bridge-lists-table\""
  key_links:
    - from: "#bridge-table-wrap / .bridge-table-wrap"
      to: ".bridge-results-table sticky thead"
      via: "overflow-x:auto + position:sticky top:0 on th"
      pattern: "bridge-table-wrap|bridge-results-table th|position:\\s*sticky"
    - from: "#bridge-lists-wrap / .bridge-lists-wrap"
      to: ".bridge-lists-table sticky thead + dark glass"
      via: "overflow-x:auto; shared row hover/zebra rules"
      pattern: "bridge-lists-wrap|bridge-lists-table"
    - from: "tbody rows"
      to: "zebra + hover readability"
      via: "nth-child(even) + tr:hover backgrounds using glass/token rgba"
      pattern: "nth-child\\(even\\)|tbody tr:hover"
    - from: "table min-width / wrap"
      to: "390px usable scroll"
      via: "min-width on table or th nowrap + overflow-x auto on wrap"
      pattern: "overflow-x:\\s*auto|min-width"
---

<objective>
Restyle **kept results** and **staging inventory** tables as dark-glass ops tables: sticky headers, zebra/hover, dense cells, and usable horizontal scroll at 390px width.

Purpose: DESK-03 — operators must read dense multi-column data on Filter without fighting light-table leftovers or unreadable glass contrast.

Output: Table CSS in `public/css/bridge.css` (+ optional dual-class hooks on wraps/tables in `public/bridge.html`). No JS/lib changes.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/79-desk-core-restyle/79-CONTEXT.md
@.planning/phases/79-desk-core-restyle/79-01-PLAN.md
@public/bridge.html
@public/css/bridge.css
@public/css/tokens.css
@public/css/phuglee-components.css
@AGENTS.md

**Prior plan:** 79-01 ships elevation hierarchy — table shells (`.bridge-results-details`, `.bridge-lists-panel`) should already sit at scrap/secondary elevation. This plan paints the **data grids** inside them.

**Locked decisions:**
- Desk density ≠ auth-modal roominess on tables (tighter cell padding than forms)
- No 4th unscoped `!important` strata
- CSS/markup only; freeze ids / data-action
- Structure frozen — do not reorder columns or rename sort keys

**Out of scope:** Train group cards, kill report KPI grid, shift HUD sticky bar, dialog tables (history list), Phase 80 theater.

<interfaces>
### Kept table contract (bridge.html)

```html
<details class="bridge-results-details" id="bridge-results-details" open>
  <summary class="bridge-results-details-summary">Kept table</summary>
  <div class="bridge-results-details-body">
    <div class="bridge-results-toolbar" id="bridge-results-toolbar" hidden>…</div>
    <div class="bridge-table-wrap" id="bridge-table-wrap" hidden>
      <table class="bridge-results-table" id="bridge-results-table">
        <thead>
          <tr>
            <th scope="col" data-sort="streetAddress">Street Address</th>
            <th scope="col" data-sort="violationIssueType">Violation/Issue</th>
            <th scope="col" data-sort="category">Category</th>
            <th scope="col" data-sort="distressedSignalTag">Distressed Tag</th>
            <th scope="col" data-sort="confidenceLevel">Confidence</th>
            <th scope="col" data-sort="violationDate">Date</th>
          </tr>
        </thead>
        <tbody id="bridge-results-body"></tbody>
      </table>
    </div>
    <div class="bridge-pagination" id="bridge-pagination" hidden></div>
  </div>
</details>
```

### Inventory table contract

```html
<div id="bridge-lists-wrap" class="bridge-lists-wrap" hidden>
  <table class="bridge-lists-table" id="bridge-lists-table">
    <thead>… check · Type · List name · Uploaded · Records · Status · City · Actions …</thead>
    <tbody id="bridge-lists-body"></tbody>
  </table>
</div>
```

### Existing CSS anchors (edit in place ~L1770 results, ~L2464 lists)

```css
.bridge-table-wrap { overflow-x: auto; … }
.bridge-results-table th { position: sticky; top: 0; … }
.bridge-results-table tbody tr:hover td { … }
.bridge-lists-wrap { overflow-x: auto; }
.bridge-lists-table th, .bridge-lists-table td { … }
/* Missing today: zebra on results; sticky + zebra + dark glass parity on lists */
```

### Optional shared hook (if Phase 77 added `.phuglee-table`)

Dual-class: `class="bridge-results-table phuglee-table"` — only if `.phuglee-table` exists in `phuglee-components.css`. Otherwise keep Filter-only table rules in `bridge.css` (research allows Filter-only tables for v3.0).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kept results table — dark glass, sticky, zebra, hover, dense</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <behavior>
    - CSS for `.bridge-results-table th` includes `position: sticky` and solid dark header background (not transparent)
    - CSS for zebra: `.bridge-results-table tbody tr:nth-child(even)` (or `even td`) sets a subtle dark stripe
    - CSS for hover: `tbody tr:hover` (or `tr:hover td`) raises row above zebra
    - `.bridge-table-wrap` keeps `overflow-x: auto` and dark glass border/background via tokens
    - Cell padding denser than auth inputs (target ~0.4–0.55rem vertical); optional mono on address/date via `var(--font-mono)` without changing DOM
    - All `data-sort` attributes and thead labels unchanged
  </behavior>
  <action>
1. **Unify dark-glass shell** on `.bridge-table-wrap`: background/border from glass tokens (`var(--glass-fill)`, `var(--glass-border)`, or equivalent rgba using Phuglee neutrals — prefer tokens over new hex). Keep `overflow-x: auto`.

2. **Sticky header** — ensure `.bridge-results-table th` stays `position: sticky; top: 0` with an opaque/near-opaque dark header background so body rows do not show through while scrolling vertically inside the wrap. If vertical scroll is only page-level, sticky still must not break; set a sensible `z-index` local to the table (not competing with dialogs — keep &lt; typeahead/dialog scales from Phase 76).

3. **Zebra + hover** — add even-row zebra using low-contrast dark stripe; hover uses restrained orange/gold wash (`rgba` from `--phuglee-orange` if needed). Sorted header (`.is-sorted`) keeps orange token accent.

4. **Density** — tighten `th`/`td` padding vs form controls; `white-space: nowrap` on headers OK; body cells may wrap address/issue but keep vertical rhythm dense. Do **not** apply auth-modal field padding.

5. **390 scroll** — give `.bridge-results-table` a `min-width` (e.g. ~640–720px) so narrow viewports scroll horizontally inside `.bridge-table-wrap` instead of crushing columns to unreadable widths.

6. **Markup** — optional dual-class only; never change `id="bridge-results-table"`, `id="bridge-table-wrap"`, `id="bridge-results-body"`, or `data-sort` keys.

7. Edit existing table rules in place (~L1770); do not append a new strata at EOF.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('public/css/bridge.css','utf8'); const need=[['sticky',/\.bridge-results-table th\s*\{[^}]*position:\s*sticky/s],['zebra',/\.bridge-results-table[^{]*tbody[^{]*tr:nth-child\(even\)|\.bridge-results-table tbody tr:nth-child\(even\)/],['hover',/\.bridge-results-table tbody tr:hover/],['wrap scroll',/\.bridge-table-wrap\s*\{[^}]*overflow-x:\s*auto/s],['min-width',/\.bridge-results-table\s*\{[^}]*min-width/s]]; let fail=0; for (const [n,re] of need) { if(!re.test(c)) { console.error('FAIL',n); fail=1; } } process.exit(fail);"</automated>
  </verify>
  <done>Kept table CSS has sticky header, zebra, hover, dark glass wrap, min-width for 390 scroll; DOM contracts unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inventory lists table parity + cache-bust + live verify</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <behavior>
    - `.bridge-lists-table th` sticky with dark header background matching kept table language
    - Zebra + hover on `.bridge-lists-table tbody tr`
    - `.bridge-lists-wrap` overflow-x auto + dark glass treatment parity
    - Table min-width ensures horizontal scroll at 390
    - `id="bridge-lists-table"`, select-all checkbox, column order unchanged
  </behavior>
  <action>
1. **Parity restyle** `.bridge-lists-wrap` / `.bridge-lists-table` to the same dark-glass language as kept table (shared private selectors OK, e.g. group `.bridge-table-wrap, .bridge-lists-wrap` and `.bridge-results-table, .bridge-lists-table` for shared density/zebra/sticky — then keep list-specific action column / type cell rules).

2. **Sticky lists header** — lists table currently lacks sticky `th`; add it with opaque dark header.

3. **Zebra + hover** — match kept table intensity (inventory can be slightly calmer but same system).

4. **390 scroll** — `min-width` on `.bridge-lists-table` (wider column set — e.g. ~800px+) + existing `overflow-x: auto` on wrap.

5. **Secondary elevation** — ensure list panel remains scrap/secondary from 79-01 (do not promote inventory to primary elevated stage).

6. **Cache bust** — bump `bridge.css?v=` in `bridge.html` if not already bumped in this phase execution.

7. **Live server** — after edits:
   ```
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
   ```
   If fail, `scripts\restart.ps1` then re-verify. Exit 0 required.

8. **Regression** — run desk cinema + a quick suite slice if available; do not wipe `data/filter-lists` or brain.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('public/css/bridge.css','utf8'); const h=fs.readFileSync('public/bridge.html','utf8'); const need=[['lists sticky',/\.bridge-lists-table th\s*\{[^}]*position:\s*sticky/s],['lists zebra',/\.bridge-lists-table[^\n]*tbody[^\n]*tr:nth-child\(even\)|\.bridge-lists-table tbody tr:nth-child\(even\)/],['lists hover',/\.bridge-lists-table tbody tr:hover/],['lists wrap',/\.bridge-lists-wrap\s*\{[^}]*overflow-x:\s*auto/s]]; let fail=0; for (const [n,re] of need) { if(!re.test(c)) { console.error('FAIL',n); fail=1; } } if(!/id=\"bridge-lists-table\"/.test(h)||!/id=\"bridge-results-table\"/.test(h)) { console.error('FAIL table ids'); fail=1; } process.exit(fail);" ; node --test tests/bridge-desk-cinema.test.js ; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>Inventory table matches dark-glass sticky/zebra/hover/scroll system; IDs intact; desk cinema green; verify-live exit 0.</done>
</task>

</tasks>

<verification>
- DESK-03: kept + inventory readable on dark glass with sticky header, hover, zebra, 390 horizontal scroll.
- No thead column renames; no JS changes to sort/filter/list render.
- Elevation: tables live inside secondary shells from 79-01 (not competing with primary scrub stage).
- Permanent: verify-live 200; desk cinema tests pass.
</verification>

<success_criteria>
1. Both tables share one dark-glass language (sticky + zebra + hover + overflow-x).
2. Dense ops padding — not auth-modal roominess.
3. Contracts frozen; CSS/markup only; server live after ship.
</success_criteria>

<output>
After completion, create `.planning/phases/79-desk-core-restyle/79-02-SUMMARY.md`
</output>
