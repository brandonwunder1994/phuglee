# Phase 26 Plan: Shell Pages — Hub, Collect, Bridge

**Requirements:** BRAND-18–20  
**Repo:** `distress-os`  
**Date:** 2026-07-06

## Goal

Editorial phuglee brand on `/heat`, `/collect`, `/bridge` — pattern accents, motion stagger, phuglee panels/buttons.

## Tasks

1. `hub.css` + `heat.html` — hero gradient, pattern accent, pricing parity, CTA strip
2. `collect-records.css` + `collect.html` — poster hero, dialogs, choice cards
3. `bridge.css` + `bridge.html` + `bridge.js` — step badges, phuglee error + retry
4. `phuglee-motion` stagger on all three pages
5. `npm test` green