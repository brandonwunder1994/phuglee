---
phase: 76-tokens-layer-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - public/css/bridge.css
  - public/bridge.html
  - public/index.html
  - public/collect.html
  - public/command.html
  - public/heat.html
  - public/vault.html
autonomous: true
requirements:
  - TOKENS-01
  - TOKENS-02
  - TOKENS-03
  - TOKENS-04
must_haves:
  truths:
    - "Filter desk chrome status colors (success/error) use --phuglee-success|--phuglee-danger or --status-*-fg — no bare #8fd4a0 / #f0a0a0 islands on status lines"
    - "City typeahead + scanned toast + history dialog z-index values come from var(--z-*)"
    - "Hero/title hierarchy still uses --font-display (Anton) + cream; body uses --font-body; dense data uses --font-mono"
    - "Desk panels continue to inherit glass DNA via existing dual-class phuglee-panel / glass tokens (no new hex glass palette)"
    - "CSS cache bust: tokens shared trio glass2→glass3 (or next) on all consumers; bridge.css ?v= bumped"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "Token-wired desk chrome (status, z-index, reduced hex islands)"
      contains: "var(--z-toast)"
    - path: "public/bridge.html"
      provides: "Cache-bust query params for tokens + bridge CSS"
      contains: "tokens.css?v="
  key_links:
    - from: "public/css/bridge.css"
      to: "public/css/tokens.css"
      via: "var(--phuglee-*), var(--z-*), var(--status-*)"
      pattern: "var\\(--(phuglee-success|phuglee-danger|z-toast|z-dialog|status-)"
    - from: "public/bridge.html"
      to: "public/css/tokens.css + bridge.css"
      via: "link ?v= query cache bust"
      pattern: "tokens\\.css\\?v=glass"
---

<objective>
Wire Filter (`bridge.css`) to Layer 0 tokens: ban hex islands in desk chrome status/search, map stacking to `--z-*`, lock type hierarchy on shared fonts, and ship cache-bust `?v=` bumps.

Purpose: Operators see Filter chrome in the same Phuglee DNA as login/home before mass paint (phases 77–80).
Output: Token-consuming `bridge.css` + bumped HTML link tags. CSS only — no JS.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-tokens-layer-audit/76-CONTEXT.md
@.planning/phases/76-tokens-layer-audit/76-01-PLAN.md
@.planning/research/STACK.md
@public/css/tokens.css
@public/css/bridge.css
@public/bridge.html
@public/css/auth.css

<interfaces>
<!-- Tokens created/confirmed in 76-01 — consume these -->
```css
var(--z-bg) var(--z-main) var(--z-sticky)
var(--z-typeahead) var(--z-typeahead-menu) var(--z-dialog) var(--z-toast)
var(--status-success-fg) var(--status-danger-fg) var(--status-warn-fg)
var(--phuglee-success) var(--phuglee-danger) var(--phuglee-warn)
var(--phuglee-cream) var(--phuglee-orange) var(--phuglee-gold) var(--phuglee-stone)
var(--font-display) var(--font-body) var(--font-mono)
var(--glass-fill) var(--glass-border) var(--glass-shadow*)
var(--desk-gap) var(--chip-*) var(--row-*)  /* optional adopt where one-liner */
```

Known bare hex islands to kill or alias (TOKENS-01 / TOKENS-04) — status & desk chrome:
```
.bridge-outcome-status.is-success  → #8fd4a0
.bridge-outcome-status.is-error    → #f0a0a0
.bridge-save-status.is-error|success → #e8a0a0 / #9fd4a8
.bridge-attach-status.is-success   → #9fd4a8
.bridge-train-status.is-error|success → #e8a0a0 / #9fd4a8
.bridge-paste-status.is-success|error → #8fd4a0 / #f0a0a0
.bridge-city-search* backgrounds/text → #14110e, #1c1712, #f5efe4, #2a2118, …
```

Known z-index islands to map (TOKENS layer audit):
```
.bridge-main / .bridge-main > *     z-index: 1        → var(--z-main)
.bridge-city-search-block           z-index: 40 !important → var(--z-typeahead)
.bridge-city-search-results         z-index: 50       → var(--z-typeahead-menu)
.bridge-history-dialog              z-index: 10000    → var(--z-dialog)
.bridge-history-dialog::backdrop    z-index: 9999     → keep below dialog (var(--z-dialog) - 1 is invalid in CSS — use a token or leave backdrop without z if dialog top-layer; if kept, add --z-dialog-backdrop: 9999 in tokens only if needed, or use var(--z-dialog) on dialog and omit backdrop z)
.bridge-scanned-toast               z-index: 12000    → var(--z-toast)
```

Cache-bust protocol (STACK.md):
- Shared trio uses synchronized tag: tokens + distress-glass + phuglee-components currently `glass2` → bump to `glass3` on **all** HTML consumers when tokens.css changed
- bridge.css only: `?v=44` → `?v=45` on bridge.html only

Hard constraints:
- CSS + HTML link query only — **no** public/js/bridge*.js, no lib/**
- Do **not** rename bridge-* IDs or data-action
- Prefer token aliases this phase; do **not** full-paint theater/kill/Train (phases 79–80)
- Do **not** flip cascade order (bridge before components) — that is Phase 78
- Do **not** import auth.css or home.css into bridge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace desk-chrome hex islands + wire z-index + type token hygiene</name>
  <files>public/css/bridge.css</files>
  <action>
Edit `public/css/bridge.css` only (CSS). Scope is **desk chrome token wiring**, not full surface restyle.

### A) Status colors (TOKENS-04) — required
Replace bare success/error greens and reds on status lines with semantic tokens:

| Selector | Replace | With |
|----------|---------|------|
| `.bridge-outcome-status.is-success` | `#8fd4a0` | `var(--status-success-fg, var(--phuglee-success))` |
| `.bridge-outcome-status.is-error` | `#f0a0a0` | `var(--status-danger-fg, var(--phuglee-danger))` |
| `.bridge-save-status.is-success` | `#9fd4a8` | same success token |
| `.bridge-save-status.is-error` | `#e8a0a0` | same danger token |
| `.bridge-attach-status.is-success` | `#9fd4a8` | success token |
| `.bridge-train-status.is-success` / `.is-error` | hex | success / danger tokens |
| `.bridge-paste-status.is-success` / `.is-error` | hex | success / danger tokens |

Also fix any other **status-line** bare greens/reds found via grep for `#8fd4a0|#9fd4a8|#f0a0a0|#e8a0a0|#f0b8b8` in status/error/success classes. Leave kill-report theater climax colors if they are domain-specific theater paint (phases 79–80) **unless** they are clearly success/error status text — when ambiguous, prefer semantic tokens.

### B) City typeahead hex island (TOKENS-01) — required
In `.bridge-city-search`, `.bridge-city-search-results`, `.bridge-city-search-option*`:
- Map solid backgrounds to glass/surface tokens, e.g. `var(--glass-bg-solid)` / `var(--phuglee-black-mid)` / `var(--bg-elevated)` rather than `#14110e` / `#1c1712`
- Map text to `var(--phuglee-cream)` instead of `#f5efe4` / `#fff8ee`
- Hover option bg → `var(--row-hover-bg)` or `var(--glass-bg-elevated)` instead of `#2a2118`
- Keep **opaque readable menu** (comment already says glass made options unreadable) — solid token OK; just no random hex

### C) Z-index scale (layer audit) — required
Wire stacking to plan-01 tokens:
- `.bridge-main` and `.bridge-main > *` → `z-index: var(--z-main)`
- `.bridge-city-search-block` → `z-index: var(--z-typeahead) !important` (keep !important if still needed to beat `.phuglee-panel > *`)
- `.bridge-city-search-results` → `z-index: var(--z-typeahead-menu)`
- `.bridge-history-dialog` → `z-index: var(--z-dialog)`
- `.bridge-scanned-toast` → `z-index: var(--z-toast)`
- Backdrop: if a numeric z is required, either drop it (top-layer dialogs) or use a token; do not leave unexplained `9999` without a comment pointing at the scale

### D) Type hierarchy hygiene (TOKENS-02) — light touch
Confirm (do not rewrite layout):
- `.bridge-title` / hero h1 keep `font-family: var(--font-display)` and cream via `var(--phuglee-cream)`
- Body text uses `var(--font-body)` (body already does)
- Dense data cells already on `var(--font-mono)` — leave; if any bare `font-family: monospace` appears in desk chrome, switch to `var(--font-mono)`
- Prefer `var(--text-eyebrow)` for eyebrow font-size if currently hardcoded `0.72rem` on `.bridge-eyebrow` (optional one-liner)

### E) Glass atmosphere (TOKENS-03) — light touch only
- Do **not** mass-delete glass re-declarations on `.bridge-panel.phuglee-panel` (later phases).
- Where desk chrome already uses raw `rgba(229, 132, 53, …)` for borders that match glass tokens, you may alias **obvious duplicates** to `var(--glass-border)` / `var(--border-hot)` in the same selectors you already touch (city search / mission board). Do not hunt the entire 85KB file.

### F) Fallback cleanup (agent discretion)
Many rules use `var(--phuglee-cream, #f5f0e8)` with slightly wrong fallback hex. When editing a nearby rule, drop the incorrect fallback or use the true token hex (`#f5f2e4`). Not required to rewrite every fallback in the file this phase.

Forbidden:
- JS changes
- Renaming IDs / data-action
- New parallel CSS file
- Full kill/Train/victory paint
- Cascade link-order flip
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$b=Get-Content 'public/css/bridge.css' -Raw; foreach ($p in @('var(--z-toast)','var(--z-dialog)','var(--z-typeahead)','var(--z-main)')) { if ($b -notmatch [regex]::Escape($p)) { Write-Error \"missing $p\"; exit 1 } }; $bad = Select-String -Path 'public/css/bridge.css' -Pattern 'bridge-(outcome|save|attach|train|paste)-status[\s\S]{0,120}#[89a-fA-F][0-9a-fA-F]{5}' -AllMatches; if ($bad) { Write-Error 'status hex island remains'; $bad | ForEach-Object { $_.Line }; exit 1 }; Write-Output 'bridge token wire ok'"</automated>
  </verify>
  <done>Status lines use semantic tokens; z-index uses --z-*; city search hex reduced; type/glass DNA preserved without theater paint.</done>
</task>

<task type="auto">
  <name>Task 2: Cache-bust ?v= for tokens trio + bridge.css</name>
  <files>public/bridge.html, public/index.html, public/collect.html, public/command.html, public/heat.html, public/vault.html</files>
  <action>
Ship cache bust per STACK.md protocol after CSS token changes:

1. **Shared system trio** — on every HTML page that loads them, bump:
   - `tokens.css?v=glass2` → `tokens.css?v=glass3`
   - `distress-glass.css?v=glass2` → `distress-glass.css?v=glass3` (same pages)
   - `phuglee-components.css?v=glass2` → `phuglee-components.css?v=glass3` (same pages)

   Pages (verified): `public/bridge.html`, `public/index.html`, `public/collect.html`, `public/command.html`, `public/heat.html`, `public/vault.html`.

2. **Page CSS** — only on `public/bridge.html`:
   - `bridge.css?v=44` → `bridge.css?v=45`

3. Do **not** change JS `?v=` params. Do not reorder stylesheet links (cascade flip is Phase 78).

4. After edits, run live verify (AGENTS.md mandatory for public/ edits):
   ```
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
   ```
   If down, `scripts\restart.ps1` then re-verify. Exit 0 required.

5. Do not touch `data/filter-lists/` or brain stores.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$pages=@('public/bridge.html','public/index.html','public/collect.html','public/command.html','public/heat.html','public/vault.html'); foreach($p in $pages){ $c=Get-Content $p -Raw; if($c -notmatch 'tokens\.css\?v=glass3'){ Write-Error \"$p tokens not glass3\"; exit 1 } }; $b=Get-Content 'public/bridge.html' -Raw; if($b -notmatch 'bridge\.css\?v=45'){ Write-Error 'bridge.css not v=45'; exit 1 }; if($b -match 'tokens\.css\?v=glass2'){ Write-Error 'stale glass2 on bridge'; exit 1 }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; Write-Output 'cache bust + live ok'"</automated>
  </verify>
  <done>All token consumers on glass3; bridge.css on v=45; verify-live exits 0.</done>
</task>

</tasks>

<verification>
- Status selectors no longer hardcode pastel green/red hex
- `var(--z-toast)` and `var(--z-dialog)` appear in bridge.css
- bridge.html links: tokens glass3 + bridge.css?v=45
- Site-wide tokens consumers share glass3
- `scripts/verify-live.ps1` exit 0
- Optional smoke: `npm test` still green (no JS change expected) — run if time; not a visual gate for this plan
</verification>

<success_criteria>
1. TOKENS-01: Desk chrome prefers `var(--phuglee-*)` / glass tokens; major hex islands on status + city search removed
2. TOKENS-02: Display/body/mono still via shared font tokens on Filter hierarchy
3. TOKENS-03: No second glass palette; city/search surfaces use tokenized solids/glass
4. TOKENS-04: Success/error status lines use canonical semantic tokens only
5. Maintainer z-scale is **used** by bridge stacking (not docs-only)
6. Cache bust shipped; live health 200
</success_criteria>

<output>
After completion, create `.planning/phases/76-tokens-layer-audit/76-02-SUMMARY.md`
</output>
