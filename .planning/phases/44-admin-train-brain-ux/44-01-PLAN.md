---
phase: 44-admin-train-brain-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridge-train-ux.test.js
  - public/bridge.html
  - public/css/bridge.css
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-03
  - TRAIN-04
must_haves:
  truths:
    - "Train brain markup exists inside Filter results with two labeled sections (marked distressed / not marked distressed)"
    - "Train wrap is hidden by default so non-admin never sees train chrome before JS gate"
    - "Mode tabs (Kept list | Train brain) exist with tablist/tab/tabpanel roles"
    - "CSS styles group cards, signal chips, description samples, and Approve/Deny actions using existing bridge/phuglee tokens"
  artifacts:
    - path: "tests/bridge-train-ux.test.js"
      provides: "Static + source tests for train shell (TRAIN-01/03/04 structure)"
      min_lines: 80
    - path: "public/bridge.html"
      provides: "Train wrap shell with two sections + mode tabs, default hidden"
      contains: "bridge-train-wrap"
    - path: "public/css/bridge.css"
      provides: "Train brain card/chip/action styles matching bridge design system"
      contains: "bridge-train-group"
  key_links:
    - from: "public/bridge.html#bridge-train-wrap"
      to: "public/bridge.html#bridge-results-panel"
      via: "train wrap nested inside results panel after KPI grid"
      pattern: "bridge-results-panel[\\s\\S]*bridge-train-wrap"
    - from: "public/bridge.html"
      to: "public/css/bridge.css"
      via: "cache-busted stylesheet link"
      pattern: "bridge\\.css\\?v="
---

<objective>
Add the Filter Train brain shell: automated tests, admin-only markup (default hidden), and bridge design-system CSS for mode tabs + group cards.

Purpose: Establish the TRAIN-01/03/04 structure before JS wiring so non-admin never sees open chrome and executors have a fixed DOM contract.
Output: `tests/bridge-train-ux.test.js`, train markup in `public/bridge.html`, train styles in `public/css/bridge.css`, cache-bust bump.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-admin-train-brain-ux/44-CONTEXT.md
@.planning/phases/44-admin-train-brain-ux/44-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md

@public/bridge.html
@public/css/bridge.css
@tests/a11y-seo.test.js
@docs/superpowers/specs/2026-07-09-filter-superpower-brain-design.md

<interfaces>
<!-- DOM contract for plan 02. Do not invent alternate IDs. -->

Train shell (insert inside `#bridge-results-panel` AFTER `#bridge-kpi-grid` / `#bridge-stub-note`, BEFORE `#bridge-results-toolbar`):

```html
<div id="bridge-train-wrap" hidden>
  <div class="bridge-results-mode" role="tablist" aria-label="Results mode">
    <button type="button" role="tab" id="bridge-mode-kept" data-mode="kept"
      class="bridge-mode-tab is-active" aria-selected="true" aria-controls="bridge-kept-view">Kept list</button>
    <button type="button" role="tab" id="bridge-mode-train" data-mode="train"
      class="bridge-mode-tab" aria-selected="false" aria-controls="bridge-train-panel">Train brain</button>
  </div>
  <p id="bridge-train-status" class="bridge-train-status" hidden role="status"></p>
  <div id="bridge-kept-view">
    <!-- NOT a new wrapper around existing toolbar/table — JS will treat toolbar+table+pagination as kept view.
         Either leave #bridge-kept-view as empty marker OR omit and use existing IDs only.
         PREFERRED: omit wrapper; JS toggles existing #bridge-results-toolbar, #bridge-table-wrap, #bridge-pagination. -->
  </div>
  <div id="bridge-train-panel" hidden role="tabpanel" aria-labelledby="bridge-mode-train">
    <section class="bridge-train-section" aria-labelledby="train-distressed-h">
      <h3 id="train-distressed-h">Marked distressed</h3>
      <p class="bridge-panel-lead">Approve if the tagger is right. Deny removes these from the kept list (training writes in phase 45).</p>
      <div id="bridge-train-distressed" class="bridge-train-groups"></div>
    </section>
    <section class="bridge-train-section" aria-labelledby="train-fn-h">
      <h3 id="train-fn-h">Not marked distressed</h3>
      <p class="bridge-panel-lead">Catch false negatives. Approve promotes into the kept list (phase 45).</p>
      <div id="bridge-train-not-distressed" class="bridge-train-groups"></div>
    </section>
  </div>
</div>
```

Required stable IDs (tests assert these exact strings):
- `bridge-train-wrap` (must have `hidden` attribute in static HTML)
- `bridge-mode-kept`, `bridge-mode-train`
- `bridge-train-status`
- `bridge-train-panel`
- `train-distressed-h`, `bridge-train-distressed`
- `train-fn-h`, `bridge-train-not-distressed`

Phase 43 ReviewGroup shape (consumed in plan 02; CSS only needs class hooks now):
```js
// ReviewGroup = {
//   groupId, section: 'distressed' | 'not_distressed',
//   violationTypeLabel, violationTypeKey, count, rowIds[],
//   sampleAddresses[], matchedIndicators[], descriptionSamples[],
//   confidenceLevels[], isSingleton
// }
// reviewGroups = { distressed: ReviewGroup[], notDistressed: ReviewGroup[] }
```

Locked decisions (CONTEXT — non-negotiable):
- Admin = session username exact `admin` (JS in plan 02)
- Group by violation type as returned in reviewGroups (no client regroup)
- Match existing bridge design system (no new visual language)
- Vanilla HTML/CSS/JS in public/bridge.*
- Deferred: real decision persistence (45), phrase panel (46)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create bridge-train-ux tests (shell assertions)</name>
  <files>tests/bridge-train-ux.test.js</files>
  <read_first>
    tests/a11y-seo.test.js
    tests/auth-session.test.js
    public/bridge.html
    .planning/phases/44-admin-train-brain-ux/44-RESEARCH.md
  </read_first>
  <behavior>
    - HTML has `#bridge-train-wrap` with default `hidden`
    - HTML has mode tabs: `bridge-mode-kept`, `bridge-mode-train`, role=tablist
    - HTML has two sections: `train-distressed-h` / `bridge-train-distressed` and `train-fn-h` / `bridge-train-not-distressed`
    - HTML has `#bridge-train-panel` and `#bridge-train-status` with role=status
    - CSS defines `.bridge-train-group`, `.bridge-results-mode`, `.bridge-mode-tab`, `.bridge-train-signals`, `.bridge-train-descriptions`, `.bridge-train-actions`
    - CSS does NOT invent a new palette (reuse --phuglee-* / rgba glass patterns already in bridge.css)
    - bridge.js cache-bust query present on bridge.html (may fail until plan 02 bumps JS; assert CSS bump after this plan)
  </behavior>
  <action>
    Create `tests/bridge-train-ux.test.js` using `node:test` + `node:assert/strict` and `fs.readFileSync` of `public/bridge.html` + `public/css/bridge.css` (static pattern from `tests/a11y-seo.test.js`).

    Include tests that FAIL until Tasks 2–3 land, then pass:
    1. `bridge train wrap exists and is hidden by default` — html includes `id="bridge-train-wrap"` and the opening tag (or nearby) includes `hidden`
    2. `bridge train mode tabs present` — ids `bridge-mode-kept`, `bridge-mode-train`; `role="tablist"`; Train brain label text
    3. `bridge train has distressed and not-distressed sections` — both heading ids + both group container ids
    4. `bridge train status line present` — `id="bridge-train-status"` and `role="status"`
    5. `bridge train CSS defines group card vocabulary` — css includes `.bridge-train-group`, `.bridge-results-mode`, `.bridge-mode-tab`, `.bridge-train-signals`, `.bridge-train-descriptions`, `.bridge-train-approve` or `.bridge-train-actions`, `.bridge-train-deny`

    Also add placeholder tests (can `test.skip` or assert source-not-yet for plan 02) ONLY if needed for Nyquist map — prefer write plan-02 behavioral tests in 44-02. Keep this file focused on shell.

    Do NOT invent browser automation. Static file tests only.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-ux.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/bridge-train-ux.test.js exists and uses node:test
    - After Tasks 2–3, `node --test tests/bridge-train-ux.test.js` exits 0
    - Tests assert exact IDs listed in interfaces
  </acceptance_criteria>
  <done>Shell test file exists; RED until markup/CSS land; GREEN after Tasks 2–3.</done>
</task>

<task type="auto">
  <name>Task 2: Add Train brain markup shell to bridge.html</name>
  <files>public/bridge.html</files>
  <read_first>
    public/bridge.html
    .planning/phases/44-admin-train-brain-ux/44-CONTEXT.md
    .planning/phases/44-admin-train-brain-ux/44-RESEARCH.md
  </read_first>
  <action>
    Edit `public/bridge.html` inside `#bridge-results-panel`:

    1. Insert `#bridge-train-wrap` **after** `#bridge-kpi-grid` and `#bridge-stub-note`, **before** `#bridge-results-toolbar`.
    2. Markup must match the interface block above (mode tablist, status line, train panel with two sections).
    3. `#bridge-train-wrap` MUST include the `hidden` attribute in static HTML (TRAIN-03 fail-closed).
    4. `#bridge-train-panel` MUST include `hidden` by default.
    5. Do **not** wrap existing toolbar/table in a new container that could break non-admin results — leave `#bridge-results-toolbar`, `#bridge-table-wrap`, `#bridge-pagination`, `#bridge-save-panel`, `#bridge-attach-panel` as siblings after the train wrap.
    6. Bump stylesheet cache: change `href="/css/bridge.css?v=5"` → `?v=6` (or next integer if already higher).
    7. Leave `bridge.js?v=` alone if plan 02 will bump it; if you touch nothing else, still ensure HTML validates and IDs are unique.

    Copy for leads (discretion, locked structure):
    - Marked distressed: "Approve if the tagger is right. Deny removes these from the kept list (training writes in phase 45)."
    - Not marked: "Catch false negatives. Approve promotes into the kept list (phase 45)."

    Do NOT add Approve/Deny buttons in static HTML — cards are JS-rendered in plan 02.
    Do NOT implement decision API or any script logic in this plan.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-ux.test.js</automated>
  </verify>
  <acceptance_criteria>
    - public/bridge.html contains id="bridge-train-wrap" with hidden
    - public/bridge.html contains bridge-mode-kept, bridge-mode-train, bridge-train-distressed, bridge-train-not-distressed
    - public/bridge.html contains train-distressed-h and train-fn-h
    - public/bridge.html link to bridge.css has increased ?v= cache bust
    - Train wrap is a descendant of bridge-results-panel (appears before bridge-results-toolbar)
  </acceptance_criteria>
  <done>Static train shell present, hidden by default, two sections labeled, mode tabs in DOM.</done>
</task>

<task type="auto">
  <name>Task 3: Add Train brain CSS matching bridge design system</name>
  <files>public/css/bridge.css</files>
  <read_first>
    public/css/bridge.css
    public/css/auth.css
    .planning/phases/44-admin-train-brain-ux/44-RESEARCH.md
  </read_first>
  <action>
    Append train-brain styles to `public/css/bridge.css` (end of file or after tag/KPI section). Match existing glass/dark bridge vocabulary — NO new color system, NO analyzer CSS imports.

    Required selectors (tests assert presence):
    - `.bridge-results-mode` — segmented control grid (2 cols), padding, dark rgba background, border, radius `var(--radius-md)`
    - `.bridge-mode-tab` + `.bridge-mode-tab.is-active` — tab button look; active uses orange/gold accent similar to existing bridge primary cues
    - `.bridge-train-status` — status line under tabs (mirror `.bridge-save-status` spacing/typography)
    - `.bridge-train-section` — section spacing between distressed / FN
    - `.bridge-train-groups` — stack container
    - `.bridge-train-group` — card: padding ~0.85rem, dark rgba bg, border, radius `var(--radius-sm)`, margin-bottom
    - `.bridge-train-group-head` / `.bridge-train-group-title` / `.bridge-train-count` — header row with type + ×count
    - `.bridge-train-badge` / `.bridge-train-badge--singleton` — singleton chip
    - `.bridge-train-signals` — chip row gap
    - `.bridge-train-descriptions` — sample list (muted small text)
    - `.bridge-train-addresses` — muted address line
    - `.bridge-train-actions` — button row gap
    - `.bridge-train-deny` — danger-tint border/color (e.g. rgba red/coral) without inventing a new brand palette
    - `.bridge-train-muted` — empty-state muted text
    - Optional `.bridge-train-group.is-pending` — subtle opacity for stub click visual (plan 02)

    Mobile: mode tabs full-width stack if needed (`@media (max-width: 640px)` grid 1fr). Avoid horizontal overflow.

    Reuse existing `.bridge-btn`, `.bridge-btn-primary`, `.bridge-btn-ghost`, `.bridge-tag`, `.bridge-tag--strong`, `.bridge-panel-lead` — do not re-style those globally.

    After CSS lands, re-run shell tests until green.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-ux.test.js</automated>
  </verify>
  <acceptance_criteria>
    - public/css/bridge.css contains .bridge-train-group and .bridge-results-mode and .bridge-mode-tab
    - public/css/bridge.css contains .bridge-train-signals and .bridge-train-descriptions and .bridge-train-actions
    - Styles use existing tokens (var(--radius-*), var(--phuglee-*) or same rgba glass patterns as .bridge-kpi)
    - node --test tests/bridge-train-ux.test.js exits 0
  </acceptance_criteria>
  <done>Train UI CSS complete and shell tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/bridge-train-ux.test.js` exits 0
- [ ] Train wrap hidden by default in static HTML
- [ ] Two section headings + group containers present
- [ ] CSS vocabulary for cards/chips/actions present
- [ ] No decision API, no bridge.js train logic (plan 02)
</verification>

<success_criteria>
- Shell tests green
- Markup contract fixed for plan 02
- Design system match (no new visual language)
- TRAIN-01 structure + TRAIN-03 fail-closed default + TRAIN-04 style hooks ready
</success_criteria>

<output>
After completion, create `.planning/phases/44-admin-train-brain-ux/44-01-SUMMARY.md`
</output>
