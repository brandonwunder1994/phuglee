---
phase: 54-regression-lock
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified: []
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Full npm test suite exits 0 (all engine + unit contracts including v1.8 locks)"
    - "scripts/verify-live.ps1 exits 0 (health + homepage HTTP 200)"
    - "No production filter-lists / bridge-brain / city-formats data wiped"
  artifacts:
    - path: "tests/bridge-engine.test.js"
      provides: "v1.8 TEST-01/02/03 processUpload locks still present from Plan 01"
      contains: "TEST-01 (v1.8)"
  key_links:
    - from: "Phase 54 Plan 02 gate"
      to: "npm test"
      via: "full node --test suite green"
      pattern: "npm test"
    - from: "Phase 54 Plan 02 gate"
      to: "scripts/verify-live.ps1"
      via: "exit 0 health + homepage 200"
      pattern: "verify-live"
---

<objective>
Milestone lock-and-ship gate: full automated suite green and local live server healthy after v1.8 processUpload regression locks land.

Purpose: TEST-03 and Phase 54 success criteria explicitly require `npm test` + `scripts/verify-live.ps1` green. Plan 01 only proves focused v1.8 patterns; this plan is the ship gate.

Output: Green full suite, green verify-live, SUMMARY documenting pass counts. No product feature work unless a real regression appears.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-regression-lock/54-RESEARCH.md
@.planning/phases/54-regression-lock/54-01-PLAN.md
@AGENTS.md
@scripts/verify-live.ps1
@package.json

<interfaces>
<!-- Gates only. Prefer zero file edits. -->

Commands (repo root):
```bash
npm test
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
```

If verify-live fails (server down):
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
```

AGENTS.md hard rules:
- Never wipe data/filter-lists/, data/bridge-brain/, modules/form-forge/data/, analyzer users
- Never start blocking `node server.js` in agent shell — use restart.ps1 / ensure-server.ps1 only
- Phase 54 should not need public/ or server.js edits; if suite fails, fix only real 51–53 regressions or Plan 01 test harness bugs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full npm test suite green</name>
  <files></files>
  <action>
    From repo root run:
    ```
    npm test
    ```

    Must exit 0. Baseline at research was **456 pass / 0 fail** — expect equal or higher pass count after Plan 01 added tests.

    If failures:
    1. Prefer fixing Plan 01 test harness issues (city pollution, FP not actually changed, long type not distressed) over product changes
    2. If a real product regression in COL/GATE/LBL path: fix the minimal 51–53 module only — no new features, no scorer retune “while here”
    3. Do not skip or delete v1.7 TEST-* or prior COL/GATE/LBL contracts to force green

    Record pass/fail counts in SUMMARY.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test exits 0; all v1.7 + v1.8 + COL/GATE/LBL contracts green</done>
</task>

<task type="auto">
  <name>Task 2: verify-live.ps1 health + homepage green</name>
  <files></files>
  <action>
    Run live gate per AGENTS.md:
    ```
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    ```

    Must exit 0 (`/api/health` and `/` HTTP 200). Script auto-ensures server if down.

    If it fails:
    1. Restart headless: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1`
    2. Re-run verify-live until exit 0
    3. Never leave blocking `node server.js` in the agent shell

    Phase 54 is tests-only — no public/ or server.js edits expected. Do not wipe runtime data stores.

    Optional (not required): re-run focused v1.8 pattern once after suite:
    `node --test --test-name-pattern="v1\\.8" tests/bridge-engine.test.js`
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>verify-live.ps1 exit 0; health + homepage 200; server left live for user</done>
</task>

</tasks>

<verification>
1. `npm test` — exit 0
2. `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` — exit 0
3. No wipe of filter lists / brain / city formats production data
4. Phase 54 success criteria #4 satisfied (suite + live)
</verification>

<success_criteria>
- Full automated suite green with Plan 01 v1.8 locks included
- Local server health + homepage green via verify-live.ps1
- Milestone lock-and-ship ready for Phase 54 verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/54-regression-lock/54-02-SUMMARY.md`
</output>
