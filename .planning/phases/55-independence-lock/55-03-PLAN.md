---
phase: 55-independence-lock
plan: 03
type: execute
wave: 2
depends_on:
  - "55-01"
  - "55-02"
files_modified:
  - docs/bridge/DATA-STANDARDS.md
  - docs/bridge/GSD-AUDIT.md
  - public/js/bridge.js
autonomous: true
requirements:
  - IND-01
  - IND-02
  - IND-04
must_haves:
  truths:
    - "DATA-STANDARDS documents already_imported hard-drop as off by default / opt-in only, not automatic always-on"
    - "GSD-AUDIT does not list Auto-push kept rows to Analyzer as a live shipped Filter feature"
    - "Filter UI does not show 'already in Analyze (hidden...)' or Analyze index suffix when counts are 0"
    - "KPI grid omits zero 'Already in Analyze' card; stub still reassures 'nothing was sent to Analyze' when appropriate"
    - "No new Send/Push to Analyze controls appear in public Filter JS"
  artifacts:
    - path: "docs/bridge/DATA-STANDARDS.md"
      provides: "Property Analyzer Cross-Reference updated for default-off policy"
      contains: "off by default"
    - path: "docs/bridge/GSD-AUDIT.md"
      provides: "Auto-push retired; independence lock status"
      contains: "Auto-push"
    - path: "public/js/bridge.js"
      provides: "Copy honesty for default-off alreadyImported / importIndexCount"
      contains: "nothing was sent to Analyze"
  key_links:
    - from: "docs/bridge/DATA-STANDARDS.md"
      to: "processUpload applyAlreadyImportedFilter"
      via: "documents engine opt-in only"
      pattern: "applyAlreadyImportedFilter|off by default"
    - from: "public/js/bridge.js renderKpis/renderResults"
      to: "stats.alreadyImported / processingMeta.importIndexCount"
      via: "hide zero Analyze-hidden messaging per UI-SPEC"
      pattern: "alreadyImported|importIndexCount|Already in Analyze"
    - from: "docs/bridge/GSD-AUDIT.md"
      to: "deleted bridge-analyzer-push"
      via: "retire auto-push row so agents do not restore"
      pattern: "retired|deleted|not.*Filter|removed"
---

<objective>
Update operator-facing Filter copy and agent-facing docs so independence (no auto-push, already_imported off by default) cannot be restored from stale GSD-AUDIT / DATA-STANDARDS, and UI does not imply Analyze hard-drop when counts are zero.

Purpose: Messaging lock for IND-01/02/04 after code locks in 55-01/02. Light copy only -- not Phase 56 List Factory UX redesign.

Output: DATA-STANDARDS + GSD-AUDIT edits; `public/js/bridge.js` copy honesty per 55-UI-SPEC; full suite + verify-live green.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-independence-lock/55-RESEARCH.md
@.planning/phases/55-independence-lock/55-UI-SPEC.md
@.planning/phases/55-independence-lock/55-VALIDATION.md
@docs/bridge/DATA-STANDARDS.md
@docs/bridge/GSD-AUDIT.md
@public/js/bridge.js
@AGENTS.md

<interfaces>
<!-- UI-SPEC locked copy (Phase 55 only) -->

Process success (no discards of interest):
`Processing complete. Save the list below -- nothing was sent to Analyze.`

Hidden-row note: only if `stats.alreadyImported > 0`:
`N already in Analyze (hidden from this list).`
Never show when count is 0.

Results meta Analyze suffix: only if `importIndexCount > 0` (index actually loaded):
`N address(es) in Analyze`
Target default: importIndexCount is 0 when filter off -- suffix already gated by `> 0` in current code; keep that gate.

KPI: omit `{ label: 'Already in Analyze', value: stats.alreadyImported }` when value is 0 (prefer omit over showing 0).

NO_USABLE_ROWS client breakdown: only push "already in Analyze" part when stats.alreadyImported or discardReasons.already_imported is truthy (already gated -- verify stays honest).

Forbidden CTAs: never add "Send to Analyze", "Push to Analyze", etc.

DATA-STANDARDS today (must change):
```
After deduplication, each kept row is checked against ... session ...
Rows that match an existing import are removed with reason already_imported.
...
Property Analyzer cross-reference runs automatically during processing (Phase 5)
```

Target policy text (substance, not exact wording):
- Cross-ref hard-drop is **off by default** (v2.0 / IND-04)
- Optional only when `applyAlreadyImportedFilter === true` (engine; no UI toggle in 55)
- Filter does **not** auto-push; legacy adapter deleted; Analyze `bridge-import-records` may remain for manual import

GSD-AUDIT today (must change):
```
| Auto-push kept rows to Analyzer | ... | bridge-analyzer-push.js | ... | (live shipped mark) |
```
-> Mark retired/deleted / not a Filter feature; remove from live shipped inventory. Update component inventory that lists bridge-analyzer-push as current Analyzer integration. Residual risk "Analyzer API down during push" should not treat push as live Filter path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DATA-STANDARDS + GSD-AUDIT for independence policy</name>
  <files>docs/bridge/DATA-STANDARDS.md, docs/bridge/GSD-AUDIT.md</files>
  <read_first>
    - docs/bridge/DATA-STANDARDS.md (Property Analyzer Cross-Reference ~79-100; Extension Points ~153-156; Filter Saved Lists no Auto-Push ~92-101)
    - docs/bridge/GSD-AUDIT.md (traceability auto-push row ~21; component inventory ~43; residual risks push ~104)
    - .planning/phases/55-independence-lock/55-RESEARCH.md (docs section + Pitfall 3)
  </read_first>
  <action>
    **DATA-STANDARDS.md:**
    1. Rewrite **Property Analyzer Cross-Reference** so it states hard-drop is **off by default** for v2.0 list factory.
    2. Document that enabling requires explicit engine flag `applyAlreadyImportedFilter === true` (not automatic every process).
    3. Keep description of pure matching behavior (when enabled): reason `already_imported`, index sources, stats fields -- framed as opt-in.
    4. Keep / strengthen **Filter Saved Lists (no Auto-Push)** -- no Filter process/save/Train writes to Analyze; legacy adapter removed from Filter tree.
    5. Fix Extension Points bullet "Property Analyzer cross-reference runs automatically during processing" -> default-off / opt-in only.
    6. Do not invent Phase 56 list UX copy here.

    **GSD-AUDIT.md:**
    1. Change Auto-push row from live shipped status to **retired / deleted** (status column must not read as shipped Filter feature). Implementation note: module deleted; independence tests in `tests/bridge-independence.test.js`.
    2. Update component inventory: remove or mark `bridge-analyzer-push.js` as deleted; Analyzer soft-read may remain (`analyzer-import-index.js` + import-filter opt-in).
    3. Residual risk "Analyzer API down during push" -> reframe as historical or "manual Analyze import only; Filter does not push."
    4. Optional: add brief IND note that Filter write isolation is locked by independence tests.

    Do not edit TEST-PLAN comprehensively unless a single row still claims auto-push is required product behavior (C-09/C-20 stay valid).
  </action>
  <acceptance_criteria>
    - DATA-STANDARDS mentions "off by default" or applyAlreadyImportedFilter
    - DATA-STANDARDS no longer claims cross-reference "runs automatically during processing" as current always-on behavior
    - GSD-AUDIT Auto-push row is retired/deleted/removed (not live shipped Filter feature)
    - bridge-analyzer-push marked deleted/retired if mentioned in GSD-AUDIT
    - No doc reintroduces "Filter auto-pushes kept rows" as current behavior
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/bridge/DATA-STANDARDS.md','utf8');const g=fs.readFileSync('docs/bridge/GSD-AUDIT.md','utf8');if(!/off by default|applyAlreadyImportedFilter/i.test(d))process.exit(1);if(/Auto-push kept rows to Analyzer[\s\S]{0,120}\|\s*\u2713/.test(g))process.exit(2);console.log('docs-ok');"</automated>
  </verify>
  <done>Docs teach default-off import filter and retired auto-push; agents cannot restore push from audit tables.</done>
</task>

<task type="auto">
  <name>Task 2: Light Filter UI copy honesty + suite + verify-live</name>
  <files>public/js/bridge.js</files>
  <read_first>
    - public/js/bridge.js (renderKpis ~1361-1376; renderResults stub/meta ~1819-1871; NO_USABLE_ROWS breakdown ~947-959)
    - .planning/phases/55-independence-lock/55-UI-SPEC.md (Copywriting Contract + already_imported display contract)
    - AGENTS.md (verify-live mandatory if public/ touched)
  </read_first>
  <action>
    Copy-only / light stats honesty in `public/js/bridge.js` per UI-SPEC -- **no** layout redesign, **no** new CTAs, **no** opt-in UI toggle:

    1. **renderKpis:** Build cards array so `Already in Analyze` is included **only when** `Number(stats.alreadyImported) > 0`. Prefer omit when 0.
    2. **renderResults stub note:** Keep `importedNote` only when `data.stats.alreadyImported` is truthy (already gated). Ensure zero-count path never invents hidden-in-Analyze text. Keep reassurance: `Save the list below -- nothing was sent to Analyze.` on clean success path (match existing wording if already present).
    3. **results meta indexLabel:** Keep existing `indexCount > 0` gate (already correct when importIndexCount is 0).
    4. **NO_USABLE_ROWS:** Keep alreadyImported part only when count truthy (already gated) -- do not lead with Analyze when 0.
    5. **Forbidden:** Do not add Send/Push/Import to Analyze buttons or fetch to `/api/bridge-import-records`.
    6. If cache-bust query on bridge.js script tag exists in `public/bridge.html`, bump it one step when you change bridge.js (match prior phase pattern if present); otherwise leave HTML alone.
    7. After public edit: run full `npm test`, then `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\verify-live.ps1`. If verify fails, `scripts\\restart.ps1` and re-verify. Do not claim live until exit 0.
    8. Never wipe `data/filter-lists/` or `data/bridge-brain/`.
  </action>
  <acceptance_criteria>
    - `Already in Analyze` in public/js/bridge.js only appears in a branch that checks alreadyImported > 0 (or equivalent truthy filter before push to cards)
    - `nothing was sent to Analyze` still present in public/js/bridge.js
    - No Send/Push to Analyze, bridge-import-records, or pushRowsToAnalyzer in public/js/bridge.js
    - `npm test` exits 0
    - `scripts\\verify-live.ps1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test && powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>UI honest for default-off; docs updated; full suite green; local server health 200 after public change.</done>
</task>

</tasks>

<verification>
- Docs acceptance node one-liner green
- `npm test` green
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` green (required -- public/ touched)
- Manual optional: process a city; stub does not claim Analyze-hidden when alreadyImported is 0; Save list still primary CTA
</verification>

<success_criteria>
- Agent-facing docs no longer teach auto-push or always-on hard-drop
- Operator UI does not imply Analyze filtering when filter is off
- Independence messaging secondary to Save list (UI-SPEC hierarchy preserved)
- Suite + live gates green for phase
</success_criteria>

<output>
After completion, create `.planning/phases/55-independence-lock/55-03-SUMMARY.md`
</output>
