---
phase: 55-independence-lock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/bridge-analyzer-push.js
  - tests/bridge-analyzer-push.test.js
  - tests/bridge-independence.test.js
  - tests/bridge-api-handlers.test.js
autonomous: true
requirements:
  - IND-01
  - IND-02
  - IND-03
must_haves:
  truths:
    - "lib/bridge-analyzer-push.js is not loadable (require.resolve throws)"
    - "Filter write-path sources never reference bridge-analyzer-push, pushRowsToAnalyzer, or bridge-import-records"
    - "POST /api/bridge/process success never includes analyzerPush and does not invent Analyze session writes"
    - "POST /api/bridge/lists save writes only under FILTER_LISTS_ROOT and does not create Analyzer session files under a temp ANALYZER_DATA_ROOT"
  artifacts:
    - path: "tests/bridge-independence.test.js"
      provides: "Static bans + module absence + process/save negative contracts (IND-01..03)"
      contains: "IND-0"
    - path: "lib/bridge-analyzer-push.js"
      provides: "Deleted legacy push adapter"
      min_lines: 0
    - path: "tests/bridge-api-handlers.test.js"
      provides: "Strengthened process/list independence asserts"
      contains: "analyzerPush"
  key_links:
    - from: "tests/bridge-independence.test.js"
      to: "lib/bridge-api.js + bridge-engine + list-store + brain-*"
      via: "static source scan forbids push require strings"
      pattern: "bridge-analyzer-push|pushRowsToAnalyzer|bridge-import-records"
    - from: "tests/bridge-independence.test.js"
      to: "require.resolve('../lib/bridge-analyzer-push')"
      via: "assert.throws module absence"
      pattern: "require\\.resolve.*bridge-analyzer-push"
    - from: "handleProcess / handleListCreate"
      to: "Analyzer session disk"
      via: "no write under ANALYZER_DATA_ROOT during process/save"
      pattern: "ANALYZER_DATA_ROOT|distressAnalyzerSession"
---

<objective>
Delete the dead Filter→Analyze push adapter and lock independence with automated negative tests so process, save, Train, and list paths cannot re-wire Analyze writes without failing CI.

Purpose: IND-01 (no write), IND-02 (adapter gone), IND-03 (negative proof). Residual `bridge-analyzer-push.js` + positive mapping tests are resurrection risk.

Output: Module deleted; positive push tests removed/reframed; new `tests/bridge-independence.test.js`; stronger handler contracts. Analyzer-side `POST /api/bridge-import-records` left alone (manual import world).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-independence-lock/55-RESEARCH.md
@.planning/phases/55-independence-lock/55-VALIDATION.md
@lib/bridge-analyzer-push.js
@lib/bridge-api.js
@lib/bridge-list-store.js
@tests/bridge-api-handlers.test.js
@tests/bridge-analyzer-push.test.js
@AGENTS.md

<interfaces>
<!-- As-built: zero production requires of push. Delete is preferred over quarantine. -->

Production require of bridge-analyzer-push: **NONE**
Only consumer: `tests/bridge-analyzer-push.test.js`

Filter write surfaces to ban (static scan):
```
lib/bridge-api.js
lib/bridge-engine/index.js
lib/bridge-list-store.js
lib/bridge-brain-decisions.js
lib/bridge-brain-apply.js
lib/bridge-brain-store.js
```

Forbidden substrings in those files:
- `bridge-analyzer-push`
- `pushRowsToAnalyzer`
- `bridge-import-records`

handleProcess contract (keep):
```js
const payload = await processUploadBatch(fileList, batchArgs);
// Filter only — do not auto-push to Analyze.
sendJson(res, 200, payload);
// response must not include analyzerPush
```

List save (handleListCreate → saveList): writes only under `FILTER_LISTS_ROOT/{scope}/`.

Analyzer session file pattern (must NOT appear under temp ANALYZER root after Filter process/save):
`distressAnalyzerSession_LATEST.json` (and any `distressAnalyzerSession_*.json`)

Config isolation pattern (from brain/list tests):
```js
const config = require('../lib/config'); // or project config module used by list/analyzer roots
// mkdtemp for FILTER_LISTS_ROOT and ANALYZER_DATA_ROOT (or PROPERTY_ANALYZER path used by session)
// restore in after()
// NEVER point tests at production data/filter-lists or real Analyzer session
```

Existing partial assert to keep/strengthen:
```js
// tests/bridge-api-handlers.test.js process success
assert.equal(json.analyzerPush, undefined);
```

Do **not** delete:
- `modules/property-analyzer/routes/bridge.js` POST `/api/bridge-import-records`
- `lib/analyzer-import-index.js` (soft read; IND-04 is separate plan)
- `lib/bridge-engine/import-filter.js`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete push module and remove positive push tests</name>
  <files>lib/bridge-analyzer-push.js, tests/bridge-analyzer-push.test.js</files>
  <read_first>
    - lib/bridge-analyzer-push.js (full module; exports pushRowsToAnalyzer, bridgeRowsToAnalyzerRecords)
    - tests/bridge-analyzer-push.test.js
    - Confirm with rg that no production file requires bridge-analyzer-push (only the test file)
  </read_first>
  <action>
    1. Verify production isolation: `rg "bridge-analyzer-push|pushRowsToAnalyzer" lib public` should show only `lib/bridge-analyzer-push.js` itself (and maybe comments). If any production require exists, stop and remove that call site first — do not leave a broken require.
    2. **Delete** `lib/bridge-analyzer-push.js` entirely (preferred over quarantine per research).
    3. **Delete** `tests/bridge-analyzer-push.test.js` entirely (positive mapping suite productizes the dead adapter). Module-absence + static bans land in Task 2 independence file — do not leave a test that requires the deleted module.
    4. Do **not** touch Analyzer module routes or `modules/property-analyzer/lib/bridge-import-records.js`.
    5. Do **not** wipe filter-lists / brain / analyzer user data.
  </action>
  <acceptance_criteria>
    - `Test-Path lib/bridge-analyzer-push.js` is False
    - `Test-Path tests/bridge-analyzer-push.test.js` is False
    - `node -e "require.resolve('./lib/bridge-analyzer-push')"` throws MODULE_NOT_FOUND
    - `rg "require\\(['\\\"].*bridge-analyzer-push" lib public` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>node -e "try { require.resolve('./lib/bridge-analyzer-push'); process.exit(1); } catch (e) { if (e.code !== 'MODULE_NOT_FOUND') process.exit(1); console.log('absent-ok'); }"</automated>
  </verify>
  <done>Push adapter and its positive unit suite are gone; require.resolve fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bridge-independence negative suite (static + process/save)</name>
  <files>tests/bridge-independence.test.js, tests/bridge-api-handlers.test.js</files>
  <read_first>
    - tests/bridge-api-handlers.test.js (process success ~361–384; lists CRUD ~466+; temp FILTER_LISTS_ROOT isolation ~121–229)
    - lib/bridge-api.js handleProcess + handleListCreate
    - lib/bridge-list-store.js saveList
    - .planning/phases/55-independence-lock/55-RESEARCH.md (Pattern 1 + Pattern 4 + code examples)
    - lib/config.js or wherever ANALYZER_DATA_ROOT / FILTER_LISTS_ROOT are defined
  </read_first>
  <behavior>
    - Static: each FILTER_WRITE_PATH source string excludes bridge-analyzer-push, pushRowsToAnalyzer, bridge-import-records
    - Module: require.resolve('../lib/bridge-analyzer-push') throws
    - Process: POST /api/bridge/process success → analyzerPush undefined; no analyzerPush/import batch id fields; no new distressAnalyzerSession_* under isolated ANALYZER root
    - Save: POST /api/bridge/lists with sample rows → 200; list file under FILTER_LISTS_ROOT; no distressAnalyzerSession_* under isolated ANALYZER root
    - Train path covered by static ban on bridge-brain-decisions.js (no separate HTTP required if static is solid)
  </behavior>
  <action>
    1. Create `tests/bridge-independence.test.js` using `node:test` + `node:assert/strict`:

    **Static bans (IND-01/02):**
    ```js
    const FILTER_WRITE_PATHS = [
      'lib/bridge-api.js',
      'lib/bridge-engine/index.js',
      'lib/bridge-list-store.js',
      'lib/bridge-brain-decisions.js',
      'lib/bridge-brain-apply.js',
      'lib/bridge-brain-store.js'
    ];
    // for each: readFileSync; assert !includes forbidden strings
    // also assert !fs.existsSync(path.join(__dirname,'../lib/bridge-analyzer-push.js'))
    // assert.throws(() => require.resolve('../lib/bridge-analyzer-push'))
    ```
    Title tests with `IND-01`, `IND-02`, or `IND-03` for traceability.

    **Process negative (IND-01/03):** Prefer reusing handler harness patterns from `bridge-api-handlers.test.js` OR call `processUpload` with temp roots. At minimum:
    - Assert process success body has `analyzerPush === undefined` and no `importBatchId` / push result fields
    - Isolate analyzer data root to mkdtemp; after process, assert no `distressAnalyzerSession` files created there

    **Save negative (IND-01/03):**
    - Isolate `FILTER_LISTS_ROOT` + analyzer root to temp dirs
    - POST list create (or call `saveList` directly) with 1 sample row
    - Assert save ok; list JSON exists under filter-lists temp
    - Assert no `distressAnalyzerSession*.json` under analyzer temp
    - Restore config roots in after/finally — never use production data paths

    2. Strengthen `tests/bridge-api-handlers.test.js` process success test:
    - Keep `assert.equal(json.analyzerPush, undefined)`
    - Add asserts: no `json.analyzerPushResult`, no truthy push/import batch fields if any exist in shape
    - Optionally assert `json.stats.alreadyImported === 0` on default process (compatible with 55-01; empty index already yields 0)

    3. Do **not** reintroduce push mapping tests. Do **not** require deleted module.

    4. If public/ is untouched, skip verify-live this plan.
  </action>
  <acceptance_criteria>
    - File `tests/bridge-independence.test.js` exists
    - `rg "IND-0[123]" tests/bridge-independence.test.js` matches ≥3
    - `rg "bridge-analyzer-push" tests/bridge-independence.test.js` matches (in ban assertions)
    - `rg "require\\(['\\\"].*bridge-analyzer-push" tests` returns no positive requires of the module
    - Automated command below exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-api-handlers.test.js</automated>
  </verify>
  <done>Independence suite locks no-push static + process/save side-effect negatives; handlers still green; push module remains absent.</done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-independence.test.js` green
- `node -e "require.resolve('./lib/bridge-analyzer-push')"` fails
- Full suite: `npm test` after plan (or at wave merge)
</verification>

<success_criteria>
- Legacy push adapter deleted and not re-required by any Filter path
- Automated negatives prove process + save never invent Analyze session writes
- Positive push mapping suite gone so suite cannot force module restoration
</success_criteria>

<output>
After completion, create `.planning/phases/55-independence-lock/55-02-SUMMARY.md`
</output>
