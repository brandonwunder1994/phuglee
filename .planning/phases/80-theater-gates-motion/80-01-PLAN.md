---
phase: 80-theater-gates-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/bridge.css
  - public/bridge.html
autonomous: true
requirements:
  - DESK-01
  - DESK-02
must_haves:
  truths:
    - "Operator sees kill report + mission board as climax-first: RAW → KILLED → KEPT visual hierarchy with Save elevated inside mission surface"
    - "Operator sees home-grade paint on theater surfaces: mission/kill, victory strip, scrub feed, lists inventory, shift queue HUD, scanned toast, attach scrap, history/type-confirm dialogs — no orphan pre-system chrome on those clusters"
    - "Cinema structure order and copy locks remain intact (mission surface contains kill host + save climax; victory strip IDs/actions unchanged)"
    - "No bridge-* IDs, data-action / data-mode / data-format values, or public/js/** / lib/** behavior changes"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "v3 theater surface paint for kill/mission/victory/lists/shift/feed/dialogs using Phuglee tokens"
      contains: "bridge-kill-report|bridge-mission-surface|bridge-victory-strip|bridge-save-panel--climax"
    - path: "public/bridge.html"
      provides: "Presentational dual-class hooks only if needed; structure order frozen"
      contains: "bridge-mission-surface|bridge-kpi-grid|bridge-save-list|bridge-victory-strip"
  key_links:
    - from: "public/bridge.html #bridge-mission-surface"
      to: "#bridge-kpi-grid + #bridge-save-panel"
      via: "DOM order inside mission surface (kill host then save climax)"
      pattern: "bridge-mission-surface[\\s\\S]*bridge-kpi-grid[\\s\\S]*bridge-save-panel"
    - from: "public/css/bridge.css .bridge-kill-stat--raw|killed|kept"
      to: "visual hierarchy RAW quieter → KILLED hot → KEPT largest gold"
      via: "font-size + semantic color tokens"
      pattern: "bridge-kill-stat--(raw|killed|kept)"
    - from: "public/css/bridge.css .bridge-save-panel--climax"
      to: "Save elevation above scrap attach / collapsible kept table"
      via: "border/background heat stronger than .bridge-attach-panel--scrap"
      pattern: "bridge-save-panel--climax"
---

<objective>
Paint Filter theater climax surfaces to home/login Phuglee grade while preserving cinema structure: kill report RAW→KILLED→KEPT hierarchy and Save elevation inside the mission board.

Purpose: Close DESK-01 (theater half of full surface pass) + DESK-02 without touching process, brain, or keep/kill engines. Phase 79 owns desk-core chrome; this plan owns post-process climax and secondary inventory chrome.

Output: Updated `bridge.css` (and dual-class markup hooks only if required) for mission/kill, victory, feed polish, lists/shift/toast, dialogs — CSS/markup only.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-theater-gates-motion/80-CONTEXT.md
@.planning/v2.1-FILTER-SCRUB-THEATER.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@public/css/bridge.css
@public/bridge.html
@public/css/tokens.css
@public/css/phuglee-components.css
@AGENTS.md

<interfaces>
<!-- Locked contracts — DO NOT rename or reorder for aesthetics -->

From public/bridge.html (mission climax structure):
```html
<div class="bridge-mission-surface" id="bridge-mission-surface">
  <p class="bridge-preview-meta" id="bridge-results-meta"></p>
  <div class="bridge-kpi-grid" id="bridge-kpi-grid"></div>
  <!-- JS injects .bridge-kill-report with .bridge-kill-stat--raw|killed|kept -->
  <section class="bridge-save-panel bridge-save-panel--climax" id="bridge-save-panel" …>
    <button … id="bridge-save-list">Save list</button>
  </section>
</div>
```

Victory strip (IDs + cinema copy locked):
```html
<div id="bridge-victory-strip" class="bridge-victory-strip" hidden …>
  <p class="bridge-victory-title" id="bridge-victory-title">DELETE THE JUNK</p>
  … TAKE OUT ANY LEADS THAT ARE A WASTE OF TIME …
  <button id="bridge-victory-download" data-action="flash-download" data-format="csv">Filter Data</button>
  <button id="bridge-victory-next">Scrub next city</button>
</div>
```

CSS class hooks already present (restyle, do not rename):
- `.bridge-kill-report`, `.bridge-kill-flow`, `.bridge-kill-stat--raw|killed|kept`, `.bridge-kill-reason`, `.bridge-proof-chip`, `.bridge-kept-sample`
- `.bridge-mission-surface`, `.bridge-save-panel--climax`, `.bridge-results-details`, `.bridge-attach-panel--scrap`
- `.bridge-victory-strip`, `.bridge-scrub-feed*`, `.bridge-lists-*`, `.bridge-shift-queue*`, `.bridge-scanned-toast*`, `.bridge-history-dialog*`
- `.bridge-inventory-hud`

Hard constraints (v3.0 + CONTEXT):
- CSS + presentational markup only — freeze every `bridge-*` id, `data-action` / `data-mode` / `data-format` / `data-step`
- No `public/js/**` or `lib/**` edits
- No 4th unscoped `!important` strata dumped at file end
- Prefer Phuglee tokens (`--phuglee-*`, semantic status) over hex islands (`#e87a4a`, `#9fd4a8`, etc.) on theater chrome
- North star: login modal + home glass/grain/Anton/Outfit energy at desk density
- Kill-report theater stays in `bridge.css` (not shared components)
- Bump `bridge.css?v=` in bridge.html when CSS ships
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill report + mission board climax paint (DESK-02)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Restyle the post-scrub climax cluster in `public/css/bridge.css` so it reads as home-grade war-room HUD, not leftover KPI tiles:

1. **`.bridge-mission-surface`** — glass panel energy (token borders/shadows, subtle orange heat gradient) elevated vs scrap surfaces; keep desk density (not auth-modal padding).
2. **Kill report hierarchy** (`.bridge-kill-report` / `.bridge-kill-flow` / `.bridge-kill-stat--raw|killed|kept`):
   - Preserve asymmetric flex flow (NOT equal 3-up KPI grid as primary readout).
   - RAW = quieter taupe / smaller; KILLED = hot danger-adjacent via semantic tokens (`--phuglee-danger` / warn family — not random `#e87a4a` hex island if token covers it); KEPT = largest cream/gold Anton display with ember glow.
   - Keep `.bridge-kill-arrow`, reason chips, proof chips, kept samples readable on grain (cream numbers, strong contrast).
3. **`.bridge-save-panel--climax`** — primary heat elevation (stronger border/glow than default panels); `#bridge-save-list` already uses `phuglee-btn-primary` — do not change button id/text (`Save list`).
4. **Scrap demotion** — ensure `.bridge-attach-panel--scrap` and `.bridge-results-details` stay quieter than mission/save (opacity/border quieter). Do not move DOM nodes.
5. **Markup**: only add dual-class presentational hooks (`phuglee-panel` already present) if a gap blocks paint. **Never** rename ids, never reorder `#bridge-kpi-grid` before/after save, never change cinema copy strings.

Avoid: rewriting `renderKpis` / kill HTML builders in JS; equal-card KPI revival; Analyze push CTAs; green SaaS success flashes.
  </action>
  <verify>
    <automated>node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-desk-cinema.test.js</automated>
  </verify>
  <done>
Kill/mission/save CSS uses token-driven hierarchy; RAW→KILLED→KEPT class contracts and mission-surface DOM order still match KILL + cinema static tests; Save remains elevated climax CTA.
  </done>
</task>

<task type="auto">
  <name>Task 2: Victory, feed, lists/shift, dialogs — orphan surface paint (DESK-01)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Complete the Phase 80 theater-side full surface pass on remaining climax/inventory chrome (desk-core hero/pipeline/dropzone/tables are Phase 79 — do not re-scope them unless an orphan class is clearly pre-system inside these clusters):

1. **Victory strip** (`.bridge-victory-strip` + children) — featured elevation (stronger than mission, war-room end state); keep IDs `bridge-victory-strip|title|meta|download|next`, locked copy (`DELETE THE JUNK`, `Filter Data`, `Scrub next city`, boundary line), and `data-action="flash-download"` / `data-format="csv"`.
2. **Scrub feed** (`.bridge-scrub-feed*`) — legibility polish on dark glass (kept/no-distress/discarded status colors via tokens); do not change JS feed contracts or remove `.is-enter` animation hook (motion twins are Plan 02).
3. **Lists inventory + shift queue** (`.bridge-lists-*`, `.bridge-inventory-hud`, `.bridge-shift-queue*`, `.bridge-lists-flash`, `.bridge-scanned-toast*`) — glass/HUD language aligned with mission; status success/error lines use `--phuglee-success` / `--phuglee-danger` instead of hex islands (`#9fd4a8`, `#e8a0a0`) where those appear in these clusters.
4. **Dialogs** (`.bridge-history-dialog`, `.bridge-history-dialog-card`, type-confirm siblings) — keep native `<dialog>`; glass/grain modal treatment matching system; do not replace with div modals; preserve z-index above chrome (existing 10000/9999).
5. **Cache bust** — bump `public/bridge.html` `bridge.css?v=44` → next integer (e.g. `?v=45`) after CSS edits.

Scope ban: no Train wrap / `is-theater` deep restyle here (Plan 02 owns DESK-04); no `display:flex !important` on hidden panels; no new keyframes without noting them for Plan 02 reduce twins; no `lib/**` / `public/js/**`.
  </action>
  <verify>
    <automated>node --test tests/bridge-desk-cinema.test.js tests/bridge-kill-rate-scrub.test.js tests/bridge-scrub-feed.test.js tests/bridge-shift-staging.test.js</automated>
  </verify>
  <done>
Victory/lists/shift/feed/dialog clusters painted with token language; cinema + kill + feed + shift static tests green; `bridge.css?v=` bumped; zero ID/`data-action` churn.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-desk-cinema.test.js tests/bridge-scrub-feed.test.js tests/bridge-shift-staging.test.js` all pass
- HTML still has `#bridge-mission-surface` wrapping `#bridge-kpi-grid` then `#bridge-save-panel` before train wrap
- `rg "data-action=|id=\"bridge-" public/bridge.html` shows no renames vs pre-edit baseline intent
- Diff excludes `public/js/**` and `lib/**`
</verification>

<success_criteria>
- DESK-02: kill + mission climax visually hierarchy-correct and Save elevated
- DESK-01 (theater half): no orphan pre-system chrome on mission/kill/victory/feed/lists/shift/dialogs clusters
- Structure, copy, and CTA contracts frozen
</success_criteria>

<output>
After completion, create `.planning/phases/80-theater-gates-motion/80-01-SUMMARY.md`
</output>
