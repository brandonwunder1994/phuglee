---
phase: 80-theater-gates-motion
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - public/css/bridge.css
  - public/bridge.html
autonomous: true
requirements:
  - DESK-04
  - STATES-04
must_haves:
  truths:
    - "Admin Train theater (.is-theater / .bridge-results-mode--theater) visually elevates Train tab + mission HUD; Rules armory stays demoted secondary mode"
    - "Non-admin Train remains fail-closed: #bridge-train-wrap keeps default hidden; CSS never forces display that overrides [hidden]"
    - "Every bridge.css animation/transition used by theater (feed enter, train card exit, dialog rise, toast, date-chip lift, lists flash, any new Plan 01 motion) has a prefers-reduced-motion: reduce twin"
    - "FEED-02 still green — reduced-motion path disables feed enter animation but does not depend on animationend for content"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "Train theater/armory mode paint + consolidated reduced-motion twins for Filter theater motion"
      contains: "is-theater|bridge-results-mode--theater|prefers-reduced-motion"
    - path: "public/bridge.html"
      provides: "Train wrap still default-hidden; mission still inside wrap; optional cache-bust bump only"
      contains: "id=\"bridge-train-wrap\"[^>]*hidden|bridge-train-mission|Rules armory"
  key_links:
    - from: "#bridge-train-wrap[hidden]"
      to: "non-admin fail-closed"
      via: "HTML default hidden + JS setHidden — CSS must not override [hidden] with display:flex !important"
      pattern: "id=\"bridge-train-wrap\"[^>]*\\bhidden\\b"
    - from: ".bridge-train-wrap.is-theater / .bridge-results-mode--theater"
      to: "Train elevated, Kept demoted, brain armory quietest"
      via: "existing class hooks toggled by bridge.js (frozen)"
      pattern: "bridge-results-mode--theater|is-theater"
    - from: "@media (prefers-reduced-motion: reduce)"
      to: ".bridge-scrub-feed-item.is-enter, .bridge-train-group.is-exiting, .bridge-history-dialog-card, toast/flash"
      via: "animation: none / transition kill or opacity-only"
      pattern: "prefers-reduced-motion:\\s*reduce"
---

<objective>
Restyle admin Train theater + Rules armory as distinct visual modes, keep non-admin fail-closed, and ship reduced-motion twins for every Filter theater animation.

Purpose: Close DESK-04 (Train gates) + STATES-04 (motion a11y) — highest product-risk CSS slice in v3.0. Depends on Plan 01 so any new climax motion is included in the reduce audit.

Output: `bridge.css` theater/armory polish + comprehensive `prefers-reduced-motion` coverage; markup only if dual-class hooks needed (no ID renames).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-theater-gates-motion/80-CONTEXT.md
@.planning/phases/80-theater-gates-motion/80-01-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/v2.1-FILTER-SCRUB-THEATER.md
@public/css/bridge.css
@public/bridge.html
@public/css/phuglee-a11y.css
@tests/bridge-train-theater.test.js
@tests/bridge-scrub-feed.test.js
@AGENTS.md

<interfaces>
<!-- Train structure LOCKED (THTR-03 fail-closed) -->
```html
<div id="bridge-train-wrap" hidden>
  <div id="bridge-train-mission" class="bridge-train-mission" hidden>…</div>
  <div class="bridge-results-mode" role="tablist">
    <button id="bridge-mode-kept" data-mode="kept" class="bridge-mode-tab">Kept list</button>
    <button id="bridge-mode-train" data-mode="train" class="bridge-mode-tab">Train brain</button>
    <button id="bridge-mode-brain" data-mode="brain" class="bridge-mode-tab bridge-mode-tab--armory">Rules armory</button>
  </div>
  <div id="bridge-train-panel" hidden role="tabpanel">… groups …</div>
  <div id="bridge-brain-panel" hidden role="tabpanel">… rules …</div>
</div>
```

JS-owned class tokens (style only — do not change JS):
- `#bridge-train-wrap.is-theater` + mission heat
- `.bridge-results-mode--theater` on tab rail
- `.bridge-train-group.is-pending` / `.is-exiting`
- `.bridge-mode-tab--armory` / `[data-mode="brain"]`

Existing motion that MUST keep reduce twins (audit + fill gaps):
| Selector / keyframes | Current reduce? |
|----------------------|-----------------|
| `.bridge-scrub-feed-item.is-enter` / `bridge-scrub-feed-enter` | yes (~L1215) |
| kill report transitions | yes (~L1400) |
| `.bridge-lists-details-summary::before` | yes (~L2049) |
| `.bridge-scanned-toast*` | yes (~L2232) |
| `.bridge-date-chip` transform | yes (~L2748) |
| `bridge-dialog-rise` on `.bridge-history-dialog-card` | **GAP — add twin** |
| `.bridge-train-group` transition + `.is-exiting` | **GAP — add twin** |
| any new Plan 01 animations | **must twin** |

Fail-closed rules (Pitfall 2/4):
- NEVER `display: flex !important` (or similar) on `#bridge-train-wrap` / `.bridge-train-wrap` that overrides `[hidden]`
- If styling visibility, pair with `[hidden] { display: none !important; }` only where already required (toast pattern) — prefer leaving UA `[hidden]` alone
- Admin visibility is JS session + `hidden`, not CSS role skin

Hard constraints:
- CSS/markup only; freeze ids + `data-mode`/`data-action`
- Mission stays **inside** `#bridge-train-wrap` before toolbar
- Cinema copy locks intact; Rules armory label stays
- Bump `bridge.css?v=` if not already bumped in 80-01 (or +1 again if further CSS change)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Train theater + Rules armory distinct modes (DESK-04)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Paint admin Train as climax mode and Rules armory as quiet secondary — without changing gate structure:

1. **Theater chrome** (`.bridge-train-wrap.is-theater`, `.bridge-results-mode--theater`):
   - Mission HUD (`.bridge-train-mission`) stronger heat when theater active (border/glow using tokens).
   - Train tab (`[data-mode="train"]`) primary energy when theater + active (gold/orange gradient face already sketched — align to home/auth-tab energy, desk density).
   - Kept tab demoted escape hatch (lower opacity, no equal peer weight).
   - Rules armory / `[data-mode="brain"]` quietest (smaller type, muted stone) — already partially present; ensure visual distinction is obvious vs Train.
2. **Train cards + armory panels** (`.bridge-train-group`, badges, approve/deny hooks, `.bridge-brain-rule*`):
   - Glass card language; deny uses `--phuglee-danger` (replace `#e8a0a0` hex islands in train deny/status if still present).
   - Keep `data-action="approve"|"deny"` on rendered cards (JS templates frozen — CSS class hooks only).
3. **Fail-closed CSS audit**:
   - Confirm no rule forces `#bridge-train-wrap` / children visible when `[hidden]`.
   - Do not remove `hidden` from wrap in HTML; do not relocate `#bridge-train-mission` outside wrap.
   - Style `:disabled` on `#bridge-train-undo` only; no invented `.is-disabled` without JS.
4. **Markup**: dual-class only if needed; zero id/`data-mode` renames; keep visible label `Rules armory`.

Avoid: “Train looks better outside results” DOM moves; CSS-only admin reveal; changing `bridge-train.js` / `bridge.js`.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-theater.test.js</automated>
  </verify>
  <done>
Theater CSS still matches THTR selectors; train wrap default-hidden; mission inside wrap; armory demoted; no display hacks that fail-open Train.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduced-motion twins for all theater motion (STATES-04)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Audit every `@keyframes` and transition &gt;150ms in `bridge.css` theater clusters; ensure each has a `prefers-reduced-motion: reduce` twin:

1. **Fill known gaps:**
   - `.bridge-history-dialog-card` / `bridge-dialog-rise` → `animation: none !important` under reduce.
   - `.bridge-train-group`, `.bridge-train-group.is-exiting`, `.is-pending` → kill transform/max-height animation under reduce; allow instant opacity hide so cards do not stick mid-exit (pair with existing JS timers — do not edit JS).
2. **Re-verify existing twins still cover:** feed enter, kill report, lists summary chevron, scanned toast, date chips.
3. **Any new motion from Plan 01** (victory glow pulse, shimmer, reveal) → same reduce media block; prefer one consolidated `@media (prefers-reduced-motion: reduce)` section near theater styles over scattering unscoped `!important` wars — document any required `!important` (kill-switch only).
4. **FEED-02 contract:** reduce path must set `animation: none !important` on feed items; content population remains JS-owned (do not make CSS depend on animationend).
5. **Do not weaken** `phuglee-a11y.css` focus rings; bridge reduce rules must not set `outline: none` on interactive theater controls.
6. **Cache bust** — ensure `bridge.css?v=` is bumped after final CSS edit.

Verification aid (run as part of task, not a new test file unless suite already has one):
```powershell
# Must list reduce media + dialog + train-group coverage
Select-String -Path public/css/bridge.css -Pattern 'prefers-reduced-motion|bridge-dialog-rise|bridge-train-group\.is-exiting|bridge-scrub-feed-enter' 
```
  </action>
  <verify>
    <automated>node --test tests/bridge-scrub-feed.test.js tests/bridge-train-theater.test.js tests/bridge-desk-cinema.test.js tests/bridge-kill-rate-scrub.test.js</automated>
  </verify>
  <done>
All theater keyframes/transitions have reduce twins including dialog rise + train exit; FEED-02 + theater + cinema + kill tests green; no fail-open Train CSS; cache-bust current.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-train-theater.test.js tests/bridge-scrub-feed.test.js` pass
- `rg "prefers-reduced-motion" public/css/bridge.css` shows twins covering feed, kill, toast, date-chip, dialog-rise, train-group exit
- HTML: `#bridge-train-wrap` opening tag still has `hidden`; mission still nested inside wrap
- Diff excludes `public/js/**` and `lib/**`
</verification>

<success_criteria>
- DESK-04: Train theater + armory visually distinct; non-admin fail-closed structure preserved
- STATES-04: every new/existing theater animation has prefers-reduced-motion twin; feed still completes without animationend
</success_criteria>

<output>
After completion, create `.planning/phases/80-theater-gates-motion/80-02-SUMMARY.md`
</output>
