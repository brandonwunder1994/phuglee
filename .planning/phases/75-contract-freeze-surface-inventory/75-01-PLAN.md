---
phase: 75-contract-freeze-surface-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/bridge/CONTRACT-FREEZE.md
  - tests/bridge-contract-freeze.test.js
  - docs/bridge/TEST-PLAN.md
autonomous: true
requirements:
  - DESK-05
must_haves:
  truths:
    - "Maintainer has a written freeze checklist of critical bridge-* IDs, data-action / data-mode / data-format / data-step values, and cinema structure-order locks"
    - "Automated greppable freeze test fails if a locked id or data-action contract is removed from bridge.html / bridge.js"
    - "No bridge-* IDs or data-* contracts are renamed as part of this plan"
  artifacts:
    - path: "docs/bridge/CONTRACT-FREEZE.md"
      provides: "Human-readable DESK-05 freeze checklist for all later v3.0 visual phases"
      contains: "DESK-05"
    - path: "tests/bridge-contract-freeze.test.js"
      provides: "Static HTML/JS contract freeze (DESK-05) — no browser"
      contains: "DESK-05"
    - path: "docs/bridge/TEST-PLAN.md"
      provides: "Pointer to contract freeze suite under v3.0 / DESK-05"
      contains: "DESK-05|contract-freeze|CONTRACT-FREEZE"
  key_links:
    - from: "tests/bridge-contract-freeze.test.js"
      to: "public/bridge.html + public/js/bridge.js"
      via: "fs.readFileSync + assert.match on real shipped strings"
      pattern: "id=\"bridge-|data-action=\"|DESK-05"
    - from: "docs/bridge/CONTRACT-FREEZE.md"
      to: "public/bridge.html IDs + bridge.js getElementById / data-action delegation"
      via: "inventory tables extracted from current surface"
      pattern: "bridge-process|bridge-train-wrap|approve|deny|flash-download"
---

<objective>
Freeze Filter DOM contracts for v3.0: write a durable DESK-05 checklist and a cheap greppable static test so later visual phases cannot rename `bridge-*` IDs or churn `data-action` / `data-mode` / `data-format` / `data-step`.

Purpose: Without a locked contract, every restyle is roulette against ~70 JS boot lookups and cinema/theater structure tests. This plan is the non-negotiable first gate of the makeover.

Output: `docs/bridge/CONTRACT-FREEZE.md` + `tests/bridge-contract-freeze.test.js` + TEST-PLAN pointer. **No CSS paint. No ID renames. No JS behavior changes.**
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-contract-freeze-surface-inventory/75-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@public/bridge.html
@public/js/bridge.js
@public/js/bridge-train.js
@tests/bridge-desk-cinema.test.js
@tests/bridge-train-theater.test.js
@docs/bridge/TEST-PLAN.md
@AGENTS.md

<interfaces>
<!-- Extracted contracts — assert these exact strings; never invent new IDs. -->

Critical boot IDs (bridge.js getElementById — sample of locked set; freeze doc must cover full HTML inventory):
```
bridge-state, bridge-city, bridge-city-dossier, bridge-outcome-drawer,
bridge-type-panel, bridge-upload-panel, bridge-loading-panel, bridge-results-panel,
bridge-dropzone, bridge-file-input, bridge-process, bridge-clear-file,
bridge-save-list, bridge-list-name, bridge-save-panel,
bridge-train-wrap, bridge-train-mission, bridge-train-panel, bridge-brain-panel,
bridge-mode-kept, bridge-mode-train, bridge-mode-brain,
bridge-victory-strip, bridge-victory-download, bridge-victory-next,
bridge-mission-surface, bridge-kpi-grid, bridge-scrub-stage,
bridge-type-column-confirm-dialog, bridge-history-dialog,
bridge-lists-panel, bridge-inventory-hud, bridge-scrub-feed
```

data-action values (JS templates + delegation — locked):
```
approve | deny | download | rename | delete | select | flash-download
```

data-mode values (mode tabs):
```
kept | train | brain
```

data-format values:
```
csv | xlsx
```

data-step values (pipeline):
```
location | type | upload | results
```

Radio contract:
```
name="bridge-upload-type" value="code_violation" | value="water_shut_off"
```

Structure-order locks (must document + test subset):
- `#bridge-train-mission` inside `#bridge-train-wrap` (THTR-03)
- `#bridge-train-wrap` default `hidden` (fail-closed)
- Save climax before train wrap region; attach scrap demoted
- Victory strip: `Filter Data` + `data-action="flash-download"` + `DELETE THE JUNK`
- Type chips not essay cards (`bridge-type-card` banned)

Static test pattern (match project standard):
```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
const ROOT = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(ROOT, 'public', 'bridge.html'), 'utf8');
const js = fs.readFileSync(path.join(ROOT, 'public', 'js', 'bridge.js'), 'utf8');
// assert.match against REAL strings — never invent
```

Hard bans for this plan:
- Do NOT edit public/js/** behavior, lib/**, or rename any id/data-* in HTML
- Do NOT restyle (no bridge.css / components paint)
- Do NOT wipe data/filter-lists or brain
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DESK-05 contract freeze checklist</name>
  <files>docs/bridge/CONTRACT-FREEZE.md</files>
  <action>
Create `docs/bridge/CONTRACT-FREEZE.md` as the durable freeze bible for v3.0 visual work. Extract from **current** `public/bridge.html` + `public/js/bridge.js` + `bridge-train.js` (read-only — do not modify them).

Document must include:

1. **Header** — Phase 75 / DESK-05; rule: CSS/markup class hooks only; never rename IDs or data-* values; wrap outside locked nodes.

2. **§ Critical `bridge-*` IDs** — Complete table of every `id="bridge-…"` in `bridge.html` (grep inventory). Columns: `id`, region (hero/pipeline/scrub/dossier/upload/loading/results/train/lists/dialogs/victory), JS consumer note (`boot` / `lazy getElementById` / `template-only`). Flag any HTML id not referenced by JS as "presentational" still freeze.

3. **§ `data-action` contract** — List locked values: `approve`, `deny`, `download`, `rename`, `delete`, `select`, `flash-download`. Note: Train card copy may change later only if tests update — **values never change**. List where each is produced (static HTML vs JS template).

4. **§ `data-mode` / `data-format` / `data-step` / radio names** — Exact allowed values and host elements.

5. **§ Structure-order locks** — At minimum:
   - train mission inside train wrap; wrap starts `hidden`
   - cinema climax: kill/mission surface + save elevated; attach scrap; table collapsible
   - victory strip IDs + required slogans (`DELETE THE JUNK`, `Filter Data`, `Scrub next city`)
   - type chips radio contract; banned `bridge-type-card`
   - type-confirm + history remain native `<dialog>` with fixed control IDs
   - no Analyze push CTAs (banned strings from desk-cinema)

6. **§ Restyle rules** — Dual-class OK (`bridge-*` + `phuglee-*`); never force `display:flex !important` on `#bridge-train-wrap`; never replace `<dialog>`; never invent CSS-only show/hide that bypasses `hidden`.

7. **§ Verification** — Point to `tests/bridge-contract-freeze.test.js` + existing theater/cinema suites; full `npm test` remains permanent bar.

Source extraction commands (PowerShell from repo root):
```powershell
rg 'id="bridge-' public/bridge.html
rg "getElementById\('bridge-" public/js
rg 'data-action' public/js/bridge.js public/js/bridge-train.js public/bridge.html
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='docs/bridge/CONTRACT-FREEZE.md'; const t=fs.readFileSync(p,'utf8'); if(!/DESK-05/.test(t)) process.exit(1); if(!/bridge-process/.test(t)) process.exit(1); if(!/data-action/.test(t)) process.exit(1); if(!/bridge-train-wrap/.test(t)) process.exit(1); console.log('CONTRACT-FREEZE ok', t.length)"</automated>
  </verify>
  <done>CONTRACT-FREEZE.md exists with DESK-05, full ID inventory, data-action/mode/format/step locks, structure-order rules, and restyle bans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Greppable DESK-05 freeze test + TEST-PLAN pointer</name>
  <files>tests/bridge-contract-freeze.test.js, docs/bridge/TEST-PLAN.md</files>
  <behavior>
    - DESK-05: critical scrub path IDs present in HTML (scrub-stage, dropzone, process, type-panel, upload-panel, mission-surface, save-list, train-wrap, victory-strip, type-column-confirm-dialog)
    - DESK-05: train wrap default hidden + mission id present; mission appears after wrap open in HTML
    - DESK-05: data-mode kept|train|brain on mode tabs; flash-download + data-format csv on victory download
    - DESK-05: radio name bridge-upload-type values code_violation + water_shut_off
    - DESK-05: bridge.js still references getElementById for process, train-wrap, victory-download, dropzone
    - DESK-05: data-action approve|deny still emitted in bridge.js and/or bridge-train.js templates
    - DESK-05: list data-action download|rename|delete|select still present in bridge.js list render
    - DESK-05: no banned Analyze push CTAs in HTML/JS (reuse cinema ban list)
  </behavior>
  <action>
1. Create `tests/bridge-contract-freeze.test.js` using node:test + static `fs.readFileSync` of `public/bridge.html`, `public/js/bridge.js`, and `public/js/bridge-train.js` (same pattern as `tests/bridge-desk-cinema.test.js`).

2. Title every test `DESK-05: …` so the requirement is greppable. Keep the file **focused** (~8–12 tests) — do not re-implement the entire cinema/theater suite; assert the freeze **spine** only:
   - Core path IDs (scrub → process → results → save → train wrap → victory → type confirm dialog)
   - Structure: train-wrap `hidden`; train-mission after wrap; save-panel id present
   - data-mode / data-format / upload-type radios
   - JS still bootstraps critical ids via string literals `getElementById('bridge-…')` or equivalent present strings
   - data-action approve/deny + list actions + flash-download
   - Banned Analyze push strings absent

3. Run: `node --test tests/bridge-contract-freeze.test.js` — must pass green against current tree (baseline is already correct; test locks as-is).

4. Append a short section to `docs/bridge/TEST-PLAN.md` (or extend an existing v3/DESK section if present) mapping:
   - DESK-05 → `tests/bridge-contract-freeze.test.js` + `docs/bridge/CONTRACT-FREEZE.md`
   - Note: existing `bridge-desk-cinema` / `bridge-train-theater` remain complementary locks

5. Do **not** change product HTML/JS/CSS to make tests pass. If a string is missing, the freeze doc must record reality — tests assert current shipped contracts only.
  </action>
  <verify>
    <automated>node --test tests/bridge-contract-freeze.test.js</automated>
  </verify>
  <done>Freeze test file green; all titles include DESK-05; TEST-PLAN points to freeze suite; zero product renames.</done>
</task>

</tasks>

<verification>
- `docs/bridge/CONTRACT-FREEZE.md` greppable for DESK-05, bridge-process, data-action, train-wrap
- `node --test tests/bridge-contract-freeze.test.js` exit 0
- Diff contains only docs + new test file (no public/js, lib, or ID renames)
</verification>

<success_criteria>
1. Written freeze checklist exists and lists critical contracts used by JS/tests
2. Greppable automated freeze test locks DESK-05 spine
3. Operator contracts unchanged (no renames, no data-action churn)
</success_criteria>

<output>
After completion, create `.planning/phases/75-contract-freeze-surface-inventory/75-01-SUMMARY.md`
</output>
