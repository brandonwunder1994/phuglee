---
phase: 75-contract-freeze-surface-inventory
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - docs/bridge/SURFACE-INVENTORY.md
  - docs/bridge/STATE-MATRIX.md
  - docs/bridge/CONTRACT-FREEZE.md
autonomous: true
requirements:
  - DESK-05
must_haves:
  truths:
    - "Maintainer can map every /bridge visual region (hero → dialogs) to a design-system target layer (tokens / phuglee components / bridge layout)"
    - "Maintainer has a state matrix of JS-toggled states that CSS may style but must not invent"
    - "Later phases can restyle without inventing parallel show/hide or new data-* attributes"
  artifacts:
    - path: "docs/bridge/SURFACE-INVENTORY.md"
      provides: "Region → layer map for phases 76–80 paint order"
      contains: "DESK-05|scrub|tokens|phuglee"
    - path: "docs/bridge/STATE-MATRIX.md"
      provides: "JS state classes/attributes CSS may style only"
      contains: "hidden|disabled|is-theater|has-file|DESK-05"
  key_links:
    - from: "docs/bridge/SURFACE-INVENTORY.md"
      to: "public/bridge.html regions + public/css tokens/components/bridge cascade"
      via: "table of region → primary stylesheet target"
      pattern: "bridge-scrub-stage|phuglee-components|tokens\\.css|bridge\\.css"
    - from: "docs/bridge/STATE-MATRIX.md"
      to: "public/js/bridge.js classList + hidden/disabled toggles"
      via: "documented state tokens only — no invented .is-disabled without JS"
      pattern: "is-theater|has-file|is-dragover|hidden|disabled"
    - from: "docs/bridge/CONTRACT-FREEZE.md"
      to: "SURFACE-INVENTORY + STATE-MATRIX"
      via: "cross-links so freeze doc points to inventory companions"
      pattern: "SURFACE-INVENTORY|STATE-MATRIX"
---

<objective>
Complete DESK-05 foundation with a surface inventory (what to restyle, which layer owns it) and a JS state matrix (what CSS may style without inventing parallel state).

Purpose: Phases 76–80 need a paint map and a hard rule that visibility/enablement states come from existing JS toggles only — prevents ghost panels, fail-open Train, and CSS stratigraphy thrash.

Output: `docs/bridge/SURFACE-INVENTORY.md` + `docs/bridge/STATE-MATRIX.md` + cross-links from CONTRACT-FREEZE. **Docs only — no visual paint, no JS changes.**
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-contract-freeze-surface-inventory/75-CONTEXT.md
@.planning/phases/75-contract-freeze-surface-inventory/75-01-SUMMARY.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@docs/bridge/CONTRACT-FREEZE.md
@public/bridge.html
@public/css/bridge.css
@public/css/phuglee-components.css
@public/css/tokens.css
@public/js/bridge.js
@AGENTS.md

<interfaces>
<!-- Target cascade (as-built + v3.0 direction from research) -->
```
Layer 0  tokens.css              → brand/glass/type values
Layer 1  glass + phuglee-*       → shared elevation + controls
Layer 2  shell + premium-atm     → authenticated app chrome
Layer 3  bridge.html + bridge.css → Filter layout + theater only
```

Regions that MUST appear in SURFACE-INVENTORY (from ROADMAP DESK / FEATURES):
```
hero / title chrome
pipeline (slim steps)
scrub stage (city search + state/city)
city dossier + outcome scrap drawer
type chips panel
upload (date chips, paste path, dropzone, process CTA)
loading / scrub feed
mission surface / kill KPI report
save climax panel
train theater + rules armory (brain panel)
kept table + filters + pagination
attach scrap
lists / staging inventory + inventory HUD
victory strip
error wrap
history dialog + type-column confirm dialog
shift queue (if present in HTML/JS)
```

Known JS-toggled states to document (from bridge.js classList / properties — extract live, do not invent):
```
hidden (attribute via setHidden)
disabled (property on buttons/inputs)
is-theater (train wrap)
bridge-results-mode--theater (mode rail)
is-active (mode tabs, pipeline steps, typeahead options)
is-complete (pipeline)
has-file, is-dragover (dropzone)
is-selected (date chips)
is-open (outcome drawer, train open count)
is-pending, is-exiting (train cards)
is-success, is-error, is-busy (status lines)
is-in, is-out (toast)
bridge-kill-report (kpi grid class)
is-sorted (table headers)
bridge-lists-flash / flash download classes if present
aria-selected / aria-pressed / aria-busy where JS sets them
```

Hard rules for STATE-MATRIX:
- CSS styles these states; CSS must NOT invent alternate show/hide that bypasses [hidden]
- Never document "use .is-disabled" unless JS already toggles that class (prefer :disabled)
- Admin fail-closed remains JS hidden on #bridge-train-wrap — CSS is never sole gate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write surface inventory mapped to design-system layers</name>
  <files>docs/bridge/SURFACE-INVENTORY.md</files>
  <action>
Create `docs/bridge/SURFACE-INVENTORY.md` for DESK-05 restyle targeting.

1. **Header** — Phase 75 companion to CONTRACT-FREEZE; north star = login/home visual DNA; application surface = `/bridge` only this milestone.

2. **§ Region table** — One row per visual region listed in interfaces (hero → dialogs). Columns:
   - Region name
   - Primary root id(s) / class anchors from current `bridge.html` (real ids only)
   - Current stylesheet home (`bridge.css` section comment if known, or `phuglee-components` if already dual-classed)
   - **Target layer for v3.0** (`tokens` | `phuglee-components` | `bridge layout/theater` | `a11y`)
   - Paint phase hint (76 tokens / 77 components / 78 hooks / 79 desk core / 80 theater)
   - Notes (e.g. "keep density desk-tight", "native dialog only", "fail-closed train")

3. **§ Elevation sketch** — Short bullet map: primary scrub stage elevated; scrap/outcome quieter; victory featured; kill report climax-first. No new CSS — documentation only.

4. **§ Orphan risk** — Call out any chrome still on one-off hex / non-phuglee classes that Phase 80 must not leave unpainted (from quick scan of bridge.html class names). Do not fix them here.

5. **§ Anti-targets** — Explicit non-goals: process engine, brain store, keep/kill logic, Analyze push, ID renames.

Cross-link `docs/bridge/CONTRACT-FREEZE.md` and `docs/bridge/STATE-MATRIX.md`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('docs/bridge/SURFACE-INVENTORY.md','utf8'); for (const s of ['DESK-05','bridge-scrub-stage','phuglee','tokens','victory','train']) { if(!t.includes(s)) { console.error('missing',s); process.exit(1);} } console.log('SURFACE-INVENTORY ok')"</automated>
  </verify>
  <done>Surface inventory maps every major /bridge region to a target layer and later paint phase without proposing renames.</done>
</task>

<task type="auto">
  <name>Task 2: Write JS state matrix + cross-link freeze doc</name>
  <files>docs/bridge/STATE-MATRIX.md, docs/bridge/CONTRACT-FREEZE.md</files>
  <action>
1. Create `docs/bridge/STATE-MATRIX.md` documenting **only states JS already toggles**. Extract via read-only grep of `public/js/bridge.js`, `bridge-train.js`, `bridge-scrub-feed.js` for `classList`, `.hidden`, `.disabled`, `setHidden`, `aria-`.

   Table columns:
   - State token (attribute/class/property)
   - Host element(s) / selectors
   - Who sets it (function or path name if obvious)
   - CSS may: (style appearance)
   - CSS must not: (e.g. force display flex on train wrap; invent .is-disabled without JS; hide via opacity only leaving focusable)

   Explicit sections:
   - Visibility (`hidden` / setHidden)
   - Enablement (`disabled`)
   - Theater / mode (`is-theater`, mode `is-active`, `data-mode`)
   - Dropzone (`has-file`, `is-dragover`)
   - Status tones (`is-success`, `is-error`, `is-busy`)
   - Train card motion (`is-pending`, `is-exiting`) — note reduced-motion is CSS twin, content must still complete
   - Dialogs (native open state — do not replace with div modals)

2. **§ Restyle checklist for implementers** — 6–10 bullets operators of later phases must follow (pair `[hidden]`, style `:disabled`, no pointer-events on parent dropzone, etc.). Align with PITFALLS.md Pitfalls 2 & 4 without rewriting that research file.

3. Update `docs/bridge/CONTRACT-FREEZE.md` with a short **Related docs** section linking SURFACE-INVENTORY + STATE-MATRIX (if not already linked). Do not rewrite the freeze inventory.

4. Do not add CSS rules, do not edit bridge.js, do not invent new state class names for "future use".
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=fs.readFileSync('docs/bridge/STATE-MATRIX.md','utf8'); const c=fs.readFileSync('docs/bridge/CONTRACT-FREEZE.md','utf8'); for (const s of ['DESK-05','hidden','disabled','is-theater','has-file']) { if(!m.includes(s)) { console.error('matrix missing',s); process.exit(1);} } if(!/SURFACE-INVENTORY|STATE-MATRIX/.test(c)) { console.error('freeze missing cross-links'); process.exit(1);} console.log('STATE-MATRIX + cross-links ok')"</automated>
  </verify>
  <done>State matrix documents JS-owned states with CSS may/must-not rules; freeze doc cross-links inventory companions.</done>
</task>

</tasks>

<verification>
- SURFACE-INVENTORY covers scrub → victory → dialogs with layer targets
- STATE-MATRIX lists hidden/disabled/is-theater/has-file at minimum
- CONTRACT-FREEZE cross-links both companions
- No product code changes (docs only this plan)
</verification>

<success_criteria>
1. Every major Filter visual region has a restyle target layer
2. State matrix prevents inventing CSS-only workflow states
3. DESK-05 foundation complete for Phase 76 token work
</success_criteria>

<output>
After completion, create `.planning/phases/75-contract-freeze-surface-inventory/75-02-SUMMARY.md`
</output>
