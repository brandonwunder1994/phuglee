---
phase: 63-idle-proof-process-climax
plan: 02
type: execute
wave: 2
depends_on:
  - "63-01"
files_modified:
  - public/bridge.html
  - public/css/bridge.css
  - public/js/bridge.js
  - tests/bridge-idle-proof-process-climax.test.js
autonomous: true
requirements:
  - IDLE-02
  - IDLE-01
must_haves:
  truths:
    - "Upload panel hierarchy: dropzone is the stage; #bridge-process is the one fire CTA"
    - "Response date is tight meta (not a peer form block above the dropzone) but still required"
    - "#bridge-response-date, getResponseAtValue(), processUpload date gate, and attachDataset date gate remain"
    - "Process multipart still does not append responseAt (attach owns KPI date server-side)"
    - "Static tests lock IDLE-01 strip + IDLE-02 hierarchy/gates; no live feed or kill-report chrome"
  artifacts:
    - path: "public/bridge.html"
      provides: "Upload climax DOM order: dropzone → response meta → Process"
      contains: "id=\"bridge-dropzone\"|id=\"bridge-response-date\"|id=\"bridge-process\""
    - path: "public/css/bridge.css"
      provides: "Demoted response meta + elevated dropzone/Process hierarchy"
      contains: "bridge-response|bridge-dropzone|bridge-actions--upload"
    - path: "public/js/bridge.js"
      provides: "Preserved date gates + process enablement; no gate removal"
      contains: "getResponseAtValue|Enter the date the city sent"
    - path: "tests/bridge-idle-proof-process-climax.test.js"
      provides: "IDLE-01/02 static regression locks"
      contains: "IDLE-01|IDLE-02"
  key_links:
    - from: "#bridge-upload-panel"
      to: "dropzone stage + Process fire"
      via: "DOM order + CSS hierarchy"
      pattern: "bridge-dropzone|bridge-process"
    - from: "processUpload"
      to: "getResponseAtValue"
      via: "click-time hard gate before FormData"
      pattern: "getResponseAtValue|Enter the date the city sent"
    - from: "buildProcessFormData"
      to: "multipart without responseAt"
      via: "append cityId/uploadType/file only"
      pattern: "buildProcessFormData"
---

<objective>
Ship **IDLE-02**: upload-step **Process climax** — dropzone as stage, Process as one fire CTA, response date demoted to tight required meta — plus automated locks for IDLE-01/02.

Purpose: Upload beat reads as desk climax without dropping Form Forge KPI date habit (process click gate + attach server requirement).

Output: Reordered/restyled `#bridge-upload-panel`; gates preserved; `tests/bridge-idle-proof-process-climax.test.js`; green suite + verify-live.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-idle-proof-process-climax/63-CONTEXT.md
@.planning/phases/63-idle-proof-process-climax/63-RESEARCH.md
@.planning/phases/63-idle-proof-process-climax/63-VALIDATION.md
@.planning/phases/63-idle-proof-process-climax/63-01-SUMMARY.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@tests/bridge-list-factory-ux.test.js
@AGENTS.md

<interfaces>
<!-- Current upload panel order (must change hierarchy) -->
```
#bridge-upload-panel
  h2 + lead
  .bridge-format-badges
  .bridge-response-row   ← full fieldset PEER block (demote)
  #bridge-dropzone       ← elevate to stage
  .bridge-actions--upload  #bridge-process + #bridge-clear-file
```

<!-- Target order (research Pattern A default; pattern B under-dropzone also OK if Process still dominates) -->
```
#bridge-upload-panel
  h2 + short lead
  format badges (tertiary)
  #bridge-dropzone          ← RANK 1 stage
  response META chip/row    ← RANK 4 tight; still visible + required
  actions: #bridge-process fire + #bridge-clear-file ghost
```

<!-- Stable IDs (D5 — preserve) -->
#bridge-upload-panel, #bridge-response-date, #bridge-response-datetime,
#bridge-response-hint, #bridge-dropzone, #bridge-file-input, #bridge-browse,
#bridge-process, #bridge-clear-file

<!-- Date contract — DO NOT REMOVE -->
getResponseAtValue():
  #bridge-response-date must match /^\d{4}-\d{2}-\d{2}$/
  → local noon Date → toISOString(); empty/invalid → ''

processUpload gate (keep):
  if !getResponseAtValue() → showError('Enter the date the city sent this list before processing.'); focusResponseDateTime(); return

attachDataset gate (keep):
  responseReceivedAt required

buildProcessFormData:
  still does NOT append responseAt — leave as-is

<!-- Copy demotion -->
- Legend / label shrink to **Received** or **Response date** (not long essay)
- Hint: one-line muted meta OR title tooltip — keep meaning: feeds Form Forge turnaround KPIs
- Must NOT display:none the date while claiming required
- Must NOT silent-default today's date

<!-- Out of scope -->
- Live scrub feed during process (64) — leave #bridge-loading-panel as-is
- Kill-rate report (65)
- Engine/processUpload algorithm changes beyond gate preservation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure upload panel — dropzone stage + demoted date meta + Process fire</name>
  <files>public/bridge.html, public/css/bridge.css, public/js/bridge.js</files>
  <read_first>
    - public/bridge.html (`#bridge-upload-panel` ~196-226)
    - public/css/bridge.css (`.bridge-response-row` ~1492+, `.bridge-dropzone*`, `.bridge-actions--upload`)
    - public/js/bridge.js (`getResponseAtValue` ~1447; `processUpload` date gate ~2777; `buildProcessFormData`; `syncFileUi`)
    - .planning/phases/63-idle-proof-process-climax/63-RESEARCH.md (IDLE-02, Pattern 3, Pitfalls 3/6)
    - AGENTS.md (verify-live after public/)
  </read_first>
  <action>
    1. **Reorder `#bridge-upload-panel` in `public/bridge.html`** to climax hierarchy:

       Preferred structure:
       ```html
       <section id="bridge-upload-panel" ...>
         <h2>...</h2>
         <p class="bridge-panel-lead">...</p>
         <div class="bridge-format-badges">...</div>

         <!-- STAGE -->
         <div class="bridge-dropzone" id="bridge-dropzone" ...>...</div>

         <!-- TIGHT META — still required, still visible -->
         <div class="bridge-response-row bridge-response-row--meta">
           <fieldset class="bridge-datetime-picker" id="bridge-response-datetime">
             <legend class="bridge-sr-only">Response received date</legend>
             <div class="bridge-datetime-fields">
               <label class="bridge-datetime-date" for="bridge-response-date">
                 <span class="bridge-datetime-label">Received</span>
                 <input type="date" id="bridge-response-date" required
                   aria-describedby="bridge-response-hint" />
               </label>
             </div>
           </fieldset>
           <p class="bridge-response-hint" id="bridge-response-hint"
              title="Feeds Form Forge turnaround KPIs">Response date · Form Forge KPIs</p>
         </div>

         <div class="bridge-actions bridge-actions--upload">
           <button ... id="bridge-process" disabled>Process upload</button>
           <button ... id="bridge-clear-file" hidden>Clear files</button>
         </div>
       </section>
       ```

       - Dropzone **before** response meta (or meta under dropzone, above Process — both OK if Process remains the largest CTA).
       - Response must **not** sit as a full peer form section **above** the dropzone with long fieldset essay.
       - Keep `required` on `#bridge-response-date` (or equivalent aria-required if native required kept).
       - Preserve all stable IDs listed in interfaces.
       - Process remains sole primary fire CTA (`phuglee-btn-primary` / primary classes); Clear stays ghost/secondary.
       - Touch targets ≥44px for Process.

    2. **CSS demotion/elevation** in `public/css/bridge.css`:
       - `.bridge-response-row--meta` (or demoted `.bridge-response-row`): compact single row; reduced margin (e.g. 0.5–0.75rem vs current 1.25rem peer block); smaller label; muted hint
       - Ensure dropzone remains the visually dominant stage (existing gradient frame OK; do not shrink it to match date)
       - `.bridge-actions--upload #bridge-process` clearly primary fire; min-height ≥44px
       - No equal multi-metric feature cards inside upload panel
       - Spacing tokens from existing scale only

    3. **JS gates — preserve, do not "fix" requiredness:**
       - Keep `getResponseAtValue()`, `focusResponseDateTime()`
       - Keep processUpload error: `Enter the date the city sent this list before processing.`
       - Keep attachDataset response date required path
       - Keep `syncFileUi` enablement based on files (date remains click-time gate unless you optionally also disable Process when date empty — **not required**)
       - Do **not** append response date to `buildProcessFormData`
       - Do **not** silent-default the date input
       - Do **not** hide date with `display:none` / `visibility:hidden` / zero-size offscreen tricks

    4. Bump `bridge.css?v=` and `bridge.js?v=` cache busters if not already bumped after 63-01 edits.

    5. Run `scripts\verify-live.ps1` after public edits (exit 0).
  </action>
  <acceptance_criteria>
    - In `#bridge-upload-panel` HTML source, `id="bridge-dropzone"` appears **before** `id="bridge-response-date"` OR date is clearly in a meta class row under dropzone (dropzone not buried under full fieldset essay above it)
    - `id="bridge-response-date"` still present with `required` or aria-required
    - `id="bridge-process"` still present with Process upload label pattern
    - public/js/bridge.js still contains `getResponseAtValue` and `Enter the date the city sent this list`
    - public/js/bridge.js `buildProcessFormData` still does not append `responseAt` / `responseReceivedAt`
    - CSS has demoted meta treatment (class containing `response` + meta or reduced compact rules)
    - verify-live.ps1 exit 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('public/bridge.html','utf8');const j=fs.readFileSync('public/js/bridge.js','utf8');const start=h.indexOf('id=\"bridge-upload-panel\"');const end=h.indexOf('id=\"bridge-loading-panel\"');const panel=h.slice(start,end>start?end:start+4000);const d=panel.indexOf('id=\"bridge-dropzone\"');const r=panel.indexOf('id=\"bridge-response-date\"');const p=panel.indexOf('id=\"bridge-process\"');if(d<0||r<0||p<0){console.error('missing ids');process.exit(1)}if(!(d<r||panel.includes('bridge-response-row--meta')||panel.includes('response-row--meta'))){console.error('date still peer-above climax without meta demotion');process.exit(2)}if(!panel.includes('bridge-response-date'))process.exit(3);if(!j.includes('function getResponseAtValue'))process.exit(4);if(!j.includes('Enter the date the city sent this list'))process.exit(5);const bi=j.indexOf('function buildProcessFormData');const b=j.slice(bi,bi+1200);if(/append\(['\"]response/.test(b)){console.error('process FormData must not send response date');process.exit(6)}console.log('63-02-climax-ok')" ; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>Upload climax hierarchy shipped; date required and visible as meta; process/attach gates intact; live healthy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Static IDLE-01/02 regression locks + suite green</name>
  <files>tests/bridge-idle-proof-process-climax.test.js</files>
  <read_first>
    - tests/bridge-list-factory-ux.test.js (static HTML/JS pattern)
    - public/bridge.html (after Task 1)
    - public/js/bridge.js (idle + gates after 63-01 and Task 1)
    - .planning/phases/63-idle-proof-process-climax/63-VALIDATION.md
  </read_first>
  <behavior>
    - IDLE-01: HTML has #bridge-idle-proof; JS has computeIdleProof + renderIdleProof + "lists staged" + "Last save"; empty copy "0 lists staged"; renderSavedLists calls renderIdleProof; no /api/bridge/idle-stats
    - IDLE-02: upload panel still has #bridge-dropzone, #bridge-response-date, #bridge-process; processUpload still gates on getResponseAtValue / "Enter the date the city sent"; buildProcessFormData does not append responseAt
    - Scope hygiene: no mandatory live feed strings inventing FEED-01 theater; no kill-rate scrub report chrome required here
    - Stable: no banned Analyze push CTAs reintroduced
  </behavior>
  <action>
    Create `tests/bridge-idle-proof-process-climax.test.js` following the static-file pattern of `tests/bridge-list-factory-ux.test.js` (node:test + assert/strict + fs read of public/bridge.html + public/js/bridge.js).

    Required tests (titles include IDLE-01 or IDLE-02):

    1. **IDLE-01: idle proof mount present** — `id="bridge-idle-proof"` in HTML
    2. **IDLE-01: computeIdleProof + renderIdleProof wired** — functions exist; `renderSavedLists` body calls `renderIdleProof`; copy keys `lists staged` and `Last save`
    3. **IDLE-01: honest empty copy** — JS or HTML includes `0 lists staged`
    4. **IDLE-01: no idle-stats endpoint** — JS does not contain `/api/bridge/idle-stats`
    5. **IDLE-02: stable upload climax ids** — HTML has `#bridge-dropzone`, `#bridge-response-date`, `#bridge-process`
    6. **IDLE-02: process date gate preserved** — JS has `getResponseAtValue` and error string about date before processing
    7. **IDLE-02: process FormData omits response date** — slice of `buildProcessFormData` does not append responseAt/responseReceivedAt
    8. Optional hygiene: banned Analyze CTAs still absent (mirror list-factory ban list) if cheap

    Run:
    ```
    node --test tests/bridge-idle-proof-process-climax.test.js
    npm test
    ```
    If any public residual: `scripts\verify-live.ps1`.

    Do **not** wipe filter-lists. Do **not** add live feed or kill-report implementation tests that require building Phase 64/65 features.
  </action>
  <acceptance_criteria>
    - tests/bridge-idle-proof-process-climax.test.js exists
    - At least 2 test titles contain IDLE-01 and 2 contain IDLE-02
    - `node --test tests/bridge-idle-proof-process-climax.test.js` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-idle-proof-process-climax.test.js ; npm test</automated>
  </verify>
  <done>IDLE-01/02 locked with automated static suite; full npm test green.</done>
</task>

</tasks>

<verification>
- Climax static gate + verify-live green
- IDLE test file green
- Full `npm test` green
- No process engine rewrite; date still required before process/attach
</verification>

<success_criteria>
- IDLE-02: Process is visual climax; response date tight meta still required
- IDLE-01 locks remain green after climax HTML edits
- No Phase 64/65 scope creep
</success_criteria>

<output>
After completion, create `.planning/phases/63-idle-proof-process-climax/63-02-SUMMARY.md`
</output>
