---
phase: 63-idle-proof-process-climax
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/bridge.html
  - public/js/bridge.js
  - public/css/bridge.css
autonomous: true
requirements:
  - IDLE-01
must_haves:
  truths:
    - "At idle (before process), operator sees live proof metrics derived from savedLists / GET /api/bridge/lists"
    - "Minimum metrics: lists staged, total records ready, last save — not fake decorative numbers"
    - "Empty inventory shows honest zeros / empty copy (0 lists staged), not marketing placeholders"
    - "Idle strip updates from the same loadSavedLists → renderSavedLists path (no dual fetch)"
    - "Strip is a compact proof row — not equal 3-up icon feature cards (DESK-02 / M5 anti-pattern)"
  artifacts:
    - path: "public/bridge.html"
      provides: "Always-on #bridge-idle-proof mount for live metrics"
      contains: "id=\"bridge-idle-proof\""
    - path: "public/js/bridge.js"
      provides: "computeIdleProof + renderIdleProof from savedLists; wired in renderSavedLists"
      contains: "computeIdleProof|renderIdleProof"
    - path: "public/css/bridge.css"
      provides: "Compact idle proof strip styles (row hierarchy, not equal cards)"
      contains: "bridge-idle-proof"
  key_links:
    - from: "loadSavedLists"
      to: "renderIdleProof"
      via: "renderSavedLists calls renderIdleProof on empty and non-empty paths"
      pattern: "renderIdleProof\\(\\)"
    - from: "computeIdleProof(lists)"
      to: "lists staged · records ready · last save"
      via: "listCount / recordTotal / lastSaveAt from createdAt"
      pattern: "recordCount|createdAt|lists staged"
    - from: "#bridge-idle-proof"
      to: "GET /api/bridge/lists summaries"
      via: "savedLists already loaded at boot"
      pattern: "id=\"bridge-idle-proof\""
---

<objective>
Ship **IDLE-01**: live idle proof metrics at desk rest from existing list/API data (`savedLists` via `GET /api/bridge/lists`).

Purpose: Operator sees inventory truth **before** process — lists staged, total records ready, last save — not only post-process KPIs or decorative step titles.

Output: Mount + CSS for a compact live strip; `computeIdleProof` / `renderIdleProof` wired into the existing lists render path. **No new API. No lists store changes. No process engine changes.**
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-idle-proof-process-climax/63-CONTEXT.md
@.planning/phases/63-idle-proof-process-climax/63-RESEARCH.md
@.planning/phases/63-idle-proof-process-climax/63-VALIDATION.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@lib/bridge-list-store.js
@AGENTS.md

<interfaces>
<!-- Source of truth: existing client inventory -->
```
loadSavedLists()
  → savedLists = data.lists   // GET /api/bridge/lists → { ok, lists: Summary[] }
  → renderSavedLists()        // table + #bridge-lists-total
  → renderIdleProof()         // NEW: desk strip from same array
```

Summary fields used (toSummary — do not require row payloads):
- recordCount → Σ total records ready
- createdAt → last save (API sorts newest-first; use lists[0].createdAt)
- length → lists staged

Optional extras (only if strip stays one row, not a grid): ready vs downloaded count — **not required**.

<!-- Locked copy targets (Command-style proof-num voice) -->
Non-empty example:
`3 lists staged · 12,480 records ready · Last save Jul 10, 2:14 PM`

Empty:
`0 lists staged · Ready when you scrub the first city`

Use existing `formatListWhen(iso)` for last-save display. Label must say **Last save** / **Last staged** — never "last process" (process does not write lists).

<!-- Placement (Claude discretion — locked choice for executor) -->
Compact **always-on strip under hero** (before decorative proof rail if it still exists).
- Prefer id `bridge-idle-proof` with role="status" aria-live="polite"
- Do **NOT** convert `.bridge-proof-rail` into three equal metric cards
- Phase 61 owns killing the decorative rail; this plan adds live proof without reintroducing M5 grids
- City dossier (62) is city-scoped; this strip is **global** inventory

<!-- Anti-scope -->
- No live activity feed (64)
- No kill-rate report (65)
- No new `/api/bridge/idle-stats`
- No fake animated counters
- Never wipe filter-lists / bridge-brain
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount idle proof strip + compact CSS (not equal cards)</name>
  <files>public/bridge.html, public/css/bridge.css</files>
  <read_first>
    - .planning/phases/63-idle-proof-process-climax/63-RESEARCH.md (IDLE-01 gap, Pattern 2 placement, Pitfall 2 equal cards)
    - public/bridge.html (hero ~48-52; proof rail ~54+; lists total `#bridge-lists-total`)
    - public/css/bridge.css (`.bridge-proof-rail` ~76+, `.bridge-lists-total` ~1339 — steal proof-num voice, not 3-up layout)
    - AGENTS.md (verify-live after public/)
  </read_first>
  <action>
    1. **HTML mount** in `public/bridge.html` immediately after `</header>` of `.bridge-hero` (before decorative proof rail if present):

    ```html
    <div id="bridge-idle-proof" class="bridge-idle-proof" role="status" aria-live="polite" data-phuglee-reveal-child>
      <p class="bridge-idle-proof-line" id="bridge-idle-proof-line">0 lists staged · Ready when you scrub the first city</p>
    </div>
    ```

    - Initial static empty copy is fine; JS will overwrite on first `loadSavedLists`.
    - Do **not** build three `<article class="bridge-proof-item">` metric tiles for idle.
    - Do **not** remove the decorative proof rail in this plan (Phase 61 owns that) — idle strip is separate live truth.

    2. **CSS** in `public/css/bridge.css` — compact **single-row** strip:
       - `.bridge-idle-proof` — full-width under hero, modest padding from {8,16,24}, margin using same scale; no equal multi-column card grid
       - `.bridge-idle-proof-line` — one line of ops metrics; use existing cream/stone tokens; mono/display accents optional on numbers only if hierarchy stays a single sentence/row
       - Mobile: wrap allowed; no horizontal overflow; do not force three equal columns
       - Do **not** reuse `.bridge-proof-rail` 3-column layout for idle metrics

    3. Bump `bridge.css?v=` cache buster in `public/bridge.html` by +1 from current.
  </action>
  <acceptance_criteria>
    - public/bridge.html contains `id="bridge-idle-proof"`
    - public/bridge.html contains `id="bridge-idle-proof-line"`
    - public/bridge.html initial empty copy includes `0 lists staged`
    - public/css/bridge.css contains `.bridge-idle-proof`
    - Idle markup is a single status strip (no three equal `bridge-proof-item` children inside `#bridge-idle-proof`)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('public/bridge.html','utf8');const c=fs.readFileSync('public/css/bridge.css','utf8');if(!h.includes('id=\"bridge-idle-proof\"'))process.exit(1);if(!h.includes('id=\"bridge-idle-proof-line\"'))process.exit(2);if(!h.includes('0 lists staged'))process.exit(3);if(!c.includes('.bridge-idle-proof'))process.exit(4);const i=h.indexOf('id=\"bridge-idle-proof\"');const chunk=h.slice(i,i+800);if((chunk.match(/bridge-proof-item/g)||[]).length>=3){console.error('idle must not be 3-up cards');process.exit(5)}console.log('63-01-html-css-ok')"</automated>
  </verify>
  <done>Live idle strip mount + compact CSS present; empty seed copy honest; not equal 3-up cards.</done>
</task>

<task type="auto">
  <name>Task 2: computeIdleProof + renderIdleProof from savedLists (single load path)</name>
  <files>public/js/bridge.js, public/bridge.html</files>
  <read_first>
    - public/js/bridge.js (`savedLists` ~103; `formatListWhen` ~1693; `renderSavedLists` ~1728-1780; `loadSavedLists` ~1782-1790; boot `loadSavedLists().catch` ~3251)
    - .planning/phases/63-idle-proof-process-climax/63-RESEARCH.md (Pattern 1, refresh hooks, Pitfalls 1/4)
    - public/bridge.html (`bridge.js?v=` cache buster)
  </read_first>
  <action>
    In `public/js/bridge.js`:

    1. Add pure helper near lists helpers (`formatListWhen` / before `renderSavedLists`):

    ```js
    function computeIdleProof(lists) {
      const rows = Array.isArray(lists) ? lists : [];
      const listCount = rows.length;
      const recordTotal = rows.reduce((s, r) => s + (Number(r.recordCount) || 0), 0);
      const lastSaveAt = rows[0]?.createdAt || null; // API sorts createdAt desc
      return { listCount, recordTotal, lastSaveAt };
    }
    ```

    2. Add `renderIdleProof()`:
       - Read `#bridge-idle-proof-line` (or `#bridge-idle-proof`)
       - Call `computeIdleProof(savedLists)`
       - **Empty** (`listCount === 0`): set text to  
         `0 lists staged · Ready when you scrub the first city`  
         (or equivalent with honest 0 records — must include `0 lists staged`; no fake nonzero)
       - **Non-empty**: set text like  
         `{n} lists staged · {m} records ready · Last save {formatListWhen(lastSaveAt)}`  
         Use `toLocaleString()` for n/m. Pluralize list/record sensibly.
       - Optional muted detail only if still one row — do not add icon tiles.

    3. **Wire single path:** Call `renderIdleProof()` from `renderSavedLists()`:
       - On the **empty early return** path (when `!savedLists.length`) — **must** still call `renderIdleProof()` before return (today empty path only clears `#bridge-lists-total`; idle must not go stale).
       - On the **non-empty** path after updating lists total.

    4. Do **not** add a second `fetch('/api/bridge/lists')` for idle. Do **not** poll/SSE.

    5. Do **not** change list store, processUpload, or attachDataset.

    6. Bump `bridge.js?v=` in `public/bridge.html` by +1 from current.

    7. After public edits:  
       `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`  
       (restart via scripts if needed until exit 0).
  </action>
  <acceptance_criteria>
    - public/js/bridge.js contains `function computeIdleProof`
    - public/js/bridge.js contains `function renderIdleProof`
    - public/js/bridge.js contains `lists staged` and `records ready` and `Last save`
    - Empty path copy includes `0 lists staged`
    - `renderSavedLists` invokes `renderIdleProof` (grep finds call site inside/near renderSavedLists)
    - No new fetch URL for idle-stats
    - `scripts\verify-live.ps1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('public/js/bridge.js','utf8');if(!j.includes('function computeIdleProof'))process.exit(1);if(!j.includes('function renderIdleProof'))process.exit(2);if(!j.includes('lists staged'))process.exit(3);if(!j.includes('records ready'))process.exit(4);if(!j.includes('Last save'))process.exit(5);const i=j.indexOf('function renderSavedLists');const chunk=j.slice(i,i+2500);if(!chunk.includes('renderIdleProof')){console.error('renderSavedLists must call renderIdleProof');process.exit(6)}if(j.includes('/api/bridge/idle-stats'))process.exit(7);console.log('63-01-js-ok')" ; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>Idle proof derives from savedLists on every lists render (empty + non-empty); live server healthy.</done>
</task>

</tasks>

<verification>
- Static HTML/CSS/JS gates green for idle mount + helpers + wire
- verify-live.ps1 exit 0 after public/
- Manual smoke optional: save a list → strip matches `#bridge-lists-total` counts; reload still matches API
</verification>

<success_criteria>
- IDLE-01: at idle, live lists staged · records ready · last save from real inventory
- Empty state is honest zeros, not decorative step titles
- Single source of truth with Saved lists load path
</success_criteria>

<output>
After completion, create `.planning/phases/63-idle-proof-process-climax/63-01-SUMMARY.md`
</output>
