---
phase: 53-display-only-short-labels
plan: 03
type: execute
wave: 3
depends_on:
  - 53-02
files_modified:
  - lib/bridge-review-groups.js
  - tests/bridge-review-groups.test.js
  - tests/bridge-export.test.js
autonomous: true
requirements:
  - LBL-01
  - LBL-02
must_haves:
  truths:
    - "Every public review group includes shortLabel derived from final violationTypeLabel"
    - "violationTypeLabel remains full cleaned type/description — never replaced by short form"
    - "groupId / violationTypeKey / descriptionKey still from stable* helpers on raw row text — short label never becomes the key"
    - "Two long types sharing a display prefix stay two groups"
    - "Input row.violationIssueType unchanged; export still emits full Violation/Issue Type from rows"
  artifacts:
    - path: "lib/bridge-review-groups.js"
      provides: "shortLabel on each public group from shortLabelForDisplay"
      contains: "shortLabel"
    - path: "tests/bridge-review-groups.test.js"
      provides: "Green LBL-01/02 group contracts"
    - path: "tests/bridge-export.test.js"
      provides: "Optional LBL-02 export full-type regression assert"
  key_links:
    - from: "lib/bridge-review-groups.js"
      to: "lib/bridge-short-label.js"
      via: "require shortLabelForDisplay; set g.shortLabel before public strip"
      pattern: "shortLabelForDisplay|shortLabel"
    - from: "buildReviewGroups"
      to: "stableTypeKey / stableDescriptionKey"
      via: "keys still from raw type/desc — not shortLabel"
      pattern: "stableTypeKey|stableDescriptionKey"
    - from: "applyDecision rebuild"
      to: "buildReviewGroups"
      via: "decisions path inherits shortLabel automatically"
      pattern: "buildReviewGroups"
---

<objective>
Attach parallel `shortLabel` on review groups in `buildReviewGroups` without mutating full labels, keys, rows, or export.

Purpose: Server-authoritative display field so process + decision rebuilds both emit the same shortLabel; LBL-02 safety for brain/export/keys.
Output: Green review-groups contracts; export regression assert for full type; Train UI still Plan 04.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-display-only-short-labels/53-RESEARCH.md
@.planning/phases/53-display-only-short-labels/53-02-SUMMARY.md
@lib/bridge-short-label.js
@lib/bridge-review-groups.js
@lib/bridge-stable-text.js
@lib/bridge-brain-decisions.js
@lib/bridge-intake-schema.js
@tests/bridge-review-groups.test.js
@tests/bridge-export.test.js
@AGENTS.md

**Locked:**
- Parallel field only: `g.shortLabel = shortLabelForDisplay(g.violationTypeLabel)` after full label is finalized
- Never assign short into `violationTypeLabel`, `violationTypeKey`, `groupId`, or row fields
- `applyDecision` already returns `buildReviewGroups(...)` — no separate decisions wire if shortLabel is set inside builder
- Do not change export column mapping beyond a regression assert that full type survives
- Do not touch Train UI / DOM scrape (Plan 04)
- Do not touch scorer / format confirm gate
- Never wipe filter lists / brain

<interfaces>
Current buildReviewGroups label finalize (~lib/bridge-review-groups.js):
```js
// After loop, before push publicGroup:
// private fields stripped: _indicatorSeen, _descSeen, _confSeen, _labelSet
// Wire shortLabel on g BEFORE strip so it is a public field:
//   g.shortLabel = shortLabelForDisplay(g.violationTypeLabel);
// Prefer setting once when label is finalized OR once per group before public push.
```

From lib/bridge-short-label.js (Plan 02):
```js
const { shortLabelForDisplay, DEFAULT_MAX } = require('./bridge-short-label');
```

Export path (must stay full type from rows):
```js
// toExportRow / rowsToCsv — Violation/Issue Type from row.violationIssueType
// shortLabel must NOT appear on export rows
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Attach shortLabel in buildReviewGroups</name>
  <files>lib/bridge-review-groups.js, tests/bridge-review-groups.test.js</files>
  <behavior>
    - Long ordinance type → shortLabel present, ≤64 chars, full violationTypeLabel preserved (no … on full field when short used)
    - Short type "High Grass" → shortLabel equals or is display-safe equivalent of full (passthrough)
    - Shared-prefix long types → 2 groups, distinct keys/ids
    - No private underscore fields on returned groups; shortLabel is public
    - Source rows' violationIssueType unchanged
  </behavior>
  <action>
    1. In `lib/bridge-review-groups.js`, require `shortLabelForDisplay` from `./bridge-short-label`.
    2. After `violationTypeLabel` is finalized for a group (before or during publicGroup construction), set:
       `g.shortLabel = shortLabelForDisplay(g.violationTypeLabel);`
       Ensure shortLabel is **not** stripped as a private field (it is public API).
    3. Do **not** change `stableTypeKey` / `stableDescriptionKey` / `groupIdFor` inputs to use shortLabel.
    4. Do **not** mutate `row.violationIssueType` or description on rows.
    5. Run and fix until Plan 01 review-groups shortLabel contracts are green. Adjust tests only if a fixture assumption was wrong (document in SUMMARY).
    6. Leave Train UI and bridge.js scrape unchanged.
  </action>
  <verify>
    <automated>node --test tests/bridge-review-groups.test.js tests/bridge-short-label.test.js</automated>
  </verify>
  <done>Groups emit parallel shortLabel; full labels and keys stable; pure short-label still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export full-type regression (LBL-02)</name>
  <files>tests/bridge-export.test.js</files>
  <behavior>
    - Long wall-of-text violationIssueType survives rowsToCsv / toExportRow unchanged (full string present in export)
    - Export does not substitute shortLabel or truncate with …
  </behavior>
  <action>
    1. Add one focused case in `tests/bridge-export.test.js`:
       - Row with long `violationIssueType` ordinance wall (no shortLabel on row).
       - `rowsToCsv([row])` (or `toExportRow` if already imported in suite) must include the full type string.
    2. Do **not** change export implementation unless a real bug is found (unexpected — export already uses rows). Prefer assert-only.
    3. Do not invent shortLabel on export rows.
  </action>
  <verify>
    <automated>node --test tests/bridge-export.test.js tests/bridge-review-groups.test.js</automated>
  </verify>
  <done>Export regression asserts full type; groups shortLabel contracts green.</done>
</task>

</tasks>

<verification>
- Review-groups LBL-01/02 contracts green
- Export still full type
- Train title / DOM scrape still Plan 04 (train-ux LBL-03 may still RED)
- `npm test` optional mid-phase; pure + groups + export green minimum
</verification>

<success_criteria>
1. Every public group has shortLabel from full violationTypeLabel
2. Full label + keys + row type unchanged (LBL-02)
3. Export regression for long type
4. No Train UI / scrape changes yet
</success_criteria>

<output>
After completion, create `.planning/phases/53-display-only-short-labels/53-03-SUMMARY.md`
</output>
