---
phase: 53-display-only-short-labels
plan: 04
type: execute
wave: 4
depends_on:
  - 53-03
files_modified:
  - public/js/bridge-train.js
  - public/js/bridge.js
  - public/bridge.html
  - tests/bridge-train-ux.test.js
autonomous: true
requirements:
  - LBL-01
  - LBL-03
must_haves:
  truths:
    - "Train card titles prefer shortLabel when present; fall back to full violationTypeLabel"
    - "Full label available via title/tooltip on the title element for hover/a11y"
    - "submitTrainDecision POST body still uses group.violationTypeLabel (full) and group.violationTypeKey — never shortLabel"
    - "resolveTrainGroupFromCard never scrapes .bridge-train-group-title for violationTypeLabel; missing groupId → null fail-closed"
    - "bridge.js fallback renderer stays in sync with short title preference"
    - "Sort/filter remain on full violationTypeLabel"
    - "After public/ edits, verify-live.ps1 health + homepage 200"
  artifacts:
    - path: "public/js/bridge-train.js"
      provides: "renderTrainGroupCard short title + full tooltip"
      contains: "shortLabel"
    - path: "public/js/bridge.js"
      provides: "fail-closed resolveTrainGroupFromCard; fallback short title; full decision body"
      contains: "resolveTrainGroupFromCard"
    - path: "tests/bridge-train-ux.test.js"
      provides: "Green LBL-01/03 train + source contracts"
  key_links:
    - from: "public/js/bridge-train.js"
      to: "group.shortLabel || group.violationTypeLabel"
      via: "title text short; title attr full"
      pattern: "shortLabel"
    - from: "public/js/bridge.js submitTrainDecision"
      to: "group.violationTypeLabel"
      via: "POST body full label only"
      pattern: "violationTypeLabel:\\s*group\\.violationTypeLabel"
    - from: "public/js/bridge.js resolveTrainGroupFromCard"
      to: "null when not found"
      via: "no querySelector .bridge-train-group-title for label"
      pattern: "resolveTrainGroupFromCard"
---

<objective>
Wire Train UI to prefer display-only shortLabel and hard-kill DOM title scrape so decisions cannot poison brain rules with truncated titles.

Purpose: LBL-01 scannable titles + LBL-03 fail-closed metadata-only decisions; complete Phase 53 presentation seam.
Output: Green train-ux contracts; cache-busted public assets; verify-live green.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-display-only-short-labels/53-RESEARCH.md
@.planning/phases/53-display-only-short-labels/53-03-SUMMARY.md
@public/js/bridge-train.js
@public/js/bridge.js
@public/bridge.html
@tests/bridge-train-ux.test.js
@AGENTS.md

**Locked:**
- Title text: `shortLabel || violationTypeLabel`; tooltip/title attr: **full** `violationTypeLabel`
- Decision body: full `group.violationTypeLabel` / `group.violationTypeKey` only — never POST shortLabel as type
- Kill scrape: `resolveTrainGroupFromCard` returns `found` or `null` — no invented DOM label
- Confirm dialogs / status toasts may show short for chrome; POST body stays full (use distinct locals if needed: `displayLabel` vs full)
- Sort/search stay on full `violationTypeLabel` (do not sort by short alone)
- Update both BridgeTrain renderer and bridge.js fallback renderer (avoid dual-path drift)
- Bump `bridge-train.js` and/or `bridge.js` cache-bust query in `public/bridge.html` after JS edits
- After public/ edits: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`
- Zero new packages; no scorer/format-gate rework; never wipe user data

**Current pitfall (must fix):**
```js
// public/js/bridge.js resolveTrainGroupFromCard — REMOVE this invent path:
return {
  groupId,
  section,
  violationTypeLabel: card.querySelector('.bridge-train-group-title')?.childNodes?.[0]?.textContent?.trim() || 'group'
};
// Replace with: if (!found) return null;
```

**Happy path already correct — keep:**
```js
violationTypeLabel: group.violationTypeLabel || '',  // FULL
violationTypeKey: group.violationTypeKey || '',
```

<interfaces>
Target render pattern (bridge-train.js + bridge.js fallback):
```js
var fullLabel = group.violationTypeLabel || 'Unknown type';
var label = group.shortLabel || fullLabel;
// title text uses esc(label)
// title attribute on .bridge-train-group-title uses esc(fullLabel) for tooltip
// aria-label on buttons may use label (short) or full — prefer short for scannable chrome is OK
```

Caller already handles null group:
```js
// onTrainDecision / resolve path — "Could not resolve this group" when null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Train titles prefer shortLabel + kill DOM scrape</name>
  <files>public/js/bridge-train.js, public/js/bridge.js, public/bridge.html, tests/bridge-train-ux.test.js</files>
  <behavior>
    - renderTrainGroupCard with shortLabel shows short in title text; full in title= tooltip
    - Without shortLabel, falls back to full violationTypeLabel (existing SAMPLE_GROUP still works)
    - XSS escape still applies to both short and full
    - bridge.js resolveTrainGroupFromCard: no .bridge-train-group-title scrape for violationTypeLabel
    - Missing groupId → null (not invented partial group with scraped label)
    - submitTrainDecision still posts group.violationTypeLabel full field
    - Fallback renderer in bridge.js also prefers shortLabel when BridgeTrain missing
  </behavior>
  <action>
    1. **public/js/bridge-train.js** — In `renderTrainGroupCard`:
       - `fullLabel = group.violationTypeLabel || 'Unknown type'`
       - `label = group.shortLabel || fullLabel`
       - Put `esc(label)` in visible title text
       - Add `title="` + esc(fullLabel) + `"` on `.bridge-train-group-title` (or parent head) so hover shows full wall
       - Keep count badge / singleton badge structure; do not scrape-friendly dual text nodes that reintroduce pitfall

    2. **public/js/bridge.js** — Sync fallback `renderTrainGroupCard` the same way (short || full + full title attr).

    3. **public/js/bridge.js** — Fix `resolveTrainGroupFromCard`:
       - Keep groupId lookup in `lastResult.reviewGroups`
       - If not found: `return null` (fail closed)
       - **Delete** any `querySelector('.bridge-train-group-title')` path used for `violationTypeLabel`
       - Do not invent `{ groupId, section, violationTypeLabel: scraped }`

    4. **public/js/bridge.js** — Audit `submitTrainDecision` and confirm/deny handlers:
       - POST body must use `group.violationTypeLabel` (full), never `group.shortLabel`
       - Status/confirm chrome may use `group.shortLabel || group.violationTypeLabel` for display only — if so, use separate variable names (`displayLabel` vs full) so POST cannot confuse them
       - Leave sort/filter on full `violationTypeLabel`

    5. **public/bridge.html** — Bump cache-bust:
       - `bridge-train.js?v=` increment (currently 3 → at least 4)
       - `bridge.js?v=` increment (currently 17 → at least 18)

    6. Fix train-ux tests until LBL-01/03 contracts green. Update existing render test only if needed so short-passthrough SAMPLE_GROUP still passes.

    7. Do not edit brain decisions server, scorer, format store, or wipe data dirs.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-ux.test.js tests/bridge-short-label.test.js tests/bridge-review-groups.test.js</automated>
  </verify>
  <done>Train titles short; decisions metadata-only; LBL-01/03 train contracts green; cache-bust bumped.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite + verify-live after public edits</name>
  <files>public/js/bridge-train.js, public/js/bridge.js, public/bridge.html</files>
  <action>
    1. Run full `npm test` and fix any regressions introduced by Train/scrape changes (do not "fix" by weakening LBL contracts).
    2. After public/ is final, run from project root:
       `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`
    3. If verify fails: restart via `scripts\restart.ps1` (headless) and re-verify until health + homepage 200. Never leave a blocking `node server.js` in the agent shell.
    4. Confirm no accidental edits to `data/filter-lists/`, `data/bridge-brain/`, scorer, or format store.
    5. Document in SUMMARY: short title pattern, scrape kill, cache-bust versions, verify-live result.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full suite green; verify-live exits 0 after public edits; Phase 53 presentation seam complete.</done>
</task>

</tasks>

<verification>
- train-ux + short-label + review-groups green
- `npm test` green
- `scripts/verify-live.ps1` exits 0
- Source contract: no DOM title scrape for decision labels
</verification>

<success_criteria>
1. LBL-01: Train titles scannable via shortLabel + full tooltip
2. LBL-03: Decisions use group metadata only; scrape path removed
3. LBL-02 already held from Plan 03 remains green under full suite
4. Live server health verified after public/ changes
</success_criteria>

<output>
After completion, create `.planning/phases/53-display-only-short-labels/53-04-SUMMARY.md`
</output>
