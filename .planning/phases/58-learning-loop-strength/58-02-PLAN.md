---
phase: 58-learning-loop-strength
plan: 02
type: execute
wave: 2
depends_on:
  - "58-01"
files_modified:
  - lib/bridge-api.js
  - lib/bridge-learning-metrics.js
  - tests/bridge-brain-api.test.js
  - tests/bridge-learning-metrics.test.js
autonomous: true
requirements:
  - LRN-01
must_haves:
  truths:
    - "Admin GET /api/bridge/brain/metrics returns legacy flat counters AND nested learning object"
    - "learning.decisionTrend is computed from brain.events (approve/deny only)"
    - "learning.gold precision/recall present when gold fixtures scorable (not degraded vs baseline on empty/good brain)"
    - "Non-admin still receives 403 ADMIN_REQUIRED on metrics"
    - "GET /api/bridge/brain metrics (or top-level learning) exposes the same paired learning shape for panel consumers"
  artifacts:
    - path: "lib/bridge-api.js"
      provides: "handleBrainMetrics attaches learning nest; preserves flat recomputeMetrics fields"
      contains: "learning"
    - path: "tests/bridge-brain-api.test.js"
      provides: "LRN-01 metrics learning payload contracts"
      contains: "LRN-01"
  key_links:
    - from: "handleBrainMetrics"
      to: "lib/bridge-learning-metrics.js buildLearningHealth"
      via: "require + attach learning on 200 body"
      pattern: "buildLearningHealth|learning"
    - from: "learning.decisionTrend"
      to: "brain.events"
      via: "computeDecisionTrend"
      pattern: "computeDecisionTrend"
    - from: "learning.gold"
      to: "tests/fixtures/bridge/gold keep+deny"
      via: "scoreGoldKeepKill after processUpload or pure score helper"
      pattern: "scoreGold|GOLD_KEEP|includeGold"
---

<objective>
Wire paired learning metrics into admin brain APIs so operators (and tests) can view decision trend + gold P/R without breaking legacy flat counters.

Purpose: LRN-01 admin-visible surface on the server. Plan 03 only adds UI chips.

Output: Extended `GET /api/bridge/brain/metrics` (+ brain GET metrics nest) with `learning`; API tests green.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-learning-loop-strength/58-RESEARCH.md
@.planning/phases/58-learning-loop-strength/58-01-SUMMARY.md
@lib/bridge-api.js
@lib/bridge-learning-metrics.js
@lib/bridge-brain-store.js
@lib/bridge-engine/index.js
@tests/bridge-brain-api.test.js
@tests/fixtures/bridge/gold/
@AGENTS.md

<interfaces>
<!-- From Plan 01 + as-built API -->

handleBrainMetrics today (lib/bridge-api.js ~790):
```js
async function handleBrainMetrics(req, res) {
  requireAdmin(req); // 403 ADMIN_REQUIRED
  const brain = loadBrain();
  sendJson(res, 200, recomputeMetrics(brain)); // flat only
}
```

Target 200 body (back-compat flat + nest):
```js
{
  totalDecisions, typeRulesActive, phraseRulesActive,
  phraseRulesProposed, suppressCount, promoteCount,
  learning: {
    decisionTrend: { recentCount, previousCount, direction, window, decisionActionsOnly: true },
    gold: {
      precision, recall, baselinePrecision, baselineRecall,
      degraded, source  // source e.g. 'fixtures/bridge/gold' or 'unavailable'
    },
    applyCoverage: {
      appliedRuleIds, rulesAppliedCount, rowHitRate, sufficient, note, source
      // On metrics GET without a process snapshot: source='unknown', sufficient=false,
      // note mentions process a city file / client lastResult — do NOT invent coverage
    },
    silentDropFailure: boolean,
    pairedOk: boolean
  }
}
```

Gold scoring on metrics GET (Claude's discretion locked here):
1. Prefer helper `async function scoreGoldAgainstBrain(brainOrNull)` colocated in bridge-learning-metrics.js OR thin helper in bridge-api that:
   - Reads keep-distress-mixed.csv + deny-junk-admin.csv from `tests/fixtures/bridge/gold` relative to repo root (`path.join(__dirname, '..', 'tests', 'fixtures', 'bridge', 'gold')` from lib → `path.join(__dirname, '..', 'tests', ...)`)
   - Calls `processUpload` for each with `confirmedTypeHeader: 'Violation Type'`, `uploadType: 'code_violation'`, isolated city id `gold-metrics-city`
   - Uses **current** loadBrain() rules (no wipe, no saveBrain)
   - Combines via scoreGoldKeepKill on each result (or merge TP/FP/FN across both files)
   - Baseline: precision=1, recall=1 (Phase 57 empty-brain green bar) unless empty-brain recompute requested
2. **Cache:** module-level in-memory cache TTL 10 minutes keyed by `brain.version` so admin panel spam does not re-process; tests may call `clearGoldScoreCache()` if exported
3. If fixtures path missing (production deploy without tests/): gold.source='unavailable', precision/recall null, degraded=false (do not fail metrics GET)
4. Query `?includeGold=0` skips gold process (trend still returned) for light probes
5. Default: include gold when fixtures exist (`includeGold` default on)

handleBrainGet: attach `metrics.learning` **or** top-level `learning` sibling — prefer nest under metrics:
```js
metrics: { ...recomputeMetrics(brain), learning: {...} }
```
so existing flat metrics chips keep working and panel can read `data.metrics.learning`.

Apply coverage on server metrics: leave unknown/insufficient unless future process_apply log exists.
**Do not** saveBrain / RMW version on process or metrics GET.
**Do not** wipe data/bridge-brain/.

HARD tests remain: non-admin 403; admin flat counts still assert.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LRN-01 API tests for learning nest</name>
  <files>tests/bridge-brain-api.test.js, tests/bridge-learning-metrics.test.js</files>
  <read_first>
    tests/bridge-brain-api.test.js
    lib/bridge-api.js
    lib/bridge-learning-metrics.js
    .planning/phases/58-learning-loop-strength/58-01-SUMMARY.md
  </read_first>
  <behavior>
    - LRN-01: admin GET /api/bridge/brain/metrics includes learning.decisionTrend with direction/window and recentCount/previousCount numbers
    - LRN-01: learning.gold has precision/recall numbers (or null + source unavailable only if fixtures missing — in test env fixtures exist so numbers required)
    - LRN-01: learning.gold.degraded === false on empty/seeded brain that still passes gold keep/deny
    - LRN-01: legacy flat fields totalDecisions/typeRulesActive still present
    - HARD non-admin 403 still green (existing test)
    - LRN-01: admin GET /api/bridge/brain exposes metrics.learning (or data.learning) with same decisionTrend keys
  </behavior>
  <action>
    Extend `tests/bridge-brain-api.test.js` with named `LRN-01:` tests using existing `callBridge`, `adminHeaders`, `seedBrainWithRules`, temp `BRIDGE_BRAIN_ROOT` isolation already in that file.

    Assert metrics JSON shape per interfaces. Do not delete existing HARD metrics tests — only extend.

    If gold process needs empty brain for baseline stability, seed emptyBrain via saveBrain before call (tests already use temp root).

    Optional: one pure test in bridge-learning-metrics for gold cache clear if you add cache helper.
  </action>
  <verify>
    <automated>node --test --test-name-pattern="LRN-01|HARD: admin GET /brain/metrics|HARD: non-admin GET /brain/metrics" tests/bridge-brain-api.test.js</automated>
  </verify>
  <acceptance_criteria>
    - New LRN-01 tests exist (may RED until Task 2)
    - Existing HARD metrics titles still present
  </acceptance_criteria>
  <done>API learning contracts declared</done>
</task>

<task type="auto">
  <name>Task 2: Wire handleBrainMetrics + handleBrainGet learning</name>
  <files>lib/bridge-api.js, lib/bridge-learning-metrics.js</files>
  <read_first>
    lib/bridge-api.js
    lib/bridge-learning-metrics.js
    lib/bridge-engine/index.js
    tests/bridge-brain-api.test.js
    tests/bridge-accuracy-gold.test.js
  </read_first>
  <action>
    1. Add gold fixture scoring helper (prefer in `lib/bridge-learning-metrics.js` as `scoreGoldFixtures(processUpload, opts)` + optional `clearGoldScoreCache`) so API stays thin. Helper must:
       - Never write brain or lists
       - Use confirmedTypeHeader 'Violation Type'
       - Aggregate keep + deny scoreGoldKeepKill
       - Return { precision, recall, baselinePrecision: 1, baselineRecall: 1, degraded, source, tp, fp, fn, tn }

    2. In `handleBrainMetrics`:
       - requireAdmin unchanged
       - flat = recomputeMetrics(brain)
       - decisionTrend = computeDecisionTrend(brain.events)
       - gold = await scoreGoldFixtures(...) unless includeGold=0 or fixtures missing
       - applyCoverage = scoreApplyCoverage({ appliedRuleIds: [], source: 'unknown' }) with note for client last process
       - learning = buildLearningHealth({ events, goldScore: gold, applyCoverage, discardBlob: '' })
       - sendJson(200, { ...flat, learning })

    3. In `handleBrainGet`: nest learning under metrics the same way (`metrics: { ...flat, learning }`) so panel one-fetch works.

    4. Parse query string for includeGold if URL available on req (existing bridge router patterns — use URL/searchParams if already used elsewhere; else default include gold).

    5. Green the LRN-01 API tests. Keep HARD flat count assertions valid (seedBrainWithRules still yields same flat numbers).
  </action>
  <verify>
    <automated>node --test --test-name-pattern="LRN-01|HARD:.*metrics|HARD: non-admin GET /brain/metrics" tests/bridge-brain-api.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Admin metrics 200 includes both flat counters and learning nest
    - learning.decisionTrend.direction is one of down|up|flat
    - learning.gold.degraded is boolean; precision/recall numbers when fixtures present
    - No saveBrain on metrics path; no data/ wipe
    - Existing HARD metrics tests pass
  </acceptance_criteria>
  <done>Admin can fetch paired learning metrics via API</done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-learning-metrics.test.js tests/bridge-brain-api.test.js` focused green
- `node --test tests/bridge-accuracy-gold.test.js` still green (no fixture edits)
</verification>

<success_criteria>
- LRN-01 server surface: paired trend + gold on GET metrics
- Back-compat flat recomputeMetrics fields preserved
- Apply coverage honest (unknown on server without process — not faked)
</success_criteria>

<output>
After completion, create `.planning/phases/58-learning-loop-strength/58-02-SUMMARY.md`
</output>
