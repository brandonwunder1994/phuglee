---
phase: 58-learning-loop-strength
plan: 03
type: execute
wave: 3
depends_on:
  - "58-02"
files_modified:
  - public/js/bridge.js
  - public/bridge.html
  - public/css/bridge.css
  - tests/bridge-learning-metrics.test.js
  - tests/bridge-brain-apply.test.js
  - tests/bridge-phrase-miner.test.js
  - tests/bridge-engine.test.js
  - tests/bridge-train-ux.test.js
  - docs/bridge/API.md
  - docs/bridge/TEST-PLAN.md
autonomous: true
requirements:
  - LRN-01
  - LRN-02
  - LRN-03
must_haves:
  truths:
    - "Admin brain panel shows decision trend direction/counts AND gold P/R (or degraded flag) chips"
    - "Apply coverage chip uses real last process brainAppliedRuleIds — empty state explains process a city file"
    - "Metrics cannot win via hide groups, auto-active phrases, or silent-drop reasons (e2e/unit locks)"
    - "Active type suppress/promote still apply on processUpload; proposed phrases do not until Activate"
    - "Phase 57 gold suite and full npm test stay green"
  artifacts:
    - path: "public/js/bridge.js"
      provides: "renderBrainPanel learning health chips + last-process coverage merge"
      contains: "learning|decisionTrend|pairedOk|brainAppliedRuleIds"
    - path: "public/bridge.html"
      provides: "Optional learning container; cache-bust bridge.js"
      contains: "brain-metrics|bridge.js\\?v="
    - path: "tests/bridge-learning-metrics.test.js"
      provides: "LRN-02 anti-game + coverage e2e hooks if not already pure-complete"
      contains: "LRN-02"
  key_links:
    - from: "renderBrainPanel"
      to: "data.metrics.learning || data.learning"
      via: "chips for trend + gold + coverage"
      pattern: "metrics\\.learning|decisionTrend"
    - from: "apply coverage UI"
      to: "lastResult.processingMeta.brainAppliedRuleIds"
      via: "merge after process; never invent ids"
      pattern: "brainAppliedRuleIds"
    - from: "LRN-03 tests"
      to: "applyBrainToRows active-only + minePhrases proposed"
      via: "named LRN-03 regression titles"
      pattern: "LRN-03|proposed|status.*active"
---

<objective>
Surface paired learning health in the admin Filter brain panel, lock LRN-02 anti-game + LRN-03 HITL gates with named tests, and gate the phase with gold + full suite (+ verify-live after public edits).

Purpose: Admin can *see* the brain getting smarter for the right reason; code cannot regress to auto-phrases or silent-drop gaming.

Output: UI chips + regression locks + docs light + green suite.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-learning-loop-strength/58-RESEARCH.md
@.planning/phases/58-learning-loop-strength/58-02-SUMMARY.md
@public/js/bridge.js
@public/bridge.html
@public/css/bridge.css
@lib/bridge-brain-apply.js
@lib/bridge-phrase-miner.js
@lib/bridge-brain-decisions.js
@tests/bridge-accuracy-gold.test.js
@docs/bridge/API.md
@AGENTS.md

<interfaces>
renderBrainPanel(data) today builds chips in #brain-metrics from flat metrics only.

Target chips (append after existing counters; compact Phuglee metric spans — reuse `.bridge-brain-metric`):
1. Trend: `trend ↓ 12 vs 28` / `trend ↑` / `trend →` from learning.decisionTrend
2. Gold: `gold P 1.00 R 1.00` or `gold degraded` if learning.gold.degraded
3. Coverage: from lastResult.processingMeta.brainAppliedRuleIds when available:
   - `apply N rules` when length > 0
   - `apply — process a city to measure` when no last process / empty ids
4. Optional paired: `learning ok` / `learning weak` from learning.pairedOk (pairedOk may be false on panel open due to unknown server coverage — prefer showing coverage from lastResult and only mark ok when coverage sufficient + !gold.degraded)

Merge rule:
```js
const learning = (data.metrics && data.metrics.learning) || data.learning || null;
const applied = (lastResult && lastResult.processingMeta && lastResult.processingMeta.brainAppliedRuleIds) || [];
// Prefer client applied ids for coverage chip; server learning for trend + gold
```

Do NOT:
- Auto-activate phrases
- Hide Train groups to reduce decision counts
- Call saveBrain from process path
- Wipe data/bridge-brain or filter-lists
- Add chart libraries

Cache-bust: bump `public/bridge.html` script `bridge.js?v=21` → `v=22` (or +1 from current).

CSS: optional subtle `.bridge-brain-metric-learning` or reuse existing metric styles only.

LRN-03 regression (named tests — extend existing files; do not weaken PHRASE locks):
- LRN-03: active suppress_type appears in processUpload brainAppliedRuleIds on matching CV row (existing engine pattern OK with title prefix)
- LRN-03: proposed suppress_phrase does not appear in brainAppliedRuleIds
- LRN-03: after status active, phrase rule id appears
- LRN-03: minePhrasesFromEvent never sets status active

LRN-02 e2e/unit (add if not covered by 58-01):
- processUpload with active type matching gold deny type → coverage sufficient + gold not degraded
- learning health fails on banned silent-drop discard blob
- assert no API field groupsHidden used as success input

Docs light (optional but recommended):
- docs/bridge/API.md: document learning nest on GET /brain/metrics
- docs/bridge/TEST-PLAN.md: LRN-01/02/03 map one short section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brain panel learning chips + last-process coverage</name>
  <files>public/js/bridge.js, public/bridge.html, public/css/bridge.css, tests/bridge-train-ux.test.js</files>
  <read_first>
    public/js/bridge.js
    public/bridge.html
    public/css/bridge.css
    .planning/phases/58-learning-loop-strength/58-02-SUMMARY.md
    tests/bridge-train-ux.test.js
  </read_first>
  <action>
    1. Update `renderBrainPanel` in `public/js/bridge.js` to read `data.metrics.learning || data.learning` and append trend + gold chips using existing `.bridge-brain-metric` spans.

    2. Coverage chip: read `lastResult?.processingMeta?.brainAppliedRuleIds` (variable already in bridge.js scope for process results). If missing/empty, muted note text "process a city to measure apply coverage" (short chip label OK).

    3. Do not invent applied rule ids. Do not auto-open Train or hide groups.

    4. Bump cache-bust `bridge.js?v=` in bridge.html.

    5. Optional light CSS only if chips need a success/warn tint — keep Phuglee tokens (`--phuglee-stone`, existing metric border).

    6. Add a thin UI contract test if the train-ux suite already scrapes brain panel HTML strings — assert presence of learning/trend/gold markers or function source contains `decisionTrend` / `brainAppliedRuleIds`. Prefer string/source contract over full DOM if that's the file's pattern.
  </action>
  <verify>
    <automated>node --test --test-name-pattern="LRN-01|brain|learning|metric" tests/bridge-train-ux.test.js tests/bridge-brain-api.test.js</automated>
  </verify>
  <acceptance_criteria>
    - renderBrainPanel references learning.decisionTrend and gold precision/recall (or degraded)
    - Coverage uses brainAppliedRuleIds from lastResult when present
    - bridge.js cache-bust bumped
    - No Analyze push / no data wipe
  </acceptance_criteria>
  <done>Admin can see paired learning metrics in Filter brain panel</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LRN-02 anti-game + LRN-03 HITL regression locks</name>
  <files>tests/bridge-learning-metrics.test.js, tests/bridge-brain-apply.test.js, tests/bridge-phrase-miner.test.js, tests/bridge-engine.test.js</files>
  <read_first>
    tests/bridge-learning-metrics.test.js
    tests/bridge-brain-apply.test.js
    tests/bridge-phrase-miner.test.js
    tests/bridge-engine.test.js
    lib/bridge-brain-apply.js
    lib/bridge-phrase-miner.js
    tests/bridge-accuracy-gold.test.js
  </read_first>
  <behavior>
    - LRN-02: comparable-process — active suppress_type for a junk type that appears in deny gold (or synthetic row) yields brainAppliedRuleIds non-empty and scoreApplyCoverage.sufficient true
    - LRN-02: proposed phrase rule does not apply (brainAppliedRuleIds excludes proposed id)
    - LRN-02: silent-drop ban still fails buildLearningHealth
    - LRN-03: type suppress/promote active on process (existing BRAIN paths retitled or dual-tagged LRN-03)
    - LRN-03: minePhrasesFromEvent leaves status proposed only
    - LRN-03: activate then apply (existing PHRASE-03 style — add LRN-03 alias title if needed)
  </behavior>
  <action>
    Add or dual-tag tests with `LRN-02:` / `LRN-03:` name prefixes.

    Prefer extending existing files over new packages. Use temp BRIDGE_BRAIN_ROOT isolation like engine suite — never touch production brain volume.

    Do not change product HITL behavior except bugfixes required for locks. Do not auto-activate phrases in production code.

    Keep Phase 57 gold asserts untouched (no fixture address deletions).
  </action>
  <verify>
    <automated>node --test --test-name-pattern="LRN-0|PHRASE-0|brainApplied|proposed" tests/bridge-learning-metrics.test.js tests/bridge-brain-apply.test.js tests/bridge-phrase-miner.test.js tests/bridge-engine.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Named LRN-02 and LRN-03 tests pass
    - Proposed phrases never auto-active in miner
    - Active type rules still apply on process
  </acceptance_criteria>
  <done>Anti-game + HITL gates locked under LRN titles</done>
</task>

<task type="auto">
  <name>Task 3: Docs light + full suite + verify-live gate</name>
  <files>docs/bridge/API.md, docs/bridge/TEST-PLAN.md</files>
  <read_first>
    docs/bridge/API.md
    docs/bridge/TEST-PLAN.md
    .planning/phases/58-learning-loop-strength/58-VALIDATION.md
  </read_first>
  <action>
    1. Light docs: document `learning` nest on GET `/api/bridge/brain/metrics` (and metrics.learning on GET `/api/bridge/brain`) — fields decisionTrend, gold, applyCoverage, pairedOk. Note apply coverage is process-meta / client lastResult; phrases proposed-only until Activate.

    2. TEST-PLAN: short LRN-01/02/03 row map pointing at bridge-learning-metrics + brain-api + gold suite.

    3. Run full gate:
       - `node --test tests/bridge-learning-metrics.test.js`
       - `node --test tests/bridge-accuracy-gold.test.js`
       - `npm test`
       - Because public/ edited: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`
       - If verify fails: `scripts\restart.ps1` then re-verify (AGENTS.md). Never leave server dead.

    4. Do not wipe runtime data stores.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test green (includes gold + learning + LRN locks)
    - verify-live.ps1 exit 0 after public edits
    - API.md mentions learning nest
    - No data/bridge-brain or filter-lists deletion
  </acceptance_criteria>
  <done>Phase 58 gated green: metrics honest, HITL intact, suite + live OK</done>
</task>

</tasks>

<verification>
- Admin metrics UI shows trend + gold + coverage markers
- LRN-02/03 automated locks pass
- `npm test` + `scripts/verify-live.ps1` green
- ACC gold 8/8 still green
</verification>

<success_criteria>
1. Paired metrics visible (API + admin panel): decision trend AND gold P/R
2. Metrics require real rule apply coverage; anti-game tests pass
3. Type rules live; phrases proposed-only until activate
</success_criteria>

<output>
After completion, create `.planning/phases/58-learning-loop-strength/58-03-SUMMARY.md`
</output>
