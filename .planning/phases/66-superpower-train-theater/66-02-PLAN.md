---
phase: 66-superpower-train-theater
plan: 02
type: execute
wave: 2
depends_on:
  - "66-01"
files_modified:
  - public/js/bridge.js
  - public/css/bridge.css
  - public/bridge.html
  - tests/bridge-train-theater.test.js
autonomous: true
requirements:
  - THTR-01
must_haves:
  truths:
    - "Mission header is theater HUD: open groups + live kept count visible during Train mode"
    - "Approve/Deny still uses existing commitTrainDecisionLocally; status line keeps Decision saved · {kept} · {remaining} copy"
    - "renderKpis still refreshes kept tile on decision; mission kept span updates on same path"
    - "When theater active (open > 0 or resultsMode train after pivot), wrap has is-theater chrome class; Train tab visually primary vs demoted Kept"
    - "Save list / attach remain reachable in train mode (existing discretion — do not hide)"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "Mission + theater mode visual hierarchy"
      contains: "bridge-train-mission|is-theater|bridge-results-mode--theater"
    - path: "public/js/bridge.js"
      provides: "Live mission kept/open update on decision light path"
      contains: "updateTrainMissionHeader"
  key_links:
    - from: "commitTrainDecisionLocally"
      to: "updateTrainMissionHeader + setTrainStatus + renderKpis"
      via: "refreshTrainUiAfterDecision light path"
      pattern: "updateTrainMissionHeader|Decision saved"
    - from: "setResultsMode / renderResults"
      to: "trainWrap.classList is-theater"
      via: "open count or resultsMode === train"
      pattern: "is-theater|bridge-results-mode--theater"
---

<objective>
Elevate Train theater chrome and wire **live kept-count feedback** into the mission HUD on existing Approve/Deny mutations — without rewriting decision APIs.

Purpose: THTR-01 operator proof that Distressed / Not Distressed decisions update kept + open counts in theater chrome (status + mission + KPI), not only a buried status line.

Output: Mission/theater CSS + live header updates on decision path + demoted Kept tab weight while theater active (ids stable).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-superpower-train-theater/66-CONTEXT.md
@.planning/phases/66-superpower-train-theater/66-RESEARCH.md
@.planning/phases/66-superpower-train-theater/66-01-SUMMARY.md
@.planning/v2.1-FILTER-SCRUB-THEATER.md
@.planning/v1.4-GRITTY-PREMIUM.md
@public/js/bridge.js
@public/css/bridge.css
@public/bridge.html
@tests/bridge-train-theater.test.js
@AGENTS.md

<interfaces>
<!-- Existing decision status (MUST preserve strings for tests/operators) -->
```js
// commitTrainDecisionLocally after apply + refresh
`Decision saved · ${keptNow.toLocaleString()} kept. Save list below when this city is ready.`
// or
`Decision saved · ${keptNow.toLocaleString()} kept · ${remaining} group(s) left. Save list when ready.`
```

<!-- Live path already ships -->
refreshTrainUiAfterDecision → renderKpis(lastResult.stats); re-render train groups if mode train/brain
// 66-01 may already call updateTrainMissionHeader — ensure always on this path

<!-- Theater chrome discretion (locked) -->
- Option A kept escape: keep #bridge-mode-kept "Kept list" — demote visually when .bridge-results-mode--theater
- Train tab #bridge-mode-train is primary climax when theater
- Do not remove role="tablist" / role="tab" (a11y + train-ux tests)
- Save/attach stay visible in train mode

<!-- CSS guidance -->
- Reuse --phuglee-* / existing grit tokens; spacing {4,8,16,24,32}
- Mission: denser HUD strip (kicker + stats), not marketing banner
- .bridge-results-mode--theater .bridge-mode-tab[data-mode="train"] stronger (active/ember weight)
- .bridge-results-mode--theater .bridge-mode-tab[data-mode="kept"] quieter (ghost/secondary)
- prefers-reduced-motion: no required motion for mission; if card exit already respects it, leave alone
- Zero new palette tokens unless matching Collect heat already on page

<!-- Forbidden -->
- Do not change POST /api/bridge/brain/decisions contract or clientApplied
- Do not change DENY_CONFIRM_THRESHOLD, hotkeys semantics (except they still work in theater)
- Do not wipe filter-lists / brain data
- Do not demote brain to armory yet — that's 66-03 (may leave equal brain tab until 03)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live mission HUD on decision path</name>
  <files>public/js/bridge.js, tests/bridge-train-theater.test.js</files>
  <behavior>
    - After every successful commitTrainDecisionLocally, mission open + kept spans match remaining + keptNow
    - refreshTrainUiAfterDecision still calls renderKpis
    - Status templates with kept + remaining still present
    - updateTrainMissionHeader invoked from decision path (not only full renderResults)
    - Undo path also refreshes mission counts when list restored
  </behavior>
  <action>
    1. Ensure `commitTrainDecisionLocally` (after computing `remaining` / `keptNow`) calls:
       ```js
       updateTrainMissionHeader(remaining, keptNow);
       ```
       before or after `setTrainStatus` (order free).

    2. Ensure `refreshTrainUiAfterDecision` does not wipe mission; if it re-renders groups only, re-call mission with current open count + rows.length if needed for consistency.

    3. On undo success after snapshot restore: recompute open + kept and `updateTrainMissionHeader`.

    4. Extend `tests/bridge-train-theater.test.js`:
       - Static: `commitTrainDecisionLocally` / decision region includes `updateTrainMissionHeader`
       - Static: still matches `/Decision saved · .* kept/`
       - Static: `renderKpis` still near decision refresh path
       - Assert no `fetchJson('/api/bridge/brain/decisions'` body field invents new keys for kept count (optional scan: clientApplied still present)

    5. Do not rewrite `persistTrainBrainDecision` or server modules.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-theater.test.js</automated>
  </verify>
  <done>Live kept/open wired to mission on mutation path; status + KPI contracts intact.</done>
</task>

<task type="auto">
  <name>Task 2: Theater CSS + is-theater class hierarchy</name>
  <files>public/css/bridge.css, public/js/bridge.js, public/bridge.html, tests/bridge-train-theater.test.js</files>
  <behavior>
    - Admin with open groups / train mode: #bridge-train-wrap has class is-theater (or data-theater="1")
    - .bridge-results-mode gets bridge-results-mode--theater when theater active
    - Mission strip styled as ops HUD; open count readable at a glance
    - Kept tab demoted vs Train when theater class present
    - train-ux CSS token reuse tests still pass
  </behavior>
  <action>
    1. **JS:** In `setResultsMode` and/or after openCount known in `renderResults` / `updateTrainMissionHeader`:
       ```js
       const wrap = document.getElementById('bridge-train-wrap');
       const modeRail = wrap && wrap.querySelector('.bridge-results-mode');
       const theaterOn = isBridgeAdmin() && (resultsMode === 'train' || openCount > 0);
       // Prefer: theater chrome when resultsMode === 'train' OR openCount > 0 while wrap visible
       if (wrap) wrap.classList.toggle('is-theater', Boolean(theaterOn && !wrap.hidden));
       if (modeRail) modeRail.classList.toggle('bridge-results-mode--theater', Boolean(theaterOn && !wrap.hidden));
       ```
       Pass openCount into helper or recompute via countOpenTrainGroups(lastResult, trainDecidedKeys).

    2. **CSS:** Add block after existing `.bridge-results-mode` / `.bridge-mode-tab` (~1848+):
       - `.bridge-train-mission` layout (flex/gap 8–16, margin-bottom 16)
       - `.bridge-train-mission-kicker` Anton or existing label voice, short
       - `.bridge-train-mission-stats` body/meta cream
       - `#bridge-train-open-count` slightly emphasized when open &gt; 0
       - `.bridge-results-mode--theater` Train tab stronger; Kept quieter
       - No equal 3-up emphasis growth for brain (66-03 will demote further)

    3. **HTML:** Optional `aria-label` on mission stats already role=status from 01 — keep.

    4. **Cache-bust:** bump `bridge.css?v=` and `bridge.js?v=` in bridge.html.

    5. **Tests:** CSS contains `.bridge-train-mission` and `bridge-results-mode--theater` (or `is-theater`).

    6. **AGENTS.md:** run verify-live after public/ edits (may share gate with 66-03 if continuing immediately; prefer run here).
  </action>
  <acceptance_criteria>
    - bridge.css defines .bridge-train-mission and theater demotion selectors
    - bridge.js toggles is-theater or bridge-results-mode--theater
    - Decision path still has Decision saved · kept strings
    - train-ux CSS vocabulary tests still green
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-train-theater.test.js tests/bridge-train-ux.test.js</automated>
  </verify>
  <done>Theater HUD chrome + live counts ship; Kept escape hatch remains via demoted tab.</done>
</task>

</tasks>

<verification>
- Theater + train-ux tests green
- Manual mental path: process (admin, open groups) → mission shows N open + kept → Deny/Approve → kept/open update without full page reload
- Save list still visible under train mode
</verification>

<success_criteria>
1. Live kept-count feedback visible in theater mission + existing status/KPI
2. Train is visual climax when open groups; Kept still reachable
3. No decision API rewrite
</success_criteria>

<output>
After completion, create `.planning/phases/66-superpower-train-theater/66-02-SUMMARY.md`
</output>
