---
phase: 66-superpower-train-theater
plan: 03
type: execute
wave: 3
depends_on:
  - "66-02"
files_modified:
  - public/bridge.html
  - public/js/bridge.js
  - public/css/bridge.css
  - tests/bridge-train-theater.test.js
  - tests/bridge-train-ux.test.js
  - tests/bridge-efficiency-path.test.js
autonomous: true
requirements:
  - THTR-02
  - THTR-03
must_haves:
  truths:
    - "Filter brain is secondary rules armory — not equal third peer tab weight when theater active"
    - "Brain panel still loadable for admin (loadBrainPanel + #bridge-brain-panel ids preserved)"
    - "Non-admin never sees train/brain chrome: wrap default hidden; renderResults non-admin clears containers; new theater chrome inside wrap only"
    - "isBridgeAdmin fail-closed preserved (exact admin / PhugleeSettings.isAdmin)"
    - "THTR + train-ux + efficiency + independence targeted suites green; npm test + verify-live green"
  artifacts:
    - path: "public/bridge.html"
      provides: "Brain armory demoted label/control; stable brain panel ids"
      contains: "bridge-mode-brain|bridge-brain-panel|armory|Rules"
    - path: "public/css/bridge.css"
      provides: "Armory demotion styles in theater"
      contains: "armory|bridge-mode-tab--armory|bridge-results-mode--theater"
    - path: "tests/bridge-train-theater.test.js"
      provides: "THTR-02 + THTR-03 locks"
      contains: "THTR-02|THTR-03"
  key_links:
    - from: "bridge-mode-brain"
      to: "setResultsMode('brain') → loadBrainPanel"
      via: "existing click handler; demoted chrome only"
      pattern: "data-mode=[\"']brain|loadBrainPanel"
    - from: "renderResults non-admin"
      to: "setHidden(trainWrap, true)"
      via: "isBridgeAdmin gate"
      pattern: "isBridgeAdmin|bridge-train-wrap"
    - from: "phase gate"
      to: "npm test + verify-live"
      via: "full regression + live health"
      pattern: "verify-live"
---

<objective>
Demote Filter brain to **rules armory** (THTR-02) and lock **non-admin hide** for all theater chrome (THTR-03), then full suite + live gate.

Purpose: Complete Phase 66 — Train theater climax without peer Brain tab competing; preserve v1.6 TRAIN-03 admin gate fail-closed.

Output: Armory chrome + THTR-02/03 tests + green `npm test` + `scripts/verify-live.ps1`.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-superpower-train-theater/66-CONTEXT.md
@.planning/phases/66-superpower-train-theater/66-RESEARCH.md
@.planning/phases/66-superpower-train-theater/66-01-SUMMARY.md
@.planning/phases/66-superpower-train-theater/66-02-SUMMARY.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@tests/bridge-train-theater.test.js
@tests/bridge-train-ux.test.js
@tests/bridge-efficiency-path.test.js
@tests/bridge-independence.test.js
@AGENTS.md

<interfaces>
<!-- THTR-02 target (discretion locked) -->
- Keep id="bridge-mode-brain" data-mode="brain" role="tab" for setResultsMode + tests
- Visible label: **Rules armory** (replaces equal "Filter brain" peer weight)
- Optional class: bridge-mode-tab--armory
- When .bridge-results-mode--theater: armory is visually quietest (smaller / ghost / trailing)
- Panel ids unchanged: bridge-brain-panel, brain-type-rules, brain-phrase-proposed, brain-phrase-active, brain-metrics
- loadBrainPanel() still runs on setResultsMode('brain')
- Do NOT auto-open brain after process; do NOT auto-activate phrases

<!-- THTR-03 locks (already as-built — do not regress) -->
```js
// renderResults
if (isBridgeAdmin()) { setHidden(trainWrap, false); ... }
else {
  setHidden(trainWrap, true);
  // clear distressed/not containers + train status
  // also hide mission if needed
}
// HTML: #bridge-train-wrap hidden by default
// All train/brain clicks + hotkeys gate isBridgeAdmin()
```

New chrome MUST:
- Live inside #bridge-train-wrap
- Never be created/appended to body or results panel outside wrap for non-admin
- Remain hidden when wrap.hidden

<!-- train-ux may assert "Train brain" label — UPDATE tests to accept theater labels -->
// Current: html.includes('Train brain')
// After: allow "Train brain" OR "Train" / "Train mission" primary tab text
// Prefer tab label: **Train** or keep **Train brain** for minimal churn — discretion:
//   LOCK: train tab text stays "Train brain" OR becomes "Train" — pick one in execute; update train-ux assert accordingly
//   Brain label MUST change to "Rules armory" for THTR-02 product language

<!-- Forbidden -->
- Non-admin train
- Brain API rewrite
- Removing isBridgeAdmin checks
- Wiping data stores
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Demote brain to Rules armory + THTR-02/03 tests</name>
  <files>public/bridge.html, public/css/bridge.css, public/js/bridge.js, tests/bridge-train-theater.test.js, tests/bridge-train-ux.test.js</files>
  <behavior>
    - Brain control label is Rules armory (secondary)
    - Theater CSS demotes armory vs Train primary
    - bridge-brain-panel + loadBrainPanel path intact
    - setResultsMode('brain') still works for admin
    - Non-admin: wrap hidden default; renderResults clears train; mission not visible outside wrap
    - isBridgeAdmin unit tests still exact-match admin
  </behavior>
  <action>
    1. **HTML:** Change `#bridge-mode-brain` button text from `Filter brain` to **`Rules armory`**. Add class `bridge-mode-tab--armory` if useful. Keep `data-mode="brain"`, `aria-controls="bridge-brain-panel"`, panel structure.

    2. **CSS:** Under theater + general:
       - `.bridge-mode-tab--armory` / `[data-mode="brain"]` quieter than Train/Kept when `.bridge-results-mode--theater`
       - Not equal third column visual weight (opacity, font-size, or order OK — do not use display:none that breaks keyboard/tab if still a tab)
       - Optional: order Train first (already is), Kept second, Armory third with reduced emphasis

    3. **JS:** Confirm tab click handler still maps data-mode brain → setResultsMode. No auto brain open on process. When non-admin, ensure `updateTrainMissionHeader` cannot unhide wrap.

    4. **Tests — theater file THTR-02:**
       - HTML contains `Rules armory` and `id="bridge-mode-brain"`
       - HTML still has `id="bridge-brain-panel"`
       - CSS has armory demotion selector OR theater brain quieter rule
       - bridge.js still has `loadBrainPanel` and `data-mode` brain path
       - Equal peer ban spirit: assert label is not `Filter brain` as equal peer (or assert `Rules armory` present and `Filter brain` absent as tab label)

    5. **Tests — THTR-03:**
       - train wrap opening tag still has `hidden`
       - mission id only appears after train-wrap open index
       - bridge.js non-admin branch still `setHidden(trainWrap, true)` / clear containers
       - isBridgeAdmin still used on train decision / mode click paths (static grep)
       - Carry-forward: `tests/bridge-train-ux.test.js` isBridgeAdmin exact admin tests still green

    6. **Update train-ux** if it required `Filter brain` string — replace with `Rules armory` or soft assert. Keep `Train brain` / Kept list asserts if labels unchanged; if Train tab renamed, update assert.

    7. Cache-bust css/js again.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-theater.test.js tests/bridge-train-ux.test.js</automated>
  </verify>
  <done>Brain is armory; THTR-02/03 automated; train-ux admin gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-suite gate + verify-live</name>
  <files>tests/bridge-train-theater.test.js, tests/bridge-efficiency-path.test.js</files>
  <behavior>
    - Efficiency path still: no Analyze CTAs, Train chrome present, Save list primary
    - Independence still green
    - Full npm test green
    - verify-live.ps1 exit 0 after public/ edits
  </behavior>
  <action>
    1. Run targeted pack:
       ```
       node --test tests/bridge-train-theater.test.js tests/bridge-train-ux.test.js tests/bridge-efficiency-path.test.js tests/bridge-list-factory-ux.test.js tests/bridge-independence.test.js
       ```

    2. If efficiency-path greps for `Filter brain`, update to `Rules armory` without weakening EFF-02.

    3. Full suite: `npm test`

    4. Live gate from project root:
       ```
       powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
       ```
       On failure: `scripts\restart.ps1` then re-verify until exit 0. Never claim done without 200.

    5. Do **not** wipe `data/filter-lists/`, `data/bridge-brain/`, or other runtime stores.

    6. Do **not** change processUpload keep/kill or brain decision server modules to "fix" tests — only presentation regressions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Phase 66 THTR-01–03 complete: theater pivot, armory demotion, admin gate, suite + live green.</done>
</task>

</tasks>

<verification>
- THTR-02/03 tests green
- train-ux + efficiency + independence green
- `npm test` green
- `scripts/verify-live.ps1` exit 0
</verification>

<success_criteria>
1. Filter brain is secondary Rules armory, not equal peer with Train/Kept under theater
2. Non-admin never sees train/brain/mission chrome
3. Full regression bar green; live server healthy
4. Phase ready for `/gsd:verify-work` then Phase 67
</success_criteria>

<output>
After completion, create `.planning/phases/66-superpower-train-theater/66-03-SUMMARY.md`
</output>
