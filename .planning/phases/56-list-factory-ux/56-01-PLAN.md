---
phase: 56-list-factory-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/bridge.html
  - public/js/bridge.js
  - public/css/bridge.css
autonomous: true
requirements:
  - LIST-01
  - LIST-03
must_haves:
  truths:
    - "Post-process primary CTA remains #bridge-save-list labeled Save list with primary/orange classes"
    - "Results toolbar export is labeled Preview CSV (not Export CSV) and stays ghost/secondary"
    - "Download all (CSV) remains primary post-save bulk path; Download all (XLSX) secondary"
    - "No Send/Push/Import to Analyze CTAs anywhere in Filter HTML/JS"
    - "Teaching copy covers Process → (Train) → Save → Download → enrich → manual Analyze import across save lead, lists lead, lists empty, and optional workflow strip"
  artifacts:
    - path: "public/bridge.html"
      provides: "LIST-01 CTA labels + LIST-03 static teaching pack surfaces"
      contains: "Preview CSV"
    - path: "public/js/bridge.js"
      provides: "Dynamic stub/flash strings stay independence-safe; no Analyze push CTAs"
      contains: "nothing was sent to Analyze"
    - path: "public/css/bridge.css"
      provides: "Light hierarchy so Preview CSV never looks primary"
      contains: "bridge-export-csv|bridge-workflow"
  key_links:
    - from: "public/bridge.html #bridge-save-list"
      to: "Save list primary post-process"
      via: "phuglee-btn-primary classes retained"
      pattern: "id=\"bridge-save-list\".*Save list"
    - from: "public/bridge.html #bridge-export-csv"
      to: "Preview CSV de-emphasized"
      via: "ghost/secondary button text rename"
      pattern: "id=\"bridge-export-csv\".*Preview CSV"
    - from: "public/bridge.html #bridge-lists-empty"
      to: "LIST-03 full factory loop"
      via: "empty body copy"
      pattern: "Save list.*Download|Download.*enrich"
---

<objective>
Ship LIST-01 CTA hierarchy and LIST-03 teaching pack copy so operators see Save list (then Download one/all) as the hero path — never Send to Analyze — with workflow language across hero/results/save/lists.

Purpose: Operator-visible list-factory elevation without touching list store/API.

Output: Updated `public/bridge.html` + light `bridge.js`/`bridge.css` copy and hierarchy polish.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-list-factory-ux/56-RESEARCH.md
@.planning/phases/56-list-factory-ux/56-UI-SPEC.md
@.planning/phases/56-list-factory-ux/56-VALIDATION.md
@.planning/phases/55-independence-lock/55-UI-SPEC.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@AGENTS.md

<interfaces>
<!-- UI-SPEC locked copy — implement exactly -->

CTAs:
- `#bridge-save-list` text: **Save list** (keep id + primary orange classes)
- `#bridge-download-all-csv` text: **Download all (CSV)** (primary when toolbar visible)
- `#bridge-download-all-xlsx` text: **Download all (XLSX)** (ghost/secondary)
- `#bridge-export-csv` text: **Preview CSV** (was Export CSV; ghost/secondary only)
- Forbidden anywhere in Filter UI: "Send to Analyze", "Push to Analyze", "Import to Analyzer", "Open in Analyze", "Push to Analyzer"

Teaching pack (locked targets):
- Save panel lead: `Store this list so you can process more cities. When ready, download from Saved lists for external enrichment. Nothing is sent to Analyze.`
- Lists panel lead: `Master staging: lists stay until you delete them. Download one or all for skip-trace / enrichment, then import into Analyze manually.`
- Lists empty (`#bridge-lists-empty`): `No saved lists yet. Process a city file, Train if needed (admin), click Save list, then Download one or all for external enrichment. Import into Analyze only after skip-trace.`
- Process stub clean path (keep Phase 55): includes `Save the list below — nothing was sent to Analyze.`
- Optional workflow strip: `Workflow: Process → (Train) → Save list → Download → enrich outside → manual Analyze import`
- Pipeline: keep 4 steps (City → Type → Upload → Results) — do NOT renumber to 5

Hero lead (baseline already good): may light-tighten Save if needed; must keep download + Analyze separate / after skip-trace.

Do NOT: rebuild list store, auto-save, re-add push, change export columns, wipe filter-lists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LIST-03 teaching pack + LIST-01 CTA labels in bridge.html</name>
  <files>public/bridge.html</files>
  <read_first>
    - .planning/phases/56-list-factory-ux/56-UI-SPEC.md (Copywriting Contract + Focal Point + Component Inventory)
    - public/bridge.html (hero `.bridge-lead` ~51; results toolbar `#bridge-export-csv` ~326; save panel ~345-354; lists panel lead + empty ~367-379; script cache-bust `bridge.js?v=` ~447)
    - .planning/phases/56-list-factory-ux/56-RESEARCH.md (Pattern 2 CTA hierarchy + Pattern 5 teaching pack surfaces)
  </read_first>
  <action>
    Edit **static HTML only** in `public/bridge.html` (no store/API):

    1. **Rename Export → Preview:** Change `#bridge-export-csv` button text from `Export CSV` to exactly **`Preview CSV`**. Keep classes `bridge-btn bridge-btn-ghost phuglee-btn phuglee-btn-secondary` — never primary/orange.

    2. **Save panel lead** (`.bridge-save-panel .bridge-panel-lead`): replace with exact UI-SPEC:
       `Store this list so you can process more cities. When ready, download from Saved lists for external enrichment. Nothing is sent to Analyze.`

    3. **Lists panel lead** (`#bridge-lists-panel .bridge-panel-lead`): replace with exact UI-SPEC:
       `Master staging: lists stay until you delete them. Download one or all for skip-trace / enrichment, then import into Analyze manually.`

    4. **`#bridge-lists-empty` body:** replace with exact UI-SPEC:
       `No saved lists yet. Process a city file, Train if needed (admin), click Save list, then Download one or all for external enrichment. Import into Analyze only after skip-trace.`

    5. **Keep CTA ids/labels:**
       - `#bridge-save-list` → **Save list** + primary classes (`bridge-btn-primary` / `phuglee-btn-primary`)
       - `#bridge-download-all-csv` → **Download all (CSV)** + primary
       - `#bridge-download-all-xlsx` → **Download all (XLSX)** + ghost/secondary
       - Do **not** add any Analyze push/send/import buttons

    6. **Optional workflow strip (required by this plan for LIST-03):** Add one compact teaching line inside results panel (near results heading / above save panel is fine), e.g.:
       ```html
       <p id="bridge-workflow-strip" class="bridge-workflow-strip bridge-panel-lead">Workflow: Process → (Train) → Save list → Download → enrich outside → manual Analyze import</p>
       ```
       Do **not** renumber pipeline steps (still 4: City → Type → Upload → Results).

    7. **Hero lead:** Keep intent of current `.bridge-lead` (download for enrichment; Analyze separate after skip-trace). Light tighten only if needed — do not bloat into a paragraph essay.

    8. **Pipeline:** leave 4 steps unchanged.

    9. Bump script cache only if Task 2 also edits JS in this plan — Task 2 will bump; if this task is HTML-only, still bump `bridge.css?v=` if CSS changes land in Task 2 of this plan.
  </action>
  <acceptance_criteria>
    - public/bridge.html contains `id="bridge-export-csv"` and button text `Preview CSV`
    - public/bridge.html does NOT contain the label `Export CSV` on that button (no `>Export CSV<`)
    - public/bridge.html contains `id="bridge-save-list"` and `Save list`
    - public/bridge.html contains `id="bridge-download-all-csv"` and `Download all (CSV)`
    - public/bridge.html save panel lead includes `download from Saved lists for external enrichment`
    - public/bridge.html lists lead includes `lists stay until you delete`
    - public/bridge.html `#bridge-lists-empty` includes `Save list` and `Download` and `skip-trace` (or external enrichment)
    - public/bridge.html contains workflow strip text `Process → (Train) → Save list → Download`
    - public/bridge.html has NO `Send to Analyze`, `Push to Analyze`, `Import to Analyzer`
    - Pipeline still shows steps 1–4 City/Type/Upload/Results only
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('public/bridge.html','utf8');const must=['id=\"bridge-save-list\"','Save list','Preview CSV','id=\"bridge-download-all-csv\"','Download all (CSV)','download from Saved lists for external enrichment','lists stay until you delete','Process → (Train) → Save list → Download'];for(const s of must){if(!h.includes(s)){console.error('MISSING',s);process.exit(1)}}if(h.includes('>Export CSV<')){console.error('still Export CSV');process.exit(2)}for(const b of['Send to Analyze','Push to Analyze','Import to Analyzer']){if(h.includes(b)){console.error('BANNED',b);process.exit(3)}}console.log('56-01-html-ok')"</automated>
  </verify>
  <done>Static Filter HTML teaches Save → Download factory loop; Preview CSV de-emphasized; Analyze push CTAs absent.</done>
</task>

<task type="auto">
  <name>Task 2: Light CSS hierarchy + JS string hygiene (no dirty-guard yet)</name>
  <files>public/css/bridge.css, public/js/bridge.js, public/bridge.html</files>
  <read_first>
    - public/css/bridge.css (`.bridge-panel-lead`, `.bridge-lists-flash`, results toolbar styles near export/save)
    - public/js/bridge.js (renderResults stub ~1840-1875; any `Export CSV` string if present)
    - public/bridge.html (`bridge.js?v=` and `bridge.css?v=` cache busters)
    - AGENTS.md (verify-live after public/; never wipe filter-lists)
  </read_first>
  <action>
    1. **CSS (light only):** Ensure `#bridge-export-csv` / Preview CSV cannot appear primary. If needed, add a small rule that keeps export toolbar button secondary (existing ghost classes should already win — only add CSS if hierarchy is weak). Style `#bridge-workflow-strip` / `.bridge-workflow-strip` with existing body/panel-lead patterns and spacing from {4,8,16,24,32,48,64} only (prefer md 16px padding if any). **No new design tokens. No accent orange on Preview CSV.**

    2. **JS string hygiene only (guards are Plan 02):**
       - Confirm clean process stub still says `Save the list below — nothing was sent to Analyze.` (Phase 55 lock).
       - Do **not** implement dirty-guard or Train-before-Save here (Plan 02 owns those).
       - Do **not** change save flash download wording yet if Plan 02 will own `resetImportAreaAfterSave` flash — optional: leave flash for 02.
       - Ensure no JS creates buttons/labels with banned Analyze CTA strings.
       - If any client export UI string still says `Export CSV`, rename to `Preview CSV`.

    3. **Cache-bust:** Bump `public/bridge.html` `bridge.js?v=19` → `v=20` and `bridge.css?v=9` → `v=10` (or +1 from current).

    4. **AGENTS.md:** Never touch `data/filter-lists/`, `data/bridge-brain/`. After public edits run:
       `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`
       If fail: `scripts\restart.ps1` then re-verify until exit 0.
  </action>
  <acceptance_criteria>
    - public/js/bridge.js still contains `nothing was sent to Analyze`
    - public/js/bridge.js has no `Send to Analyze` / `Push to Analyze` / `Import to Analyzer`
    - public/bridge.html script tag references bumped bridge.js version (not still `?v=19` if that was pre-edit)
    - If workflow-strip CSS added, selector exists in bridge.css
    - `scripts\verify-live.ps1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('public/js/bridge.js','utf8');const h=fs.readFileSync('public/bridge.html','utf8');if(!j.includes('nothing was sent to Analyze'))process.exit(1);for(const b of['Send to Analyze','Push to Analyze','Import to Analyzer']){if(j.includes(b))process.exit(2)}if(/bridge\.js\?v=19/.test(h))process.exit(3);console.log('56-01-js-ok')" ; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>Hierarchy + independence strings stable; live server healthy after public touch.</done>
</task>

</tasks>

<verification>
- HTML one-liner green (Save list, Preview CSV, Download all, teaching pack, no banned CTAs)
- JS banned-string check green
- verify-live.ps1 green
</verification>

<success_criteria>
- Operator-facing Filter page teaches Process → (Train) → Save → Download → enrich → manual Analyze
- Save list is undisputed post-process primary CTA; Preview CSV is secondary; Download all primary post-save
- Zero Analyze push CTAs
</success_criteria>

<output>
After completion, create `.planning/phases/56-list-factory-ux/56-01-SUMMARY.md`
</output>
