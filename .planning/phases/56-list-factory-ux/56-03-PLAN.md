---
phase: 56-list-factory-ux
plan: 03
type: execute
wave: 3
depends_on:
  - "56-01"
  - "56-02"
files_modified:
  - tests/bridge-list-factory-ux.test.js
  - tests/bridge-list-store.test.js
  - docs/bridge/DATA-STANDARDS.md
  - docs/bridge/API.md
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "Automated factory-ux tests ban Analyze push CTAs and lock Save list + Download all + Preview CSV + workflow teaching strings"
    - "Static check proves handleProcess does not call clearAllLists (process does not wipe list store)"
    - "List store multi-city accumulate assertion exists (two saves → two summaries until delete)"
    - "Docs teach Process → Save → Download → enrich → manual Analyze; API.md not claiming always-on alreadyImported hard-drop"
    - "Independence suite still green; full npm test green; verify-live green if any public residual"
  artifacts:
    - path: "tests/bridge-list-factory-ux.test.js"
      provides: "LIST-01/02/03 static UX + process-no-wipe locks"
      contains: "LIST-01"
    - path: "tests/bridge-list-store.test.js"
      provides: "LIST-02 multi-city accumulate lock"
      contains: "LIST-02|accumulate|City A"
    - path: "docs/bridge/DATA-STANDARDS.md"
      provides: "Light LIST workflow wording"
      contains: "Save|Download"
  key_links:
    - from: "tests/bridge-list-factory-ux.test.js"
      to: "public/bridge.html + public/js/bridge.js"
      via: "static string bans and required teaching copy"
      pattern: "Send to Analyze|Save list|Preview CSV"
    - from: "tests/bridge-list-factory-ux.test.js"
      to: "lib/bridge-api.js handleProcess"
      via: "no clearAllLists in process slice"
      pattern: "clearAllLists"
    - from: "docs/bridge/DATA-STANDARDS.md"
      to: "list factory workflow"
      via: "Process/Save/Download/manual Analyze language"
      pattern: "Download|manual|Analyze"
---

<objective>
Lock LIST-01/02/03 with automated tests and light docs so agents cannot reintroduce Analyze push CTAs, wipe-on-process, or drop the Save → Download teaching narrative.

Purpose: Regression bar for list-factory UX after Plans 01–02 land.

Output: `tests/bridge-list-factory-ux.test.js`, optional list-store accumulate assert, light DATA-STANDARDS/API edits, green suite (+ verify-live if public touched).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-list-factory-ux/56-RESEARCH.md
@.planning/phases/56-list-factory-ux/56-VALIDATION.md
@.planning/phases/56-list-factory-ux/56-UI-SPEC.md
@.planning/phases/56-list-factory-ux/56-01-SUMMARY.md
@.planning/phases/56-list-factory-ux/56-02-SUMMARY.md
@tests/bridge-list-store.test.js
@tests/bridge-independence.test.js
@docs/bridge/DATA-STANDARDS.md
@docs/bridge/API.md
@lib/bridge-api.js
@AGENTS.md

<interfaces>
<!-- Required test coverage (node:test + assert/strict) -->

LIST-01 static:
- html+js must NOT include: `Send to Analyze`, `Push to Analyze`, `Import to Analyzer`, `Open in Analyze`, `Push to Analyzer`
- html must include: `id="bridge-save-list"`, `Save list`, `id="bridge-download-all-csv"`, `Download all`, `Preview CSV`
- html must NOT use `>Export CSV<` as button label

LIST-02 static:
- `handleProcess` slice in `lib/bridge-api.js` must not include `clearAllLists`
- Optional: processUpload client must not call clearAllLists

LIST-02 unit (list-store):
- Two saves under same username accumulate to ≥2 lists (not replace-all)

LIST-03 static:
- corpus (html+js) includes workflow teaching: Save list + Download + (enrich or skip-trace) + Analyze
- empty/lists lead strings present (from Plan 01): e.g. `lists stay until you delete` OR full empty-state loop
- dirty-guard / soft train / flash strings from Plan 02 present in bridge.js

Docs (light):
- DATA-STANDARDS Filter Saved Lists: mention multi-city persist until delete; Save → Download → enrich → manual Analyze
- API.md: fix stale line that claims addresses in Analyze are still always removed (`alreadyImported`) — align with IND-04 off-by-default from Phase 55

Never wipe real FILTER_LISTS_ROOT in tests — temp root only (existing pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bridge-list-factory-ux.test.js + LIST-02 accumulate assert</name>
  <files>tests/bridge-list-factory-ux.test.js, tests/bridge-list-store.test.js</files>
  <read_first>
    - .planning/phases/56-list-factory-ux/56-RESEARCH.md (Static UX locks code example + Validation Architecture)
    - .planning/phases/56-list-factory-ux/56-VALIDATION.md (Wave 0 requirements)
    - public/bridge.html (final copy after 01)
    - public/js/bridge.js (final guards after 02)
    - lib/bridge-api.js (locate `async function handleProcess` for slice bounds)
    - tests/bridge-list-store.test.js (temp FILTER_LISTS_ROOT pattern)
    - tests/bridge-independence.test.js (style reference only — keep green)
  </read_first>
  <behavior>
    - LIST-01: banned Analyze CTA strings absent from public/bridge.html and public/js/bridge.js
    - LIST-01: Save list CTA id + label present; Download all control present; Preview CSV present; Export CSV button label absent
    - LIST-02: handleProcess source slice does not call clearAllLists
    - LIST-02: multi-city accumulate — two saveList calls → both appear in listSummaries until deleted
    - LIST-03: workflow corpus includes Save list + Download + enrich/skip-trace language + Analyze (manual/import, not push)
    - LIST-02 seam strings: dirty-guard `not saved yet` and soft train `Train group(s) are still visible` present in bridge.js
    - LIST-01 flash/path: `download from Saved lists for enrichment` present in bridge.js
  </behavior>
  <action>
    **1. Create `tests/bridge-list-factory-ux.test.js`** using `node:test` + `node:assert/strict`:

    - Read `public/bridge.html`, `public/js/bridge.js`, and `lib/bridge-api.js` as UTF-8.
    - Tests named with LIST-01 / LIST-02 / LIST-03 prefixes for traceability.
    - Implement bans + required strings per interfaces above.
    - For process-no-wipe: find `async function handleProcess` start; slice until next top-level `async function handle` after it; assert `clearAllLists` not in slice.
    - Do **not** invent Puppeteer/JSDOM for confirm dialogs.

    **2. Extend `tests/bridge-list-store.test.js`** with one explicit LIST-02 accumulate test if not already equivalent:
    - Username isolated e.g. `list02-multi`
    - saveList City A + City B
    - assert summaries contain both names / length ≥ 2 for those names
    - Use temp root only (existing before/after) — never real production filter-lists path

    **3. Run:**
    ```
    node --test tests/bridge-list-factory-ux.test.js tests/bridge-list-store.test.js tests/bridge-independence.test.js
    ```
    Fix product code only if a Plan 01/02 string drifted — do not rebuild store.
  </action>
  <acceptance_criteria>
    - tests/bridge-list-factory-ux.test.js exists and includes LIST-01, LIST-02, LIST-03 test titles
    - File asserts absence of Send to Analyze / Push to Analyze
    - File asserts presence of Save list, Preview CSV, Download all
    - File asserts handleProcess does not clearAllLists
    - list-store has multi-city accumulate assertion (LIST-02)
    - `node --test tests/bridge-list-factory-ux.test.js tests/bridge-list-store.test.js tests/bridge-independence.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-list-factory-ux.test.js tests/bridge-list-store.test.js tests/bridge-independence.test.js</automated>
  </verify>
  <done>Factory UX + persistence locks automated and green with independence suite.</done>
</task>

<task type="auto">
  <name>Task 2: Light docs + full suite + verify-live</name>
  <files>docs/bridge/DATA-STANDARDS.md, docs/bridge/API.md</files>
  <read_first>
    - docs/bridge/DATA-STANDARDS.md (Filter Saved Lists section ~94-104)
    - docs/bridge/API.md (process response notes ~100-106; Saved lists ~113+)
    - .planning/phases/56-list-factory-ux/56-UI-SPEC.md (workflow teaching — docs should match product, not invent push)
    - AGENTS.md (never wipe volumes; verify-live if public residual)
  </read_first>
  <action>
    **1. DATA-STANDARDS.md — light LIST workflow (not a rewrite):**
    - Under Filter Saved Lists, ensure multi-city lists **persist until operator deletes** (process/restart/deploy do not wipe).
    - Workflow line: Process → (Train admin optional) → Save list → Download one/all → external enrich/skip-trace → **manual** Analyze import.
    - Keep no-auto-push language from Phase 55.
    - Do not change export column contracts.

    **2. API.md — fix stale always-on alreadyImported claim:**
    - Current stale line (~105): claims addresses in Analyze session are still removed (`stats.alreadyImported`).
    - Align with IND-04: hard-drop **off by default**; only when `applyAlreadyImportedFilter === true`; default `alreadyImported` is 0.
    - Keep Saved lists routes table accurate (download / download-all if documented).

    **3. Full gate:**
    ```
    npm test
    ```
    If any public file was touched this plan (should be none), also run verify-live. If only docs/tests: verify-live still recommended once to confirm server up — run if health uncertain:
    ```
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    ```

    **4.** Never delete `data/filter-lists/` or brain data.
  </action>
  <acceptance_criteria>
    - DATA-STANDARDS mentions persist until delete OR multi-city staging + Download + manual Analyze
    - API.md does not claim Analyze addresses are always removed on every process
    - API.md mentions off by default or applyAlreadyImportedFilter (or equivalent default-off wording)
    - `npm test` exits 0
    - factory-ux tests still in suite via tests/**/*.test.js glob
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/bridge/DATA-STANDARDS.md','utf8');const a=fs.readFileSync('docs/bridge/API.md','utf8');if(!/Download|manual/i.test(d))process.exit(1);if(/Addresses already present in the Analyze session are still removed/.test(a))process.exit(2);if(!/off by default|applyAlreadyImportedFilter/i.test(a+d))process.exit(3);console.log('docs-ok')" ; npm test</automated>
  </verify>
  <done>Docs aligned with list factory + IND-04; full test suite green; LIST-01/02/03 locked.</done>
</task>

</tasks>

<verification>
- factory-ux + list-store + independence green
- npm test green
- Docs stale always-on alreadyImported fixed
- Optional verify-live if public residual
</verification>

<success_criteria>
- Agents cannot reintroduce Analyze push CTAs without failing tests
- Process path statically proven not to wipe lists
- Multi-city accumulate locked
- Teaching + download primary path locked by static strings from Plans 01–02
</success_criteria>

<output>
After completion, create `.planning/phases/56-list-factory-ux/56-03-SUMMARY.md`
</output>
