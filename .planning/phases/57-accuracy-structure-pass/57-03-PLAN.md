---
phase: 57-accuracy-structure-pass
plan: 03
type: execute
wave: 3
depends_on:
  - 57-01
  - 57-02
files_modified:
  - tests/bridge-accuracy-gold.test.js
  - tests/bridge-engine.test.js
  - docs/bridge/TAGGING-RULES.md
  - docs/bridge/TEST-PLAN.md
autonomous: true
requirements:
  - ACC-02
  - ACC-03
must_haves:
  truths:
    - "ACC-02 gold bans silent-drop solely for no Type / unresolved map / cleaner kept counts — banned reason patterns never appear"
    - "GATE-04 __none__ and COL-02 style no-Type inventory still reviewable (kept or FN)"
    - "All-FN zero-kept policy still succeeds with notDistressedRows reviewable"
    - "v1.7–v1.8 locks still green: COL single Type, empty-only promote, stable groups, display-only short labels, format confirm, water BRAIN-03"
    - "Optional thin ACC-03 wrapper titles or documented dependency — no re-implementation of scorer/groups/labels"
  artifacts:
    - path: "tests/bridge-accuracy-gold.test.js"
      provides: "ACC-02 banned-reason + no-type locks; optional ACC-03 re-assert titles"
      contains: "ACC-02"
    - path: "tests/bridge-engine.test.js"
      provides: "Existing COL/GATE/MAP/GROUP/LBL/BRAIN locks kept green"
      contains: "COL-02"
  key_links:
    - from: "ACC-02 gold"
      to: "result.discarded + stats.discardReasons"
      via: "banned reason pattern scan"
      pattern: "no_type|discardReasons"
    - from: "ACC-03 regression"
      to: "COL-|GATE-|TEST-|MAP-|LBL|BRAIN|water|GROUP engine tests"
      via: "node --test-name-pattern keep-green"
      pattern: "COL-|GATE-|TEST-03|shortLabel"
    - from: "processUpload pipeline"
      to: "forceTypeColumn exclusive + promote empty-only"
      via: "no blend reopen"
      pattern: "forceTypeColumn|promoteCategoryFromRaw"
---

<objective>
Lock ACC-02 silent-drop bans and ACC-03 v1.7–v1.8 regression surface so Phase 58/60 inherit a frozen accuracy structure without re-implementing Type/group/label/format features.

Purpose: Accuracy work must never invent no-Type drops or cleaner-list hard-drops; existing COL/GATE/MAP/GROUP/LBL/BRAIN tests remain the ACC-03 authority.

Output: Strengthened gold ACC-02 asserts + ACC-03 keep-green wave (+ optional thin wrappers / docs fixture map).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-accuracy-structure-pass/57-RESEARCH.md
@.planning/phases/57-accuracy-structure-pass/57-01-SUMMARY.md
@.planning/phases/57-accuracy-structure-pass/57-02-SUMMARY.md
@tests/bridge-accuracy-gold.test.js
@tests/bridge-engine.test.js
@lib/bridge-intake-schema.js
@docs/bridge/TAGGING-RULES.md
@docs/bridge/TEST-PLAN.md
@AGENTS.md

<interfaces>
ACC-02 allowlist (thin discard keys from lib/bridge-intake-schema.js DISCARD_REASONS):
- no_address, blank_row, non_property, duplicate
- stats may tally no_distress_signal for FN (reviewable inventory in notDistressedRows)
- already_imported only with applyAlreadyImportedFilter === true (gold never enables)

ACC-02 banned reason regex (must never appear in discarded reasons or stats.discardReasons keys):
```js
/no_type(_column)?|unresolved_map|missing_type|low_confidence|cleaner_list|silent_drop/i
```

Existing ACC-03 authority tests (keep green — do not re-implement):
- COL-01 / COL-02 / COL-03 / COL-04 — tests/bridge-engine.test.js + type-column-score unit
- GATE-02 / GATE-03 / GATE-04 / GATE-06 — format confirm first/changed / __none__
- MAP / promote empty-only — category-promote + engine
- GROUP / TEST-01..03 (v1.7) — stable keys / timestamps
- LBL / TEST-03 (v1.8) — shortLabel display-only
- BRAIN-03 water type suppress ignore
- IND-04 default-off already_imported (do not re-enable)

Optional thin ACC-03 wrappers in gold file (research discretion):
```js
test('ACC-03: delegates COL/GATE/LBL locks to engine suite (smoke)', async () => {
  // Prefer: document in comment + one smoke that processUpload still returns
  // processingMeta.columnMap / typeResolution / reviewGroups with shortLabel when applicable
  // OR skip smoke and only run engine pattern in verify — either OK if SUMMARY lists ACC-03 commands
});
```

Phase 55 IND-04: already_imported off by default — do not change.
Phase 56 list UX: do not rebuild list UI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen ACC-02 silent-drop bans + zero-kept FN inventory lock</name>
  <files>tests/bridge-accuracy-gold.test.js</files>
  <read_first>
    - tests/bridge-accuracy-gold.test.js (57-01/02 ACC-02 tests)
    - tests/bridge-engine.test.js (COL-02 ~1006; GATE-04 __none__ ~1220; all-FN zero-kept ~612)
    - lib/bridge-intake-schema.js (DISCARD_REASONS keys)
    - .planning/phases/57-accuracy-structure-pass/57-RESEARCH.md (Pitfall 1, 3; ACC-02 map)
  </read_first>
  <behavior>
    - ACC-02 no-type gold: still green (weeds inventory kept or FN; no no_type discard)
    - ACC-02 banned reasons: after processing keep-distress-mixed, deny-junk-admin, no-type-notes-only — no banned pattern in discard blob
    - ACC-02 all-FN style: process a pure-junk subset (or deny fixture alone) → result.ok true, rows.length may be 0, notDistressedRows.length >= 1, reviewGroups.notDistressed present — inventory not vanished
    - ACC-02 does not treat no_distress_signal stats tally as a failure (FN is correct deny path)
    - Optional: assert stats.discardReasons keys ⊆ allowlist ∪ {no_distress_signal, already_imported} when present
  </behavior>
  <action>
    1. Ensure gold suite has explicit ACC-02 coverage (add if thin after 57-01):
       - Keep/strengthen `ACC-02: gold no-type-notes-only keeps weeds inventory (no no_type discard)`
       - Keep/strengthen `ACC-02: gold processes never invent banned silent-drop reasons` to scan **keep + deny + no-type** fixtures
       - Add if missing: `ACC-02: gold all-junk file succeeds with empty kept and reviewable FN pool` — process deny-junk-admin.csv alone; assert `ok`, `rows` has no Gold Deny Strong, `notDistressedRows.length >= 1`, `reviewGroups.notDistressed.length >= 1`

    2. Centralize banned regex once in the test file:
       ```js
       const BANNED_DROP = /no_type(_column)?|unresolved_map|missing_type|low_confidence|cleaner_list|silent_drop/i;
       ```

    3. Do **not** add production hard-drop paths. If a ban fails because production invented a reason, fix production by **removing** the drop (ACC-02), not by allowing the reason.

    4. Do not enable `applyAlreadyImportedFilter` in gold tests.
  </action>
  <acceptance_criteria>
    - tests/bridge-accuracy-gold.test.js contains BANNED_DROP or equivalent /no_type/ ban asserts
    - At least one ACC-02 test title references all-junk / FN pool / zero-kept inventory
    - `node --test --test-name-pattern="ACC-02" tests/bridge-accuracy-gold.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="ACC-02" tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>ACC-02 silent-drop bans + reviewable FN inventory locked in gold suite.</done>
</task>

<task type="auto">
  <name>Task 2: ACC-03 regression keep-green + optional wrappers/docs</name>
  <files>tests/bridge-accuracy-gold.test.js, tests/bridge-engine.test.js, docs/bridge/TAGGING-RULES.md, docs/bridge/TEST-PLAN.md</files>
  <read_first>
    - tests/bridge-engine.test.js (COL-*, GATE-*, MAP, GROUP/TEST, LBL/TEST-03, BRAIN-03, IND-04)
    - tests/bridge-accuracy-gold.test.js
    - docs/bridge/TEST-PLAN.md (if exists — case ID table)
    - docs/bridge/TAGGING-RULES.md (optional ACC fixture map note)
    - .planning/phases/57-accuracy-structure-pass/57-VALIDATION.md
  </read_first>
  <action>
    1. **ACC-03 keep-green (required):** Run focused engine + unit locks — do not rewrite features:
       ```
       node --test --test-name-pattern="COL-|GATE-|TEST-|MAP-|LBL|BRAIN|water|GROUP|IND-04" tests/bridge-engine.test.js
       ```
       Also run if quick: `node --test tests/bridge-type-column-score.test.js tests/bridge-category-promote.test.js tests/bridge-review-groups.test.js tests/bridge-short-label.test.js` (skip any file that does not exist).

    2. **Optional thin ACC-03 titles** in gold file (pick one approach, not both heavy):
       - **A (prefer light):** Add 1–2 smoke tests titled `ACC-03: …` that process type-trap or keep fixture and assert:
         - `processingMeta.columnMap` is an object
         - exclusive Type: `violationIssueType` is string or null/undefined — never comma-joined multi-header blend like `Status,Vio Cat`
         - if distressed groups exist, `shortLabel` is display-only (group has full `violationTypeLabel`; row.violationIssueType is not replaced by short label alone when long type present)
       - **B:** Skip new tests; document in SUMMARY + TEST-PLAN that ACC-03 is the existing engine pattern suite

    3. **Docs (optional, light):** If `docs/bridge/TEST-PLAN.md` has a case table, add a row mapping ACC-01/02/03 → `tests/bridge-accuracy-gold.test.js` + gold fixtures. If TAGGING-RULES: add a short "Gold fixtures (Phase 57)" note pointing at `tests/fixtures/bridge/gold/` — no policy rewrite.

    4. **Full suite gate:** `npm test` must exit 0.

    5. **Do not** touch public/ unless unavoidable — if you do, run `scripts/verify-live.ps1`. Prefer no public/ edits.

    6. **Do not** re-implement scorer, promote, groups, short-label, or format store. **Do not** re-enable already_imported default-on. **Do not** wipe volumes.
  </action>
  <acceptance_criteria>
    - Engine ACC-03 pattern command exits 0
    - Full gold file exits 0: `node --test tests/bridge-accuracy-gold.test.js`
    - `npm test` exits 0
    - SUMMARY lists the exact ACC-03 commands that stayed green
    - No new npm packages; no production Type-blend reopen
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-accuracy-gold.test.js; node --test --test-name-pattern="COL-|GATE-|TEST-|MAP-|LBL|BRAIN|water|GROUP|IND-04" tests/bridge-engine.test.js; npm test</automated>
  </verify>
  <done>ACC-02 bans hardened; ACC-03 v1.7–v1.8 locks green; gold suite ready for Phase 58/60.</done>
</task>

</tasks>

<verification>
- ACC-02 pattern green
- ACC-03 engine pattern green
- Full gold + npm test green
- Independence IND-04 still green within engine pattern
</verification>

<success_criteria>
- No silent drop solely for no Type / unresolved map / cleaner counts
- Single Type winner, empty-only promote, stable keys, display-only short labels, format confirm still locked
- Named gold suite is the durable ACC artifact for learning metrics and milestone QA
</success_criteria>

<output>
After completion, create `.planning/phases/57-accuracy-structure-pass/57-03-SUMMARY.md`
</output>
