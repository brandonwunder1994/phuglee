---
phase: 57-accuracy-structure-pass
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - lib/bridge-distress-tagger.js
  - tests/bridge-distress-tagger.test.js
  - tests/bridge-accuracy-gold.test.js
autonomous: true
requirements:
  - ACC-01
must_haves:
  truths:
    - "All ACC-01 gold processUpload tests pass (keep Strong, junk FN, water never type-suppressed)"
    - "If gold was already green after 57-01, production tagger is unchanged (no speculative rewrite)"
    - "Any tagger fix preserves trash-can exclusion, parking-not-distress, permit-not-Strong behavior"
    - "Existing bridge-distress-tagger unit suite stays green after any tagger edit"
  artifacts:
    - path: "tests/bridge-accuracy-gold.test.js"
      provides: "ACC-01 green contracts after residual fixes"
      contains: "ACC-01"
    - path: "lib/bridge-distress-tagger.js"
      provides: "Base keep/kill regex (edit only if gold red)"
      contains: "INDICATOR_CATEGORIES"
  key_links:
    - from: "lib/bridge-distress-tagger.js tagRow"
      to: "filterDistressOnly / processUpload"
      via: "Strong vs Standard → kept vs FN"
      pattern: "STRONG_DISTRESSED_TAG|Standard Code Violation"
    - from: "ACC-01 gold red"
      to: "INDICATOR_CATEGORIES or stripTrashCanMentions"
      via: "minimal pattern fix only"
      pattern: "INDICATOR_CATEGORIES"
---

<objective>
Drive ACC-01 gold suite to green by fixing only proven residual keep/kill (or Type) failures — never speculative tagger rewrites.

Purpose: ACC-01 requires implement-not-audit-only. If fixtures already pass, shipping the lock is the implementation. If red, fix the minimal surface that makes gold green without breaking ACC-02/03 locks.

Output: Green `node --test tests/bridge-accuracy-gold.test.js` ACC-01 cases; optional narrow tagger/unit updates.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-accuracy-structure-pass/57-RESEARCH.md
@.planning/phases/57-accuracy-structure-pass/57-01-SUMMARY.md
@docs/bridge/TAGGING-RULES.md
@lib/bridge-distress-tagger.js
@lib/bridge-engine/normalizer.js
@lib/bridge-brain-apply.js
@tests/bridge-accuracy-gold.test.js
@tests/bridge-distress-tagger.test.js
@AGENTS.md

<interfaces>
From lib/bridge-distress-tagger.js (primary ACC-01 fix surface):
```js
const STRONG_DISTRESSED_TAG = 'Strong Distressed Signal';
// INDICATOR_CATEGORIES: vegetation, trash_debris, vehicles, structures, exterior_neglect
// stripTrashCanMentions(text) — cans/bins not distress
// tagRow(row) → distressedSignalTag + matchedIndicators
// filterDistressOnly(rows, uploadType) → { kept, discarded: [{row, reason:'no_distress_signal'}] }
// water_shut_off: always high-value tag; no keyword scan
```

Fix priority (research):
1. `lib/bridge-distress-tagger.js` — keep/kill residual
2. `lib/bridge-engine/normalizer.js` — only if Type map / promote residual proven by gold type-trap
3. `lib/bridge-engine/index.js` — only if pipeline policy fails (prefer avoid)
4. Never auto-activate phrases; never new hard-drop reasons; never re-enable already_imported default-on

Water: `applyBrainToRows` early-return for water_shut_off must stay — if water gold fails, fix brain apply early-return, not tagger keywords.

"Junk denied" = Standard → notDistressedRows (FN), not inventing thin discard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose gold reds and apply minimal ACC-01 fixes</name>
  <files>lib/bridge-distress-tagger.js, tests/bridge-distress-tagger.test.js, tests/bridge-accuracy-gold.test.js, lib/bridge-engine/normalizer.js, lib/bridge-brain-apply.js</files>
  <read_first>
    - .planning/phases/57-accuracy-structure-pass/57-01-SUMMARY.md (which ACC-01 tests failed, if any)
    - tests/bridge-accuracy-gold.test.js
    - lib/bridge-distress-tagger.js (INDICATOR_CATEGORIES + stripTrashCanMentions + non_property)
    - tests/bridge-distress-tagger.test.js (existing unit matrix)
    - docs/bridge/TAGGING-RULES.md
    - lib/bridge-brain-apply.js (water early-return)
  </read_first>
  <action>
    1. Run `node --test tests/bridge-accuracy-gold.test.js` and capture which ACC-01 tests fail (if none fail, **skip production edits** — go to Task 2 documentation path).

    2. For each red ACC-01 case, classify root cause:
       - **False FN (real distress not Strong)** → loosen/add pattern in the matching INDICATOR_CATEGORIES entry only; keep vegetation/trash/blight intentionally loose per TAGGING-RULES
       - **False Strong (junk kept)** → tighten exclusion (e.g. stripTrashCanMentions, avoid bare parking/permit matches) without killing real trash/debris/weeds
       - **Water suppress applies** → restore/preserve early-return in `applyBrainToRows` for `water_shut_off`; do not route water through type suppress
       - **Type trap wrong column** → only then touch `normalizer.forceTypeColumn` / scorer; prefer not to reopen COL blend

    3. Apply **minimal** diffs. Rules:
       - No multi-column Type blend
       - No new discard reason keys for "cleaner lists"
       - No phrase auto-activate
       - No `already_imported` default-on
       - Do not wipe volumes
       - Zero new npm packages

    4. If a tagger pattern changes, add/adjust a focused unit in `tests/bridge-distress-tagger.test.js` for that edge (e.g. specific phrase → Strong or Standard) so the residual cannot regress without gold.

    5. Re-run gold until ACC-01 titles pass. Do not weaken gold asserts to force green unless a fixture row itself violates TAGGING-RULES (then fix fixture text, not the product bar).

    6. If already green after 57-01: leave `lib/bridge-distress-tagger.js` byte-identical (no drive-by refactors). Record "no production diff required" in SUMMARY.
  </action>
  <acceptance_criteria>
    - `node --test --test-name-pattern="ACC-01" tests/bridge-accuracy-gold.test.js` exits 0
    - If lib/bridge-distress-tagger.js changed: `node --test tests/bridge-distress-tagger.test.js` exits 0
    - No new discard reason strings like no_type / low_confidence_drop introduced in lib/
    - SUMMARY notes either "fixes: <files>" or "already green — fixtures are the lock"
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="ACC-01" tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>All ACC-01 gold contracts green; residual keep/kill fixed only when proven red.</done>
</task>

<task type="auto">
  <name>Task 2: Guard against keep-set regression on mixed gold + full gold file</name>
  <files>tests/bridge-accuracy-gold.test.js</files>
  <read_first>
    - tests/bridge-accuracy-gold.test.js (after Task 1)
    - tests/bridge-distress-tagger.test.js (if tagger changed)
  </read_first>
  <action>
    1. Run the **full** gold file (ACC-01 + ACC-02 together): `node --test tests/bridge-accuracy-gold.test.js`.

    2. If ACC-02 broke because of an ACC-01 fix (e.g. junk now Strong, or weeds dropped from no-type path), fix the conflict in tagger/fixture without relaxing ACC-02 bans.

    3. If tagger changed, also run: `node --test tests/bridge-distress-tagger.test.js`.

    4. Spot-check: do **not** measure success as "fewer kept rows." Keep fixture Strong count must remain ≥ number of keep-matrix addresses; deny fixture Strong count must stay 0 for Gold Deny addresses.

    5. Optional micro-assert only if missing: ensure deny test explicitly checks `notDistressedRows` presence (FN reviewable), not merely "absent from rows" — if already present, do not churn.
  </action>
  <acceptance_criteria>
    - Full `node --test tests/bridge-accuracy-gold.test.js` exits 0
    - Grep/test: ACC-01 deny still asserts FN pool (notDistressedRows) for junk addresses
    - No speculative large rewrite of INDICATOR_CATEGORIES (prefer surgical pattern edits)
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>Full gold suite green after any ACC-01 residual work; FN vs kept semantics intact.</done>
</task>

</tasks>

<verification>
- ACC-01 pattern green; full gold file green
- Tagger unit suite green if tagger touched
- Prefer zero lib/ churn when already green
</verification>

<success_criteria>
- Real distress gold kept Strong; junk gold denied to FN; water gold never type-suppressed
- Fixes are code (or fixtures-as-lock when already green) — not audit-only markdown
- No silent-drop "accuracy" hacks introduced
</success_criteria>

<output>
After completion, create `.planning/phases/57-accuracy-structure-pass/57-02-SUMMARY.md`
</output>
