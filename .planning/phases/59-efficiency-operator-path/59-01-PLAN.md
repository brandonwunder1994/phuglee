---
phase: 59-efficiency-operator-path
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/bridge-efficiency-path.test.js
autonomous: true
requirements:
  - EFF-01
  - EFF-02
must_haves:
  truths:
    - "EFF-01 as-built pillars are locked in tests: GATE-03 auto_reuse, download-all anchors, Train group stacking chrome"
    - "EFF-01 polish contracts exist RED: Format reused UI string, post-save Download this list affordance"
    - "EFF-02 anti-patterns banned: no Analyze push CTAs, no auto-save-on-process, GATE-02 confirm still present, Train chrome not stripped, gold suite still required"
  artifacts:
    - path: "tests/bridge-efficiency-path.test.js"
      provides: "EFF-01 path locks + EFF-02 negative static contracts"
      contains: "EFF-01"
      min_lines: 80
  key_links:
    - from: "tests/bridge-efficiency-path.test.js"
      to: "lib/bridge-engine/index.js"
      via: "static scan for auto_reuse + TYPE_COLUMN_CONFIRM_REQUIRED"
      pattern: "auto_reuse|TYPE_COLUMN_CONFIRM_REQUIRED"
    - from: "tests/bridge-efficiency-path.test.js"
      to: "public/js/bridge.js"
      via: "static scan for day-2 polish contracts + no auto-save"
      pattern: "Format reused|Download this list|saveCurrentList"
    - from: "tests/bridge-efficiency-path.test.js"
      to: "public/bridge.html"
      via: "download-all + save list CTA carry-forward"
      pattern: "bridge-download-all-csv|bridge-save-list"
---

<objective>
Wave 0 TDD: create `tests/bridge-efficiency-path.test.js` that locks day-2 efficiency pillars and EFF-02 anti-patterns.

Purpose: Drive Phase 59 polish with red contracts and freeze already-shipped GATE/LIST/Train path so “efficiency” cannot rubber-stamp confirm, hide Train, silent-drop, or re-couple Analyze.

Output: New efficiency test file. **As-built locks GREEN; polish contracts intentionally RED** until Plan 02. No production code in this plan.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-efficiency-operator-path/59-RESEARCH.md
@.planning/phases/59-efficiency-operator-path/59-VALIDATION.md
@tests/bridge-list-factory-ux.test.js
@tests/bridge-independence.test.js
@tests/bridge-engine.test.js
@public/js/bridge.js
@public/bridge.html
@lib/bridge-engine/index.js
@AGENTS.md

<interfaces>
<!-- Mirror Phase 56 static-test style (node:test + fs source scans). Zero new packages. -->

```js
// tests/bridge-efficiency-path.test.js skeleton
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(ROOT, 'public', 'bridge.html'), 'utf8');
const js = fs.readFileSync(path.join(ROOT, 'public', 'js', 'bridge.js'), 'utf8');
const engine = fs.readFileSync(path.join(ROOT, 'lib', 'bridge-engine', 'index.js'), 'utf8');
// optional: train.js, independence module paths

const BANNED_CTAS = [
  'Send to Analyze',
  'Push to Analyze',
  'Import to Analyzer',
  'Open in Analyze',
  'Push to Analyzer'
];
```

<!-- As-built backend (do not re-implement — only assert still present) -->
GATE-03: `source = 'auto_reuse'` when fingerprint matches memory (lib/bridge-engine/index.js).
GATE-02: `TYPE_COLUMN_CONFIRM_REQUIRED` on first/changed format.
Download all: `#bridge-download-all-csv` / `#bridge-download-all-xlsx` in bridge.html.
Train: `renderTrainGroups` / `.bridge-train-group` / `data-action="approve"` in bridge.js or bridge-train.js.
Save: `#bridge-save-list`, `async function saveCurrentList` — must NOT be called from processUpload success path.

<!-- Polish contracts (RED until Plan 02) -->
1. bridge.js contains `Format reused` (results meta chip for typeResolution.source === 'auto_reuse')
2. bridge.js contains post-save flash download affordance: `Download this list` (or id `bridge-flash-download-csv`) wired to downloadSavedList
3. Optional but recommended same file: `durationMs` surfaced as seconds fragment in renderResults

<!-- EFF-02 must stay green forever -->
- No banned Analyze CTAs in html/js
- processUpload path does not auto-call saveCurrentList / POST lists
- TYPE_COLUMN_CONFIRM_REQUIRED still in engine
- renderTrainGroups still invoked for admin on renderResults
- gold suite file still exists: tests/bridge-accuracy-gold.test.js
- bridge-analyzer-push.js still absent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EFF-01 as-built path locks + polish RED contracts</name>
  <files>tests/bridge-efficiency-path.test.js</files>
  <behavior>
    - EFF-01 GATE-03: engine source contains `auto_reuse` and memoryMatch / formatMatched day-2 branch markers
    - EFF-01 bulk download: HTML has `#bridge-download-all-csv` and `#bridge-download-all-xlsx` with Download all copy
    - EFF-01 Save primary carry-forward: HTML has `#bridge-save-list` / Save list
    - EFF-01 stacked Train: bridge.js or bridge-train.js still builds `.bridge-train-group` cards with Approve/Deny data-action
    - EFF-01 polish RED (must fail until Plan 02): bridge.js includes operator-visible `Format reused` string
    - EFF-01 polish RED: bridge.js includes `Download this list` (flash one-click) and references `downloadSavedList` from save/flash path (not only table actions)
    - Optional assert string `durationMs` used in renderResults meta path once Plan 02 lands (may be RED here)
  </behavior>
  <action>
    Create `tests/bridge-efficiency-path.test.js` using the same static-scan pattern as `tests/bridge-list-factory-ux.test.js`.

    **Do NOT modify production code** in this plan. Wave 0 is RED for polish, GREEN for as-built.

    Implement tests with clear titles prefixed `EFF-01:`:

    1. **As-built auto_reuse (GREEN):** read `lib/bridge-engine/index.js`; assert includes `auto_reuse` and `formatMatched` (or memoryMatch branch). Comment that runtime GATE-03 lives in `tests/bridge-engine.test.js` and must stay green at phase gate.

    2. **As-built bulk download (GREEN):** HTML contains `id="bridge-download-all-csv"`, `Download all (CSV)`, `id="bridge-download-all-xlsx"`.

    3. **As-built Save CTA (GREEN):** HTML contains `id="bridge-save-list"` and `Save list`.

    4. **As-built stacked Train chrome (GREEN):** JS (bridge.js and/or bridge-train.js) contains `bridge-train-group` and `data-action="approve"` + `data-action="deny"`.

    5. **Polish — format reuse chip (RED until 02):** `assert.match(js, /Format reused/)` — exact operator copy fragment Plan 02 will ship.

    6. **Polish — post-save download (RED until 02):** assert `Download this list` appears in bridge.js and that `resetImportAreaAfterSave` (or save success path) can pass a list id into flash download (assert function signature growth or `bridge-flash-download` / `downloadSavedList` near flash construction). Prefer:
       - `assert.match(js, /Download this list/)`
       - `assert.match(js, /bridge-flash-download/)` OR `data-action="flash-download"`

    Group polish tests so Plan 02 can make the whole file green without rewriting assertions.
  </action>
  <verify>
    <automated>node --test tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>
    File exists. As-built EFF-01 tests pass. Polish EFF-01 tests fail (RED). No production files changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EFF-02 anti-pattern locks (must stay GREEN)</name>
  <files>tests/bridge-efficiency-path.test.js</files>
  <behavior>
    - No banned Analyze CTAs in bridge.html or bridge.js
    - lib/bridge-analyzer-push.js does not exist
    - processUpload does not call saveCurrentList / auto-save lists
    - TYPE_COLUMN_CONFIRM_REQUIRED still present in engine (GATE-02 not rubber-stamped away)
    - renderResults still calls renderTrainGroups for admin path (Train not stripped for “efficiency”)
    - tests/bridge-accuracy-gold.test.js still exists on disk (accuracy bar not dropped)
    - No auto-download-on-save (download only via explicit click / downloadSavedList / downloadAll)
  </behavior>
  <action>
    Append `EFF-02:` tests to the same file (do not split files).

    1. **Banned CTAs:** same BANNED_CTAS list as factory-ux; none appear in html or js.

    2. **No push module:** `fs.existsSync(lib/bridge-analyzer-push.js) === false`.

    3. **No auto-save on process:** slice `async function processUpload` body (~first 4000–6000 chars after declaration, or until next top-level `async function`); assert it does **not** include `saveCurrentList(` and does **not** POST `/api/bridge/lists`.

    4. **GATE-02 still present:** engine source includes `TYPE_COLUMN_CONFIRM_REQUIRED`.

    5. **Train not skipped:** bridge.js `renderResults` region still contains `renderTrainGroups` and `isBridgeAdmin`.

    6. **Gold suite present:** `tests/bridge-accuracy-gold.test.js` exists.

    7. **No auto-download on save:** `resetImportAreaAfterSave` / save success path must not call `downloadSavedList` or `downloadAllSavedLists` **without** a user click handler — i.e. no bare `downloadSavedList(...)` invocation inside `resetImportAreaAfterSave` body. Flash may *create* a button that calls download on click; that is fine. Assert the reset function does not auto-invoke download (scan function body for immediate `downloadSavedList(` / `downloadAllSavedLists(` calls outside event listeners). After Plan 02, re-check this still holds.

    These EFF-02 tests must **pass now** and stay green through Plans 02–03.
  </action>
  <verify>
    <automated>node --test tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>
    EFF-02 tests pass. EFF-01 polish tests still RED. Document in SUMMARY which tests are intentionally red for Plan 02.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-efficiency-path.test.js` runs
- As-built EFF-01 + all EFF-02 = pass
- Polish EFF-01 (`Format reused`, `Download this list` / flash download) = fail until 59-02
- No edits under `public/`, `lib/`, or runtime data stores
</verification>

<success_criteria>
1. Wave 0 efficiency test file exists with EFF-01 and EFF-02 coverage
2. Production code untouched
3. Plan 02 has clear red contracts to green
</success_criteria>

<output>
After completion, create `.planning/phases/59-efficiency-operator-path/59-01-SUMMARY.md`
</output>
