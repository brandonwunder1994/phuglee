---
phase: 59-efficiency-operator-path
plan: 03
type: execute
wave: 3
depends_on:
  - "59-02"
files_modified:
  - public/js/bridge.js
  - public/bridge.html
  - tests/bridge-efficiency-path.test.js
  - tests/bridge-train-ux.test.js
  - docs/bridge/DATA-STANDARDS.md
  - docs/bridge/TEST-PLAN.md
autonomous: true
requirements:
  - EFF-01
  - EFF-02
must_haves:
  truths:
    - "Admin Train mode: A/Enter approves first visible undecided group; D denies (Deny≥10 confirm still applies)"
    - "Keyboard ignored when focus is in input/textarea/contenteditable or resultsMode !== train"
    - "Keyboard never bulk-decides all groups or hides Train groups"
    - "EFF-02 cross-suite green: efficiency-path + GATE-02/03 engine + gold + independence + list-factory-ux"
    - "Full npm test green; verify-live green after public/ edits"
  artifacts:
    - path: "public/js/bridge.js"
      provides: "Train keyboard listener with input guardrails"
      contains: "keydown"
    - path: "tests/bridge-efficiency-path.test.js"
      provides: "EFF keyboard + final EFF-02 locks"
      contains: "EFF-02"
  key_links:
    - from: "document keydown (train)"
      to: "onTrainDecision"
      via: "first .bridge-train-group with approve/deny when resultsMode===train"
      pattern: "keydown|onTrainDecision|bridge-train-group"
    - from: "phase gate"
      to: "gold + independence + GATE"
      via: "npm test + targeted suites"
      pattern: "bridge-accuracy-gold|bridge-independence|auto_reuse"
---

<objective>
Finish EFF-01 residual Train speed (optional A/D keyboard) and freeze EFF-02 with cross-suite + live gates; light docs for day-2 path.

Purpose: Complete shorter operator path without trading accuracy or independence; leave Phase 60 a green bar.

Output: Train keyboard + green full suite + verify-live + brief EFF docs line.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-efficiency-operator-path/59-RESEARCH.md
@.planning/phases/59-efficiency-operator-path/59-02-SUMMARY.md
@public/js/bridge.js
@public/js/bridge-train.js
@tests/bridge-efficiency-path.test.js
@tests/bridge-train-ux.test.js
@tests/bridge-engine.test.js
@tests/bridge-accuracy-gold.test.js
@tests/bridge-independence.test.js
@docs/bridge/DATA-STANDARDS.md
@docs/bridge/TEST-PLAN.md
@AGENTS.md

<interfaces>
<!-- Existing Train decision entry -->
```js
// public/js/bridge.js
async function onTrainDecision(action, group, card) { /* deny confirm when count >= 10 */ }
function resolveTrainGroupFromCard(card) { /* group from lastResult */ }
// Click path:
const btn = event.target.closest('[data-action="approve"], [data-action="deny"]');
const card = btn.closest('.bridge-train-group');
```

<!-- Keyboard contract (client-only) -->
```js
// document-level keydown (register once near other Train listeners)
// Guards (all required):
// 1. resultsMode === 'train'
// 2. isBridgeAdmin()
// 3. target is not INPUT, TEXTAREA, SELECT, or contentEditable
// 4. not modified by ctrl/meta/alt (allow plain keys only)
// Keys:
//   'a' or 'A' or 'Enter' → approve first actionable card
//   'd' or 'D' → deny first actionable card
// First card = first .bridge-train-group in #bridge-train-panel (or distressed then not_distressed)
//   that has an enabled [data-action="approve"] button
// Reuse onTrainDecision so Deny≥10 confirm stays
// preventDefault when handling
// Must NOT: loop all groups, skip confirm, fire when search focused
```

<!-- Card markup -->
```html
<article class="bridge-train-group" data-group-id="..." data-section="...">
  <button data-action="approve">...</button>
  <button data-action="deny">...</button>
</article>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Train A/D keyboard with guardrails</name>
  <files>public/js/bridge.js, public/bridge.html, tests/bridge-efficiency-path.test.js, tests/bridge-train-ux.test.js</files>
  <behavior>
    - Static: bridge.js registers keydown handler mentioning train / approve / deny keys
    - Static: handler checks resultsMode === 'train' and ignores INPUT/TEXTAREA (or tagName checks)
    - Static: uses onTrainDecision or same approve/deny path (not a parallel silent API)
    - Deny threshold constant DENY_CONFIRM_THRESHOLD still used (no bypass)
    - Does not remove renderTrainGroups or filterUndecidedTrainGroups
  </behavior>
  <action>
    1. Add a single `document.addEventListener('keydown', ...)` near existing Train panel listeners in `bridge.js`.

    2. Implement guards and key map per interfaces block. Helper sketch:
       ```js
       function handleTrainHotkeys(event) {
         if (resultsMode !== 'train' || !isBridgeAdmin()) return;
         const el = event.target;
         if (el && (el.closest && el.closest('input, textarea, select, [contenteditable="true"]'))) return;
         if (el && /^(INPUT|TEXTAREA|SELECT)$/.test(el.tagName)) return;
         if (event.ctrlKey || event.metaKey || event.altKey) return;
         const key = event.key;
         let action = null;
         if (key === 'a' || key === 'A' || key === 'Enter') action = 'approve';
         else if (key === 'd' || key === 'D') action = 'deny';
         else return;
         const panel = document.getElementById('bridge-train-panel');
         if (!panel || panel.hidden) return;
         const card = panel.querySelector('.bridge-train-group button[data-action="approve"]:not([disabled])')
           ?.closest('.bridge-train-group');
         if (!card) return;
         event.preventDefault();
         const group = resolveTrainGroupFromCard(card);
         onTrainDecision(action, group, card).catch(...);
       }
       ```

    3. Bump `bridge.js?v=` cache-bust again if Plan 02 already shipped a bump.

    4. Extend tests:
       - `tests/bridge-efficiency-path.test.js`: EFF-01 keyboard static strings (`keydown`, train mode guard, `approve`/`deny` keys or `'a'`/`'d'`).
       - `tests/bridge-train-ux.test.js` (optional thin): assert bridge.js contains INPUT/TEXTAREA ignore pattern so search is safe.

    5. Do **not** change Deny≥10 confirm, decision API, or brain apply. Pure client UX.
  </action>
  <verify>
    <automated>node --test tests/bridge-efficiency-path.test.js tests/bridge-train-ux.test.js</automated>
  </verify>
  <done>
    Keyboard path present with focus/mode guards; efficiency + train-ux tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: EFF-02 cross-suite gate + light docs + verify-live</name>
  <files>docs/bridge/DATA-STANDARDS.md, docs/bridge/TEST-PLAN.md, tests/bridge-efficiency-path.test.js</files>
  <behavior>
    - GATE-02 still rejects first format without confirm
    - GATE-03 still auto_reuse on matching fingerprint
    - Gold keep/kill suite green
    - Independence no-push suite green
    - List factory UX green
    - Full npm test green
    - verify-live health+home 200
  </behavior>
  <action>
    1. Run targeted regression pack (must all pass):
       ```
       node --test tests/bridge-efficiency-path.test.js tests/bridge-list-factory-ux.test.js tests/bridge-engine.test.js tests/bridge-accuracy-gold.test.js tests/bridge-independence.test.js tests/bridge-train-ux.test.js
       ```

    2. Run full suite: `npm test`

    3. Live gate (public/ was edited this phase):
       ```
       powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
       ```
       If fail: `scripts\restart.ps1` then re-verify. Never claim done without 200.

    4. Light docs only (no redesign):
       - `docs/bridge/DATA-STANDARDS.md`: add a short **Day-2 / known format** note under process/format section if one exists — matching fingerprint reuses Type (auto_reuse, no modal); first/changed still confirms; path remains Process → (Train) → Save → Download; no Analyze push.
       - `docs/bridge/TEST-PLAN.md`: one line that `tests/bridge-efficiency-path.test.js` locks EFF-01/02 (reuse UI, post-save download, keyboard guards, anti silent-drop/push/auto-save).

    5. **Do not** wipe filter-lists, bridge-brain, city-formats, or Form Forge/Analyzer user data.

    6. If any gold/GATE/independence red: fix only Phase 59 regressions (likely accidental string/CTA breakage). Do not retune accuracy taggers or format fingerprint algorithms.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    npm test green; verify-live green; docs mention EFF day-2 path; EFF-01 and EFF-02 phase goals satisfied.
  </done>
</task>

</tasks>

<verification>
- Train keyboard static contracts green
- `node --test` efficiency + engine GATE + gold + independence + factory-ux green
- `npm test` green
- `scripts/verify-live.ps1` exit 0
</verification>

<success_criteria>
1. Day-2 path shorter and operator-visible (reuse chip, stacked Train + keyboard, bulk + post-save download)
2. No efficiency change increased silent drops, skipped Train, or re-coupled Analyze
3. Phase ready for `/gsd:verify-work` then Phase 60 packaging
</success_criteria>

<output>
After completion, create `.planning/phases/59-efficiency-operator-path/59-03-SUMMARY.md`
</output>
