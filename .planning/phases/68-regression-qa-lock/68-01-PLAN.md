---
phase: 68-regression-qa-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridge-scrub-theater.test.js
  - docs/bridge/TEST-PLAN.md
  - .planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - "v1.6–v2.0 independence / gold / brain / processUpload permanent bars remain greppable and are mapped under QA-01 (v2.1) packaging (no re-implementation)"
    - "Theater surface contracts for FEED/KILL/THTR reduced-motion + Save primary + train fail-closed exist as automated static tests OR product-phase dual-tags are greppable and documented (gates-only when already green)"
    - "docs/bridge/TEST-PLAN.md maps QA-01..03 (v2.1) to concrete automated files, verify-live + /bridge, and checklist"
    - "68-QA-CHECKLIST.md exists for mobile 390 + desktop 1440 overflow, 44px CTAs, and reduced-motion FEED/KILL/THTR human gate"
  artifacts:
    - path: "docs/bridge/TEST-PLAN.md"
      provides: "v2.1 permanent regression bar map (QA-01..03 → files + live + checklist)"
      contains: "QA-01 (v2.1)"
    - path: ".planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md"
      provides: "QA-03 layout/motion human checklist template"
      contains: "390"
    - path: "tests/bridge-scrub-theater.test.js"
      provides: "Optional theater static contracts when 64–67 hooks lack dual-tags"
      contains: "QA-03 (v2.1)"
  key_links:
    - from: "TEST-PLAN §O QA-01 (v2.1)"
      to: "tests/bridge-independence.test.js + bridge-accuracy-gold + engine + brain/train/list/LRN/EFF suites"
      via: "full npm test permanent bar (keep green; do not rewrite)"
      pattern: "QA-01 \\(v2\\.1\\)|TEST-01 \\(v2\\.0\\)|TEST-02 \\(v2\\.0\\)"
    - from: "TEST-PLAN §O QA-02 (v2.1)"
      to: "scripts/verify-live.ps1 + explicit /bridge 200"
      via: "Plan 02 ship gate (documented here; executed in 68-02)"
      pattern: "QA-02 \\(v2\\.1\\)|verify-live|/bridge"
    - from: "theater contracts QA-03 (v2.1)"
      to: "public/css/bridge.css + public/js/bridge.js + public/bridge.html (shipped 64–67 hooks)"
      via: "static fs.readFileSync + assert.match (no Playwright)"
      pattern: "prefers-reduced-motion|QA-03 \\(v2\\.1\\)|FEED|KILL|THTR"
    - from: "68-QA-CHECKLIST.md"
      to: "/bridge at 390 and 1440 + DevTools reduced-motion"
      via: "human layout/motion gate when CSS cannot prove overflow"
      pattern: "390|1440|reduced-motion|44"
---

<objective>
Package the v2.1 Filter Scrub Theater milestone permanent regression bar: keep v1.6–v2.0 locks greppable under QA-01, add theater surface contracts (or document product-phase dual-tags) for QA-03, map QA-01..03 in TEST-PLAN §O, and ship a mobile/reduced-motion checklist template.

Purpose: Phases 61–67 ship product theater. Phase 68 freezes the composed bar so future work cannot re-couple Analyze, drop gold, ship a dead Filter route, or regress FEED/KILL/THTR a11y/layout. Prefer **gates-only when already green**; add focused static tests only for missing greppable locks.

Output: TEST-PLAN §O v2.1 map; `68-QA-CHECKLIST.md`; `tests/bridge-scrub-theater.test.js` **if** inventory finds missing FEED/KILL/THTR contracts (skip file creation if product phases already dual-tag and reduced-motion/44px are greppable). Full suite + verify-live + `/bridge` deferred to Plan 02.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-regression-qa-lock/68-CONTEXT.md
@.planning/phases/68-regression-qa-lock/68-RESEARCH.md
@.planning/phases/68-regression-qa-lock/68-VALIDATION.md
@docs/bridge/TEST-PLAN.md
@tests/bridge-independence.test.js
@tests/bridge-accuracy-gold.test.js
@tests/bridge-list-factory-ux.test.js
@tests/bridge-train-ux.test.js
@tests/bridge-efficiency-path.test.js
@package.json
@AGENTS.md

<interfaces>
<!-- Lock-and-ship only. Product theater lands in 61–67. Phase 68 packages + gaps. -->

Title naming (MANDATORY for any new permanent-bar packaging tests):
- Always `QA-0N (v2.1): …` — never bare TEST-01/02/03 and never overwrite v1.7/v1.8/v2.0 titles
- Dual-tag product IDs when useful: `FEED-02 / QA-03 (v2.1): …`, `KILL-03 / QA-03 (v2.1): …`, `THTR-03 / QA-03 (v2.1): …`
- Leave all v1.7 / v1.8 / v2.0 titled tests **untouched**

Permanent bars to KEEP green (do not re-implement):
```text
tests/bridge-independence.test.js     → TEST-01 (v2.0) no-push + already_imported
tests/bridge-accuracy-gold.test.js    → TEST-02 (v2.0) gold ACC
tests/bridge-engine.test.js           → COL/GATE/water + IND-04 + v1.7/v1.8 TEST
tests/bridge-train-ux.test.js         → admin gate / train wrap hidden
tests/bridge-list-factory-ux.test.js  → LIST Save primary / no Analyze push CTAs
tests/bridge-efficiency-path.test.js  → EFF day-2 path
tests/bridge-learning-metrics.test.js → LRN anti-game
tests/bridge-brain-*.test.js          → brain store/apply/API
```

Static theater contract pattern (project standard — list-factory / train-ux / efficiency):
```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
const ROOT = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(ROOT, 'public', 'bridge.html'), 'utf8');
const css = fs.readFileSync(path.join(ROOT, 'public', 'css', 'bridge.css'), 'utf8');
const js = fs.readFileSync(path.join(ROOT, 'public', 'js', 'bridge.js'), 'utf8');
// assert.match against REAL strings from 64–67 inventory — never invent IDs
```

Wave 0 inventory rule:
- Read 64–67 SUMMARY/VERIFICATION (or grep public/bridge.* for feed/kill/theater/reduced-motion)
- Assert only shipped hooks
- If product phases already dual-tag FEED/KILL/THTR + reduced-motion greppable → gates-only: TEST-PLAN + checklist, no new test file

QA-02 packaging (document in §O; execute in Plan 02):
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
$bridge = Invoke-WebRequest -Uri "http://127.0.0.1:3000/bridge" -UseBasicParsing -TimeoutSec 8
if ($bridge.StatusCode -ne 200) { throw "/bridge not 200" }
```
Option A preferred (Plan-only /bridge check). Option B (extend verify-live.ps1) only if executor wants single AGENTS command forever — document in SUMMARY.

AGENTS.md: never wipe data/filter-lists/, data/bridge-brain/. Zero new npm packages. No Playwright. No processUpload rewrite. No React.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory theater hooks + add QA-03 surface contracts (or gates-only confirm)</name>
  <files>tests/bridge-scrub-theater.test.js</files>
  <read_first>
    - .planning/phases/68-regression-qa-lock/68-RESEARCH.md — contract catalog, title convention, pitfalls
    - .planning/phases/64-live-scrub-feed/*SUMMARY* or *VERIFICATION* if present (else grep public/)
    - .planning/phases/65-kill-rate-scrub-report/* if present
    - .planning/phases/66-superpower-train-theater/* if present
    - public/css/bridge.css — prefers-reduced-motion, min-height 44px, feed/report/theater selectors
    - public/js/bridge.js — feed/kill/theater render paths (actual hooks only)
    - public/bridge.html — stable IDs: bridge-process, bridge-save-list, bridge-train-wrap
    - tests/bridge-list-factory-ux.test.js — static assert pattern + BANNED_CTAS
    - tests/bridge-train-ux.test.js — admin gate / train wrap hidden
    - rg across tests/ for FEED-|KILL-|THTR-|QA-03|scrub.?feed|kill.?rate|prefers-reduced-motion
  </read_first>
  <behavior>
    - Inventory real FEED/KILL/THTR DOM/CSS/JS strings after 61–67; never invent IDs
    - If product-phase tests already dual-tag FEED/KILL/THTR and reduced-motion + Save primary + train fail-closed are greppable: **skip new file**; record greppable titles in SUMMARY as gates-only
    - Otherwise create `tests/bridge-scrub-theater.test.js` with static HTML/CSS/JS contracts only (no browser automation)
    - New titles must include `QA-03 (v2.1)` (and dual FEED/KILL/THTR tags when locking those surfaces)
    - Existing train-ux / list-factory / independence / gold titles remain untouched and green
  </behavior>
  <action>
    **A — Inventory (mandatory first step):**

    From repo root, collect shipped hooks:
    ```
    rg -n "prefers-reduced-motion|min-height:\s*44|bridge-scrub|scrub-feed|kill-rate|train-theater|RAW|KILLED|KEPT" public/css/bridge.css public/js/bridge.js public/bridge.html
    rg -n "FEED-|KILL-|THTR-|QA-03|scrub.?theater|scrub.?feed" tests/
    ```
    Also open any `64-*` / `65-*` / `66-*` SUMMARY or VERIFICATION for official class/id names.

    Decision tree:
    | Condition | Action |
    |-----------|--------|
    | Product tests already greppable for FEED/KILL/THTR + `prefers-reduced-motion` in bridge CSS + Save primary + train hidden | Gates-only: do **not** create `bridge-scrub-theater.test.js`; document titles in SUMMARY |
    | Missing reduced-motion / 44px / feed mount / kill hierarchy / theater pivot / Save primary / banned Analyze CTAs | Create focused static tests for **only** the gaps |

    **B — If creating `tests/bridge-scrub-theater.test.js`:**

    File header: Phase 68 / v2.1 Filter Scrub Theater surface locks (QA-03 + FEED/KILL/THTR). Static contracts — no Playwright.

    Minimum greppable contracts when hooks exist (adapt strings to inventory):

    1. `QA-03 (v2.1): bridge.css defines prefers-reduced-motion for theater`
       - `assert.match(css, /prefers-reduced-motion:\s*reduce/)`
       - Prefer also matching feed/report/theater selector fragments **inside** reduce blocks if stable from 64–66

    2. `FEED-02 / QA-03 (v2.1): …` (or equivalent) — feed container/status vocabulary greppable; reduced-motion path not required for comprehension (static summary OR reduce CSS targets feed)

    3. `KILL-03 / QA-03 (v2.1): Save list remains primary post-scrub CTA`
       - `id="bridge-save-list"` (or shipped Stage equivalent) + Save/Stage language
       - Ban Analyze push CTAs (`Send to Analyze`, `Push to Analyze`, etc. — reuse list-factory BANNED list spirit)
       - Optional: RAW → KILLED → KEPT hierarchy strings if 65 shipped them

    4. `THTR-03 / QA-03 (v2.1): train wrap stays fail-closed hidden by default` (thin dual-tag OK)
       - Prefer not duplicating full train-ux suite — one greppable fail-closed assert if theater chrome IDs changed; else document that train-ux covers THTR-03

    5. Optional CTA touch targets if greppable:
       - `QA-03 (v2.1): primary CTAs declare min-height 44px` matching process / save / train approve-deny rules **only if product CSS uses min-height** (else leave to checklist)

    Pure helper units (discretion): only if 64–67 extracted pure functions (e.g. stageScrubFeedRows) and they lack coverage — use `vm` sandbox like train-ux; do **not** re-test processUpload keep/kill (gold owns accuracy).

    **C — Hard rules:**
    - Do not invent `#bridge-scrub-feed` if product never shipped that string
    - Do not edit product modules unless a lock fails for a real regression (minimal fix only)
    - Do not touch gold fixtures, independence bans, or v1.7/v1.8/v2.0 titles
    - Do not wipe data/filter-lists/ or data/bridge-brain/
    - Zero new npm packages

    **D — Verify quick bar after any new/edited tests:**
    ```
    node --test tests/bridge-scrub-theater.test.js
    # or if gates-only:
    node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js tests/bridge-train-ux.test.js tests/bridge-list-factory-ux.test.js
    ```
  </action>
  <acceptance_criteria>
    - Either `tests/bridge-scrub-theater.test.js` exists with ≥1 greppable `QA-03 (v2.1)` title and `node --test tests/bridge-scrub-theater.test.js` exits 0
      OR SUMMARY documents gates-only with greppable product-phase titles covering FEED/KILL/THTR reduced-motion + Save primary + train fail-closed
    - `rg -n "QA-03 \\(v2\\.1\\)|FEED-|KILL-|THTR-|prefers-reduced-motion" tests/` returns matches for theater a11y/layout bar (new file and/or product dual-tags)
    - No Playwright, no new test framework, no gold fixture deletion
    - Existing `TEST-01 (v2.0)` and `TEST-02 (v2.0)` still greppable in independence + gold files
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>Theater surface contracts locked (new static file or gates-only product dual-tags); independence + gold still green; no invented DOM IDs</done>
</task>

<task type="auto">
  <name>Task 2: TEST-PLAN §O v2.1 map + QA-03 checklist template</name>
  <files>docs/bridge/TEST-PLAN.md, .planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md</files>
  <read_first>
    - docs/bridge/TEST-PLAN.md section N (v2.0 permanent bar) and Execution order — insert §O before Execution order
    - .planning/phases/68-regression-qa-lock/68-RESEARCH.md § QA-03 checklist template
    - .planning/REQUIREMENTS.md QA-01..03
    - Task 1 outcome (theater file path or gates-only title list)
  </read_first>
  <action>
    **A — Append TEST-PLAN §O:**

    In `docs/bridge/TEST-PLAN.md`, insert **before** `## Execution order (GSD verify loop)` (after section N). Title exactly:

    `## O. v2.1 permanent regression bar (QA-01..03)`

    Intro blurb (1–3 lines): Milestone lock for Filter Scrub Theater. Packaging + gates — product theater is phases 61–67. Note v1.7/v1.8/v2.0 TEST titles mean different contracts — do not rename them. New packaging titles use `QA-0N (v2.1)`.

    Table rows (IDs + Auto must name real files/commands):

    | ID | Case | Expected | Auto |
    |----|------|----------|------|
    | QA-01 (v2.1) | v1.6–v2.0 independence / gold / brain / processUpload locks | Full suite green | ✓ `npm test` (+ independence, gold, engine COL/GATE/water, brain, train, list, LRN, EFF) |
    | QA-01 (v2.1) | Independence no-push + already_imported default-off | TEST-01 (v2.0) still greppable | ✓ `tests/bridge-independence.test.js` |
    | QA-01 (v2.1) | Gold ACC keep/deny/water/type/silent-drop | TEST-02 (v2.0) fixtures intact | ✓ `tests/bridge-accuracy-gold.test.js` + `tests/fixtures/bridge/gold/*` |
    | QA-02 (v2.1) | Live server after milestone | health + homepage + `/bridge` HTTP 200 | ✓ `scripts/verify-live.ps1` + explicit `/bridge` check |
    | QA-03 (v2.1) | Theater FEED/KILL/THTR reduced-motion + surface contracts | Static contracts green (or product dual-tags) | ✓ `tests/bridge-scrub-theater.test.js` **or** list actual product test files from inventory |
    | QA-03 (v2.1) | Mobile 390 + desktop 1440 layout; CTAs ≥ 44px | No horizontal overflow; tap targets; motion reduce paths verified | ✓ phase `68-QA-CHECKLIST.md` (+ CSS asserts when greppable) |

    Command block under the table:
    ```bash
    node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
    node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js
    node --test tests/bridge-scrub-theater.test.js
    # (omit scrub-theater line if gates-only; substitute product dual-tag files)
    npm test
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    # QA-02 /bridge explicit (Option A)
    # Invoke-WebRequest http://127.0.0.1:3000/bridge → 200
    ```

    Do **not** delete or rewrite section N (v2.0 bar).

    **B — Create `68-QA-CHECKLIST.md`:**

    Path: `.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md`

    Contents (mirror research template; `/bridge` only):

    - Header: Phase 68 QA-03 Checklist; Date / Browser blanks; how to emulate reduced-motion; how to set 390 and 1440
    - **Layout table:** Viewport 390 and 1440 × No horizontal overflow × Primary CTAs ≥ 44×44 × Notes × Pass — surfaces: desk / feed / report / train / shift
    - **Reduced motion table:** FEED / KILL / THTR × static summary or non-essential motion off × comprehension without motion × Pass
    - **Admin gate smoke (optional non-blocking if train-ux automated):** admin + open groups sees train; non-admin no train/brain chrome
    - **Screenshots optional** under `screenshots/` only if useful
    - **Automated gates attached:** npm test; verify-live; `/bridge` 200 checkboxes

    Checklist may be blank (template only) after Plan 01 — Plan 02 fills Pass columns / records evidence.

    **Out of scope:**
    - Full monorepo `npm run verify` as only gate
    - Playwright / visual regression CI
    - Product desk features
    - Extending verify-live.ps1 (optional Option B — only if executor chooses; default document Option A)
  </action>
  <acceptance_criteria>
    - `rg -n "QA-0[123] \\(v2\\.1\\)" docs/bridge/TEST-PLAN.md` returns ≥3 matches (all three IDs)
    - `rg -n "verify-live|/bridge|68-QA-CHECKLIST|bridge-scrub-theater|bridge-independence|bridge-accuracy-gold" docs/bridge/TEST-PLAN.md` returns matches in §O
    - Section N `TEST-01 (v2.0)` still present in TEST-PLAN
    - File `.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md` exists with 390, 1440, reduced-motion, and FEED/KILL/THTR content
    - Quick permanent bar still green: `node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>TEST-PLAN §O maps QA-01..03 (v2.1); checklist template ready for Plan 02 human gate; packaging complete</done>
</task>

</tasks>

<verification>
1. Theater contracts green (new file) OR gates-only greppable dual-tags documented
2. `docs/bridge/TEST-PLAN.md` §O includes QA-01/02/03 (v2.1)
3. `68-QA-CHECKLIST.md` exists with layout + reduced-motion tables
4. Independence + gold quick bar still green
5. v1.7 / v1.8 / v2.0 TEST titles untouched
6. No wipe of filter lists / brain; no product scope creep unless red lock forces minimal fix
7. Full suite + verify-live + /bridge + filled checklist deferred to 68-02
</verification>

<success_criteria>
- QA-01 packaging: v1.6–v2.0 permanent bars mapped; no re-implementation; titles still greppable
- QA-02 packaging: TEST-PLAN documents verify-live + explicit `/bridge` 200 (execution in Plan 02)
- QA-03 packaging: theater static contracts as needed + checklist template for 390/1440/44px/reduced-motion
- Zero product feature scope; packaging only unless red lock forces minimal fix
</success_criteria>

<output>
After completion, create `.planning/phases/68-regression-qa-lock/68-01-SUMMARY.md`
</output>
