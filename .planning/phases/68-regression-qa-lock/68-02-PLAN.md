---
phase: 68-regression-qa-lock
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified: []
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - "Full npm test suite exits 0 with independence + gold + engine + brain/train/list/LRN/EFF + any theater contracts included (QA-01)"
    - "scripts/verify-live.ps1 exits 0 with health + homepage HTTP 200"
    - "/bridge returns HTTP 200 after verify-live (QA-02 explicit gap fill)"
    - "QA-03 checklist completed for 390/1440 layout, 44px CTAs, and reduced-motion FEED/KILL/THTR (or automated CSS/static contracts fully prove greppable halves with checklist notes for overflow)"
    - "No production filter-lists / bridge-brain / Form Forge / analyzer user data wiped"
  artifacts:
    - path: "tests/bridge-independence.test.js"
      provides: "TEST-01 (v2.0) / QA-01 independence permanent bar still present"
      contains: "TEST-01 (v2.0)"
    - path: "tests/bridge-accuracy-gold.test.js"
      provides: "TEST-02 (v2.0) / QA-01 gold permanent bar still present"
      contains: "TEST-02 (v2.0)"
    - path: "docs/bridge/TEST-PLAN.md"
      provides: "§O v2.1 QA permanent bar from Plan 01"
      contains: "QA-01 (v2.1)"
    - path: ".planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md"
      provides: "QA-03 human layout/motion evidence"
      contains: "390"
    - path: "scripts/verify-live.ps1"
      provides: "Live health + homepage gate"
      contains: "health"
    - path: "package.json"
      provides: "CI = npm test node --test tests/**/*.test.js"
      contains: "tests/**/*.test.js"
  key_links:
    - from: "Phase 68 Plan 02 ship gate"
      to: "npm test"
      via: "full node --test suite green (QA-01)"
      pattern: "npm test"
    - from: "Phase 68 Plan 02 ship gate"
      to: "scripts/verify-live.ps1 + /bridge"
      via: "exit 0 health+home; Invoke-WebRequest /bridge 200 (QA-02)"
      pattern: "verify-live|/bridge"
    - from: "Phase 68 Plan 02 ship gate"
      to: "68-QA-CHECKLIST.md + theater static tests"
      via: "layout 390/1440 + reduced-motion FEED/KILL/THTR (QA-03)"
      pattern: "QA-03|390|prefers-reduced-motion"
---

<objective>
Milestone lock-and-ship gate: prove the full automated suite, live server (including `/bridge`), and theater mobile/a11y motion bar stay green after v2.1 permanent bar packaging.

Purpose: QA-01 requires full `npm test` green on v1.6–v2.0 locks; QA-02 requires verify-live exit 0 plus `/bridge` + homepage 200; QA-03 requires 390/1440 layout, ≥44px CTAs, and reduced-motion FEED/KILL/THTR verified (automated where greppable + checklist). Plan 01 packages docs/contracts; this plan is the ship gate.

Output: Green focused permanent bar packs, green full suite, green verify-live, green `/bridge` 200, filled checklist evidence (or documented automated coverage), SUMMARY with exact pass counts. Zero product feature work unless a real regression appears.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-regression-qa-lock/68-RESEARCH.md
@.planning/phases/68-regression-qa-lock/68-VALIDATION.md
@.planning/phases/68-regression-qa-lock/68-01-PLAN.md
@.planning/phases/68-regression-qa-lock/68-01-SUMMARY.md
@.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md
@docs/bridge/TEST-PLAN.md
@AGENTS.md
@scripts/verify-live.ps1
@package.json

<interfaces>
<!-- Gates only. Prefer zero file edits when green. -->

Ship commands (repo root — cwd distress-os):
```powershell
# Permanent bar quick pack (QA-01 diagnostic)
node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js

# Theater pack (if Plan 01 created the file; else product dual-tag files from SUMMARY)
node --test tests/bridge-scrub-theater.test.js

# Full CI (QA-01)
npm test

# Live (QA-02)
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
$bridge = Invoke-WebRequest -Uri "http://127.0.0.1:3000/bridge" -UseBasicParsing -TimeoutSec 8
if ($bridge.StatusCode -ne 200) { throw "/bridge not 200" }
Write-Host "QA-02 ok bridge=$($bridge.StatusCode)"
```

If verify-live fails (server down):
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
# then re-check /bridge
```

Research baseline (2026-07-10, pre-61–67 theater):
- full suite: **577 pass / 0 fail** (will rise after 61–67 + Plan 01 tests — record exact post-phase count; do not force 577)
- verify-live: LIVE ok health=200 home=200
- `/bridge` 200: assert at ship (not in verify-live today)

AGENTS.md hard rules:
- Never wipe data/filter-lists/, data/bridge-brain/, modules/form-forge/data/, analyzer users
- Never start blocking `node server.js` in agent shell — use restart.ps1 / ensure-server.ps1 / verify-live only
- After any public/ or server.js fix: re-run verify-live before claiming live
- Prefer empty `files_modified` in SUMMARY when gates are already green
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focused permanent bar + theater contracts still green</name>
  <files></files>
  <read_first>
    - .planning/phases/68-regression-qa-lock/68-01-SUMMARY.md (pass counts, gates-only vs scrub-theater path)
    - docs/bridge/TEST-PLAN.md section O v2.1 permanent bar
    - tests/bridge-independence.test.js (TEST-01 (v2.0) still present)
    - tests/bridge-accuracy-gold.test.js (TEST-02 (v2.0) still present)
    - tests/bridge-scrub-theater.test.js if present; else product dual-tag files named in SUMMARY
    - package.json test script
  </read_first>
  <action>
    From repo root, run the **permanent bar quick pack**, **engine composition pack**, and **theater pack**. Do not edit product code unless a command fails for a real regression.

    **1 — Independence + gold (QA-01 core):**
    ```
    node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
    ```
    Must exit 0. Confirm greppable `TEST-01 (v2.0)` and `TEST-02 (v2.0)`.

    **2 — processUpload Type / format / water composition (QA-01 supporting):**
    ```
    node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js
    ```
    Must exit 0. Coverage intent: Type COL/v1.8, Format GATE/v1.8 FP, water shut-off, IND-04.

    **3 — Theater / supporting UX (QA-03 automated half + keep train/list):**
    ```
    # If Plan 01 created it:
    node --test tests/bridge-scrub-theater.test.js
    # Always keep these green when present:
    node --test tests/bridge-train-ux.test.js tests/bridge-list-factory-ux.test.js
    ```
    If gates-only: run the product dual-tag files listed in 68-01-SUMMARY instead of scrub-theater.

    Optional strengthen:
    ```
    node --test --test-name-pattern="v2\\.0|v2\\.1|QA-0" tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js tests/bridge-scrub-theater.test.js
    ```

    **If failures:**
    1. Prefer Plan 01 harness issues (wrong invented selector, assert too strict) over product changes
    2. If real product regression from 61–67: minimal fix only — no new desk features
    3. Do not skip/delete gold fixtures, independence bans, v1.7/v1.8/v2.0 titles, or theater contracts to force green
    4. Do not wipe runtime data stores

    Record focused pass counts in SUMMARY.
  </action>
  <acceptance_criteria>
    - Independence + gold command exits 0
    - Engine focused pattern exits 0
    - Theater pack (scrub-theater or product dual-tags) exits 0
    - `rg -n "TEST-01 \\(v2\\.0\\)" tests/bridge-independence.test.js` still matches
    - `rg -n "TEST-02 \\(v2\\.0\\)" tests/bridge-accuracy-gold.test.js` still matches
    - `rg -n "QA-0[123] \\(v2\\.1\\)" docs/bridge/TEST-PLAN.md` still matches
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>Independence + gold + engine composition + theater packs green; 0 fails</done>
</task>

<task type="auto">
  <name>Task 2: Full npm test + verify-live + /bridge 200 + QA-03 checklist evidence</name>
  <files>.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md</files>
  <read_first>
    - AGENTS.md (live server rules + never wipe user data)
    - scripts/verify-live.ps1 (health + homepage only — /bridge is separate)
    - package.json (`"test": "node --test tests/**/*.test.js"`)
    - .planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md (fill Pass columns)
    - .planning/phases/68-regression-qa-lock/68-RESEARCH.md baselines
  </read_first>
  <action>
    **A — Full suite (QA-01 ship gate):**

    From repo root:
    ```
    npm test
    ```
    Must exit 0 with 0 fail. Pass count ≥ research baseline 577 is expected to rise after 61–67 — **record exact** pass/fail in SUMMARY; do not fail the phase solely because count ≠ 577 if all green.

    If failures:
    1. Fix harness first
    2. Then minimal product fix only if a real regression
    3. Never delete tests or fixtures to greenwash

    **B — Live gate (QA-02):**

    ```powershell
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    if ($LASTEXITCODE -ne 0) { throw "verify-live failed" }

    $bridge = Invoke-WebRequest -Uri "http://127.0.0.1:3000/bridge" -UseBasicParsing -TimeoutSec 8
    if ($bridge.StatusCode -ne 200) { throw "/bridge not 200" }
    Write-Host "QA-02 ok health+home via verify-live; bridge=$($bridge.StatusCode)"
    ```

    If verify-live fails:
    1. `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1`
    2. Re-run verify-live then `/bridge` check until both pass
    3. Never leave blocking `node server.js` in the agent shell
    4. Never wipe data/filter-lists/ or data/bridge-brain/

    Optional Option B: if executor chooses to thin-extend `verify-live.ps1` with `/bridge`, document in SUMMARY and still prove exit 0; not required if Option A check above is recorded.

    **C — QA-03 checklist evidence:**

    Fill `.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md` Pass columns for:

    1. **Layout 390 + 1440** on `/bridge` — no page-level horizontal overflow on desk/feed/report/train/shift surfaces. Practical tip: DevTools device toolbar; optional `document.documentElement.scrollWidth <= window.innerWidth` console check.
    2. **Primary CTAs ≥ 44×44** — Process, Save/Stage, Train Approve/Deny (admin path). If CSS `min-height: 44px` already grepped by automated tests, note that and still mark Pass for tap targets.
    3. **Reduced-motion FEED / KILL / THTR** — DevTools Rendering → emulate `prefers-reduced-motion: reduce`; confirm static summary / non-essential motion off; comprehension without motion.

    Admin gate smoke optional if train-ux automated already covers THTR-03.

    Screenshots optional under `.planning/phases/68-regression-qa-lock/screenshots/`.

    If environment cannot open a real browser, document blockers honestly and maximize automated greps already green — but phase complete **requires** checklist Pass for overflow when CSS cannot prove layout (CONTEXT). Prefer completing checklist in-session when browser is available.

    **D — SUMMARY must state:**
    - Exact npm test pass/fail counts
    - verify-live result (health=200 home=200)
    - `/bridge` StatusCode 200
    - Theater pack path used (scrub-theater vs product dual-tags)
    - Checklist completed (yes/no + any residual notes)
    - Confirmation QA-01/02/03 (v2.1) permanent bar is green for milestone ship

    Prefer **zero product file edits** when gates are green.
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 with 0 fail
    - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exits 0
    - Invoke-WebRequest `http://127.0.0.1:3000/bridge` returns StatusCode 200
    - `68-QA-CHECKLIST.md` has Pass evidence for 390/1440 + reduced-motion FEED/KILL/THTR (or explicit documented exception with automated substitutes only for greppable halves)
    - No deletions under `data/filter-lists/`, `data/bridge-brain/`, or gold fixtures
    - `.planning/phases/68-regression-qa-lock/68-02-SUMMARY.md` exists after completion with counts
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test green; verify-live exit 0; /bridge 200; QA-03 checklist evidence recorded; v2.1 milestone regression bar ship-ready</done>
</task>

</tasks>

<verification>
1. Independence + gold quick bar — exit 0
2. Engine focused Type/format/water + IND-04 pattern — exit 0
3. Theater pack (scrub-theater or dual-tags) — exit 0
4. `npm test` — exit 0, 0 fail
5. `scripts/verify-live.ps1` — exit 0, health + homepage 200
6. Explicit `/bridge` HTTP 200
7. QA-03 checklist filled (layout + reduced-motion)
8. No wipe of operator runtime data
9. Phase 68 success criteria 1–3 (QA-01..03) satisfied
</verification>

<success_criteria>
- QA-01: All Filter independence / accuracy / brain / processUpload locks from v1.6–v2.0 stay green under `npm test`
- QA-02: verify-live exit 0; `/bridge` + homepage HTTP 200
- QA-03: Mobile 390 + desktop 1440 no overflow; primary CTAs ≥ 44px; reduced-motion paths verified for FEED/KILL/THTR
- Milestone v2.1 regression QA lock ready for `/gsd:verify-work 68`
</success_criteria>

<output>
After completion, create `.planning/phases/68-regression-qa-lock/68-02-SUMMARY.md`
</output>
