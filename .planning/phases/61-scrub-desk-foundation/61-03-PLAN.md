---
phase: 61-scrub-desk-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 61-02
files_modified:
  - public/js/bridge-train.js
  - public/bridge.html
autonomous: true
gap_closure: true
requirements:
  - DESK-06
must_haves:
  truths:
    - "JS-generated train approve/deny buttons emit phuglee-btn* classes (not bridge-btn*)"
    - "Buttons use unified phuglee-btn vocabulary throughout (DESK-06 / ROADMAP SC6)"
    - "Live BridgeTrain.renderTrainGroupCard path matches bridge.js fallback vocabulary"
    - "Semantic hooks bridge-train-approve / bridge-train-deny and data-action approve|deny preserved"
  artifacts:
    - path: "public/js/bridge-train.js"
      provides: "Live train card renderer with phuglee-btn approve/deny"
      contains: "phuglee-btn-primary bridge-train-approve"
    - path: "public/bridge.html"
      provides: "Cache-busted bridge-train.js script tag after class migration"
      contains: "bridge-train.js?v="
  key_links:
    - from: "public/js/bridge.js renderTrainGroupCard"
      to: "window.BridgeTrain.renderTrainGroupCard"
      via: "prefer BridgeTrain when present (production path)"
      pattern: "BridgeTrain\\.renderTrainGroupCard"
    - from: "public/js/bridge-train.js approve/deny buttons"
      to: "phuglee-btn phuglee-btn-primary|secondary"
      via: "class strings on train action buttons"
      pattern: "phuglee-btn phuglee-btn-primary bridge-train-approve|phuglee-btn-secondary bridge-train-deny"
---

<objective>
Close DESK-06 verification gap: migrate the **live** train approve/deny CTA classes in `public/js/bridge-train.js` from dead `bridge-btn*` to `phuglee-btn*`.

Purpose: Plan 02 fixed `bridge.js` fallback templates and deleted `.bridge-btn*` CSS, but production prefers `window.BridgeTrain.renderTrainGroupCard` from `bridge-train.js`, which still emits unstyled `bridge-btn` markup.

Output: Live train cards use `phuglee-btn` primary/secondary; zero Filter CTA sources still emit `bridge-btn*`; live verify green.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-scrub-desk-foundation/61-VERIFICATION.md
@.planning/phases/61-scrub-desk-foundation/61-02-SUMMARY.md
@.planning/REQUIREMENTS.md
@public/js/bridge-train.js
@public/js/bridge.js
@public/bridge.html
@AGENTS.md

<interfaces>
<!-- Gap root cause + exact target classes. Do not re-touch desk shell or ops slang. -->

### Why this plan exists

| Path | File | Status after 61-02 |
|------|------|--------------------|
| Production train cards | `public/js/bridge-train.js` `renderTrainGroupCard` ~L123–131 | **STALE** — still `bridge-btn bridge-btn-primary\|ghost` |
| Fallback only | `public/js/bridge.js` ~L257–258 | Already `phuglee-btn*` |
| CSS | `public/css/bridge.css` | `.bridge-btn*` rules **deleted** — remaining markup unstyled |

`bridge.js` does:
```js
if (window.BridgeTrain && typeof window.BridgeTrain.renderTrainGroupCard === 'function') {
  return window.BridgeTrain.renderTrainGroupCard(group);
}
```
`bridge.html` loads `bridge-train.js` **before** `bridge.js`, so production never hits the fallback.

### Target button classes (match bridge.js fallback)

| Control | Classes (keep hooks) |
|---------|----------------------|
| Approve | `phuglee-btn phuglee-btn-primary bridge-train-approve` |
| Deny | `phuglee-btn phuglee-btn-secondary bridge-train-deny` |

Preserve: `type="button"`, `data-action="approve|deny"`, titles, aria-labels, `trainActionButtonLabels` copy, group structure.

### Out of scope

- Desk shell / atmosphere / proof rail (already verified)
- Ops slang H2s (already verified)
- Engine / brain / API
- Re-adding `.bridge-btn*` CSS aliases
- Phases 62–68 work
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate bridge-train.js approve/deny to phuglee-btn + cache-bust</name>
  <files>public/js/bridge-train.js, public/bridge.html</files>
  <behavior>
    - renderTrainGroupCard approve button includes `phuglee-btn phuglee-btn-primary bridge-train-approve` (not bridge-btn)
    - renderTrainGroupCard deny button includes `phuglee-btn phuglee-btn-secondary bridge-train-deny` (not bridge-btn-ghost)
    - data-action approve|deny and outcome labels unchanged
    - Grep: zero `bridge-btn` in public/js Filter CTA sources (bridge-train.js + bridge.js)
  </behavior>
  <read_first>
    - .planning/phases/61-scrub-desk-foundation/61-VERIFICATION.md (gap: bridge-train.js L123–131)
    - public/js/bridge-train.js (renderTrainGroupCard ~L109–136)
    - public/js/bridge.js (fallback ~L251–259 — mirror these classes)
    - public/bridge.html (script tag `bridge-train.js?v=` ~L426)
  </read_first>
  <action>
    1. In **`public/js/bridge-train.js`** `renderTrainGroupCard` action buttons (~L123–131):
       - Replace `class="bridge-btn bridge-btn-primary bridge-train-approve"` → `class="phuglee-btn phuglee-btn-primary bridge-train-approve"`
       - Replace `class="bridge-btn bridge-btn-ghost bridge-train-deny"` → `class="phuglee-btn phuglee-btn-secondary bridge-train-deny"`
       - Do **not** change labels, titles, aria-labels, `data-action`, group markup, or any pure helpers.

    2. Grep **`public/js`** for remaining `bridge-btn` on Filter CTA sources. Expect **zero** matches in `bridge-train.js` and `bridge.js`. If other intentional non-Filter modules still use `bridge-btn`, leave them; do not expand scope. Filter path must be clean.

    3. Bump cache query on `public/bridge.html` script tag for train module (e.g. `bridge-train.js?v=5` → `v=6`) so hard-refresh is not required for the class fix. Optional: bump `bridge.js?v=` only if you also touch it (prefer not to).

    4. Do **not** wipe runtime data; do **not** reintroduce `.bridge-btn*` CSS.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('public/js/bridge-train.js','utf8');const j=fs.readFileSync('public/js/bridge.js','utf8');const h=fs.readFileSync('public/bridge.html','utf8');const fail=(m)=>{console.error(m);process.exit(1)};if(/bridge-btn/.test(t))fail('bridge-btn still in bridge-train.js');if(/class=\"[^\"]*bridge-btn/.test(j)||/bridge-btn bridge-btn/.test(j))fail('bridge-btn still in bridge.js CTA templates');if(!/phuglee-btn phuglee-btn-primary bridge-train-approve/.test(t))fail('train approve not phuglee primary');if(!/phuglee-btn phuglee-btn-secondary bridge-train-deny/.test(t))fail('train deny not phuglee secondary');if(!/bridge-train-approve/.test(t)||!/bridge-train-deny/.test(t))fail('semantic hooks missing');if(!/bridge-train\.js\?v=\d+/.test(h))fail('bridge-train.js cache query missing');console.log('61-03-train-phuglee-ok');"</automated>
  </verify>
  <done>Live BridgeTrain train approve/deny emit phuglee-btn only; hooks preserved; script cache bumped; no bridge-btn left in Filter train CTA sources.</done>
</task>

<task type="auto">
  <name>Task 2: Gate — train UX tests + verify-live</name>
  <files>public/js/bridge-train.js, public/bridge.html</files>
  <read_first>
    - AGENTS.md (verify-live mandatory after public/ edits)
    - tests/bridge-train-ux.test.js (renderTrainGroupCard contracts)
  </read_first>
  <action>
    1. Run focused train suite (or full suite if preferred):  
       `node --test tests/bridge-train-ux.test.js`  
       Optionally also `npm test` if time allows — must not break existing train contracts (data-action, labels, XSS escape).

    2. Live gate after public/ edits:  
       `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`  
       Must exit 0. If down, restart via `scripts\restart.ps1` / ensure-server — never blocking `node server.js` in agent shell.

    3. Note in SUMMARY: DESK-06 gap closed; admin train approve/deny now styled via phuglee; re-verification can re-check truth #6 / #10 only.
  </action>
  <verify>
    <automated>node --test tests/bridge-train-ux.test.js</automated>
  </verify>
  <done>Train UX tests pass; verify-live.ps1 exit 0; DESK-06 live train path closed.</done>
</task>

</tasks>

<verification>
- Static gate `61-03-train-phuglee-ok` exits 0
- `node --test tests/bridge-train-ux.test.js` green
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exit 0
- Grep: no `bridge-btn` in `public/js/bridge-train.js` or Filter CTA templates in `bridge.js`
</verification>

<success_criteria>
- DESK-06 complete: live train approve/deny use `phuglee-btn` vocabulary
- ROADMAP phase 61 success criterion 6 TRUE (no dual button system on Filter CTAs)
- Semantic train hooks and data-action contracts unchanged
- No runtime data wiped; suite/live gates green
</success_criteria>

<output>
After completion, create `.planning/phases/61-scrub-desk-foundation/61-03-SUMMARY.md`
</output>
