---
phase: 61-scrub-desk-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - public/bridge.html
  - public/js/bridge.js
  - public/css/bridge.css
autonomous: true
requirements:
  - DESK-06
  - DESK-04
must_haves:
  truths:
    - "Primary Filter CTAs use phuglee-btn vocabulary only — no dual bridge-btn + phuglee-btn on the same control as the default pattern"
    - "JS-generated train approve/deny, train pager, and brain Activate/Disable buttons emit phuglee-btn* classes (not bridge-btn*)"
    - "First-paint and step chrome use ops slang (not corporate “Select city profile” as the primary city H2 voice)"
    - "Button and control IDs unchanged (#bridge-process, #bridge-save-list, #bridge-outcome-save, train/brain hooks)"
    - "LIST-01 lock: #bridge-save-list label remains Save list (primary); Analyze independence copy stays honest"
    - "npm test green and scripts/verify-live.ps1 exit 0 after public/ edits"
  artifacts:
    - path: "public/bridge.html"
      provides: "Unified phuglee-btn on static CTAs + ops H2/leads/labels"
      contains: "phuglee-btn"
    - path: "public/js/bridge.js"
      provides: "Template strings use phuglee-btn; process CTA ops text Scrub it"
      contains: "phuglee-btn"
    - path: "public/css/bridge.css"
      provides: "Optional thin bridge-btn alias or dead-rule cleanup so dual systems do not re-grow"
      contains: "phuglee-btn|bridge-btn"
  key_links:
    - from: "public/bridge.html #bridge-process"
      to: "phuglee-btn-primary + Scrub it"
      via: "single vocabulary classes; no bridge-btn dual"
      pattern: "id=\"bridge-process\"[^>]*phuglee-btn"
    - from: "public/js/bridge.js train templates"
      to: "phuglee-btn phuglee-btn-primary|secondary"
      via: "~L257–258 approve/deny; ~L518–519 pager; ~L678–679 brain status"
      pattern: "phuglee-btn-primary bridge-train-approve|phuglee-btn"
    - from: "public/bridge.html #bridge-step-location"
      to: "ops city voice"
      via: "Pick the city (not Select city profile)"
      pattern: "Pick the city|Name the city"
    - from: "public/bridge.html #bridge-save-list"
      to: "LIST-01 Save list primary"
      via: "id + Save list text preserved"
      pattern: "id=\"bridge-save-list\".*Save list|Save list.*id=\"bridge-save-list\""
---

<objective>
Unify Filter button systems onto **`phuglee-btn`** and rewrite first-paint / step chrome to **ops slang** (DESK-06), finishing slim teaching voice from DESK-04 — then gate the phase with **`npm test`** + **`scripts/verify-live.ps1`**.

Purpose: One CTA vocabulary site-wide on Filter; desk reads like Collect/Command voice, not a corporate form wizard.

Output: Cleaned `public/bridge.html` + light `public/js/bridge.js` class/string updates + optional `bridge.css` btn alias cleanup; suite green; live verify exit 0.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-FILTER-SCRUB-THEATER.md
@.planning/phases/61-scrub-desk-foundation/61-CONTEXT.md
@.planning/phases/61-scrub-desk-foundation/61-RESEARCH.md
@.planning/phases/61-scrub-desk-foundation/61-VALIDATION.md
@.planning/phases/61-scrub-desk-foundation/61-01-PLAN.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@public/css/phuglee-components.css
@AGENTS.md

<interfaces>
<!-- Locked ops voice + button vocabulary. Preserve all IDs. -->

### Button class target (DESK-06)

| Role | Classes |
|------|---------|
| Primary fire | `phuglee-btn phuglee-btn-primary` |
| Secondary / ghost | `phuglee-btn phuglee-btn-secondary` |
| List row micro-actions | Keep `bridge-list-action*` (table chrome) |
| Extra hooks | Keep semantic hooks: `bridge-train-approve`, `bridge-train-deny`, `bridge-list-action--danger`, etc. |

**HTML dual-class cleanup** (strip `bridge-btn bridge-btn-primary|ghost` when `phuglee-btn*` already present or alone):

| Selector | Target classes | Label (ops) |
|----------|----------------|-------------|
| `#bridge-process` | `phuglee-btn phuglee-btn-primary` | **Scrub it** |
| `#bridge-clear-file` | `phuglee-btn phuglee-btn-secondary` | Clear files (OK) |
| `#bridge-outcome-save` | `phuglee-btn phuglee-btn-primary` | **Log city reply** |
| `#bridge-train-undo` | `phuglee-btn phuglee-btn-secondary` | Undo (keep title) |
| `#bridge-export-csv` | `phuglee-btn phuglee-btn-secondary` | **Preview CSV** (LIST-01) |
| `#bridge-save-list` | `phuglee-btn phuglee-btn-primary` | **Save list** (LIST-01 lock) |
| `#bridge-attach` | `phuglee-btn phuglee-btn-secondary` | Attach versioned dataset (OK) |
| `#bridge-download-all-csv` | `phuglee-btn phuglee-btn-primary` | Download all (CSV) |
| `#bridge-download-all-xlsx` | `phuglee-btn phuglee-btn-secondary` | Download all (XLSX) |
| `#bridge-clear-all-lists` | `phuglee-btn phuglee-btn-secondary` + danger hook | Clear all lists |
| `#bridge-history-open` | already phuglee | **Prior attaches** |
| `#bridge-retry`, dialog OK/Cancel | already phuglee | leave |

### JS templates (`public/js/bridge.js`)

| Location (approx) | Today | Target |
|-------------------|-------|--------|
| ~L257–258 train approve/deny | `bridge-btn bridge-btn-primary\|ghost` | `phuglee-btn phuglee-btn-primary` + `bridge-train-approve`; deny → `phuglee-btn phuglee-btn-secondary bridge-train-deny` |
| ~L518–519 train pager Prev/Next | `bridge-btn bridge-btn-ghost` | `phuglee-btn phuglee-btn-secondary` |
| ~L678–679 brain Activate/Disable | `bridge-btn bridge-btn-primary\|ghost` | `phuglee-btn phuglee-btn-primary` / `phuglee-btn phuglee-btn-secondary` |

Process button dynamic text (~L1333, ~L1346, ~L1363):

- Idle / single: **`Scrub it`**
- Multi: **`Scrub ${count} files`**
- Do not leave `Process upload` as the default visible label after migration

### Ops slang map (first paint + step chrome)

| Surface | Locked ops copy |
|---------|-----------------|
| `#bridge-step-location` H2 | **Pick the city** (badge span may remain) |
| City panel lead | **Name the city you're scrubbing. Only Form Forge cities show here.** |
| `#bridge-history-open` | **Prior attaches** |
| `#bridge-outcome-save` | **Log city reply** |
| `#bridge-step-type` H2 | **What did the clerk send?** |
| Type panel lead | **Tag the list type so history and filters stay clean.** |
| `#bridge-step-upload` H2 | **Drop the clerk file** |
| Upload panel lead | Tighten to ops: drop/browse up to 5 from same city; photos OK if clerk printed it — keep file-count honesty |
| `#bridge-process` | **Scrub it** |
| Save panel heading (`#bridge-save-heading`) | **Stage the list** |
| `#bridge-save-list` | **Save list** (do **not** rename — LIST-01) |
| Results H2 | **Scrub results** optional; **Results & save** OK if short |
| Dialog “Attachment history” heading | **Prior attaches** OK |

**Do not** remove Analyze-boundary honesty (“nothing sent to Analyze”, separate import after skip-trace).

### CSS optional cleanup

After markup/JS are clean:

- Prefer delete unused `.bridge-btn*` rules **or** leave thin aliases mapping to `phuglee-btn` look for one release
- Grep `public/bridge.html` + `public/js/bridge.js` for `bridge-btn` — CTA sources should be **zero** (list-action classes excluded)

### Out of scope

- City dossier / demote no-list path (62)
- Idle live metrics (63)
- Process theater feed / kill report / train theater hierarchy (64–66)
- Green SaaS success redesign (SHIFT-03 / 67)
- Engine keep/kill rewrite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ops slang labels + strip dual bridge-btn from static HTML CTAs</name>
  <files>public/bridge.html</files>
  <read_first>
    - .planning/phases/61-scrub-desk-foundation/61-RESEARCH.md (Pattern 6–7 + Pitfall 3)
    - public/bridge.html (city H2 ~97; history ~112; outcome-save ~167; type H2 ~174; upload H2 ~197; process/clear ~223–224; train undo ~257; export/save/download ~317–369; history dialog heading)
    - .planning/phases/56-list-factory-ux/56-01-PLAN.md (LIST-01 Save list / Preview CSV locks)
  </read_first>
  <action>
    Edit **`public/bridge.html`** voice + button classes only:

    1. Apply **ops slang map** from interfaces to H2s, panel leads, `#bridge-history-open`, `#bridge-outcome-save`, `#bridge-process`, `#bridge-save-heading`. Keep Analyze boundary sentences where they already teach independence.

    2. On every dual-class CTA (`bridge-btn` + `phuglee-btn`), strip `bridge-btn*` so only `phuglee-btn phuglee-btn-primary|secondary` (+ semantic hooks) remain. Fix `#bridge-outcome-save` and `#bridge-train-undo` which are bridge-only today.

    3. **LIST-01:** `#bridge-save-list` text stays **`Save list`**; `#bridge-export-csv` stays **`Preview CSV`** (secondary). No “Send/Push to Analyze” language.

    4. Preserve all `id=` attributes. Bump `bridge.js?v=` cache query if present at script tag.

    5. Do not change progressive `hidden` behavior or panel structure from plan 01.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('public/bridge.html','utf8');const fail=(m)=>{console.error(m);process.exit(1)};if(/Select city profile/i.test(h))fail('corporate city H2 remains');if(!/Pick the city/i.test(h))fail('need Pick the city');if(!/What did the clerk send/i.test(h))fail('need type ops H2');if(!/Drop the clerk file/i.test(h))fail('need upload ops H2');if(!/Prior attaches/i.test(h))fail('need Prior attaches');if(!/Log city reply/i.test(h))fail('need Log city reply');if(!/Scrub it/i.test(h))fail('need Scrub it');if(!/id=\"bridge-save-list\"[^>]*>[\s\S]*?Save list|Save list<\/button>/.test(h)){/* soft */}if(!h.includes('id=\"bridge-save-list\"'))fail('save id');if(!h.includes('Save list'))fail('Save list text');const dual=/class=\"[^\"]*bridge-btn[^\"]*phuglee-btn[^\"]*\"/g;if(dual.test(h))fail('dual bridge-btn+phuglee-btn still present');const ctaBridge=(h.match(/class=\"[^\"]*bridge-btn[^\"]*\"/g)||[]).filter(s=>!s.includes('bridge-list-action'));if(ctaBridge.length)fail('bridge-btn CTA remains: '+ctaBridge.slice(0,3).join(' | '));['bridge-process','bridge-save-list','bridge-outcome-save','bridge-clear-file','bridge-export-csv'].forEach(id=>{if(!h.includes('id=\"'+id+'\"'))fail('missing '+id)});console.log('61-02-html-voice-btn-ok');"</automated>
  </verify>
  <done>Static Filter chrome speaks ops slang; HTML CTAs are phuglee-btn only; Save list / Preview CSV locks held.</done>
</task>

<task type="auto">
  <name>Task 2: JS-generated buttons → phuglee-btn + Scrub it process labels</name>
  <files>public/js/bridge.js, public/css/bridge.css</files>
  <read_first>
    - public/js/bridge.js (train approve/deny ~250–260; train pager ~510–525; brain rule buttons ~670–685; processBtn.textContent ~1330–1370)
    - public/css/bridge.css (`.bridge-btn*` ~963–1005; train action hooks ~2195)
    - public/css/phuglee-components.css (`.phuglee-btn` system)
  </read_first>
  <action>
    1. In **`public/js/bridge.js`**, replace all CTA template `bridge-btn*` class strings with `phuglee-btn*`:
       - Train approve: `phuglee-btn phuglee-btn-primary bridge-train-approve`
       - Train deny: `phuglee-btn phuglee-btn-secondary bridge-train-deny`
       - Pager Prev/Next: `phuglee-btn phuglee-btn-secondary`
       - Brain Activate/Disable: primary when active path needs fire; secondary for ghost/disable path (map former ghost → secondary)

    2. Update `processBtn.textContent` assignments from `Process upload` / `Process N files` to **`Scrub it`** / **`Scrub ${count} files`**. Grep for remaining `Process upload` in operator-visible strings and fix.

    3. Do **not** rename IDs, `data-action`, `data-train-page`, `setPipelineStep` step keys, or API calls.

    4. In **`public/css/bridge.css`**: either delete dead `.bridge-btn*` rules if unused, **or** leave thin aliases that visually match phuglee (prefer delete if grep shows no markup left). Keep train layout rules that target `.bridge-train-actions` without requiring old btn classes.

    5. Grep cleanliness gate: `bridge-btn` must not appear in `public/bridge.html` or `public/js/bridge.js` except comments (list-action classes are different: `bridge-list-action`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('public/js/bridge.js','utf8');const fail=(m)=>{console.error(m);process.exit(1)};if(/class=\"[^\"]*bridge-btn/.test(j)||/bridge-btn bridge-btn/.test(j))fail('bridge-btn still in bridge.js templates');if(!/phuglee-btn phuglee-btn-primary bridge-train-approve|phuglee-btn-primary bridge-train-approve/.test(j))fail('train approve not phuglee');if(!/bridge-train-deny/.test(j)||!/phuglee-btn/.test(j))fail('train deny path');if(!/Scrub it/.test(j))fail('need Scrub it process label');if(/Process upload/.test(j))fail('Process upload still in JS');if(!/Scrub \$\{count\} files|Scrub \${count} files|Scrub.*files/.test(j))fail('need multi-file Scrub N files');console.log('61-02-js-btn-ok');"</automated>
  </verify>
  <done>Train/brain/pager templates and process labels use phuglee-btn + ops Scrub voice; no dual button system in JS sources.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — npm test + verify-live.ps1</name>
  <files>public/bridge.html, public/css/bridge.css, public/js/bridge.js</files>
  <read_first>
    - AGENTS.md (verify-live mandatory after public/ edits)
    - .planning/phases/61-scrub-desk-foundation/61-VALIDATION.md
    - .planning/ROADMAP.md (Phase 61 success criteria)
  </read_first>
  <action>
    1. From project root `C:\Users\brand\Projects\distress-os`, run full suite: **`npm test`**. Fix only regressions caused by this phase (do not weaken v1.6–v2.0 locks). No data wipes.

    2. Run live gate:  
       `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1`  
       Must exit 0 (health + homepage 200). If server down, restart via `scripts\restart.ps1` / ensure-server — never leave blocking `node server.js` in the agent shell.

    3. Static phase checklist (quick):
       - No `bridge-proof-rail` in HTML
       - `premium-bg--strong` + `heat-field` + `heat-atmosphere.css`
       - `.bridge-desk` / primary / side present
       - Grep: no dual `bridge-btn`+`phuglee-btn` on CTAs; JS free of `bridge-btn` CTA classes
       - Hero lead short; H1 “Scrub the Mess”

    4. Manual spot (note in SUMMARY): desktop 1440 first paint vs Collect grit; mobile ~390 stack primary then scrap; city select still advances `#bridge-pipeline`.

    5. Do **not** claim phase complete if suite or verify-live fails.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full suite green; verify-live.ps1 exit 0; DESK-01–06 observable; SUMMARY ready for phase close.</done>
</task>

</tasks>

<verification>
- Task static gates exit 0
- `npm test` green (QA spirit for phase 61; formal lock remains phase 68)
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exit 0
- Manual: `/bridge` first paint side-by-side Collect — same grit world; no equal 3-up; phuglee CTAs; ops slang
</verification>

<success_criteria>
- DESK-06: unified `phuglee-btn` + ops slang throughout Filter chrome and JS templates
- DESK-04 teaching voice complete (no corporate wizard H2s on first steps)
- LIST-01 Save list / Preview CSV preserved
- All stable DOM IDs preserved
- Suite + live verify pass
- Phase 61 ROADMAP success criteria 1–6 TRUE when combined with 61-01 shell
</success_criteria>

<output>
After completion, create `.planning/phases/61-scrub-desk-foundation/61-02-SUMMARY.md`
</output>
