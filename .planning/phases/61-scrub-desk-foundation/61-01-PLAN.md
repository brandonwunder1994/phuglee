---
phase: 61-scrub-desk-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/bridge.html
  - public/css/bridge.css
autonomous: true
requirements:
  - DESK-01
  - DESK-02
  - DESK-03
  - DESK-04
  - DESK-05
must_haves:
  truths:
    - "First paint at /bridge shows dominant work surface + supporting scrap (asymmetric desk), not a centered essay-wizard only stack"
    - "Equal 3-up decorative proof rail is gone — no section.bridge-proof-rail and no replacement equal 3-card feature grid"
    - "Atmosphere is Collect-grade: premium-bg--strong + heat-field with heat-atmosphere.css linked"
    - "Hero is left-aligned solid cream Anton “Scrub the Mess” with a short ops lead — no centered gradient clip H1"
    - "Teaching chrome is slim: no triple stack of proof rail + long essay lead + heavy pipeline; #bridge-pipeline + data-step keys still usable"
    - "All stable DOM IDs for progressive panels, process, save, train, and pipeline survive the desk wrap"
  artifacts:
    - path: "public/bridge.html"
      provides: "Collect-grade atmosphere; no proof rail; bridge-desk primary+scrap; short hero lead; heat markup"
      contains: "bridge-desk"
    - path: "public/css/bridge.css"
      provides: "Asymmetric desk grid; solid cream left hero; dead proof-rail CSS removed; slim pipeline weight"
      contains: "bridge-desk"
  key_links:
    - from: "public/bridge.html .premium-bg"
      to: "Collect-grade wash"
      via: "premium-bg--strong (not --subtle)"
      pattern: "premium-bg--strong"
    - from: "public/bridge.html .heat-field"
      to: "public/css/heat-atmosphere.css"
      via: "link + heat glow/grid/noise markup"
      pattern: "heat-atmosphere\\.css|heat-field"
    - from: "public/bridge.html .bridge-desk"
      to: ".bridge-desk-primary + .bridge-desk-side"
      via: "asymmetric grid shell wrapping pipeline + step panels"
      pattern: "bridge-desk-primary|bridge-desk-side"
    - from: "public/bridge.html #bridge-pipeline"
      to: "setPipelineStep contract"
      via: "preserve id, .bridge-pipeline-step, data-step location|type|upload|results"
      pattern: "id=\"bridge-pipeline\""
    - from: "public/css/bridge.css .bridge-hero h1"
      to: "Command/Collect cream Anton"
      via: "left solid cream; gradient clip unset"
      pattern: "phuglee-cream|background-clip:\\s*unset|background:\\s*none"
---

<objective>
Ship the Filter first-paint **scrub desk shell**: Collect-grade atmosphere, kill the equal proof rail, asymmetric primary+scrap desk, left solid cream Anton hero with short ops lead, and slim teaching chrome — without changing process/engine behavior or stable DOM IDs.

Purpose: DESK-01–05 foundation so `/bridge` opens in the same grit world as Collect/Command, not a centered multi-step form wizard.

Output: Updated `public/bridge.html` + `public/css/bridge.css` (markup shell + layout/atmosphere/hero only). Button vocabulary + ops H2/CTA slang land in plan 02.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-FILTER-SCRUB-THEATER.md
@.planning/v1.4-GRITTY-PREMIUM.md
@.planning/codebase/filter-page-ui-map.md
@.planning/phases/61-scrub-desk-foundation/61-CONTEXT.md
@.planning/phases/61-scrub-desk-foundation/61-RESEARCH.md
@.planning/phases/61-scrub-desk-foundation/61-VALIDATION.md
@public/bridge.html
@public/css/bridge.css
@public/collect.html
@public/css/distress-collect-hub.css
@public/command.html
@public/css/command-center.css
@public/css/heat-atmosphere.css
@public/css/premium-atmosphere.css
@AGENTS.md

<interfaces>
<!-- Contracts executor must implement exactly. Plan 02 owns button classes + most ops H2/CTA strings. -->

### Atmosphere (DESK-03) — match Collect head/body stack

```html
<!-- head: add after heat-base.css -->
<link rel="stylesheet" href="/css/heat-atmosphere.css">

<!-- body: replace premium-bg--subtle -->
<div class="premium-bg premium-bg--strong" aria-hidden="true">
  <div class="premium-bg-photo"></div>
  <div class="premium-bg-grain"></div>
  <div class="premium-bg-wear"></div>
</div>
<div class="heat-field" aria-hidden="true">
  <div class="heat-glow heat-glow-a"></div>
  <div class="heat-glow heat-glow-b"></div>
  <div class="heat-grid"></div>
  <div class="heat-noise"></div>
</div>
<!-- Keep .bridge-bg only if it still reads intentional under strong+heat; soften or drop if double-orange muds -->
```

### Kill proof rail (DESK-02)

- **Delete** entire `<section class="bridge-proof-rail …">…</section>` (today ~L54–85).
- Do **not** replace with any equal 3-up icon/title/metric strip.
- Remove dead `.bridge-proof-*` rules from `bridge.css` (desktop + mobile blocks).

### Hero (DESK-05) + slim lead (DESK-04)

```html
<header class="bridge-hero" data-phuglee-reveal-child>
  <p class="bridge-step-label">Step 02 · Filter</p>
  <h1 class="bridge-title">Scrub the Mess</h1>
  <p class="bridge-lead">Drop the clerk file. Kill the junk. Stage clean distressed leads. Analyze stays separate.</p>
</header>
```

CSS targets (mirror Collect/Command — kill gradient clip completely):

```css
.bridge-hero { text-align: left; margin-bottom: 1.35rem; }
.bridge-hero h1,
.bridge-title {
  font-family: var(--font-display);
  font-size: clamp(2.2rem, 8vw, 3.75rem);
  font-weight: 400;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  line-height: 1.05;
  color: var(--phuglee-cream);
  background: none;
  -webkit-background-clip: unset;
  background-clip: unset;
  -webkit-text-fill-color: unset; /* if any legacy fill */
}
.bridge-lead {
  max-width: 36rem;
  margin: 0; /* not 0 auto */
  color: var(--phuglee-taupe-mid);
}
```

### Desk shell (DESK-01)

Wrap pipeline + progressive step panels (city → type → upload → loading → results) inside desk; keep `#bridge-lists-panel` and error wrap **below** desk (shift inventory is phase 67).

```html
<div class="bridge-desk">
  <div class="bridge-desk-primary">
    <nav class="bridge-pipeline" aria-label="Intake steps" data-phuglee-reveal-child>
      <ol class="bridge-pipeline-list" id="bridge-pipeline">
        <li class="bridge-pipeline-step is-active" data-step="location">…</li>
        <!-- type | upload | results — keys unchanged -->
      </ol>
    </nav>
    <!-- existing bridge-panel sections: city (no id on outer), #bridge-type-panel, #bridge-upload-panel,
         #bridge-loading-panel, #bridge-results-panel — preserve all internal IDs -->
  </div>
  <aside class="bridge-desk-side" aria-label="Staging scrap">
    <!-- Quiet scrap only — no fake metrics (IDLE-01 is phase 63) -->
    <a href="#bridge-lists-panel" class="bridge-desk-scrap phuglee-panel">
      <span class="bridge-desk-scrap-label">Already staged?</span>
      <span class="bridge-desk-scrap-title">Saved lists</span>
      <span class="bridge-desk-scrap-desc">Jump to staging inventory below.</span>
      <span class="bridge-desk-scrap-cta">Open lists →</span>
    </a>
  </aside>
</div>
```

Desk CSS (Collect fractions; discretion OK within ~1.5–1.7 / ~0.85):

```css
.bridge-desk {
  display: grid;
  grid-template-columns: minmax(0, 1.7fr) minmax(200px, 0.85fr);
  gap: 1rem;
  align-items: start;
  margin-bottom: 1.5rem;
}
@media (max-width: 720px) {
  .bridge-desk { grid-template-columns: 1fr; }
}
/* Prefer ~1040 if desk+scrap cramped; 920 OK if asymmetry clear */
.bridge-main { max-width: 1040px; }
```

DOM order: primary then scrap (mobile stacks work first). Do **not** import `.collect-desk*` class names — keep `bridge-*` namespace.

### Pipeline contract (DESK-04 — keep usable)

- Preserve `#bridge-pipeline`, `.bridge-pipeline-step`, `data-step="location|type|upload|results"`.
- Slim visual weight only (smaller chips / denser list) — **no** sticky rewrite required; **do not** rename step keys.
- `public/js/bridge.js` `setPipelineStep` must keep working without JS edits in this plan.

### Stable IDs (non-exhaustive — D5)

Must survive wrap unchanged:  
`bridge-pipeline`, `bridge-state`, `bridge-city`, `bridge-history-open`, `bridge-city-outcome`, `bridge-outcome-save`, `bridge-type-panel`, `bridge-upload-panel`, `bridge-process`, `bridge-clear-file`, `bridge-dropzone`, `bridge-file-input`, `bridge-response-date`, `bridge-loading-panel`, `bridge-results-panel`, `bridge-kpi-grid`, `bridge-train-wrap`, `bridge-save-list`, `bridge-lists-panel`, `bridge-error-wrap`, dialogs, train/brain nodes.

### Out of scope (this plan)

- Button class unify / ops H2·CTA slang → plan 02 (DESK-06)
- City dossier, idle metrics, live feed, kill report, train theater, shift HUD → phases 62–67
- Engine / API / `lib/bridge-engine/*` / wipe `data/filter-lists` or `data/bridge-brain`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atmosphere + kill proof rail + desk wrap + short hero in bridge.html</name>
  <files>public/bridge.html</files>
  <read_first>
    - .planning/phases/61-scrub-desk-foundation/61-CONTEXT.md
    - .planning/phases/61-scrub-desk-foundation/61-RESEARCH.md (Patterns 1–5 + Code Examples)
    - public/bridge.html (head CSS links ~19–31; body premium-bg ~37–44; hero ~47–52; proof rail ~54–85; pipeline ~87–94; panels through results; lists panel)
    - public/collect.html (premium-bg--strong + heat-field + collect-desk scrap ~63–86)
  </read_first>
  <action>
    Edit **`public/bridge.html` shell only** (no JS, no engine):

    1. **Head:** After `heat-base.css`, add  
       `<link rel="stylesheet" href="/css/heat-atmosphere.css">`.  
       Bump `bridge.css?v=` query (e.g. 14 → 15) so clients pick up layout.

    2. **Atmosphere:** Change `premium-bg--subtle` → `premium-bg--strong`. Insert Collect-style `.heat-field` block (glow-a/b, grid, noise) after premium-bg (or after `.bridge-bg`). Soften/remove `.bridge-bg` only if it fights heat.

    3. **Hero:** Keep title text **Scrub the Mess**. Prefer add class `bridge-title` on `h1`. Replace `.bridge-lead` with locked short ops lead:  
       `Drop the clerk file. Kill the junk. Stage clean distressed leads. Analyze stays separate.`  
       Keep step label `Step 02 · Filter`.

    4. **DESK-02:** Delete entire `section.bridge-proof-rail` and all three `bridge-proof-item` articles. No substitute equal grid.

    5. **DESK-01 desk shell:** Immediately after hero, open:
       - `.bridge-desk` → `.bridge-desk-primary` (contains existing `nav.bridge-pipeline` + city panel + `#bridge-type-panel` + `#bridge-upload-panel` + `#bridge-loading-panel` + `#bridge-results-panel`)
       - `.bridge-desk-side` with quiet scrap link to `#bridge-lists-panel` (copy per interfaces — “Already staged?” / Saved lists). No invented counts.
       Close desk **before** `#bridge-lists-panel` and error/dialog markup.

    6. **Preserve every stable ID** listed in interfaces. Do not rename `data-step` values. Do not flatten progressive `hidden` panels into one mega-form.

    7. **Do not** rewrite button classes or panel H2/CTA ops slang in this task (plan 02) — leave existing dual-class buttons functional.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('public/bridge.html','utf8');const fail=(m)=>{console.error(m);process.exit(1)};if(!h.includes('premium-bg--strong'))fail('need premium-bg--strong');if(h.includes('premium-bg--subtle'))fail('subtle still present');if(!h.includes('heat-atmosphere.css'))fail('need heat-atmosphere.css');if(!h.includes('heat-field'))fail('need heat-field');if(/bridge-proof-rail/.test(h))fail('proof rail still in HTML');if(!h.includes('bridge-desk'))fail('need bridge-desk');if(!h.includes('bridge-desk-primary')||!h.includes('bridge-desk-side'))fail('need primary+side');if(!h.includes('id=\"bridge-pipeline\"'))fail('pipeline id');['location','type','upload','results'].forEach(s=>{if(!h.includes('data-step=\"'+s+'\"'))fail('missing data-step '+s)});['bridge-process','bridge-save-list','bridge-state','bridge-city','bridge-type-panel','bridge-upload-panel','bridge-results-panel','bridge-lists-panel'].forEach(id=>{if(!h.includes('id=\"'+id+'\"'))fail('missing #'+id)});if(!/Scrub the Mess/.test(h))fail('H1 text');if(!/Drop the clerk file/.test(h))fail('short lead');console.log('61-01-html-static-ok');"</automated>
  </verify>
  <done>HTML first paint shell: strong+heat, no proof rail, desk primary+scrap, short left-ready hero lead, pipeline + all stable IDs intact.</done>
</task>

<task type="auto">
  <name>Task 2: Desk grid + solid cream left hero + dead proof CSS + slim pipeline in bridge.css</name>
  <files>public/css/bridge.css</files>
  <read_first>
    - public/css/bridge.css (`.bridge-main` ~19–26; `.bridge-hero` / h1 / lead ~32–73; `.bridge-proof-*` ~75–210 + mobile ~766+; `.bridge-pipeline*` ~229–360; mobile pipeline)
    - public/css/distress-collect-hub.css (`.collect-desk` grid recipe)
    - public/css/command-center.css (`.command-title` / mission asymmetry)
    - public/bridge.html (after Task 1 — desk class names)
  </read_first>
  <action>
    Edit **`public/css/bridge.css`** layout/chrome only:

    1. **Hero (DESK-05):** Set `.bridge-hero { text-align: left; }`. On `.bridge-hero h1` / `.bridge-title`: solid `color: var(--phuglee-cream)`; **explicitly** clear gradient (`background: none`), `background-clip` / `-webkit-background-clip: unset`, and any `-webkit-text-fill-color` that leaves text transparent. Scale toward Collect (`clamp(2.2rem, 8vw, 3.75rem)` discretionary). `.bridge-lead { margin: 0; max-width: ~36rem; }` — not centered.

    2. **Desk (DESK-01):** Add `.bridge-desk` 2-col grid `minmax(0, 1.7fr) minmax(200px, 0.85fr)`, gap ~1rem, `align-items: start`. Primary elevated/work weight; side scrap quieter (smaller type, softer panel). `@media (max-width: 720px) { .bridge-desk { grid-template-columns: 1fr; } }`. Style `.bridge-desk-scrap` as a quiet elevated card (not a second equal hero — avoid `1fr 1fr`).

    3. **Main width:** Raise `.bridge-main` `max-width` to **1040px** (or keep 920 if desk still clearly asymmetric — metaphor over pixels). Keep horizontal padding.

    4. **DESK-02 CSS cleanup:** Delete (or fully orphan-remove) `.bridge-proof-rail`, `.bridge-proof-item*`, `.bridge-proof-icon*`, `.bridge-proof-metric*`, `.bridge-proof-num*`, and mobile proof overrides. Grep should find **zero** `.bridge-proof` rules after cleanup.

    5. **Pipeline slim (DESK-04):** Keep structure; reduce visual tutorial weight (tighter padding/font on `.bridge-pipeline-step`, slightly smaller margin). Do not rename classes. Chips stay horizontal (no sticky rewrite).

    6. Do **not** implement DESK-06 button migration here (optional thin note only). Do not redesign KPI grid / train tabs / green success flash.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('public/css/bridge.css','utf8');const fail=(m)=>{console.error(m);process.exit(1)};if(!/\.bridge-desk\s*\{/.test(c))fail('missing .bridge-desk');if(!/bridge-desk-primary|bridge-desk-side/.test(c))fail('missing desk regions');if(!/1\.7fr|1\.55fr|1\.6fr/.test(c))fail('need asymmetric fr columns');if(/bridge-proof-rail/.test(c))fail('proof-rail CSS still present');const hero=c.match(/\.bridge-hero\s*h1[^{]*\{[^}]+\}|\.bridge-title\s*\{[^}]+\}/g)||[];const blob=hero.join('\n')+c;if(!/phuglee-cream/.test(blob))fail('cream hero color missing');if(/background-clip:\s*text/.test(c)&&/color:\s*transparent/.test(c)){/* allow if not on hero — soft check */}const h1block=(c.match(/\.bridge-hero\s*h1\s*\{[\s\S]*?\n\}/)||[])[0]||'';if(h1block.includes('background-clip: text')||h1block.includes('color: transparent'))fail('hero still gradient-clip');if(!/text-align:\s*left/.test(c))fail('need text-align left for hero path');console.log('61-01-css-static-ok');"</automated>
  </verify>
  <done>CSS ships asymmetric desk, solid cream left hero, no proof-rail rules, slim pipeline; first paint structure ready for DESK-06 voice pass.</done>
</task>

</tasks>

<verification>
- Static HTML/CSS gates above exit 0
- Manual first paint (executor): open `http://127.0.0.1:3000/bridge` idle — 2-col desk + scrap on desktop; no 3-up rail; strong+heat vs Collect side-by-side; H1 left cream Anton; pipeline still highlights after city select
- Full `npm test` + `verify-live.ps1` owned by plan 02 after button/voice (still recommended smoke if server was down)
- No runtime data wiped; no new packages
</verification>

<success_criteria>
- DESK-01: dominant work + scrap at first paint
- DESK-02: proof rail gone; no equal 3-card replacement
- DESK-03: `premium-bg--strong` + heat field language live
- DESK-04: no triple teaching stack; `#bridge-pipeline` usable
- DESK-05: left solid cream Anton “Scrub the Mess” + short ops lead
- Stable IDs preserved for process/save/train/pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/61-scrub-desk-foundation/61-01-SUMMARY.md`
</output>
