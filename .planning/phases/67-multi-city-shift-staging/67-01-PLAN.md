---
phase: 67-multi-city-shift-staging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/bridge.css
  - public/js/bridge.js
  - public/bridge.html
  - tests/bridge-shift-staging.test.js
autonomous: true
requirements:
  - SHIFT-03
must_haves:
  truths:
    - "Post-save #bridge-lists-flash uses brand heat (ember/gold tokens or rgba of #eeb746/#e58435), not green SaaS rgba(120,180,140) / #9fd4a8"
    - "Optional Download this list (CSV) flash CTA remains click-only with data-action=flash-download / #bridge-flash-download-csv"
    - "resetImportAreaAfterSave still full-resets city/type/results/train; never auto-calls downloadSavedList"
    - "Flash teaching still matches LIST/EFF anchors: pick the next city + download from Saved lists for enrichment"
    - ".bridge-list-status--downloaded is not green SaaS; Ready stays heat-forward"
  artifacts:
    - path: "public/css/bridge.css"
      provides: "Ember/gold flash + heat status chips; green islands removed from lists success surface"
      contains: "bridge-lists-flash"
    - path: "public/js/bridge.js"
      provides: "Shift-voice flash copy; Download this list (CSV) still built on save"
      contains: "Download this list"
    - path: "tests/bridge-shift-staging.test.js"
      provides: "SHIFT-03 static locks for heat flash + no green islands + download CTA"
      contains: "SHIFT-03"
  key_links:
    - from: "resetImportAreaAfterSave"
      to: "#bridge-lists-flash + #bridge-flash-download-csv"
      via: "DOM build; click-only download"
      pattern: "bridge-flash-download|flash-download|Download this list"
    - from: "public/css/bridge.css .bridge-lists-flash"
      to: "ember/gold heat tokens"
      via: "background/border/color without rgba(120, 180, 140)"
      pattern: "eeb746|e58435|phuglee-gold|phuglee-orange|--ember|--heat-core"
    - from: "tests/bridge-shift-staging.test.js"
      to: "public/css/bridge.css + public/js/bridge.js"
      via: "static source scans"
      pattern: "SHIFT-03"
---

<objective>
Ship SHIFT-03: post-save success reads brand heat (ember/gold), not green SaaS — while preserving the optional Download this list (CSV) path, full post-save reset contracts, and LIST/EFF flash string anchors.

Purpose: Kill the green “saved!” island on the multi-city factory so success matches v2.1 design bible heat language.

Output: CSS heat restyle (+ downloaded status green purge), light flash copy tighten, new `tests/bridge-shift-staging.test.js` SHIFT-03 locks, cache-bust.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-multi-city-shift-staging/67-CONTEXT.md
@.planning/phases/67-multi-city-shift-staging/67-RESEARCH.md
@.planning/phases/67-multi-city-shift-staging/67-VALIDATION.md
@public/css/bridge.css
@public/js/bridge.js
@public/bridge.html
@public/css/tokens.css
@tests/bridge-list-factory-ux.test.js
@tests/bridge-efficiency-path.test.js
@AGENTS.md

<interfaces>
<!-- Locked DO NOT BREAK (existing static tests) -->
```
resetImportAreaAfterSave:
  - selectedCity = null; selectedUploadType = ''; lastResult = null
  - clearResponseDateTime; train clear; setPipelineStep('location')
  - flash text matches /Filter reset|pick the next city/i
  - no bare downloadSavedList( inside reset body
  - LIST-01: /download from Saved lists for enrichment/
  - EFF-01: /Download this list/ + bridge-flash-download OR data-action="flash-download"
```

<!-- Current green anti-pattern (must leave lists success surface) -->
```css
/* public/css/bridge.css ~1285–1316 and ~1432–1435 */
.bridge-lists-flash {
  background: rgba(120, 180, 140, 0.12);
  border: 1px solid rgba(120, 180, 140, 0.28);
  color: #9fd4a8;
}
.bridge-flash-download { /* same green family */ }
.bridge-list-status--downloaded {
  background: rgba(120, 180, 140, 0.14);
  color: #9fd4a8;
}
```

<!-- Brand heat targets (tokens.css — use, do not invent greens) -->
```
--phuglee-gold / --heat-core: #eeb746
--phuglee-orange / --ember: #e58435
--phuglee-cream, stone/taupe for muted downloaded
```

<!-- Target flash copy (keeps LIST/EFF anchors) -->
```
With name:
  Staged “{name}”. Filter reset — pick the next city, or download from Saved lists for enrichment.
Without name:
  List staged. Filter reset — pick the next city, or download from Saved lists for enrichment.
Button (unchanged semantics):
  Download this list (CSV)
  id=bridge-flash-download-csv
  data-action=flash-download
  dataset.listId + format=csv
```

<!-- Discretion locked by this plan -->
- Purge green on `.bridge-list-status--downloaded` in the same CSS pass (research rec).
- Ready status may stay ember-tinted (already is).
- Do not add toast libraries or new backend routes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SHIFT-03 static test file (heat flash + no green + download CTA)</name>
  <files>tests/bridge-shift-staging.test.js</files>
  <read_first>
    - tests/bridge-efficiency-path.test.js (static scan style)
    - tests/bridge-list-factory-ux.test.js (reset slice pattern)
    - .planning/phases/67-multi-city-shift-staging/67-RESEARCH.md (SHIFT-03 + Validation Architecture)
  </read_first>
  <behavior>
    - SHIFT-03: `.bridge-lists-flash` rule block in bridge.css must NOT contain `rgba(120, 180, 140` or `#9fd4a8`
    - SHIFT-03: flash styles include gold/orange heat (`#eeb746` or `#e58435` or `phuglee-gold`/`phuglee-orange`/`--ember`/`--heat-core`/`var(--ember)` etc.)
    - SHIFT-03: `.bridge-flash-download` is not green SaaS (`rgba(120, 180, 140` absent in that rule region)
    - SHIFT-03: `.bridge-list-status--downloaded` not green SaaS
    - SHIFT-03 / EFF carry: bridge.js still has `Download this list` and `flash-download` or `bridge-flash-download`
    - SHIFT-03: flash teaching still has pick-the-next-city OR Filter reset AND download from Saved lists for enrichment
    - Carry-forward: resetImportAreaAfterSave still clears selectedCity/lastResult and never auto-invokes downloadSavedList(
  </behavior>
  <action>
    Create `tests/bridge-shift-staging.test.js` using `node:test` + `fs` source scans (zero new packages).

    Prefix titles `SHIFT-03:` for this plan’s assertions (Plans 02–03 will append SHIFT-02 / SHIFT-01).

    Implementation sketch:
    1. Read `public/css/bridge.css` and `public/js/bridge.js`.
    2. Extract CSS rule bodies with simple slice between `.bridge-lists-flash {` / `.bridge-flash-download {` / `.bridge-list-status--downloaded {` and next `}` at same depth (or generous char window).
    3. Assert green islands absent in those three regions.
    4. Assert heat markers present in `.bridge-lists-flash` region.
    5. Assert JS flash CTA + teaching anchors + reset isolation (reuse factory-ux patterns; keep tests independent so phase can run alone).

    **This task may be RED until Task 2 lands CSS/JS** — that is expected for TDD. Prefer writing assertions that Task 2 will satisfy exactly.
  </action>
  <acceptance_criteria>
    - File `tests/bridge-shift-staging.test.js` exists
    - Contains at least one test title with `SHIFT-03`
    - Scans both `bridge.css` and `bridge.js`
    - Asserts absence of green flash palette on lists flash
    - Asserts Download this list / flash-download present
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-shift-staging.test.js</automated>
  </verify>
  <done>SHIFT-03 static contracts exist. May be RED until Task 2; after Task 2 must be GREEN with factory + efficiency suites still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ember/gold flash + downloaded status restyle + shift flash copy</name>
  <files>public/css/bridge.css, public/js/bridge.js, public/bridge.html</files>
  <read_first>
    - public/css/bridge.css (~1285–1435 lists flash/status)
    - public/js/bridge.js (resetImportAreaAfterSave ~1797–1923 flash DOM build)
    - public/css/tokens.css (--phuglee-gold, --phuglee-orange, --ember, --heat-core)
    - public/bridge.html (cache busters bridge.css?v= / bridge.js?v=)
    - tests/bridge-list-factory-ux.test.js (LIST flash anchors)
    - tests/bridge-efficiency-path.test.js (Download this list)
  </read_first>
  <behavior>
    - Flash visual heat, not green
    - Downloaded chip not green
    - Flash copy shift voice but keeps LIST/EFF string anchors
    - Full reset + click-only download unchanged
  </behavior>
  <action>
    1. **CSS — `.bridge-lists-flash`:** Replace green rgba/hex with ember/gold heat, e.g.:
       - background: `rgba(229, 132, 53, 0.12)` or gold-tinted `rgba(238, 183, 70, 0.12)`
       - border: `1px solid rgba(238, 183, 70, 0.35)` (or orange edge)
       - color: `var(--phuglee-gold, #eeb746)` or cream-on-heat `#f0d78a` / gold
       Prefer `var(--phuglee-gold)` / `var(--phuglee-orange)` / `var(--ember)` when practical.

    2. **CSS — `.bridge-flash-download` (+ :hover):** Restyle to heat-adjacent cream/gold border, not green. Keep secondary (not primary orange full CTA). Still look clickable.

    3. **CSS — `.bridge-list-status--downloaded`:** Replace green island with muted cream/stone or soft gold-muted chip (e.g. taupe border + cream text, or desaturated gold). **Ready** may remain ember.

    4. **JS — flash teaching strings only** inside `resetImportAreaAfterSave` (keep DOM build, ids, data-action, 10s hide, focus city):
       - Named: `Staged “${savedLabel}”. Filter reset — pick the next city, or download from Saved lists for enrichment.`
       - Unnamed: `List staged. Filter reset — pick the next city, or download from Saved lists for enrichment.`
       - Button text remains `Download this list (CSV)` (or at least contains `Download this list`).
       - Do **not** call `downloadSavedList(` in reset body.
       - Do **not** weaken working-set clear (`lastResult = null`, `selectedCity = null`, etc.).

    5. **Clear-all flash** (~2017–2019): optional light copy only; if it reuses `.bridge-lists-flash`, heat CSS already covers it. Do not introduce green.

    6. **Do not** touch `lib/bridge-list-store.js`, list API routes, or wipe `data/filter-lists/`.

    7. **Cache-bust:** bump `public/bridge.html` `bridge.css?v=` and `bridge.js?v=` by +1 from current (currently css v=14, js v=34 — use current+1 at edit time).

    8. After public edits: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` (restart via `scripts\restart.ps1` if needed).
  </action>
  <acceptance_criteria>
    - `.bridge-lists-flash` block in bridge.css has no `rgba(120, 180, 140` and no `#9fd4a8`
    - `.bridge-lists-flash` includes heat color (`#eeb746` or `#e58435` or `phuglee-gold`/`phuglee-orange`/`--ember`/`--heat-core`)
    - `.bridge-list-status--downloaded` has no `rgba(120, 180, 140` / `#9fd4a8`
    - bridge.js still contains `Download this list` and `data-action` flash-download wiring
    - bridge.js flash path still matches `/pick the next city/i` and `download from Saved lists for enrichment`
    - reset still sets `selectedCity = null` and `lastResult = null`
    - Cache busters bumped in bridge.html
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-shift-staging.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>Heat flash + status green purge shipped; LIST/EFF flash contracts green; shift-staging SHIFT-03 green; live verify after public touch.</done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-shift-staging.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js` exit 0
- `scripts/verify-live.ps1` exit 0 after public/ edits
- No list store/API changes; no data wipe
</verification>

<success_criteria>
- Post-save success is ember/gold heat, not green SaaS
- Download this list remains click-only
- Full post-save reset + LIST/EFF anchors preserved
</success_criteria>

<output>
After completion, create `.planning/phases/67-multi-city-shift-staging/67-01-SUMMARY.md`
</output>
