---
phase: 67-multi-city-shift-staging
plan: 02
type: execute
wave: 2
depends_on:
  - "67-01"
files_modified:
  - public/bridge.html
  - public/js/bridge.js
  - public/css/bridge.css
  - tests/bridge-shift-staging.test.js
autonomous: true
requirements:
  - SHIFT-02
must_haves:
  truths:
    - "Saved lists panel reads as staging inventory: counts + type heat + ready/download language above the table"
    - "HUD metrics derive only from client savedLists (lists API summaries) — no fake decorative numbers"
    - "Rename / download / delete / download-all / clear-all row+toolbar actions and APIs still wired"
    - "Empty state still teaches factory loop; HUD hidden or zeroed when no lists"
    - "No new backend routes or list store schema changes"
  artifacts:
    - path: "public/bridge.html"
      provides: "Staging inventory heading + #bridge-inventory-hud mount"
      contains: "bridge-inventory-hud"
    - path: "public/js/bridge.js"
      provides: "renderInventoryHud / metrics from savedLists inside renderSavedLists"
      contains: "bridge-inventory-hud|renderInventoryHud"
    - path: "public/css/bridge.css"
      provides: "HUD tile/chip layout (counts first, table second)"
      contains: "bridge-inventory"
    - path: "tests/bridge-shift-staging.test.js"
      provides: "SHIFT-02 static HUD + action preservation locks"
      contains: "SHIFT-02"
  key_links:
    - from: "renderSavedLists"
      to: "#bridge-inventory-hud"
      via: "live sums of recordCount / status / uploadType / unique cities"
      pattern: "inventory-hud|renderInventoryHud|Ready"
    - from: "public/bridge.html lists toolbar"
      to: "download-all + clear-all ids"
      via: "unchanged action IDs"
      pattern: "bridge-download-all-csv|bridge-clear-all-lists"
    - from: "table row actions"
      to: "data-action rename|download|delete"
      via: "renderSavedLists row template"
      pattern: "data-action=\"rename\"|data-action=\"download\"|data-action=\"delete\""
---

<objective>
Ship SHIFT-02: elevate Saved lists into a **staging inventory HUD** (counts, type heat, ready/download language) while preserving the full table and all list APIs/actions.

Purpose: Operators see war-room inventory (what is staged for enrich) before scanning spreadsheet rows — without inventing a second store.

Output: HTML mount + heading voice, `renderSavedLists` HUD fill from `savedLists`, CSS tiles/chips, SHIFT-02 tests, cache-bust.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-multi-city-shift-staging/67-CONTEXT.md
@.planning/phases/67-multi-city-shift-staging/67-RESEARCH.md
@.planning/phases/67-multi-city-shift-staging/67-01-SUMMARY.md
@.planning/phases/67-multi-city-shift-staging/67-VALIDATION.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@lib/bridge-list-store.js
@tests/bridge-shift-staging.test.js
@tests/bridge-list-factory-ux.test.js
@AGENTS.md

<interfaces>
<!-- Durable source of truth (KEEP) -->
```
GET /api/bridge/lists → savedLists[] summaries:
  id, name, createdAt, updatedAt, status ('ready'|'downloaded'),
  cityId, city, state, uploadType, sourceFile, recordCount, downloadedAt
```

<!-- Metrics (derive client-side only) -->
```
listsStaged     = savedLists.length
recordsStaged   = sum(recordCount)
readyCount      = filter status === 'ready' .length
downloadedCount = filter status === 'downloaded' .length
cvCount / waterCount via listUploadTypeBadge(uploadType).kind
citiesTouched   = unique cityId OR (city+state)
```

<!-- Target composition (research) -->
```
┌─ Staging inventory ─────────────────────────────────────┐
│  #bridge-inventory-hud                                  │
│  [N lists] [R records] [Ready X] [Downloaded Y]         │
│  Type heat: ⚠️ CV · 💧 Water · Cities: Z                │
├─ table (unchanged columns + actions) ───────────────────┤
│  Type · Name · Uploaded · Records · Status · City · …   │
└─ #bridge-lists-total (keep as a11y live or merge voice) ┘
```

<!-- HTML changes (minimal) -->
```html
<!-- Heading: Staging inventory (ops voice). Keep toolbar IDs. -->
<h2 id="bridge-lists-heading">… Staging inventory</h2>
<!-- Mount between header and empty/table: -->
<div id="bridge-inventory-hud" class="bridge-inventory-hud" hidden
     role="region" aria-label="Staging inventory summary"></div>
```

<!-- Preserve forever this phase -->
- #bridge-download-all-csv / #bridge-download-all-xlsx / #bridge-clear-all-lists
- Row data-action rename | download | delete
- statusLabel Ready / Downloaded
- listUploadTypeBadge (reuse for type heat)
- No DELETE on save; no auto-download; no Analyze push
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SHIFT-02 HUD + action-preservation tests</name>
  <files>tests/bridge-shift-staging.test.js</files>
  <read_first>
    - tests/bridge-shift-staging.test.js (from 67-01)
    - public/bridge.html (#bridge-lists-panel)
    - public/js/bridge.js (renderSavedLists ~1728–1780)
    - .planning/phases/67-multi-city-shift-staging/67-RESEARCH.md (HUD composition)
  </read_first>
  <behavior>
    - SHIFT-02: HTML has `id="bridge-inventory-hud"`
    - SHIFT-02: HTML lists heading or panel copy includes `Staging inventory` (or inventory ops voice)
    - SHIFT-02: bridge.js updates inventory HUD from savedLists (function or id string markers + Ready/Downloaded language)
    - SHIFT-02: row actions still present in renderSavedLists template (rename, download csv/xlsx, delete)
    - SHIFT-02: download-all + clear-all IDs still in HTML
    - SHIFT-02: no new `/api/bridge/shift` route strings in bridge.js
  </behavior>
  <action>
    Append `SHIFT-02:` tests to `tests/bridge-shift-staging.test.js` (do not gut SHIFT-03 tests).

    1. HTML contains `id="bridge-inventory-hud"`.
    2. HTML contains `Staging inventory` (heading preferred).
    3. JS contains inventory render markers: e.g. `bridge-inventory-hud` and at least one of `renderInventoryHud` / `inventory-hud` / metric labels `Ready` used in HUD builder (be specific enough to force implementation).
    4. JS `renderSavedLists` region still builds `data-action="rename"`, `data-action="download"`, `data-action="delete"`.
    5. HTML still has `bridge-download-all-csv`, `bridge-download-all-xlsx`, `bridge-clear-all-lists`.
    6. JS does not introduce `/api/bridge/shift`.
    7. Keep SHIFT-03 green assertions intact.
  </action>
  <acceptance_criteria>
    - tests/bridge-shift-staging.test.js contains `SHIFT-02`
    - Asserts `bridge-inventory-hud` in HTML
    - Asserts list action data-actions and download-all IDs preserved
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-shift-staging.test.js</automated>
  </verify>
  <done>SHIFT-02 contracts present (may RED until Task 2); SHIFT-03 still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inventory HUD mount + render from savedLists + CSS</name>
  <files>public/bridge.html, public/js/bridge.js, public/css/bridge.css</files>
  <read_first>
    - public/bridge.html (#bridge-lists-panel ~360–389)
    - public/js/bridge.js (listUploadTypeBadge, renderSavedLists, loadSavedLists, statusLabel)
    - public/css/bridge.css (lists panel, totals, type chips)
    - .planning/phases/67-multi-city-shift-staging/67-RESEARCH.md (Pattern 3 HUD over table)
  </read_first>
  <behavior>
    - Non-empty savedLists → HUD visible with live counts + type heat + Ready/Downloaded language
    - Empty savedLists → HUD hidden; empty teaching box still shows
    - Table columns/actions unchanged
    - Totals strip may stay or thin-echo HUD (do not break role=status live)
  </behavior>
  <action>
    1. **HTML (`public/bridge.html`):**
       - Change lists H2 visible title from `Saved lists` to **`Staging inventory`** (keep `id="bridge-lists-heading"`). Badge `·` may stay.
       - Light-tighten panel lead to inventory voice if needed, but **keep** factory teaching intent (lists stay until delete; download for enrich; Analyze manual). Do not reintroduce Analyze push.
       - Insert mount **after** `.bridge-lists-header`, **before** `#bridge-lists-empty`:
         ```html
         <div id="bridge-inventory-hud" class="bridge-inventory-hud" hidden
              role="region" aria-label="Staging inventory summary"></div>
         ```
       - Do **not** remove table, empty, toolbar, or total element.

    2. **JS — `renderInventoryHud(lists)` helper** (name flexible if tests allow `bridge-inventory-hud` fill):
       - Compute metrics from argument or `savedLists` only.
       - When `!lists.length`: hide HUD, clear innerHTML, return.
       - When lists present: show HUD; fill with compact tiles/chips, e.g.:
         - `{N} lists` · `{R} records`
         - `{ready} Ready` · `{downloaded} Downloaded` (status language)
         - Type heat: `⚠️ {cv} CV` · `💧 {water} Water` (use `listUploadTypeBadge`)
         - `Cities: {unique}`
       - Prefer textContent / safe DOM or esc() if template strings use list fields (no raw user HTML).
       - Call from `renderSavedLists()` on both empty and non-empty paths so HUD stays in sync after load/save/delete/clear.

    3. **JS — do not** change fetch URLs, POST body shape, download handlers, or delete confirm flows. Extend only presentation.

    4. **CSS:**
       - `.bridge-inventory-hud` as a compact strip: flex wrap, gap from scale {4,8,16}, dark glass/panel consistent with lists.
       - Tiles/chips with display-scale numbers (slightly larger weight), heat-forward Ready, muted Downloaded (align with 67-01 status colors).
       - Type heat chips can reuse/adapt `.bridge-list-type--violation` / `--water` language.
       - Mobile: wrap tiles; no horizontal page overflow.
       - No green SaaS islands.

    5. **`#bridge-lists-total`:** Keep element. Either leave existing “Total: N records across M lists” or shorten to avoid triple-counting noise — if shortened, keep meaningful live text. Prefer keeping for a11y continuity.

    6. **Cache-bust** bridge.html css/js versions +1.

    7. **AGENTS:** never wipe filter-lists; after public edits run verify-live.ps1.
  </action>
  <acceptance_criteria>
    - public/bridge.html has `id="bridge-inventory-hud"` and heading text `Staging inventory`
    - public/js/bridge.js references `bridge-inventory-hud` and builds Ready/Downloaded inventory language from lists
    - public/js/bridge.js `renderSavedLists` still emits rename/download/delete data-actions
    - public/bridge.html still has download-all and clear-all button ids
    - public/css/bridge.css has `.bridge-inventory-hud` (or `#bridge-inventory-hud`) rules
    - No `/api/bridge/shift` in bridge.js
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-shift-staging.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>Staging inventory HUD live from savedLists; table actions preserved; SHIFT-02 tests green; live healthy.</done>
</task>

</tasks>

<verification>
- shift-staging + list-factory-ux + efficiency-path green
- verify-live.ps1 after public/
- Manual smoke spirit: two saved lists → HUD shows 2 lists, summed records, Ready/Downloaded split, type heat
</verification>

<success_criteria>
- Counts first, table second
- Ready/download + type heat language present
- All list APIs/actions still work (no backend rewrite)
</success_criteria>

<output>
After completion, create `.planning/phases/67-multi-city-shift-staging/67-02-SUMMARY.md`
</output>
