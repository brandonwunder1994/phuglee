---
phase: 60-regression-qa-lock
plan: 02
type: execute
wave: 2
depends_on:
  - 60-01
files_modified: []
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Full npm test suite exits 0 with independence + gold + engine contracts included"
    - "Independence bar (TEST-01 no-push + already_imported default-off) still green after packaging"
    - "Gold ACC fixtures (TEST-02) still execute under npm test and stay green"
    - "processUpload Type, format, and water e2e patterns still green (TEST-03 composition half)"
    - "scripts/verify-live.ps1 exits 0 with health + homepage HTTP 200 (TEST-03 live half)"
    - "No production filter-lists / bridge-brain / Form Forge / analyzer user data wiped"
  artifacts:
    - path: "tests/bridge-independence.test.js"
      provides: "TEST-01 (v2.0) permanent bar from Plan 01 still present"
      contains: "TEST-01 (v2.0)"
    - path: "tests/bridge-accuracy-gold.test.js"
      provides: "TEST-02 (v2.0) gold permanent bar from Plan 01 still present"
      contains: "TEST-02 (v2.0)"
    - path: "scripts/verify-live.ps1"
      provides: "Live health + homepage gate"
      contains: "health"
    - path: "package.json"
      provides: "CI = npm test node --test tests/**/*.test.js"
      contains: "tests/**/*.test.js"
  key_links:
    - from: "Phase 60 Plan 02 ship gate"
      to: "npm test"
      via: "full node --test suite green (≥ research baseline 519 pass / 0 fail; may rise if Plan 01 added thin tests)"
      pattern: "npm test"
    - from: "Phase 60 Plan 02 ship gate"
      to: "scripts/verify-live.ps1"
      via: "exit 0 health + homepage 200"
      pattern: "verify-live"
    - from: "Focused engine pattern (Type/format/water)"
      to: "tests/bridge-engine.test.js COL/GATE/water + v1.8 TEST"
      via: "node --test --test-name-pattern"
      pattern: "COL-|GATE-|water|TEST-0"
---

<objective>
Milestone lock-and-ship gate: prove the full automated suite and live server stay green after v2.0 permanent bar packaging, with processUpload Type/format/water composition still covered.

Purpose: TEST-03 and Phase 60 success criteria require `npm test` + `scripts/verify-live.ps1` green after milestone work, and re-assert independence (TEST-01) + gold (TEST-02) remain in the suite. Plan 01 only packages titles/docs; this plan is the ship gate.

Output: Green focused Type/format/water + independence + gold bars, green full suite, green verify-live, SUMMARY with exact pass counts. Zero product feature work unless a real regression appears.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-regression-qa-lock/60-RESEARCH.md
@.planning/phases/60-regression-qa-lock/60-VALIDATION.md
@.planning/phases/60-regression-qa-lock/60-01-PLAN.md
@AGENTS.md
@scripts/verify-live.ps1
@package.json

<interfaces>
<!-- Gates only. Prefer zero file edits when green. -->

Ship commands (repo root):
```powershell
node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js
npm test
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
```

If verify-live fails (server down):
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
```

Research baseline (2026-07-10) to meet or beat:
- independence + gold: 18 pass (may rise after Plan 01 thin tests)
- engine focused pattern: 23 pass
- full suite: **519 pass / 0 fail** (may rise)
- verify-live: LIVE ok health=200 home=200

AGENTS.md hard rules:
- Never wipe data/filter-lists/, data/bridge-brain/, modules/form-forge/data/, analyzer users
- Never start blocking `node server.js` in agent shell — use restart.ps1 / ensure-server.ps1 only
- Phase 60 should not need public/ or server.js edits; if suite fails, fix only real regressions from 55–59 modules or Plan 01 harness bugs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focused permanent bar + processUpload Type/format/water e2e still green</name>
  <files></files>
  <read_first>
    - .planning/phases/60-regression-qa-lock/60-01-SUMMARY.md (if present — pass counts / packaging notes)
    - tests/bridge-independence.test.js (confirm TEST-01 (v2.0) titles exist from Plan 01)
    - tests/bridge-accuracy-gold.test.js (confirm TEST-02 (v2.0) packaging)
    - docs/bridge/TEST-PLAN.md section N v2.0 permanent bar
    - package.json test script
  </read_first>
  <action>
    From repo root, run the **permanent bar quick pack** and the **TEST-03 processUpload composition pack**. Do not edit product code unless a command fails for a real regression.

    **1 — Independence + gold (TEST-01 + TEST-02):**
    ```
    node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
    ```
    Must exit 0. Expect ≥18 pass (research baseline; Plan 01 may add 1–3).

    **2 — processUpload Type / format / water composition (TEST-03 half):**
    ```
    node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js
    ```
    Must exit 0. Research baseline **23 pass**. Coverage intent:
    - Type: COL-* and/or TEST-01 (v1.8) trap/map
    - Format: GATE-* and/or TEST-02 (v1.8) FP change
    - Water: water_* / water shut-off / BRAIN water-related titles matching pattern
    - IND-04: already_imported default-off still in engine (complements independence)

    Optional strengthen if pattern is too broad/narrow on this Node version: also run
    ```
    node --test --test-name-pattern="v2\\.0" tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
    ```
    and confirm ≥1 pass for TEST-01/02 (v2.0) titles.

    **If failures:**
    1. Prefer Plan 01 harness issues (require.cache not restored, city format pollution, stub not reset) over product changes
    2. If real product regression: fix minimal module from Phases 55–59 only — no new features, no scorer retune “while here”
    3. Do not skip/delete gold fixtures, independence bans, v1.7/v1.8 TEST titles, or COL/GATE contracts to force green
    4. Do not wipe runtime data stores

    Record focused pass counts in SUMMARY.
  </action>
  <acceptance_criteria>
    - `node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js` exits 0
    - `node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js` exits 0
    - Output shows 0 fail for both commands
    - `rg -n "TEST-01 \\(v2\\.0\\)" tests/bridge-independence.test.js` still matches
    - `rg -n "TEST-02 \\(v2\\.0\\)" tests/bridge-accuracy-gold.test.js` still matches
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>Independence + gold bars green; engine Type/format/water (+ IND-04) focused pattern green; 0 fails</done>
</task>

<task type="auto">
  <name>Task 2: Full npm test suite + verify-live.ps1 ship gate</name>
  <files></files>
  <read_first>
    - AGENTS.md (live server rules + never wipe user data)
    - scripts/verify-live.ps1 (health + homepage contract)
    - package.json (`"test": "node --test tests/**/*.test.js"`)
    - .planning/phases/60-regression-qa-lock/60-RESEARCH.md baseline counts (519 / live 200)
  </read_first>
  <action>
    **A — Full suite (CI contract for TEST-01 + TEST-02 + all milestone locks):**

    From repo root:
    ```
    npm test
    ```

    Must exit 0. Research baseline **519 pass / 0 fail** — expect equal or higher after Plan 01 thin tests. Independence and gold files must execute as part of the glob (no skip).

    If failures outside Plan 01 packaging:
    1. Fix harness first
    2. Then minimal product fix only if a real 55–59 regression
    3. Never delete tests or fixtures to greenwash

    Record exact `tests passed` / `tests failed` counts in SUMMARY.

    **B — Live gate (TEST-03 live half):**

    ```
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    ```

    Must exit 0 (`/api/health` and `/` HTTP 200). Script auto-ensures server if down.

    If it fails:
    1. Restart headless: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1`
    2. Re-run verify-live until exit 0
    3. Never leave blocking `node server.js` in the agent shell
    4. Never wipe data/filter-lists/ or data/bridge-brain/ as “cleanup”

    Phase 60 Plan 02 expects **zero product file edits** when gates are green. Prefer empty `files_modified` in SUMMARY if nothing changed.

    **C — SUMMARY must state:**
    - Exact npm test pass/fail counts
    - verify-live result (health=200 home=200)
    - Confirmation TEST-01/02/03 (v2.0) permanent bar is green for milestone ship
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 with 0 fail and pass count ≥ 519
    - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exits 0
    - Output / SUMMARY records pass count and LIVE ok health=200 home=200
    - No deletions under `data/filter-lists/`, `data/bridge-brain/`, or gold fixtures
    - `.planning/phases/60-regression-qa-lock/60-02-SUMMARY.md` exists after completion with counts
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test green (≥519 pass / 0 fail); verify-live.ps1 exit 0; v2.0 milestone regression bar ship-ready</done>
</task>

</tasks>

<verification>
1. Independence + gold quick bar — exit 0
2. Engine focused Type/format/water + IND-04 pattern — exit 0
3. `npm test` — exit 0, 0 fail
4. `scripts/verify-live.ps1` — exit 0, health + homepage 200
5. No wipe of operator runtime data
6. Phase 60 success criteria 1–3 satisfied
</verification>

<success_criteria>
- TEST-01: Independence no-push + already_imported default-off still green in full CI
- TEST-02: Gold ACC fixtures still run in npm test and stay green
- TEST-03: processUpload Type/format/water e2e still covered; verify-live green after milestone work
- Milestone v2.0 regression QA lock ready for `/gsd:verify-work 60`
</success_criteria>

<output>
After completion, create `.planning/phases/60-regression-qa-lock/60-02-SUMMARY.md`
</output>
