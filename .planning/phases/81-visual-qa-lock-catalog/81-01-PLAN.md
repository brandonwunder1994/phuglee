---
phase: 81-visual-qa-lock-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phuglee/COMPONENT-CATALOG.md
  - docs/phuglee/FILTER-PARITY-MATRIX.md
  - docs/bridge/TEST-PLAN.md
  - .planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md
autonomous: true
requirements:
  - SYS-01
  - SYS-02
  - QA-01
  - QA-04
must_haves:
  truths:
    - "Maintainer can open a short component catalog listing tokens + class names + do/don't for later Collect/Hub adoption"
    - "Maintainer can open a screenshot parity matrix pairing login/home vs Filter for button, input, panel, modal, chip"
    - "81-QA-CHECKLIST.md exists for 390px + 1440px primary scrub path layout QA (template ready for Plan 02 fill)"
    - "docs/bridge/TEST-PLAN.md maps v3.0 QA-01..04 + SYS-01..02 to files/commands without overwriting v1.7–v2.1 bars"
    - "Behavior freeze is documented: no process/API/brain/keep-kill/list workflow changes; no Analyze re-coupling; no data wipes"
  artifacts:
    - path: "docs/phuglee/COMPONENT-CATALOG.md"
      provides: "SYS-01 short component catalog (tokens + classes + do/don't)"
      contains: "phuglee-btn"
      min_lines: 40
    - path: "docs/phuglee/FILTER-PARITY-MATRIX.md"
      provides: "SYS-02 login/home vs Filter screenshot parity matrix checklist"
      contains: "parity"
      min_lines: 30
    - path: ".planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md"
      provides: "QA-01 390/1440 layout checklist + QA-04 freeze + ship gates"
      contains: "390"
    - path: "docs/bridge/TEST-PLAN.md"
      provides: "v3.0 Visual QA permanent bar map (QA-01..04, SYS-01..02)"
      contains: "QA-01 (v3.0)"
  key_links:
    - from: "docs/phuglee/COMPONENT-CATALOG.md"
      to: "public/css/tokens.css + public/css/phuglee-components.css"
      via: "documents real class/token names from shipped CSS"
      pattern: "phuglee-btn|phuglee-panel|--phuglee-"
    - from: "docs/phuglee/FILTER-PARITY-MATRIX.md"
      to: "login/home (index) vs /bridge dual-class surfaces"
      via: "pair rows for button/input/panel/modal/chip"
      pattern: "login|home|/bridge|chip|modal"
    - from: "TEST-PLAN §P QA-02/03 (v3.0)"
      to: "npm test + scripts/verify-live.ps1"
      via: "documented ship commands (executed in 81-02)"
      pattern: "QA-0[1234] \\(v3\\.0\\)|verify-live|npm test"
    - from: "81-QA-CHECKLIST.md"
      to: "/bridge at 390 and 1440 primary scrub path"
      via: "human layout gate when CSS cannot prove overflow"
      pattern: "390|1440|scrub"
---

<objective>
Package the v3.0 Filter Visual Makeover ship artifacts: short Phuglee component catalog, login/home↔Filter parity matrix, 390/1440 QA checklist template, TEST-PLAN v3.0 bar map, and behavior-freeze documentation.

Purpose: Phases 75–80 paint Filter to home/login grade. Phase 81 freezes the makeover as shippable — maintainers get reusable system docs (SYS-01/02); operators get a layout QA checklist (QA-01 packaging); behavior freeze is explicit (QA-04 packaging). Full suite + verify-live + filled checklist deferred to Plan 02.

Output: `docs/phuglee/COMPONENT-CATALOG.md`, `docs/phuglee/FILTER-PARITY-MATRIX.md`, `81-QA-CHECKLIST.md`, TEST-PLAN §P v3.0 map. Zero product CSS/JS changes unless a catalog claim needs a one-line accuracy fix (prefer docs only).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-visual-qa-lock-catalog/81-CONTEXT.md
@docs/bridge/TEST-PLAN.md
@public/css/tokens.css
@public/css/phuglee-components.css
@public/bridge.html
@public/index.html
@AGENTS.md
@scripts/verify-live.ps1

# Prior QA packaging pattern (mirror structure, do not rewrite v2.1 bars)
@.planning/phases/68-regression-qa-lock/68-01-PLAN.md
@.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md

<interfaces>
<!-- Docs + packaging only. Product makeover is 75–80. Phase 81 locks ship bar. -->

Title naming (MANDATORY for any new TEST-PLAN packaging rows):
- Always `QA-0N (v3.0)` / `SYS-0N (v3.0)` — never overwrite v1.7 / v1.8 / v2.0 / v2.1 titles
- Leave sections N (v2.0) and O (v2.1) **untouched**

Canonical CSS sources for catalog (read real names; never invent):
```text
public/css/tokens.css              → --phuglee-*, glass, type, semantic status
public/css/phuglee-components.css  → .phuglee-btn*, .phuglee-panel*, .phuglee-input,
                                     .phuglee-modal-*, .phuglee-empty-state, .phuglee-loading-*
public/bridge.html                 → dual-class hooks (bridge-* + phuglee-*)
public/index.html / login surfaces → north-star pairs for parity matrix
```

Known shipped classes (verify with grep before writing catalog):
```text
.phuglee-btn / -primary / -secondary / -ghost / -danger
.phuglee-panel / -featured / -float / distress-glass--float
.phuglee-input / .phuglee-textarea / .phuglee-select
.phuglee-modal-backdrop / .phuglee-modal-panel
.phuglee-empty-state / .phuglee-loading-state / .phuglee-loading-bar
.phuglee-eyebrow / .phuglee-hero-title / .phuglee-pattern-bg*
```

Chip language: inventory real type-chip classes from bridge.html / bridge.css / phuglee-components (code/water radio chips). Document what exists — do not invent `.phuglee-chip` if product uses different names.

Behavior freeze (QA-04) — document and later prove in Plan 02:
- No edits to `lib/**`, processUpload, brain APIs, keep/kill engine, list store
- No `public/js/bridge*.js` behavior changes (markup dual-class only if 75–80 already did)
- No Analyze re-coupling chrome (`Send to Analyze`, push adapters, etc.)
- Never wipe `data/filter-lists/`, `data/bridge-brain/`, Form Forge data, analyzer users

AGENTS.md: never wipe runtime data for screenshots. Screenshots optional under phase `screenshots/` only if useful — prefer checklist Pass notes over bulk image storage.

Zero new npm packages. No Storybook. No Playwright required for Plan 01 (template only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component catalog + screenshot parity matrix (SYS-01 / SYS-02)</name>
  <files>docs/phuglee/COMPONENT-CATALOG.md, docs/phuglee/FILTER-PARITY-MATRIX.md</files>
  <read_first>
    - public/css/tokens.css — --phuglee-* color/glass/type/status tokens
    - public/css/phuglee-components.css — real class inventory (buttons, panels, forms, modals, empty/loading)
    - public/bridge.html — dual-class usage (phuglee-btn, phuglee-panel, dialogs)
    - public/index.html (and login modal markup if present) — north-star button/panel/modal/chip pairs
    - .planning/REQUIREMENTS.md SYS-01, SYS-02
    - .planning/phases/81-visual-qa-lock-catalog/81-CONTEXT.md — short markdown, not Storybook
  </read_first>
  <action>
    Create directory `docs/phuglee/` if missing. Write two short maintainer docs — accuracy over length. Prefer ≤150 lines each. Inventory real CSS class/token names with ripgrep before writing; never invent components.

    **A — `docs/phuglee/COMPONENT-CATALOG.md` (SYS-01)**

    Header: Phuglee shared component catalog · v3.0 Filter makeover · for Collect/Hub later adoption.

    Required sections:
    1. **Tokens** — table of key `--phuglee-*` + glass/type/status tokens operators/maintainers must reuse (link source: `public/css/tokens.css`). Include semantic success/warn/danger ban on random hex.
    2. **Classes** — table rows for:
       - Buttons: primary / secondary / ghost / danger (+ disabled/hover/focus note)
       - Forms: input / textarea / select
       - Panels/cards: panel + featured/float variants
       - Modal: backdrop + panel
       - Empty + loading states
       - Chips (type code/water or actual shipped chip class names)
    3. **Do** — use dual-class on Filter (`bridge-*` id + `phuglee-*` class); extend `phuglee-components.css` not a parallel theme; honor `hidden`/`disabled`; freeze `bridge-*` IDs and `data-action` values; cache-bust `?v=` on CSS when shipping visual edits.
    4. **Don't** — invent hex islands in desk chrome; rename `bridge-*` IDs; force Train visible with CSS-only `display:flex !important`; wipe filter-lists/brain for screenshots; re-couple Analyze push chrome; add Storybook/React for this catalog.

    Keep tone operational (solo dev + Claude), not enterprise design-system essay.

    **B — `docs/phuglee/FILTER-PARITY-MATRIX.md` (SYS-02)**

    Header: Screenshot parity matrix checklist · login/home vs Filter (`/bridge`).

    Intro: North star is login modal + home page (glass, grain, Anton/Outfit, raised CTAs). Filter may be denser but same brand DNA.

    Required pair table (checklist columns: Component | Login/home reference | Filter surface | Visual parity notes | Pass):

    | Component | Login/home | Filter |
    |-----------|------------|--------|
    | Button primary | home/login primary CTA | `#bridge-process`, `#bridge-save-list`, victory download, etc. |
    | Button secondary/ghost | home secondary | clear/cancel/secondary desk CTAs |
    | Input | login fields if present / home form energy | city search, save name, Train search |
    | Panel/card | home glass cards | `.bridge-panel.phuglee-panel` desk sections |
    | Modal/dialog | login modal glass/grain | history + type-column confirm `<dialog>`s |
    | Chip | auth-tab energy if present | code/water type chips |

    Pass column blank after Plan 01 (template) — Plan 02 fills Pass when visual QA runs, OR marks Pass with notes if 75–80 already verified and static dual-class greps suffice for evidence.

    Optional: how to capture screenshots without wiping data (hard-refresh only; never delete lists/brain).

    **C — Hard rules:**
    - No Storybook / no new framework
    - No product CSS/JS rewrites in this task (docs only)
    - No data wipes
    - If a class was planned in 77 but missing after inventory, document "gap — see Phase 77/80" rather than inventing it
  </action>
  <acceptance_criteria>
    - `docs/phuglee/COMPONENT-CATALOG.md` exists with tokens + class names + do/don't sections; greppable `phuglee-btn` and `--phuglee-`
    - `docs/phuglee/FILTER-PARITY-MATRIX.md` exists with pair rows for button, input, panel, modal, chip; greppable `parity` or component table
    - Neither file is Storybook; both are short markdown
    - No edits under `data/filter-lists/` or `data/bridge-brain/`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('docs/phuglee/COMPONENT-CATALOG.md','utf8'); const p=fs.readFileSync('docs/phuglee/FILTER-PARITY-MATRIX.md','utf8'); if(!/phuglee-btn/.test(c)) throw new Error('catalog missing phuglee-btn'); if(!/--phuglee-/.test(c)) throw new Error('catalog missing tokens'); if(!/button|input|panel|modal|chip/i.test(p)) throw new Error('parity matrix missing pairs'); console.log('SYS-01/02 docs ok')"</automated>
  </verify>
  <done>SYS-01 catalog and SYS-02 parity matrix exist as short maintainer markdown grounded in real classes</done>
</task>

<task type="auto">
  <name>Task 2: 81-QA-CHECKLIST template + TEST-PLAN §P v3.0 bar map</name>
  <files>.planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md, docs/bridge/TEST-PLAN.md</files>
  <read_first>
    - docs/bridge/TEST-PLAN.md — section O (v2.1) and Execution order; insert §P before Execution order
    - .planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md — structure to mirror (layout tables + automated gates)
    - .planning/REQUIREMENTS.md QA-01..04, SYS-01..02
    - AGENTS.md — verify-live + never wipe data
    - scripts/verify-live.ps1 — health + homepage only (/bridge is explicit Option A)
  </read_first>
  <action>
    **A — Create `81-QA-CHECKLIST.md`**

    Path: `.planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md`

    Contents (template; Pass columns blank after Plan 01 — Plan 02 fills):

    - Header: Phase 81 Visual QA Lock · Date/Browser blanks · hard-refresh note (`Ctrl+Shift+R`) after CSS `?v=` bumps
    - **Layout table (QA-01):** Viewport **390** and **1440** × No horizontal overflow × Primary scrub path completable (city → type → dropzone → Process) × Primary CTAs ≥ 44×44 when visible × Notes × Pass
      - Surfaces to note: hero/pipeline, scrub desk, dossier, tables (horizontal scroll OK inside table; page-level overflow is fail), mission/kill, save, lists/shift HUD, victory, dialogs
    - **Behavior freeze table (QA-04):** rows for
      - process/API/brain/keep-kill/list engine unchanged (no intentional `lib/**` makeover diffs)
      - no Analyze re-coupling chrome
      - `bridge-*` IDs + `data-action` / `data-mode` / `data-format` intact
      - no wipe of `data/filter-lists/` or `data/bridge-brain/`
    - **Catalog / parity evidence:** links to `docs/phuglee/COMPONENT-CATALOG.md` and `docs/phuglee/FILTER-PARITY-MATRIX.md` + Pass when Plan 02 confirms files + optional filled parity rows
    - **Automated gates attached:** checkboxes for `npm test` (≥679 pass / 0 fail), `scripts/verify-live.ps1` exit 0, `/bridge` HTTP 200
    - **Screenshots optional** under `.planning/phases/81-visual-qa-lock-catalog/screenshots/` — never wipe runtime data to stage demos
    - Residual notes section

    **B — Append TEST-PLAN §P**

    In `docs/bridge/TEST-PLAN.md`, insert **before** `## Execution order (GSD verify loop)` (after section O). Title exactly:

    `## P. v3.0 Filter Visual Makeover ship bar (QA-01..04, SYS-01..02)`

    Intro (1–3 lines): Milestone lock for Filter Visual Makeover. Packaging + ship gates after phases 75–80 surface paint. CSS/markup-only makeover; function freeze. New packaging titles use `QA-0N (v3.0)` / `SYS-0N (v3.0)`. Do not rename v1.7–v2.1 bars.

    Table:

    | ID | Case | Expected | Auto |
    |----|------|----------|------|
    | QA-01 (v3.0) | 390 + 1440 primary scrub path layout | No broken layout / page-level overflow | ✓ phase `81-QA-CHECKLIST.md` |
    | QA-02 (v3.0) | Full automated suite permanent bar | ≥679 pass / 0 fail (record exact) | ✓ `npm test` |
    | QA-03 (v3.0) | Live server after visual changes | health + homepage HTTP 200 | ✓ `scripts/verify-live.ps1` |
    | QA-03 (v3.0) | Filter route reachable | `/bridge` HTTP 200 | ✓ explicit Invoke-WebRequest (Option A) |
    | QA-04 (v3.0) | Behavior freeze | No process/API/brain/keep-kill/list workflow change; no Analyze re-coupling | ✓ independence + gold still green + freeze checklist |
    | SYS-01 (v3.0) | Component catalog note | tokens + classes + do/don't | ✓ `docs/phuglee/COMPONENT-CATALOG.md` |
    | SYS-02 (v3.0) | Screenshot parity matrix | login/home vs Filter pairs | ✓ `docs/phuglee/FILTER-PARITY-MATRIX.md` |

    Command block:
    ```bash
    npm test
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    # QA-03 /bridge explicit (Option A)
    # Invoke-WebRequest http://127.0.0.1:3000/bridge → 200
    ```

    Human layout gate: `.planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md`  
    Catalog: `docs/phuglee/COMPONENT-CATALOG.md`  
    Parity: `docs/phuglee/FILTER-PARITY-MATRIX.md`

    Do **not** delete or rewrite sections N or O.

    **Out of scope for Plan 01:**
    - Running full suite / verify-live (Plan 02)
    - Filling Pass columns (Plan 02)
    - Product restyle work (75–80)
    - Extending verify-live.ps1 (optional Option B only if executor chooses later)
  </action>
  <acceptance_criteria>
    - `81-QA-CHECKLIST.md` exists with 390, 1440, behavior freeze, and automated gate checkboxes
    - `rg -n "QA-0[1234] \\(v3\\.0\\)|SYS-0[12] \\(v3\\.0\\)" docs/bridge/TEST-PLAN.md` returns matches for all six IDs
    - Section O `QA-01 (v2.1)` still present in TEST-PLAN
    - Section N `TEST-01 (v2.0)` still present
    - Quick permanent bar still green: `node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>TEST-PLAN §P maps v3.0 QA/SYS IDs; checklist template ready for Plan 02 ship gate</done>
</task>

</tasks>

<verification>
1. COMPONENT-CATALOG.md documents real tokens/classes + do/don't
2. FILTER-PARITY-MATRIX.md pairs login/home vs Filter for button/input/panel/modal/chip
3. 81-QA-CHECKLIST.md template covers 390/1440 + freeze + ship gates
4. TEST-PLAN §P includes QA-01..04 and SYS-01..02 (v3.0)
5. v1.7–v2.1 TEST/QA titles untouched
6. Independence + gold still green
7. No wipe of filter lists / brain; no product scope creep
8. Full suite + verify-live + filled checklist deferred to 81-02
</verification>

<success_criteria>
- SYS-01: Short component catalog exists for later site-wide adoption
- SYS-02: Screenshot parity matrix checklist exists (login/home vs Filter)
- QA-01 packaging: 390/1440 checklist template ready
- QA-04 packaging: behavior freeze documented in checklist + TEST-PLAN
- Zero Storybook; docs only; permanent bars still greppable
</success_criteria>

<output>
After completion, create `.planning/phases/81-visual-qa-lock-catalog/81-01-SUMMARY.md`
</output>
