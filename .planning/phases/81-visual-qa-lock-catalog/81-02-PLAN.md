---
phase: 81-visual-qa-lock-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - .planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md
  - docs/phuglee/FILTER-PARITY-MATRIX.md
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - SYS-01
  - SYS-02
must_haves:
  truths:
    - "Full npm test suite exits 0 with ≥679 pass and 0 fail (QA-02)"
    - "scripts/verify-live.ps1 exits 0 with health + homepage HTTP 200 (QA-03)"
    - "/bridge returns HTTP 200 after verify-live (QA-03 explicit)"
    - "81-QA-CHECKLIST filled for 390 and 1440 primary scrub path without broken layout (QA-01)"
    - "Behavior freeze holds — independence + gold green; freeze rows Pass; no data wipes (QA-04)"
    - "Component catalog + parity matrix files still present; parity Pass notes recorded (SYS-01/02)"
  artifacts:
    - path: ".planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md"
      provides: "Filled QA-01 layout + QA-04 freeze + automated gate evidence"
      contains: "390"
    - path: "docs/phuglee/COMPONENT-CATALOG.md"
      provides: "SYS-01 catalog still present"
      contains: "phuglee-btn"
    - path: "docs/phuglee/FILTER-PARITY-MATRIX.md"
      provides: "SYS-02 parity matrix with Pass evidence from Plan 02"
      contains: "Pass"
    - path: "docs/bridge/TEST-PLAN.md"
      provides: "§P v3.0 bar from Plan 01"
      contains: "QA-01 (v3.0)"
    - path: "scripts/verify-live.ps1"
      provides: "Live health + homepage gate"
      contains: "health"
    - path: "package.json"
      provides: "CI = npm test node --test tests/**/*.test.js"
      contains: "tests/**/*.test.js"
  key_links:
    - from: "Phase 81 Plan 02 ship gate"
      to: "npm test"
      via: "full suite green ≥679 (QA-02)"
      pattern: "npm test"
    - from: "Phase 81 Plan 02 ship gate"
      to: "scripts/verify-live.ps1 + /bridge"
      via: "exit 0 health+home; Invoke-WebRequest /bridge 200 (QA-03)"
      pattern: "verify-live|/bridge"
    - from: "Phase 81 Plan 02 ship gate"
      to: "81-QA-CHECKLIST.md 390/1440 + freeze"
      via: "layout Pass + QA-04 freeze Pass (QA-01/04)"
      pattern: "390|1440|freeze|Pass"
    - from: "Phase 81 Plan 02 ship gate"
      to: "docs/phuglee/* catalog + parity"
      via: "SYS-01/02 evidence recorded"
      pattern: "COMPONENT-CATALOG|FILTER-PARITY"
---

<objective>
Milestone lock-and-ship gate: prove full suite (≥679), live server (health + home + `/bridge`), 390/1440 layout QA, behavior freeze, and catalog/parity evidence after v3.0 Filter Visual Makeover packaging.

Purpose: Plan 01 wrote maintainer docs and checklist templates. Plan 02 is the permanent ship bar — no product feature work unless a real regression appears. Prefer zero product file edits when gates are green.

Output: Green `npm test`, green `verify-live`, green `/bridge` 200, filled `81-QA-CHECKLIST.md`, parity matrix Pass notes, SUMMARY with exact pass counts. **Never wipe** filter-lists or brain.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-visual-qa-lock-catalog/81-CONTEXT.md
@.planning/phases/81-visual-qa-lock-catalog/81-01-PLAN.md
@.planning/phases/81-visual-qa-lock-catalog/81-01-SUMMARY.md
@.planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md
@docs/bridge/TEST-PLAN.md
@docs/phuglee/COMPONENT-CATALOG.md
@docs/phuglee/FILTER-PARITY-MATRIX.md
@AGENTS.md
@scripts/verify-live.ps1
@package.json

# Prior ship-gate pattern
@.planning/phases/68-regression-qa-lock/68-02-PLAN.md
@.planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md

<interfaces>
<!-- Gates only. Prefer zero product edits when green. -->

Ship commands (repo root — cwd distress-os):
```powershell
# Permanent bar quick pack (QA-04 supporting + diagnostic)
node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js

# Full CI (QA-02) — bar is ≥679 pass / 0 fail; record exact
npm test

# Live (QA-03)
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
$bridge = Invoke-WebRequest -Uri "http://127.0.0.1:3000/bridge" -UseBasicParsing -TimeoutSec 8
if ($bridge.StatusCode -ne 200) { throw "/bridge not 200" }
Write-Host "QA-03 ok bridge=$($bridge.StatusCode)"
```

If verify-live fails (server down):
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
# then re-check /bridge
```

v2.1 baseline (Phase 68 filled checklist): **679 pass / 0 fail**. v3.0 bar is **≥679** — count may rise if 75–80 added static CSS locks; never drop below 679 to greenwash.

AGENTS.md hard rules:
- Never wipe data/filter-lists/, data/bridge-brain/, modules/form-forge/data/, analyzer users
- Never start blocking `node server.js` in agent shell — use restart.ps1 / ensure-server.ps1 / verify-live only
- After any public/ or server.js fix: re-run verify-live before claiming live
- Prefer empty product `files_modified` in SUMMARY when gates are already green (checklist + parity Pass notes only)

Layout evidence options (QA-01) — pick what works in-session:
1. Headless metrics (preferred when available): measure `document.documentElement.scrollWidth <= window.innerWidth` at 390 and 1440 on `/bridge` (JS disabled OK if client auth redirects — same as Phase 68)
2. DevTools device toolbar manual notes
3. CSS min-height 44px greps for Process/Save CTAs as supporting evidence (page overflow still needs measurement or honest residual note)

Behavior freeze evidence (QA-04):
- Independence + gold green
- Grep still finds no Analyze push CTAs on Filter (`Send to Analyze`, `Push to Analyze` banned spirit from list-factory)
- Checklist freeze rows marked Pass with note that makeover is CSS/markup-only
- Do not delete gold fixtures or independence bans
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focused permanent bar still green (QA-04 supporting)</name>
  <files></files>
  <read_first>
    - .planning/phases/81-visual-qa-lock-catalog/81-01-SUMMARY.md
    - docs/bridge/TEST-PLAN.md section P v3.0 bar
    - tests/bridge-independence.test.js (TEST-01 (v2.0) still present)
    - tests/bridge-accuracy-gold.test.js (TEST-02 (v2.0) still present)
    - package.json test script
  </read_first>
  <action>
    From repo root, run the **permanent bar quick pack** and **engine composition pack**. Do not edit product code unless a command fails for a real regression.

    **1 — Independence + gold (QA-04 freeze proof + QA-02 core):**
    ```
    node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js
    ```
    Must exit 0. Confirm greppable `TEST-01 (v2.0)` and `TEST-02 (v2.0)`.

    **2 — processUpload Type / format / water composition:**
    ```
    node --test --test-name-pattern="IND-04|GATE-|COL-|water|TEST-0" tests/bridge-engine.test.js
    ```
    Must exit 0.

    **3 — Supporting UX locks (optional strengthen if present):**
    ```
    node --test tests/bridge-train-ux.test.js tests/bridge-list-factory-ux.test.js
    ```

    **If failures:**
    1. Prefer harness / accidental Plan 01 doc typos over product changes
    2. If real product regression from 75–80 visual work: minimal CSS/markup fix only — no process/brain/API changes
    3. Do not skip/delete gold fixtures, independence bans, or v1.7–v2.1 titles to force green
    4. Do not wipe runtime data stores

    Record focused pass counts in SUMMARY.
  </action>
  <acceptance_criteria>
    - Independence + gold command exits 0
    - Engine focused pattern exits 0
    - `rg -n "TEST-01 \\(v2\\.0\\)" tests/bridge-independence.test.js` still matches
    - `rg -n "TEST-02 \\(v2\\.0\\)" tests/bridge-accuracy-gold.test.js` still matches
    - `rg -n "QA-0[1234] \\(v3\\.0\\)" docs/bridge/TEST-PLAN.md` still matches
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-independence.test.js tests/bridge-accuracy-gold.test.js</automated>
  </verify>
  <done>Independence + gold + engine composition packs green; freeze supporting evidence recorded</done>
</task>

<task type="auto">
  <name>Task 2: Full npm test + verify-live + 390/1440 checklist + parity Pass</name>
  <files>.planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md, docs/phuglee/FILTER-PARITY-MATRIX.md</files>
  <read_first>
    - AGENTS.md (live server rules + never wipe user data)
    - scripts/verify-live.ps1
    - package.json (`"test": "node --test tests/**/*.test.js"`)
    - .planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md
    - docs/phuglee/COMPONENT-CATALOG.md
    - docs/phuglee/FILTER-PARITY-MATRIX.md
    - .planning/phases/68-regression-qa-lock/68-QA-CHECKLIST.md (fill style reference)
  </read_first>
  <action>
    **A — Full suite (QA-02 ship gate):**

    From repo root:
    ```
    npm test
    ```
    Must exit 0 with 0 fail. Pass count **≥ 679** (v2.1 baseline). Record exact pass/fail/duration in SUMMARY. If count is higher than 679 due to new static locks, that is OK — never fail solely because count ≠ 679 when all green and ≥679.

    If failures:
    1. Fix harness first
    2. Then minimal CSS/markup fix only if visual makeover broke a static contract
    3. Never delete tests or fixtures to greenwash
    4. Never change process/brain/lib behavior to "make tests pass" for visual work

    **B — Live gate (QA-03):**

    ```powershell
    powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
    if ($LASTEXITCODE -ne 0) { throw "verify-live failed" }

    $bridge = Invoke-WebRequest -Uri "http://127.0.0.1:3000/bridge" -UseBasicParsing -TimeoutSec 8
    if ($bridge.StatusCode -ne 200) { throw "/bridge not 200" }
    Write-Host "QA-03 ok health+home via verify-live; bridge=$($bridge.StatusCode)"
    ```

    If verify-live fails:
    1. `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\restart.ps1`
    2. Re-run verify-live then `/bridge` check until both pass
    3. Never leave blocking `node server.js` in the agent shell
    4. Never wipe data/filter-lists/ or data/bridge-brain/

    Optional: if CSS cache-bust `?v=` was bumped during a minimal fix, note hard-refresh for operators in SUMMARY.

    **C — QA-01 layout checklist fill:**

    Fill `.planning/phases/81-visual-qa-lock-catalog/81-QA-CHECKLIST.md` Pass columns for:

    1. **Layout 390 + 1440** on `/bridge` — no page-level horizontal overflow on primary scrub path surfaces (hero/pipeline, desk, tables may scroll internally, mission/save/lists/dialogs). Practical: DevTools device toolbar and/or headless `scrollWidth <= innerWidth` at each width.
    2. **Primary scrub path completable** without broken layout (city → type → dropzone → Process chrome usable at both widths).
    3. **Primary CTAs ≥ 44×44** when greppable/visible (Process, Save/Stage) — CSS min-height evidence OK as supporting note.

    **D — QA-04 freeze rows:**

    Mark freeze table Pass with evidence:
    - independence + gold green
    - no intentional process/API/brain/list engine changes in this ship gate
    - no Analyze re-coupling chrome
    - no wipe of filter-lists / brain

    **E — SYS-01 / SYS-02 evidence:**

    - Confirm catalog file still present and greppable
    - Fill parity matrix Pass columns (or mark Pass with notes that dual-class greps + visual north-star alignment from 75–80 satisfy pairs when live browser screenshot capture is unavailable). Prefer honest residual notes over fake screenshot claims.
    - Optional screenshots under phase `screenshots/` only if useful — **never** wipe operator data to stage demos

    **F — SUMMARY must state:**
    - Exact npm test pass/fail counts (≥679 / 0)
    - verify-live result (health=200 home=200)
    - `/bridge` StatusCode 200
    - Checklist completed (yes/no + residual notes)
    - Catalog + parity paths confirmed
    - Confirmation QA-01..04 + SYS-01..02 (v3.0) ship bar green
    - Hard-refresh note if CSS cache-bust changed

    Prefer **zero product file edits** when gates are green (only checklist + parity Pass notes).
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 with 0 fail and pass count ≥ 679
    - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exits 0
    - Invoke-WebRequest `http://127.0.0.1:3000/bridge` returns StatusCode 200
    - `81-QA-CHECKLIST.md` has Pass evidence for 390/1440 + freeze + automated gates
    - `docs/phuglee/COMPONENT-CATALOG.md` and `FILTER-PARITY-MATRIX.md` still exist; parity has Pass notes
    - No deletions under `data/filter-lists/`, `data/bridge-brain/`, or gold fixtures
    - `.planning/phases/81-visual-qa-lock-catalog/81-02-SUMMARY.md` exists after completion with counts
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test ≥679 green; verify-live exit 0; /bridge 200; 390/1440 + freeze checklist filled; catalog/parity evidenced; v3.0 visual makeover ship-ready</done>
</task>

</tasks>

<verification>
1. Independence + gold quick bar — exit 0
2. Engine focused Type/format/water + IND-04 pattern — exit 0
3. `npm test` — exit 0, ≥679 pass, 0 fail
4. `scripts/verify-live.ps1` — exit 0, health + homepage 200
5. Explicit `/bridge` HTTP 200
6. 81-QA-CHECKLIST filled (390/1440 + freeze + automated gates)
7. SYS-01 catalog + SYS-02 parity Pass notes present
8. No wipe of operator runtime data
9. Phase 81 success criteria (QA-01..04, SYS-01..02) satisfied
</verification>

<success_criteria>
- QA-01: Operator on 390px and 1440px can complete primary scrub path without broken layout (checklist Pass)
- QA-02: Full suite green ≥679 / 0 fail
- QA-03: verify-live exit 0; `/bridge` + homepage HTTP 200
- QA-04: Behavior freeze holds (independence/gold + freeze checklist; no data wipes)
- SYS-01: Component catalog note available for later pages
- SYS-02: Screenshot parity matrix checklist available and Pass-noted
- Milestone v3.0 Visual QA Lock ready for `/gsd:verify-work 81`
</success_criteria>

<output>
After completion, create `.planning/phases/81-visual-qa-lock-catalog/81-02-SUMMARY.md`
</output>
