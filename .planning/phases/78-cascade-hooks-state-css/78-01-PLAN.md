---
phase: 78-cascade-hooks-state-css
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/bridge.html
  - public/css/bridge.css
  - public/css/phuglee-components.css
autonomous: true
requirements:
  - FORMS-01
must_haves:
  truths:
    - "Filter stylesheet cascade is shared components then bridge page then a11y last"
    - "Operator text inputs, selects, textareas, and search fields share the phuglee form language (city search, state/city selects, save name, Train search, kept filters, paste/outcome notes)"
    - "Disabled form controls read as disabled via :disabled / [disabled] — not a CSS-only fake class"
    - "CSS never force-displays #bridge-train-wrap over the hidden attribute (no display:flex !important fail-open)"
    - "No bridge-* IDs, data-action / data-mode / data-format / data-step contracts renamed"
  artifacts:
    - path: "public/bridge.html"
      provides: "Correct cascade link order + dual-class form hooks"
      contains: "phuglee-components.css"
    - path: "public/css/phuglee-components.css"
      provides: "Form control disabled/focus system language"
      contains: ".phuglee-input"
    - path: "public/css/bridge.css"
      provides: "Layout-only form overrides; no Train display fail-open"
      contains: "bridge-city-search"
  key_links:
    - from: "public/bridge.html link order"
      to: "phuglee-components before bridge before a11y"
      via: "stylesheet cascade"
      pattern: "phuglee-components.css"
    - from: "public/bridge.html form controls"
      to: "public/css/phuglee-components.css"
      via: "dual-class phuglee-input select textarea"
      pattern: "phuglee-input|phuglee-select|phuglee-textarea"
    - from: "public/css/bridge.css"
      to: "#bridge-train-wrap[hidden]"
      via: "no display override that beats hidden"
      pattern: "bridge-train-wrap"
---

<objective>
Fix Filter CSS cascade order and dual-class wire every text input, select, textarea, and search field to the shared Phuglee form system — without renaming IDs or failing-open Train for non-admins.

Purpose: FORMS-01 + cascade foundation so later desk/theater paint composes on top of the system instead of fighting inverted load order.

Output: bridge.html link order + form class hooks; thinned bridge.css form look rules; form :disabled polish in phuglee-components.css.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/78-cascade-hooks-state-css/78-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@public/bridge.html
@public/css/bridge.css
@public/css/phuglee-components.css
@public/css/phuglee-a11y.css
@AGENTS.md

<interfaces>
<!-- Contracts executor must honor. CSS/markup only — no public/js/** or lib/** edits. -->

### Target cascade (ARCHITECTURE Pattern 2)

After shell/status sheets, order must be:

1. phuglee-components.css (shared system)
2. bridge.css (page composition — last among visual sheets)
3. phuglee-a11y.css (always final)

Today (inverted — fix this): bridge.css?v=44 then phuglee-components then phuglee-a11y.

### Shared form API (already in phuglee-components.css)

Classes: .phuglee-input, .phuglee-textarea, .phuglee-select
Look: cream text, dark fill, stone border, orange focus ring

### Dual-class form targets in bridge.html (add classes; keep every id/name)

| Control | id | Add class(es) |
|---------|-----|----------------|
| City search | #bridge-city-search | phuglee-input (keep bridge-city-search) |
| State select | #bridge-state | phuglee-select |
| City select | #bridge-city | phuglee-select |
| Outcome notes | #bridge-other-source-notes | phuglee-textarea |
| Outcome type | #bridge-outcome-type | phuglee-select |
| Paste box | #bridge-paste-text | phuglee-textarea (keep bridge-paste-textarea) |
| List name | #bridge-list-name | phuglee-input |
| Train search | #bridge-train-search | phuglee-input |
| Kept filter search | #bridge-filter-search | phuglee-input |
| Category select | #bridge-filter-category | phuglee-select |
| Tag select | #bridge-filter-tag | phuglee-select |
| Confidence select | #bridge-filter-confidence | phuglee-select |

### Locked anti-patterns (PITFALLS 2 + CONTEXT)

- Never force display:flex !important (or block/grid) on #bridge-train-wrap that beats [hidden]
- Never rename id="bridge-*" or data-action / data-mode / data-format / data-step
- Never replace native controls with custom widgets that need JS
- Train wrap stays: div id="bridge-train-wrap" with default hidden (fail-closed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip cascade order + cache-bust CSS queries</name>
  <files>public/bridge.html</files>
  <action>
In public/bridge.html head, reorder stylesheets so after shell/status sheets the order is:

1. phuglee-components.css (shared system)
2. bridge.css (page composition — last among visual sheets)
3. phuglee-a11y.css (always final)

Do not load home/auth CSS on /bridge. Do not invent a parallel bridge-makeover.css.

Bump cache queries on changed sheets (at minimum bridge.css?v= from 44 to next integer; bump phuglee-components.css?v= if that file is also edited in this plan). Keep other shell ?v= values unless you touch those files.

Verify skip link + phuglee-a11y.css remain present (Pitfall 7).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('public/bridge.html','utf8'); const iC=h.indexOf('phuglee-components.css'); const iB=h.indexOf('/css/bridge.css'); const iA=h.indexOf('phuglee-a11y.css'); const ok = iC >= 0 && iB >= 0 && iA >= 0 && iC &lt; iB && iB &lt; iA; if (!ok) { console.error({iC,iB,iA}); process.exit(1); } console.log('cascade order OK');"</automated>
  </verify>
  <done>bridge.html loads phuglee-components then bridge then phuglee-a11y; cache query on bridge.css bumped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dual-class form hooks + thin bridge form look rules</name>
  <files>public/bridge.html, public/css/bridge.css, public/css/phuglee-components.css</files>
  <action>
Markup (bridge.html only — classes additive):
Add phuglee-input / phuglee-select / phuglee-textarea to every operator form control listed in interfaces (city search, state/city, outcome notes+type, paste textarea, list name, train search, filter search + three filter selects). Preserve all existing domain classes (bridge-city-search, bridge-paste-textarea, etc.) and every id/name/disabled/maxlength/placeholder/aria attribute.

Shared system (phuglee-components.css):
Add minimal disabled states so system language is complete:
- .phuglee-input:disabled, .phuglee-select:disabled, .phuglee-textarea:disabled (and [disabled]) → reduced opacity, not-allowed cursor, no focus glow
- Do not invent .is-disabled without JS wiring

bridge.css after cascade flip:
Equal-specificity page rules will now win over components. Thin/delete look duplicates so dual-classed controls inherit system paint:
- Prefer deleting or reducing paint rules on .bridge-row select, .bridge-city-search (border/bg/color/focus), #bridge-train-search paint, .bridge-paste-textarea paint, .bridge-other-source-wrap textarea paint when those elements carry phuglee-*
- Keep layout/domain rules: typeahead z-index: 40 !important, absolute results panel, paste min-height, toolbar flex sizing, label typography
- Use dual-class composition only where desk density must override system padding, e.g. .bridge-city-search.phuglee-input { /* layout densify only */ } — not a second palette
- Prefer var(--phuglee-*) / existing tokens; no new hex islands

No public/js/** or lib/** changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('public/bridge.html','utf8'); const need=['bridge-city-search','bridge-state','bridge-city','bridge-list-name','bridge-train-search','bridge-filter-search','bridge-paste-text','bridge-other-source-notes','bridge-outcome-type','bridge-filter-category','bridge-filter-tag','bridge-filter-confidence']; for (const id of need) { const idx=h.indexOf('id=\"'+id+'\"'); if (idx &lt; 0) { console.error('missing id',id); process.exit(1);} const slice=h.slice(Math.max(0,idx-120), idx+160); if (!/phuglee-(input|select|textarea)/.test(slice)) { console.error('missing phuglee form class near',id); process.exit(1);} } const c=fs.readFileSync('public/css/phuglee-components.css','utf8'); if (!c.includes('phuglee-input:disabled') && !c.includes('.phuglee-input:disabled')) { if (!/phuglee-input[^\n]*:disabled|:disabled[^\n]*phuglee-input/.test(c) && !c.includes('phuglee-input,\n.phuglee-textarea,\n.phuglee-select') ) { /* require explicit disabled rule block */ } if (!/:disabled/.test(c) || !c.includes('phuglee-input')) { console.error('missing form :disabled rules'); process.exit(1);} } console.log('form dual-class OK');"</automated>
  </verify>
  <done>All listed form controls dual-classed; shared :disabled styles exist; bridge form look rules thinned so system paint wins for dual-classed fields.</done>
</task>

<task type="auto">
  <name>Task 3: State CSS guards — hidden/disabled + Train fail-closed</name>
  <files>public/css/bridge.css</files>
  <action>
Audit and lock state CSS after cascade flip:

1. Train fail-closed: Grep bridge.css for any rule that forces display on #bridge-train-wrap or .bridge-train-wrap with !important. Remove or rewrite so [hidden] / UA hidden still wins. Theater chrome may style #bridge-train-wrap.is-theater children only — never unhide the wrap via display hacks.

2. Workflow panels: Ensure critical hideable surfaces rely on the hidden attribute (already JS-driven). Where bridge already uses .foo[hidden] { display: none !important; } (toast/error), leave those. Do not introduce class-only show/hide that bypasses hidden for #bridge-loading-panel, #bridge-results-panel, #bridge-train-wrap, #bridge-save-panel, dialogs.

3. Disabled CTAs: Confirm process/save/outcome buttons remain styled via button:disabled / .phuglee-btn:disabled (already in components) — no new .is-disabled without JS.

4. Smoke grep after edits: no display flex/block/grid !important targeting train wrap.

Do not relocate #bridge-train-mission or remove default hidden on train wrap (Pitfall 4).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('public/css/bridge.css','utf8'); const re=/(#bridge-train-wrap|\.bridge-train-wrap)\s*\{[^}]*display\s*:\s*(flex|block|grid)\s*!important/i; if (re.test(c)) { console.error('Train wrap display !important fail-open risk'); process.exit(1);} const h=fs.readFileSync('public/bridge.html','utf8'); if (!/id=\"bridge-train-wrap\"[^&gt;]*\bhidden\b/.test(h)) { console.error('train wrap must default hidden'); process.exit(1);} console.log('Train hidden guards OK');"</automated>
  </verify>
  <done>No Train display fail-open; hidden attribute remains sole gate for train wrap; disabled styling stays :disabled-based.</done>
</task>

</tasks>

<verification>
- Cascade order check green
- Form dual-class check green
- Train display-hack check green
- Spot: node --test tests/bridge-train-theater.test.js tests/bridge-desk-cinema.test.js still green
- No edits under public/js/ or lib/
</verification>

<success_criteria>
1. /bridge CSS order is components then bridge then a11y
2. Operator form controls share phuglee-input / select / textarea language
3. Disabled selects (state/city boot) visually muted via system :disabled
4. Non-admin Train cannot be CSS-unhidden via display !important
5. All bridge-* IDs and data-* contracts unchanged
</success_criteria>

<output>
After completion, create .planning/phases/78-cascade-hooks-state-css/78-01-SUMMARY.md
</output>
