---
phase: 78-cascade-hooks-state-css
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - public/bridge.html
  - public/css/bridge.css
autonomous: true
requirements:
  - FORMS-04
  - CARDS-02
  - STATES-02
must_haves:
  truths:
    - "Dropzone shows distinct idle / dragover / has-file / error visual states without changing multi-file or accept list"
    - "History and type-column confirm remain native dialog elements with system modal glass/grain treatment"
    - "Loading / scrub-in-progress uses shared phuglee loading patterns; feed summary + list stay legible"
    - "Scrub feed population does not depend on CSS animationend (no new animationend-gated visibility)"
    - "Type confirm and history dialogs stay above chrome (z-index usable); confirm control IDs frozen"
  artifacts:
    - path: "public/bridge.html"
      provides: "Dropzone + dialog dual-class hooks; loading panel shared classes retained"
      contains: "bridge-dropzone"
    - path: "public/css/bridge.css"
      provides: "Dropzone state matrix; dialog glass/grain; loading/feed presentation without animationend gate"
      contains: "bridge-dropzone.is-dragover"
  key_links:
    - from: "#bridge-dropzone"
      to: "JS classList has-file / is-dragover"
      via: "CSS state selectors only"
      pattern: "bridge-dropzone.is-dragover|bridge-dropzone.has-file|bridge-dropzone.is-error"
    - from: "#bridge-history-dialog and #bridge-type-column-confirm-dialog"
      to: "native dialog + phuglee-panel float card"
      via: "keep showModal-compatible structure"
      pattern: "bridge-history-dialog|bridge-type-column-confirm-dialog"
    - from: "#bridge-loading-panel"
      to: ".phuglee-loading-state + #bridge-scrub-feed"
      via: "shared loading + feed DOM (no animationend dependency in CSS)"
      pattern: "phuglee-loading-state"
---

<objective>
Wire dropzone visual state matrix, system-grade dialog glass/grain on history + type-confirm, and shared loading patterns for scrub-in-progress — pure CSS/markup on top of Plan 01 cascade.

Purpose: FORMS-04, CARDS-02, STATES-02 so high-stakes upload, confirm, and process-climax surfaces match Phuglee system energy without behavior change.

Output: Restyled dropzone states + dialogs + loading/feed presentation in bridge.html / bridge.css.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/78-cascade-hooks-state-css/78-CONTEXT.md
@.planning/phases/78-cascade-hooks-state-css/78-01-PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@public/bridge.html
@public/css/bridge.css
@public/css/phuglee-components.css
@AGENTS.md

<interfaces>
<!-- JS already toggles these — CSS only. Do not edit bridge.js. -->

### Dropzone contract (FORMS-04)

Element: #bridge-dropzone.bridge-dropzone (tabindex=0, role=button)
File input: #bridge-file-input with accept=".xlsx,.xls,.csv,.tsv,.txt,.pdf,.doc,.docx,.jpg,.jpeg,.png" and multiple — MUST NOT change.

JS (bridge.js) already:
- classList add/remove is-dragover on dragenter/leave/drop
- classList add/remove has-file when files selected/cleared

Required CSS states:

| State | Selector | Visual intent |
|-------|----------|---------------|
| idle | .bridge-dropzone | dashed calm border, glass fill, readable hint |
| dragover | .bridge-dropzone.is-dragover | solid orange border, warm fill lift |
| has-file | .bridge-dropzone.has-file | confirmed file energy (distinct from idle) |
| error | .bridge-dropzone.is-error | danger border/tint via semantic danger token — CSS-ready |
| focus | .bridge-dropzone:focus-visible | visible focus ring (a11y) |

Note: There are TWO .bridge-dropzone rule clusters in bridge.css (~L375 glass strata + ~L982 layout). Consolidate into one coherent state matrix — do not leave conflicting double definitions fighting after cascade flip.

### Dialog contract (CARDS-02) — keep native dialog

- dialog#bridge-history-dialog with card.bridge-history-dialog-card.phuglee-panel.distress-glass--float
- dialog#bridge-type-column-confirm-dialog.bridge-type-confirm-dialog with same card pattern

Frozen IDs: bridge-history-dialog, bridge-history-close, bridge-history-list, bridge-type-column-confirm-dialog, bridge-type-column-confirm-ok, bridge-type-column-confirm-cancel, bridge-type-column-confirm-close, bridge-type-column-candidates, bridge-type-column-samples, bridge-type-column-suggested.

Optional: add phuglee-modal-panel on .bridge-history-dialog-card for shared rise energy. Do not replace dialog with a div modal kit. Do not remove ::backdrop usability.

### Loading + feed (STATES-02)

#bridge-loading-panel.bridge-panel.phuglee-panel[hidden] contains:
- .bridge-loading.phuglee-loading-state
- .phuglee-loading-bar
- #bridge-loading-copy.phuglee-loading-copy
- #bridge-scrub-feed-summary
- #bridge-scrub-feed

Rules:
- Keep shared loading classes wired
- Feed rows/summary must remain readable (cream/taupe contrast on glass)
- Forbidden: CSS that hides feed content until animationend
- If feed rows use enter animations, final keyframe must leave content fully visible; reduced-motion sets animation:none with full opacity
- #bridge-loading-panel[hidden] must stay truly hidden
- No public/js/bridge-scrub-feed.js changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dropzone state matrix (idle / dragover / has-file / error)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Consolidate the two .bridge-dropzone rule blocks in bridge.css into one state matrix that uses tokens (var(--phuglee-orange), glass/shadow tokens where available, var(--phuglee-danger) or existing danger semantic for error).

Implement visual states:
- idle — dashed border, calm glass, clear Drop files here hierarchy
- is-dragover — solid orange emphasis (already partially present; keep focus-visible distinct or shared intentionally)
- has-file — distinct confirmed state (not identical to idle); file name line (.bridge-drop-file) stays cream/readable
- is-error — danger border/background tint (CSS-ready for class toggle; do not require JS change this phase)

Preserve:
- #bridge-dropzone id, tabindex=0, role=button, aria-describedby=bridge-drop-hint
- #bridge-file-input accept list string and multiple attribute exactly
- Browse link #bridge-browse behavior (class-only polish OK)

Markup: optional additive class only if needed for composition. Do not change accept/multiple.

Minimize new !important — existing dropzone border !important may stay if required to beat residual strata; do not expand the arms race (Pitfall 3). Prefer specificity + cascade (bridge now loads after components).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('public/css/bridge.css','utf8'); for (const s of ['.bridge-dropzone.is-dragover','.bridge-dropzone.has-file','.bridge-dropzone.is-error']) { if (!c.includes(s)) { console.error('missing',s); process.exit(1);} } const h=fs.readFileSync('public/bridge.html','utf8'); if (!h.includes('bridge-file-input') || !h.includes('multiple')) { console.error('multiple missing'); process.exit(1);} if (!h.includes('accept=\".xlsx')) { console.error('accept list changed/missing'); process.exit(1);} console.log('dropzone states + accept OK');"</automated>
  </verify>
  <done>Dropzone CSS defines idle + is-dragover + has-file + is-error; accept list and multiple unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: History + type-confirm dialogs — system modal glass/grain</name>
  <files>public/bridge.html, public/css/bridge.css</files>
  <action>
Restyle native dialogs to match system modal glass/grain (CARDS-02) without replacing dialog elements:

Markup:
- Keep both elements as native dialog
- Keep inner cards as bridge-history-dialog-card phuglee-panel distress-glass--float
- Optionally add phuglee-modal-panel for shared rise animation (if cascade + reduced-motion: ensure prefers-reduced-motion kills the rise if you introduce/retain animation — bridge already has bridge-dialog-rise; align duration/easing with phuglee-modal-rise or drop duplicate and use one)
- Do not change ok/cancel/close/candidates/samples IDs or button data-*

CSS:
- ::backdrop — dark blurred veil consistent with .phuglee-modal-backdrop DNA (tokenized where possible)
- Card — elevated float glass (already dual-classed); ensure padding, head hierarchy (display title, calm lead), close control focus-visible
- Preserve z-index: 10000 (or documented scale) so type confirm is never buried under shell nav
- Type-confirm option selected state may keep orange accent via tokens (not random hex)
- Confirm logic frozen — no JS; no structure rewrite of radiogroup candidates container

Hard ban: do not convert to div+backdrop custom modal that breaks showModal().
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('public/bridge.html','utf8'); const must=['id=\"bridge-history-dialog\"','id=\"bridge-type-column-confirm-dialog\"','id=\"bridge-type-column-confirm-ok\"','id=\"bridge-type-column-confirm-cancel\"','phuglee-panel','distress-glass--float']; for (const m of must) { if (!h.includes(m)) { console.error('missing',m); process.exit(1);} } if (!h.includes('&lt;dialog') || h.indexOf('&lt;dialog') &lt; 0) { /* dialog tags present */ } if ((h.match(/&lt;dialog/g) || []).length &lt; 2) { console.error('need two native dialogs'); process.exit(1);} const c=fs.readFileSync('public/css/bridge.css','utf8'); if (!c.includes('.bridge-history-dialog::backdrop')) { console.error('backdrop missing'); process.exit(1);} console.log('dialogs native + glass OK');"</automated>
  </verify>
  <done>Both dialogs remain native dialog with glass/float cards and usable backdrop; confirm IDs intact.</done>
</task>

<task type="auto">
  <name>Task 3: Loading panel + scrub feed presentation (no animationend gate)</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <action>
Ensure scrub-in-progress feedback uses shared loading patterns (STATES-02):

1. Loading shell: #bridge-loading-panel keeps bridge-panel phuglee-panel; inner .bridge-loading.phuglee-loading-state with .phuglee-loading-bar + #bridge-loading-copy.phuglee-loading-copy. If any of these classes were lost, restore. Layout-only tweaks in bridge.css (padding/gap) OK; do not re-paint a parallel loading bar that ignores shared classes.

2. Feed legibility: Style #bridge-scrub-feed / .bridge-scrub-feed items and #bridge-scrub-feed-summary for cream/readable contrast on glass. Keep list semantics (ol/li as JS emits).

3. No animationend dependency: Do not add CSS such that feed rows stay opacity:0 / visibility:hidden until animation completes without a non-animated final state. If feed rows use enter animations, final keyframe must leave content fully visible, and reduced-motion must set animation: none with full opacity (FEED-02 friendly). Prefer not adding new enter animations this phase unless reduced-motion twin is included.

4. hidden: #bridge-loading-panel[hidden] (and feed/summary [hidden]) must not be force-displayed.

5. Bump bridge.css?v= if not already bumped in 78-01 after these edits.

No public/js/bridge-scrub-feed.js changes — population timing stays JS-owned.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('public/bridge.html','utf8'); if (!h.includes('id=\"bridge-loading-panel\"')) { console.error('loading panel missing'); process.exit(1);} if (!h.includes('phuglee-loading-state') || !h.includes('phuglee-loading-bar') || !h.includes('phuglee-loading-copy')) { console.error('shared loading classes missing'); process.exit(1);} if (!h.includes('id=\"bridge-scrub-feed\"')) { console.error('feed missing'); process.exit(1);} const c=fs.readFileSync('public/css/bridge.css','utf8'); if (/animationend/i.test(c)) { console.error('CSS must not reference animationend'); process.exit(1);} console.log('loading+feed hooks OK');" &amp;&amp; node --test tests/bridge-scrub-feed.test.js tests/bridge-train-theater.test.js tests/bridge-desk-cinema.test.js</automated>
  </verify>
  <done>Shared loading classes present; feed CSS has no animationend gate; theater/cinema/feed static tests green.</done>
</task>

</tasks>

<verification>
- Dropzone four-state selectors present; accept/multiple unchanged
- History + type-confirm still native dialog with frozen control IDs
- Loading uses phuglee-loading-*; scrub-feed tests green
- Optional: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-live.ps1 after public/ edits (AGENTS.md)
- Diff excludes public/js/** and lib/**
</verification>

<success_criteria>
1. Operator sees distinct dropzone idle / dragover / has-file / error visuals
2. Operator opens history + type-column confirm as native dialogs with system glass/grain
3. Operator sees shared loading bar/copy during scrub; feed remains legible and not animationend-gated
4. Multi-file + accept list + confirm IDs + train fail-closed unchanged
5. Focused bridge theater/cinema/feed tests still pass
</success_criteria>

<output>
After completion, create .planning/phases/78-cascade-hooks-state-css/78-02-SUMMARY.md
</output>
