---
phase: 65-kill-rate-scrub-report
plan: 02
type: execute
wave: 2
depends_on:
  - "65-01"
files_modified:
  - public/js/bridge.js
  - public/css/bridge.css
  - public/bridge.html
autonomous: true
requirements:
  - KILL-01
  - KILL-02
must_haves:
  truths:
    - "After process, results head shows display-scale RAW → KILLED → KEPT hierarchy (not equal peer KPI tiles as primary)"
    - "Kill-reason breakdown chips render from stats.discardReasons (plus non-zero counters) with counts"
    - "Proof chips/HUD surface duration, Format reused when auto_reuse, parser, and independence / discard story — not only one buried meta sentence"
    - "Optional 3 sample kept dossiers render from lastResult.rows when kept rows exist"
    - "Train refresh still updates KEPT via renderKpis(lastResult.stats)"
    - "No engine rewrite; no Analyze push; Format reused string preserved"
  artifacts:
    - path: "public/js/bridge.js"
      provides: "renderKpis kill-rate report + proof chips + optional samples"
      contains: "bridge-kill-flow"
    - path: "public/css/bridge.css"
      provides: "asymmetric kill-flow HUD, reason chips, proof chips, sample dossiers"
      contains: "bridge-kill-stat"
    - path: "public/bridge.html"
      provides: "cache-bust for js/css after surface change"
      contains: "bridge-kpi-grid"
  key_links:
    - from: "renderResults"
      to: "renderKpis(stats)"
      via: "still called after meta context; kill report owns hierarchy"
      pattern: "renderKpis\\(stats\\)"
    - from: "refreshTrainUiAfterDecision"
      to: "renderKpis(lastResult.stats)"
      via: "KEPT updates after Approve/Deny"
      pattern: "renderKpis\\(lastResult\\.stats\\)"
    - from: "renderKpis"
      to: "stats.discardReasons + processingMeta"
      via: "reason chips + proof chips from existing payload"
      pattern: "discardReasons|durationMs|auto_reuse"
---

<objective>
Reforge post-process results head into a cinematic kill-rate scrub report: display-scale RAW → KILLED → KEPT, kill-reason breakdown, proof chips from existing process meta, optional sample kept dossiers.

Purpose: KILL-01 + KILL-02 — mission readout replaces equal KPI tile grid; proof is HUD chips, not buried prose.

Output: Client-only `bridge.js` + `bridge.css` (+ cache-bust). Zero engine/API changes. Zero new packages.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-kill-rate-scrub-report/65-CONTEXT.md
@.planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md
@.planning/phases/65-kill-rate-scrub-report/65-01-SUMMARY.md
@.planning/phases/65-kill-rate-scrub-report/65-VALIDATION.md
@public/js/bridge.js
@public/css/bridge.css
@public/bridge.html
@lib/bridge-intake-schema.js
@tests/bridge-kill-rate-scrub.test.js
@tests/bridge-list-factory-ux.test.js
@tests/bridge-efficiency-path.test.js
@AGENTS.md

<interfaces>
<!-- Display math (client-only; no engine fields) -->
```text
RAW    = Number(stats.totalParsed)
       || (KEPT + fallbackKilled)
KEPT   = Number(stats.kept)
       || (lastResult?.rows || []).length
KILLED = max(0, RAW - KEPT)
// Prefer RAW−KEPT so hierarchy always sums.
// Fallback if totalParsed missing:
//   discarded + deduplicated + alreadyImported (clamped)
```

<!-- Kill-reason sources (count > 0 only; sort desc) -->
Preferred: Object.entries(stats.discardReasons || {})
Operator labels (map known keys / human strings):
| Engine / map key or human string | Chip label |
| No usable street address | No address |
| Blank or empty row | Blank row |
| Clearly non-property record | Non-property |
| Near-duplicate within upload | Deduped |
| Already imported in Analyze | Already in Analyze |
| No distressed signal… / no_distress_signal | No distress signal |
| Could not parse row | Parse error |

Also surface counters when > 0 and not already represented:
- stats.noDistress → No distress signal
- stats.deduplicated → Deduped
- stats.alreadyImported → Already in Analyze (IND-04: omit when 0)

Not kills (secondary under KEPT / proof only):
- needsReview / lowConfidence → Needs review chip

<!-- Proof chips (from processingMeta + stats) -->
- durationMs finite ≥ 0 → `Scrubbed in X.Xs` or `X.Xs scrub`
- typeResolution.source === 'auto_reuse' → `Format reused` (+ Type: header or No type column)
- parser → `xlsx` / `csv` / `pdf` / `stub` chip
- importIndexCount > 0 → `N in Analyze index` (proof only — not a push)
- independence fixed short: `Nothing sent to Analyze` (preserve LIST-03 / IND language)
- discard story: top reasons already in reason strip; clean path may omit discard chip

<!-- renderKpis rewrite target shape (keep function name) -->
```js
function renderKpis(stats) {
  if (!kpiGrid) return;
  const s = stats || {};
  const data = lastResult || {};
  const meta = data.processingMeta || {};
  const rows = data.rows || [];

  const kept = Number(s.kept);
  const keptN = Number.isFinite(kept) ? kept : rows.length;
  let raw = Number(s.totalParsed);
  if (!Number.isFinite(raw) || raw < 0) {
    const fallbackKilled =
      (Number(s.discarded) || 0) +
      (Number(s.deduplicated) || 0) +
      (Number(s.alreadyImported) || 0);
    raw = keptN + Math.max(0, fallbackKilled);
  }
  const killed = Math.max(0, raw - keptN);

  // Build HTML:
  // .bridge-kill-flow → RAW | → | KILLED | → | KEPT (asymmetric classes)
  // .bridge-kill-reasons → chips
  // .bridge-proof-chips → duration, Format reused, parser, independence, needs review
  // .bridge-kept-samples → 0–3 dossier cards from rows (skip if empty)
  kpiGrid.innerHTML = /* ... */;
  kpiGrid.classList.add('bridge-kill-report'); // host may keep id bridge-kpi-grid
}
```

<!-- Meta sentence slim-down -->
`#bridge-results-meta` becomes short ops context only:
`{city}, {state} · {uploadType label} · {fileLabel}`
— do NOT concatenate duration / Format reused / parser into the long prose line (those move to proof chips).
Preserve `Format reused` string somewhere in JS (proof chip) for EFF tests.

<!-- Stub note -->
When proof chips + reasons cover discard story, keep `#bridge-stub-note` for `data.stub === true` and edge prose; hide more aggressively when kill report already shows discarded/reasons. Still keep `Save the list below — nothing was sent to Analyze.` path string somewhere (independence tests) — proof chip `Nothing sent to Analyze` plus existing clean-path string is OK.

<!-- Hierarchy visual rules (anti equal-3-up) -->
- NOT three identical .bridge-kpi cards
- Asymmetric scale: KEPT largest (survivor), or KILLED mid emphasized — use classes:
  - .bridge-kill-stat--raw (stone/cream, quieter)
  - .bridge-kill-stat--killed (ember/heat)
  - .bridge-kill-stat--kept (gold/orange accent — NOT SaaS green if avoidable)
- Values: font-display, clamp larger than current 1.45rem (e.g. kept clamp 1.75–2.4rem; raw quieter)
- Labels uppercase micro: RAW / KILLED / KEPT
- Arrows → between stats (aria-hidden)
- prefers-reduced-motion: no count-up required (static numbers only)

<!-- Sample dossiers (ship — research discretion: cheap) -->
First 3 of rows (or prefer Strong tag first if cheap single sort):
- streetAddress title
- violationIssueType subtitle
- distressedSignalTag via existing bridge-tag--* if helpers exist
Skip when rows.length === 0.

<!-- Out of scope -->
- Engine processUpload / keep-kill rewrite
- Train theater (66) — leave #bridge-train-wrap alone
- Live feed (64)
- Renaming #bridge-save-list or Preview CSV
- Auto-push Analyze
- Wiping filter-lists / bridge-brain
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite renderKpis into kill-rate report + slim renderResults meta into proof chips</name>
  <files>public/js/bridge.js, public/bridge.html</files>
  <read_first>
    - .planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md (mapping table, proof chips, risks)
    - public/js/bridge.js (renderKpis ~1645–1663; renderResults ~2254–2351; refreshTrainUiAfterDecision ~443–447)
    - lib/bridge-intake-schema.js (DISCARD_REASONS labels ~88–96; emptyProcessingStats)
    - tests/bridge-kill-rate-scrub.test.js
    - tests/bridge-efficiency-path.test.js (Format reused lock)
    - tests/bridge-list-factory-ux.test.js (nothing was sent to Analyze)
  </read_first>
  <action>
    1. **Replace `renderKpis(stats)` body** in `public/js/bridge.js` with kill-rate report builder per interfaces:
       - Compute RAW / KILLED / KEPT with RAW−KEPT invariant.
       - Emit hierarchy HTML with labels **RAW**, **KILLED**, **KEPT** and classes `bridge-kill-flow`, `bridge-kill-stat`, `bridge-kill-stat--raw|killed|kept`, values via `toLocaleString()`.
       - Build **kill-reason chips** from `stats.discardReasons` (count &gt; 0), sorted by count desc; map to operator-facing short labels; merge non-zero counters (noDistress, deduplicated, alreadyImported) without double-label spam.
       - Build **proof chips** row (`.bridge-proof-chips` / `.bridge-proof-chip`):
         - duration from `lastResult.processingMeta.durationMs`
         - `Format reused` when `typeResolution.source === 'auto_reuse'` (keep exact substring `Format reused`)
         - parser when present
         - Analyze index count when &gt; 0
         - Needs review when needsReview/lowConfidence &gt; 0
         - Independence: `Nothing sent to Analyze` (or keep longer `nothing was sent to Analyze` string in a chip/aria — tests need the existing independence phrase somewhere still)
       - Optional **3 sample dossiers** (`.bridge-kept-samples` / `.bridge-kept-sample`) from `lastResult.rows` when length &gt; 0; escape all user text with existing `esc()`.
       - Guard `if (!kpiGrid) return;`.
       - Keep function name `renderKpis` so Train path stays valid.

    2. **Slim `renderResults` meta** (`#bridge-results-meta`):
       - Short context line only: kept count optional OR city/type/file only — e.g.  
         `{uploadLabel} · {city}, {state} · {fileLabel}`  
         Do **not** bury duration / Format reused / parser only in this sentence.
       - Still call `renderKpis(stats)` after setting lastResult (order: set lastResult first — already true — so renderKpis can read processingMeta/rows).

    3. **Stub note:** Prefer hiding when kill report already shows discard reasons and review chips; keep stub path for `data.stub` and ensure independence string `nothing was sent to Analyze` remains in source (chip and/or stub clean path).

    4. **Do not** change Train wrap, table, export handler, save handler, engine.

    5. **Cache-bust** in `public/bridge.html`: bump `bridge.js?v=` (currently 34 → 35 or next) and `bridge.css?v=` when Task 2 lands CSS (coordinate to single bump if same plan wave).

    6. AGENTS.md: no data wipes; no blocking node server in shell.
  </action>
  <acceptance_criteria>
    - `public/js/bridge.js` contains `bridge-kill-flow` and `RAW` and `KILLED` and `KEPT`
    - `function renderKpis` still present
    - `renderKpis(lastResult.stats)` still present in train refresh path
    - `discardReasons` referenced inside/near kill-report builder
    - `Format reused` still present in bridge.js
    - `nothing was sent to Analyze` still present (case-insensitive match OK for tests using that phrase)
    - No new `Send to Analyze` / `Push to Analyze` strings
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>Kill report JS ships; KILL-01/02 static tests that only need JS strings go green (CSS classes may still need Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Kill-flow HUD CSS — asymmetric hierarchy, reason chips, proof chips, samples</name>
  <files>public/css/bridge.css, public/bridge.html</files>
  <read_first>
    - public/css/bridge.css (.bridge-kpi-grid ~677–716)
    - public/css/home.css (.home-territory-hud-stat ~632–656) for display-scale reference
    - public/js/bridge.js (classes emitted by Task 1)
    - .planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md (hierarchy rules; no equal 3-up)
  </read_first>
  <action>
    Extend `public/css/bridge.css` (page-local only; no React; no shared design-system extraction):

    1. **Retire equal-card-as-primary:** Keep legacy `.bridge-kpi*` rules harmless or unused; primary results chrome is kill report:
       - `#bridge-kpi-grid.bridge-kill-report` / `.bridge-kill-report` — stack flow, not `repeat(auto-fit, minmax(120px, 1fr))` equal peers for the three heroes.
       - `.bridge-kill-flow` — flex/grid row with wrap; left-aligned mission readout; gap; baseline alignment.
       - `.bridge-kill-stat--raw|killed|kept` — **asymmetric** font sizes (kept largest), heat on killed (ember/orange-red), gold/orange on kept, quieter stone on raw. Use `--font-display` / Anton stack already used by KPI values.
       - Labels: uppercase, tracked, micro stone.
       - `.bridge-kill-arrow` muted gold/stone.

    2. **Reasons:** `.bridge-kill-reasons` flex wrap chips; smaller than heroes; count + label.

    3. **Proof chips:** `.bridge-proof-chips` / `.bridge-proof-chip` — HUD strip, quieter than heroes; duration/reuse/independence readable at a glance.

    4. **Samples:** `.bridge-kept-samples` horizontal wrap; compact dossier cards; reuse `.bridge-tag` colors if present.

    5. **Mobile:** at 390px width, no forced horizontal page overflow; flow may wrap; stats remain readable.

    6. **Motion:** static only (no required count-up). If any transition added, gate with `prefers-reduced-motion: reduce`.

    7. Bump `bridge.css?v=` in `public/bridge.html` (and ensure js cache-bust from Task 1 is applied).

    Do **not** introduce green SaaS success island for KEPT if avoidable (prefer cream/gold/orange survivor heat).
  </action>
  <acceptance_criteria>
    - `public/css/bridge.css` contains `.bridge-kill-flow` and `.bridge-kill-stat`
    - `public/css/bridge.css` contains `.bridge-kill-stat--kept` and `.bridge-kill-stat--killed` (or equivalent modifier classes used by JS)
    - `public/css/bridge.css` contains `.bridge-proof-chip` or `.bridge-proof-chips`
    - Equal auto-fit KPI grid is no longer the only/primary kill presentation (kill-report classes exist)
    - `bridge.css?v=` and `bridge.js?v=` bumped in bridge.html vs pre-plan values
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>KILL-01 hierarchy + KILL-02 proof chip contracts green (Stage language may still be Plan 03). Visual HUD is asymmetric kill-flow, not equal KPI tiles.</done>
</task>

</tasks>

<verification>
- Focused: `node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js`
- No engine test changes required
- After public/ edits in execute session: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` (Plan 03 may own final live gate if execute batches)
</verification>

<success_criteria>
- Results open as RAW → KILLED → KEPT kill-rate report with reason + proof chips
- Train still refreshes KEPT via renderKpis
- LIST/EFF static locks green
</success_criteria>

<output>
After completion, create `.planning/phases/65-kill-rate-scrub-report/65-02-SUMMARY.md`
</output>
