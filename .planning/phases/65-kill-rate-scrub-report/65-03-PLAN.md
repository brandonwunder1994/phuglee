---
phase: 65-kill-rate-scrub-report
plan: 03
type: execute
wave: 3
depends_on:
  - "65-02"
files_modified:
  - public/bridge.html
  - public/js/bridge.js
  - public/css/bridge.css
  - tests/bridge-kill-rate-scrub.test.js
autonomous: true
requirements:
  - KILL-03
  - KILL-01
  - KILL-02
must_haves:
  truths:
    - "Primary post-scrub CTA remains #bridge-save-list labeled Save list (primary/orange)"
    - "Stage language appears in save panel heading or lead without renaming the Save list button"
    - "Save panel is elevated adjacent to the kill report (not only after long table scroll as the sole placement)"
    - "Preview CSV stays secondary ghost; no Analyze push CTAs"
    - "Workflow strip + independence teaching preserved"
    - "Full kill-rate suite + LIST/EFF/IND locks green; verify-live exits 0 after public edits"
  artifacts:
    - path: "public/bridge.html"
      provides: "Elevated save/stage placement + Stage copy; stable Save list / Preview CSV IDs"
      contains: "bridge-save-list"
    - path: "public/css/bridge.css"
      provides: "Save panel visual dominance near report if needed"
      contains: "bridge-save-panel|bridge-kill-report"
    - path: "tests/bridge-kill-rate-scrub.test.js"
      provides: "KILL-03 Stage + elevation contracts green"
      contains: "KILL-03"
  key_links:
    - from: "public/bridge.html #bridge-save-list"
      to: "primary post-scrub operator action"
      via: "phuglee-btn-primary + Save list label"
      pattern: "id=\"bridge-save-list\".*Save list"
    - from: "public/bridge.html #bridge-export-csv"
      to: "secondary preview"
      via: "ghost/secondary Preview CSV"
      pattern: "id=\"bridge-export-csv\".*Preview CSV"
    - from: "kill report"
      to: "save panel"
      via: "DOM order or sticky stage adjacency after report"
      pattern: "bridge-kpi-grid|bridge-kill-report|bridge-save-panel"
---

<objective>
Elevate Save list / Stage as the primary post-scrub operator action adjacent to the kill-rate win; keep Preview CSV secondary; freeze Analyze boundary; green the full Phase 65 suite and live gate.

Purpose: KILL-03 — hierarchy without a primary CTA is incomplete; Save must win the scroll and weight after the scrub report.

Output: HTML/CSS (and light JS if needed) layout + Stage voice + final test/live verification.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-kill-rate-scrub-report/65-CONTEXT.md
@.planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md
@.planning/phases/65-kill-rate-scrub-report/65-02-SUMMARY.md
@.planning/phases/65-kill-rate-scrub-report/65-VALIDATION.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@tests/bridge-kill-rate-scrub.test.js
@tests/bridge-list-factory-ux.test.js
@tests/bridge-efficiency-path.test.js
@tests/bridge-independence.test.js
@AGENTS.md

<interfaces>
<!-- CTA locks (never break) -->
| Control | ID | Label | Weight |
| Save list | #bridge-save-list | **Save list** (exact) | primary orange |
| Preview CSV | #bridge-export-csv | **Preview CSV** (exact) | ghost/secondary |
| Forbidden | — | Send/Push/Import to Analyze… | absent |

<!-- Stage voice (button stays Save list) -->
Save panel heading and/or lead must include Stage language, e.g.:
- H3: `Stage this scrub` or `Stage filtered list` (replace or extend "Save filtered list")
- Lead may open with: `Stage this kept list for multi-city shift.` then existing enrichment / Analyze-independence teaching.
Keep: `Nothing is sent to Analyze` / nothing-sent independence intent from LIST-03.

<!-- Layout elevation (pick one; prefer A) -->
**A (preferred):** Move `#bridge-save-panel` in `bridge.html` to sit **immediately after** kill report hosts (`#bridge-results-meta`, `#bridge-kpi-grid`, `#bridge-stub-note`) and **before** `#bridge-train-wrap`. Keep workflow strip near save or just above save. Table/toolbar can remain below train wrap (review after stage).

**B (acceptable):** Keep DOM order but add a compact stage strip after kill report that contains the **same** `#bridge-save-list` button (single instance — do not duplicate IDs). Prefer A to avoid ID duplication.

Do **not** hide save when rows exist (existing `setHidden(savePanel, !showTable)` stays).

<!-- Preview -->
Leave `#bridge-export-csv` in toolbar as secondary; do not promote to primary orange; do not rename to Export CSV.

<!-- Phase boundaries -->
- No Train theater pivot (66)
- No Analyze auto-push
- No engine rewrite
- No data wipe
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elevate Save/Stage adjacent to kill report + Stage copy</name>
  <files>public/bridge.html, public/css/bridge.css, public/js/bridge.js</files>
  <read_first>
    - public/bridge.html (results panel ~235–358: kpi grid, train wrap, toolbar, table, workflow strip, save panel)
    - public/js/bridge.js (setHidden savePanel in renderResults; saveCurrentList)
    - .planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md (CTA hierarchy + layout problem)
    - tests/bridge-list-factory-ux.test.js (Save list / Preview CSV / workflow / teaching pack)
  </read_first>
  <action>
    1. **DOM elevation (Option A):** In `public/bridge.html` `#bridge-results-panel`, reorder so after:
       - H2 Results &amp; save
       - `#bridge-results-meta`
       - `#bridge-kpi-grid`
       - `#bridge-stub-note`
       place:
       - `#bridge-workflow-strip` (optional move with save)
       - `#bridge-save-panel` (entire section, stable IDs)
       then existing `#bridge-train-wrap`, toolbar, table, pagination, attach panel.

    2. **Stage copy:** Update save panel H3 and/or `.bridge-panel-lead` to include **Stage** language while button remains exactly **Save list**. Preserve enrichment + Analyze independence teaching (LIST-03). Example lead:
       `Stage this kept list for your multi-city shift. Download later from Saved lists for external enrichment. Nothing is sent to Analyze.`

    3. **CSS:** Ensure save panel reads as fire primary near report — spacing, optional subtle heat border; min-height CTA ≥ 44px (existing buttons). Do not make Preview CSV look primary.

    4. **JS:** Confirm `setHidden(savePanel, !showTable)` and `getElementById('bridge-save-list')` still work after move (IDs unchanged). No auto-save. No Analyze CTA.

    5. If sample dossiers were deferred in Plan 02, ship 3-card strip now (optional KILL-01); otherwise leave as-is.

    6. Cache-bust `bridge.html` script/link query params after edits.
  </action>
  <acceptance_criteria>
    - `id="bridge-save-list"` and `Save list` still in HTML with primary classes
    - `id="bridge-export-csv"` and `Preview CSV` still present; no `>Export CSV<`
    - HTML save panel region includes `Stage` (heading or lead)
    - In HTML source order, `id="bridge-save-panel"` appears before `id="bridge-train-wrap"` (elevation)
    - Workflow strip still contains `Process → (Train) → Save list → Download`
    - No banned Analyze push strings
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js</automated>
  </verify>
  <done>Save/Stage elevated next to kill report; Preview secondary; LIST teaching intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green remaining KILL tests + regression suite + verify-live</name>
  <files>tests/bridge-kill-rate-scrub.test.js</files>
  <read_first>
    - tests/bridge-kill-rate-scrub.test.js (after Plan 01/02)
    - public/bridge.html (post Task 1 order + Stage copy)
    - public/js/bridge.js (kill report final)
    - AGENTS.md (verify-live mandatory after public/)
  </read_first>
  <behavior>
    - All KILL-01/02/03 tests in bridge-kill-rate-scrub.test.js pass
    - LIST factory + efficiency path + independence suites pass
    - verify-live.ps1 exits 0
  </behavior>
  <action>
    1. Update `tests/bridge-kill-rate-scrub.test.js` if needed so KILL-03 Stage + elevation asserts match shipped markup (e.g. assert `Stage` near save; optionally assert save-panel index &lt; train-wrap index in HTML).

    2. Run focused then broader regression:
       - `node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js`
       - `node --test tests/bridge-independence.test.js` (or full independence suite path used by project)
       - Prefer `npm test` before claiming phase plan complete (executor may note long runtime).

    3. From project root after public/ edits:
       ```powershell
       powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
       ```
       If fail: `scripts\restart.ps1` then re-verify. Never leave blocking `node server.js` in agent shell.

    4. Do not wipe filter-lists or bridge-brain.
  </action>
  <acceptance_criteria>
    - `node --test tests/bridge-kill-rate-scrub.test.js` exits 0
    - `node --test tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js` exits 0
    - verify-live.ps1 exits 0 (document in SUMMARY)
    - No production engine files modified in this phase
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-kill-rate-scrub.test.js tests/bridge-list-factory-ux.test.js tests/bridge-efficiency-path.test.js tests/bridge-independence.test.js</automated>
  </verify>
  <done>Phase 65 automated contracts green; live server healthy; KILL-01–03 success criteria met for execute handoff.</done>
</task>

</tasks>

<verification>
- KILL suite green
- LIST + EFF + IND green
- `scripts/verify-live.ps1` exit 0
- Manual spot-check (optional): process a real file → RAW/KILLED/KEPT visible; Save near report; Preview secondary
</verification>

<success_criteria>
- KILL-03 primary Save/Stage path true in DOM + copy
- Analyze boundary preserved
- Live preview healthy after public edits
</success_criteria>

<output>
After completion, create `.planning/phases/65-kill-rate-scrub-report/65-03-SUMMARY.md`
</output>
