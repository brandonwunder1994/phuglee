---
phase: 65-kill-rate-scrub-report
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/bridge-kill-rate-scrub.test.js
autonomous: true
requirements:
  - KILL-01
  - KILL-02
  - KILL-03
must_haves:
  truths:
    - "Wave 0 static suite exists for KILL-01 RAW/KILLED/KEPT hierarchy labels and kill-reason breakdown strings"
    - "Wave 0 contracts lock KILL-02 proof chips: Format reused, scrub duration, nothing sent to Analyze independence wording"
    - "Wave 0 contracts lock KILL-03 Save list primary + Preview CSV secondary + banned Analyze push CTAs"
    - "Carry-forward LIST/EFF anchors remain asserted so kill-report work cannot rename locked CTAs"
  artifacts:
    - path: "tests/bridge-kill-rate-scrub.test.js"
      provides: "KILL-01/02/03 static contracts (RED until Plan 02/03 green the production surface)"
      contains: "KILL-01"
      min_lines: 80
  key_links:
    - from: "tests/bridge-kill-rate-scrub.test.js"
      to: "public/js/bridge.js"
      via: "static scan for RAW/KILLED/KEPT kill-report + proof chip strings"
      pattern: "RAW|KILLED|KEPT|Format reused|nothing was sent to Analyze"
    - from: "tests/bridge-kill-rate-scrub.test.js"
      to: "public/bridge.html"
      via: "Save list + Preview CSV + bridge-kpi-grid host IDs"
      pattern: "bridge-save-list|bridge-export-csv|bridge-kpi-grid"
    - from: "tests/bridge-kill-rate-scrub.test.js"
      to: "public/css/bridge.css"
      via: "kill-report CSS class contracts (asymmetric hierarchy, not equal KPI grid only)"
      pattern: "bridge-kill-"
---

<objective>
Wave 0 TDD: create `tests/bridge-kill-rate-scrub.test.js` that locks KILL-01–03 surface contracts and freezes LIST/EFF CTA carry-forwards.

Purpose: Drive Phase 65 kill-rate report reforge with red contracts so equal KPI tiles cannot ship as the primary results readout and Save/Preview/independence cannot regress.

Output: New static test file only. **CTA carry-forwards GREEN today; hierarchy/proof contracts intentionally RED** until Plan 02/03. No production code in this plan.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-kill-rate-scrub-report/65-CONTEXT.md
@.planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md
@.planning/phases/65-kill-rate-scrub-report/65-VALIDATION.md
@tests/bridge-list-factory-ux.test.js
@tests/bridge-efficiency-path.test.js
@public/js/bridge.js
@public/bridge.html
@public/css/bridge.css
@AGENTS.md

<interfaces>
<!-- Mirror Phase 56/59 static-test style (node:test + fs source scans). Zero new packages. -->

```js
// tests/bridge-kill-rate-scrub.test.js skeleton
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(ROOT, 'public', 'bridge.html'), 'utf8');
const js = fs.readFileSync(path.join(ROOT, 'public', 'js', 'bridge.js'), 'utf8');
const css = fs.readFileSync(path.join(ROOT, 'public', 'css', 'bridge.css'), 'utf8');

const BANNED_CTAS = [
  'Send to Analyze',
  'Push to Analyze',
  'Import to Analyzer',
  'Open in Analyze',
  'Push to Analyzer'
];
```

<!-- As-built GREEN today (carry-forward — must stay green forever) -->
- `#bridge-save-list` + text `Save list` + primary classes in HTML
- `#bridge-export-csv` + text `Preview CSV` (not `>Export CSV<`)
- `#bridge-kpi-grid` host still present (reforge content, keep id)
- `nothing was sent to Analyze` still in bridge.js (independence)
- No banned Analyze push CTAs in html/js
- `function renderKpis` still exists (or alias) so Train path `renderKpis(lastResult.stats)` keeps working
- `Format reused` string already present (EFF-01) — keep asserting

<!-- RED until Plan 02 (KILL-01 / KILL-02 production surface) -->
1. bridge.js emits display-scale hierarchy labels: literal `RAW`, `KILLED`, `KEPT` (uppercase mission labels in kill-report HTML)
2. bridge.js builds kill-reason breakdown from `discardReasons` (source contains `discardReasons` inside renderKpis / kill-report builder)
3. bridge.js proof chips include duration language matching `/scrub|s scrub|Scrubbed/i` OR format duration chip using `durationMs` near kill report (not only buried meta sentence)
4. bridge.css defines kill-report hierarchy classes: at least `.bridge-kill-flow` and `.bridge-kill-stat` (or `.bridge-kill-report`) — not only `.bridge-kpi-grid` equal auto-fit cards as the sole results KPI chrome
5. Equal peer KPI template as primary is retired: `renderKpis` no longer builds only the 5–6 equal `bridge-kpi` card map as the sole output (assert kill-flow markers present)

<!-- RED until Plan 03 (KILL-03 elevation + optional samples) -->
6. Stage language near save panel: HTML or JS contains `Stage` in results/save teaching (button text remains **Save list**)
7. Optional dossiers: bridge.js contains `bridge-kept-samples` or `bridge-kept-sample` class string when shipping samples (Plan 03 may mark green; if Plan 02 ships samples, 02 makes green)

<!-- Do NOT assert -->
- Engine processUpload rewrite
- Train theater (phase 66)
- Live scrub feed (phase 64)
- Exact pixel sizes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create KILL-01/02/03 static suite with RED hierarchy + GREEN CTA locks</name>
  <files>tests/bridge-kill-rate-scrub.test.js</files>
  <read_first>
    - .planning/phases/65-kill-rate-scrub-report/65-RESEARCH.md (phase requirements + display math + proof chips + CTA table)
    - tests/bridge-list-factory-ux.test.js (static scan style, BANNED_CTAS)
    - tests/bridge-efficiency-path.test.js (EFF Format reused + Save list carry-forward)
    - public/js/bridge.js (renderKpis ~1645; renderResults ~2254)
  </read_first>
  <behavior>
    - GREEN: HTML has id bridge-save-list, Save list, primary; id bridge-export-csv, Preview CSV; no Export CSV button label; id bridge-kpi-grid
    - GREEN: no banned Analyze CTAs in html/js
    - GREEN: js contains nothing was sent to Analyze; Format reused; function renderKpis
    - RED until 02: js contains RAW, KILLED, KEPT as kill-report labels (mission hierarchy)
    - RED until 02: js renderKpis path (or kill-report helper called from it) uses discardReasons for reason chips
    - RED until 02: css contains .bridge-kill-flow (or .bridge-kill-report) and .bridge-kill-stat
    - RED until 02: css kill-stat kept uses gold/orange heat (not only green success); at minimum class bridge-kill-stat--kept exists
    - RED until 03: Stage language present near save (Stage in save panel heading/lead or kill-report CTA strip) while button remains Save list
    - Optional RED/soft: bridge-kept-sample string for dossier strip
  </behavior>
  <action>
    Create `tests/bridge-kill-rate-scrub.test.js` with node:test + assert/strict + fs reads of public/bridge.html, public/js/bridge.js, public/css/bridge.css.

    Group tests with titles containing `KILL-01`, `KILL-02`, `KILL-03`, and `carry-forward` as appropriate:

    **Carry-forward / KILL-03 locks (must pass on current main):**
    1. `KILL-03 carry-forward: Save list primary CTA` — match `id="bridge-save-list"`, `Save list`, `phuglee-btn-primary`
    2. `KILL-03 carry-forward: Preview CSV secondary` — match `id="bridge-export-csv"`, `Preview CSV`; assert `>Export CSV<` absent
    3. `KILL-03: no Analyze push CTAs` — ban list from interfaces
    4. `carry-forward: independence wording in bridge.js` — `/nothing was sent to Analyze/`
    5. `carry-forward: Format reused string (EFF)` — `/Format reused/`
    6. `carry-forward: renderKpis still defined` — `/function\s+renderKpis/`
    7. `carry-forward: kill report host id` — `id="bridge-kpi-grid"`

    **KILL-01 hierarchy (expect FAIL until Plan 02):**
    8. `KILL-01: RAW → KILLED → KEPT labels in bridge.js` — js must match `/\bRAW\b/`, `/\bKILLED\b/`, `/\bKEPT\b/` inside kill-report builder strings (not only comments). Prefer also matching class markers `bridge-kill-stat--raw` or similar.
    9. `KILL-01: discardReasons drives kill-reason breakdown` — inside a slice of `function renderKpis` (± helpers named `renderKillReport` / `buildKillReasons` if present), assert `discardReasons` is referenced.
    10. `KILL-01: CSS kill-flow hierarchy classes` — css matches `.bridge-kill-flow` OR `.bridge-kill-report`, and `.bridge-kill-stat`

    **KILL-02 proof chips (expect FAIL until Plan 02):**
    11. `KILL-02: proof chip / duration surface` — js matches `bridge-proof` OR (`durationMs` used to build a chip/string with `s` scrub / `Scrubbed` / `scrub` near results, not only the old single meta sentence). Practical assert: `/bridge-proof-chip|proof-chips|Scrubbed in|s scrub/i` in js.
    12. `KILL-02: format reuse still chip-capable` — `/Format reused/` remains (already green; keep)

    **KILL-03 Stage voice (expect FAIL until Plan 03 if not shipped in 02):**
    13. `KILL-03: Stage language near save without renaming button` — html or js matches `/Stage/` in results/save teaching (e.g. Stage this scrub / Stage list language) AND button text remains `Save list` (not `Stage list` only).

    **Optional soft contract (document as optional if samples deferred):**
    14. Prefer including `KILL-01 optional: kept sample dossiers class` matching `/bridge-kept-sample/` — mark test title with `optional` only if you use test.skip; **default: include as RED until Plan 02 or 03 ships samples** (research says ship if cheap).

    Do **not** edit production files. Do **not** wipe data. Do **not** add npm packages.
  </action>
  <acceptance_criteria>
    - File `tests/bridge-kill-rate-scrub.test.js` exists and contains `KILL-01`, `KILL-02`, `KILL-03`
    - File contains `BANNED_CTAS` or explicit banned Analyze push string asserts
    - File reads `bridge.html`, `bridge.js`, and `bridge.css`
    - Automated command below runs (may report failures on RED contracts — that is expected)
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-kill-rate-scrub.test.js</automated>
  </verify>
  <done>
    Wave 0 suite exists. Carry-forward CTA/independence tests pass on current tree. Hierarchy/proof (and Stage/sample) contracts fail until Plan 02/03 — failures prove the RED gate is real.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-kill-rate-scrub.test.js` executes
- Document in SUMMARY which tests are green vs red (expected red list: KILL-01 hierarchy, KILL-02 proof structure, KILL-03 Stage, samples)
- No production edits
</verification>

<success_criteria>
- Static KILL contracts file ready to drive Plan 02/03
- LIST/EFF CTA locks cannot be broken silently by kill-report work
</success_criteria>

<output>
After completion, create `.planning/phases/65-kill-rate-scrub-report/65-01-SUMMARY.md`
</output>
