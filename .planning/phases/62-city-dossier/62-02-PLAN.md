---
phase: 62-city-dossier
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - public/bridge.html
  - public/css/bridge.css
  - public/js/bridge.js
  - tests/bridge-city-dossier.test.js
autonomous: true
requirements:
  - CITY-01
  - CITY-02
must_haves:
  truths:
    - "After selecting a city, operator sees a city dossier with prior attaches, last scrub, lists staged for that city, and relevant status — not dual selects in a void"
    - "Type panel still reveals immediately on city select (happy path pick city → type → drop file) — history fetch does not block type"
    - "No-list outcomes live in a secondary scrap/drawer, default collapsed — not a 5-radio wall competing with step 1"
    - "saveCityOutcome POST payload unchanged (cityId, response_status, request_type, notes, response_raw); five radio values and water_shutoff request_type preserved"
    - "Dossier clears/hides on state reset, empty city, and post-save import-area reset paths"
  artifacts:
    - path: "public/bridge.html"
      provides: "Dossier shell + demoted outcome drawer markup"
      contains: "bridge-city-dossier"
    - path: "public/css/bridge.css"
      provides: "Case-file dossier + collapsed scrap styles"
      contains: "bridge-city-dossier|bridge-outcome-drawer"
    - path: "public/js/bridge.js"
      provides: "Dossier compose on city select; eager history; drawer toggle; preserved outcome save"
      contains: "renderCityDossier|buildDossierModel|loadCityDossierHistory"
  key_links:
    - from: "public/js/bridge.js onCityChange"
      to: "GET /api/bridge/history/:cityId"
      via: "async loadCityDossierHistory / shared loadHistory with cityId race guard"
      pattern: "loadCityDossierHistory|loadHistory\\("
    - from: "public/js/bridge.js renderCityDossier"
      to: "savedLists"
      via: "filter by cityId"
      pattern: "cityId"
    - from: "public/js/bridge.js saveCityOutcome"
      to: "POST /api/bridge/city-outcome"
      via: "unchanged JSON body fields"
      pattern: "response_status|request_type|response_raw"
    - from: "public/bridge.html #bridge-city-outcome"
      to: "secondary drawer"
      via: "collapsed by default after city select"
      pattern: "bridge-outcome-drawer"
---

<objective>
Ship CITY-01 city ops dossier and CITY-02 demoted no-list outcomes on `/bridge` by composing existing history + lists APIs client-side and relocating the five-radio wall into a secondary scrap/drawer — without new backend routes or breaking City Tracker save.

Purpose: Happy path is pick city → type → drop file with case-file proof; exception path is secondary scrap.

Output: Updated `public/bridge.html`, `public/css/bridge.css`, `public/js/bridge.js`; `tests/bridge-city-dossier.test.js` fully green; suite + verify-live green.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-city-dossier/62-CONTEXT.md
@.planning/phases/62-city-dossier/62-RESEARCH.md
@.planning/phases/62-city-dossier/62-VALIDATION.md
@.planning/phases/62-city-dossier/62-01-SUMMARY.md
@public/bridge.html
@public/js/bridge.js
@public/css/bridge.css
@public/collect.html
@public/index.html
@lib/bridge-api.js
@lib/bridge-list-store.js
@AGENTS.md

<interfaces>
<!-- Locked decisions from CONTEXT — implement exactly -->
- On city select: case-file panel with prior attaches, last scrub signals, lists staged for city, relevant status
- Data from existing APIs: GET /api/bridge/history/:cityId, GET /api/bridge/lists (in-memory savedLists), city-outcome POST only
- Five outcomes move to secondary scrap/drawer — not primary wall
- Happy path: pick city → (type) → drop file
- Deferred: multi-city queue (67), global idle KPIs (63)

<!-- Discretion (planner defaults) -->
- Layout: **Hybrid** — dossier summary **inline** under city selects / city-actions; outcome demotion in **collapsed drawer/scrap** under dossier (Collect-style secondary). If phase 61 already shipped a scrap rail with stable hooks, prefer putting demoted outcomes there; else inline `<details>` / button+panel drawer is correct.
- Empty copy (ops voice): `No attaches yet for this city. Drop a file or log a tracker reply.`
- Keep `#bridge-history-dialog` for full history detail; dossier shows summary + last scrub; “Attachment history” may become “Full history” label optional.
- Outcome save button: prefer `phuglee-btn phuglee-btn-primary` (DESK-06 align) while keeping `#bridge-outcome-save`.

<!-- DOM contract to preserve -->
| ID / name | Role |
|-----------|------|
| `#bridge-city-outcome` | Panel container (may move under drawer) |
| `input[name="bridge-city-outcome"]` | Five radios — exact values |
| `#bridge-other-source-wrap` / `#bridge-other-source-notes` | Notes |
| `#bridge-outcome-type` | request_type: code_violation \| water_shutoff |
| `#bridge-outcome-save` | Save to City Tracker |
| `#bridge-outcome-status` | Inline status |
| `saveCityOutcome`, `syncCityOutcomeUi`, `resetCityOutcomeUi` | Keep working |

<!-- saveCityOutcome body — MUST stay -->
```js
{
  cityId: selectedCity.id,
  response_status: responseStatus,
  request_type: requestType, // code_violation | water_shutoff
  notes,
  response_raw: notes
}
```

<!-- Dossier model (client-only) -->
```js
function buildDossierModel(selectedCity, history, lists) {
  const attaches = Array.isArray(history) ? history : [];
  const lastScrub = attaches.length
    ? [...attaches].sort((a, b) =>
        String(b.attached_at || '').localeCompare(String(a.attached_at || ''))
      )[0]
    : null;
  const stagedLists = (lists || []).filter(
    (l) => String(l.cityId || '') === String(selectedCity.id)
  );
  const listStatus = {
    ready: stagedLists.filter((l) => (l.status || 'ready') === 'ready').length,
    downloaded: stagedLists.filter((l) => l.status === 'downloaded').length,
    recordCount: stagedLists.reduce((n, l) => n + (Number(l.recordCount) || 0), 0)
  };
  return { city: selectedCity, attaches, lastScrub, stagedLists, listStatus };
}
```

<!-- Race guard -->
```js
const cityId = selectedCity.id;
// after await history:
if (selectedCity?.id !== cityId) return;
```

<!-- Do NOT -->
- New GET /api/bridge/dossier
- Block type panel on history await
- Rename radio values or request_type strings
- Wipe filter-lists on outcome save
- Rewrite processUpload / bridge-engine
- Implement phase 63 idle global metrics or 67 shift queue
- Import territory map JS (visual language only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dossier shell + demote outcome wall into scrap/drawer (HTML)</name>
  <files>public/bridge.html</files>
  <read_first>
    - public/bridge.html (city panel ~96–171; history dialog ~400+)
    - public/collect.html (`.collect-desk-tracker` secondary scrap pattern ~82–86)
    - public/index.html (`.home-territory-dossier` chrome only ~579–582)
    - .planning/phases/62-city-dossier/62-RESEARCH.md (Patterns 5–6, Exact Files table)
    - tests/bridge-city-dossier.test.js (contracts from Plan 01)
  </read_first>
  <action>
    Edit **city step markup** in `public/bridge.html` only:

    1. **Add dossier shell** after city selects / inside city step, visible when city selected (wire show/hide in Task 3; HTML can start `hidden`):
       - Container: `#bridge-city-dossier` with classes e.g. `bridge-city-dossier` (case-file chrome — stamp/label “Case file” optional).
       - Sections (ids or stable classes matching tests):
         - **Last scrub** — `#bridge-dossier-last-scrub` (placeholder “Loading…” / empty later)
         - **Prior attaches** — `#bridge-dossier-attaches` (count/summary + keep link to full history via existing `#bridge-history-open`)
         - **Lists staged** — `#bridge-dossier-lists` (ready/downloaded/record counts)
         - Optional status line can live inside lists section
       - Empty ops copy region for full-empty state (filled by JS).

    2. **Relocate outcome wall into secondary scrap/drawer:**
       - Wrap existing `#bridge-city-outcome` (keep that id on the panel that contains radios) inside a drawer shell e.g.:
         ```html
         <div class="bridge-outcome-drawer" id="bridge-outcome-drawer" data-bridge-outcome-drawer hidden>
           <button type="button" id="bridge-outcome-drawer-toggle" class="…" aria-expanded="false">
             City replied — no usable list
           </button>
           <div id="bridge-city-outcome" class="bridge-city-outcome" hidden>
             … existing radios, notes, type, save — IDs unchanged …
           </div>
         </div>
         ```
       - Or use `<details class="bridge-outcome-drawer" id="bridge-outcome-drawer">` with `<summary>` as collapsed entry — still keep `#bridge-city-outcome` and all radio IDs/names/values.

    3. **Preserve** all five radio `value`s, `name="bridge-city-outcome"`, notes wrap, outcome type options (`code_violation`, `water_shutoff`), `#bridge-outcome-save`, `#bridge-outcome-status`.

    4. **Keep** `#bridge-city-actions` + `#bridge-history-open` (label may read “Full history” or stay “Attachment history”).

    5. **Outcome save button classes:** prefer `phuglee-btn phuglee-btn-primary` (may keep bridge-btn if needed for CSS, but DESK-06 prefers phuglee).

    6. Bump `bridge.js?v=` and `bridge.css` cache query if present.

    Do not touch process/results/lists inventory panels (phase 67).
  </action>
  <acceptance_criteria>
    - `id="bridge-city-dossier"` present in bridge.html
    - `bridge-dossier-last-scrub` and `bridge-dossier-lists` present as id or class
    - `bridge-outcome-drawer` present; five radio values unchanged
    - `#bridge-outcome-type` still has water_shutoff not water_shut_off
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('public/bridge.html','utf8');const need=['bridge-city-dossier','bridge-dossier-last-scrub','bridge-dossier-lists','bridge-outcome-drawer','name=\"bridge-city-outcome\"','needs_clarification','approved_bad_data','water_shutoff','id=\"bridge-outcome-save\"'];for(const n of need){if(!h.includes(n)){console.error('missing',n);process.exit(1);}}const ot=h.match(/id=\"bridge-outcome-type\"[\\s\\S]*?<\\/select>/);if(ot&&ot[0].includes('water_shut_off')&&!ot[0].includes('water_shutoff')){console.error('outcome type uses water_shut_off');process.exit(2);}console.log('html-gate-ok');"</automated>
  </verify>
  <done>HTML has dossier shell and demoted outcome drawer; tracker control IDs preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Case-file dossier + collapsed scrap styles (CSS)</name>
  <files>public/css/bridge.css</files>
  <read_first>
    - public/css/bridge.css (`.bridge-city-outcome` ~373–443 and surrounding city step styles)
    - public/css/distress-collect-hub.css (`.collect-desk-tracker` demotion ~137+)
    - public/index.html / related CSS for `.home-territory-dossier` visual cues only
    - .planning/phases/62-city-dossier/62-RESEARCH.md (Pattern 7 layout discretion)
  </read_first>
  <action>
    Extend `public/css/bridge.css`:

    1. **`.bridge-city-dossier`** — case-file feel: subtle border, tight padding, cream stamp/eyebrow optional, not a huge marketing card. Hierarchy below dual selects, above or adjacent to type step reveal. Separate sections with small labels (Last scrub / Attaches / Staged lists).

    2. **`.bridge-outcome-drawer`** — secondary scrap: reduced visual weight vs type cards; toggle looks like Collect tracker scrap (muted border, not five equal primary cards on first paint).

    3. **Collapsed default:** when drawer closed, **do not** show full `.bridge-outcome-options` radiogroup. Expanded state only after toggle/details open.

    4. **Existing outcome option styles** may remain for expanded state; avoid making them dominate step 1.

    5. Respect mobile: dossier stacks; CTAs ≥ 44px where interactive; no horizontal overflow.

    6. Prefer existing `--phuglee-*` tokens; no new design system package.
  </action>
  <acceptance_criteria>
    - bridge.css contains `.bridge-city-dossier` and `.bridge-outcome-drawer` (or equivalent selectors used in HTML)
    - No removal of core outcome option styles needed for expanded path
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('public/css/bridge.css','utf8');if(!/bridge-city-dossier/.test(c))process.exit(1);if(!/bridge-outcome-drawer/.test(c))process.exit(2);console.log('css-gate-ok');"</automated>
  </verify>
  <done>Dossier + scrap drawer styles land with secondary hierarchy vs type step.</done>
</task>

<task type="auto">
  <name>Task 3: Compose dossier on city select; drawer behavior; preserve outcome save + resets</name>
  <files>public/js/bridge.js, tests/bridge-city-dossier.test.js</files>
  <read_first>
    - public/js/bridge.js (`onCityChange` ~1594; `saveCityOutcome` ~1547; `loadHistory`/`renderHistory` ~2206–2251; `resetDownstream` ~1282; `resetImportAreaAfterSave` ~1800; `savedLists` / `loadSavedLists` / `renderSavedLists`)
    - public/bridge.html (after Task 1)
    - tests/bridge-city-dossier.test.js
    - .planning/phases/62-city-dossier/62-RESEARCH.md (Pitfalls 1–9)
  </read_first>
  <action>
    Implement client composition in `public/js/bridge.js`:

    1. **Cache elements:** `#bridge-city-dossier`, facet nodes, `#bridge-outcome-drawer`, `#bridge-outcome-drawer-toggle` (if button pattern).

    2. **`buildDossierModel` + `renderCityDossier(model)`** per research — sections:
       - Last scrub: type label · kept count · attached date · filename (or empty)
       - Prior attaches: count + short summary; rely on history dialog for full list/downloads
       - Staged lists: filter `savedLists` by `String(l.cityId)===String(cityId)` — show ready/downloaded counts + total records; honest empty if none
       - Empty: ops copy when no history and no lists

    3. **`loadCityDossierHistory(cityId)`** async:
       - Set historyState loading in dossier
       - `fetchJson('/api/bridge/history/'+encodeURIComponent(cityId))` (reuse `loadHistory` internals or share one fetch that also updates modal list when open)
       - Race guard: ignore if `selectedCity?.id !== cityId`
       - Derive lastScrub; call `renderCityDossier`
       - Soft error: dossier shows error/empty; **do not** break type panel
       - After successful attach, existing `loadHistory` path should also refresh dossier if city still selected

    4. **`onCityChange` order (critical):**
       - `resetDownstream('city')` as today
       - Set `selectedCity`
       - **Immediately** `setHidden(typePanel, false)`, city actions visible, `setPipelineStep('type')`
       - Show dossier; `renderCityDossier` with lists immediately (history may still load)
       - Kick `loadCityDossierHistory(selectedCity.id)` without awaiting before type reveal
       - `resetCityOutcomeUi()`
       - **CITY-02:** drawer **closed** by default — do not force-open radiogroup; hide expanded outcome body until toggle
       - Clear city: hide dossier + drawer + outcome; reset UI

    5. **Drawer toggle:** open/close expands `#bridge-city-outcome`; set `aria-expanded`; do not auto-submit.

    6. **Preserve `saveCityOutcome` payload exactly** — only rebind if nodes moved; keep POST fields and validation (`other_source` notes required).

    7. **Resets:** hide/clear dossier in `resetDownstream('state')`, empty city branch, and `resetImportAreaAfterSave` (same places as `cityOutcomePanel` / `cityActions`). Avoid ghost dossier after save.

    8. **Lists refresh:** when `savedLists` updates (save/rename/delete/load), if `selectedCity` set, re-render dossier list facet only (no full page rewrite).

    9. **Make `tests/bridge-city-dossier.test.js` GREEN** — adjust only if Plan 01 used alternate allowed names that you also implement; prefer matching research names (`renderCityDossier`, `loadCityDossierHistory`, `buildDossierModel`).

    10. Do **not** modify `lib/bridge-engine/*` or wipe data stores.
  </action>
  <acceptance_criteria>
    - onCityChange shows type panel without awaiting history
    - Dossier filters lists by cityId; last scrub from newest attached_at
    - Outcome drawer default collapsed after city select
    - saveCityOutcome body fields unchanged
    - tests/bridge-city-dossier.test.js exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-city-dossier.test.js tests/bridge-api-handlers.test.js; npm test</automated>
  </verify>
  <done>Dossier composes on city select; outcomes demoted; payload safe; static + suite green.</done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-city-dossier.test.js` exit 0
- city-outcome + history handler tests still green
- `npm test` exit 0
- After public/ edits: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exit 0
- Manual smoke (executor notes): open `/bridge` → pick city → dossier visible, type cards visible, outcome radios hidden until scrap open → open scrap → save one status (if Forge session available)
</verification>

<success_criteria>
- CITY-01: city select opens ops dossier (attaches / last scrub / staged lists / status)
- CITY-02: no-list outcomes secondary scrap/drawer; happy path file scrub unobstructed
- No new dossier API; history + lists reused
- Outcome save payload preserved
- Independence/accuracy locks still green; live health green
</success_criteria>

<output>
After completion, create `.planning/phases/62-city-dossier/62-02-SUMMARY.md`
</output>
