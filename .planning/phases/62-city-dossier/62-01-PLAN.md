---
phase: 62-city-dossier
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/bridge-city-dossier.test.js
  - tests/bridge-api-handlers.test.js
autonomous: true
requirements:
  - CITY-01
  - CITY-02
must_haves:
  truths:
    - "Static contracts lock dossier shell IDs, last-scrub/staged-lists facets, and eager history fetch on city select (CITY-01)"
    - "Static contracts lock collapsed exception scrap/drawer by default — five radios not primary wall on happy path (CITY-02)"
    - "Outcome radio values, request_type options, and saveCityOutcome POST body fields stay locked (preserve tracker payload)"
    - "POST /api/bridge/city-outcome handler unit tests cover five statuses + other_source notes + water_shutoff (soft API gap closed)"
  artifacts:
    - path: "tests/bridge-city-dossier.test.js"
      provides: "CITY-01/02 static HTML/JS contracts"
      contains: "CITY-01"
      min_lines: 80
    - path: "tests/bridge-api-handlers.test.js"
      provides: "city-outcome POST handler coverage"
      contains: "city-outcome"
  key_links:
    - from: "tests/bridge-city-dossier.test.js"
      to: "public/bridge.html"
      via: "static scan for dossier + demoted outcome markup"
      pattern: "bridge-city-dossier|bridge-outcome-drawer|bridge-city-outcome"
    - from: "tests/bridge-city-dossier.test.js"
      to: "public/js/bridge.js"
      via: "static scan for build/render dossier + loadHistory on city change + saveCityOutcome payload"
      pattern: "renderCityDossier|loadCityDossierHistory|saveCityOutcome|/api/bridge/city-outcome"
    - from: "tests/bridge-api-handlers.test.js"
      to: "lib/bridge-api.js handleCityOutcome"
      via: "callBridge POST city-outcome"
      pattern: "city-outcome|INVALID_STATUS|MISSING_NOTES"
---

<objective>
Wave 0 TDD: create static + handler contracts for CITY-01 city dossier and CITY-02 demoted no-list outcomes **before** production UI lands.

Purpose: Drive Phase 62 implementation with red contracts; freeze outcome POST payload and radio values so demotion cannot break City Tracker; lock dossier composition hooks so history is not modal-only.

Output: New `tests/bridge-city-dossier.test.js` (intentionally RED until Plan 02) + green city-outcome handler tests in `tests/bridge-api-handlers.test.js`. No production UI changes in this plan.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-city-dossier/62-CONTEXT.md
@.planning/phases/62-city-dossier/62-RESEARCH.md
@.planning/phases/62-city-dossier/62-VALIDATION.md
@public/bridge.html
@public/js/bridge.js
@lib/bridge-api.js
@tests/bridge-api-handlers.test.js
@tests/bridge-list-factory-ux.test.js
@AGENTS.md

<interfaces>
<!-- Mirror Phase 56/59 static-test style (node:test + fs source scans). Zero new packages. -->

```js
// tests/bridge-city-dossier.test.js skeleton
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(ROOT, 'public', 'bridge.html'), 'utf8');
const js = fs.readFileSync(path.join(ROOT, 'public', 'js', 'bridge.js'), 'utf8');
const css = fs.readFileSync(path.join(ROOT, 'public', 'css', 'bridge.css'), 'utf8');
```

<!-- CITY-01 contracts (RED until Plan 02) -->
- bridge.html contains `#bridge-city-dossier` (ops case-file shell under city step)
- bridge.html dossier surfaces text/ids for: last scrub, prior attaches / staged lists facets
  (accept ids: `#bridge-dossier-last-scrub`, `#bridge-dossier-attaches`, `#bridge-dossier-lists` OR class hooks `bridge-dossier-last-scrub` etc.)
- bridge.js contains `renderCityDossier` (or `buildDossierModel` + render) and filters lists by `cityId`
- bridge.js on city select path calls history load for dossier (not only `openHistoryDialog` → `loadHistory`)
  — assert `onCityChange` body (or helper it calls) references `loadCityDossierHistory` OR `loadHistory(` outside dialog-only openHistoryDialog
- Prefer explicit `loadCityDossierHistory` name from RESEARCH; if executor uses shared cache, still assert city-change path invokes history fetch

<!-- CITY-02 contracts (RED until Plan 02) -->
- bridge.html wraps `#bridge-city-outcome` inside a secondary scrap/drawer control
  (accept: `#bridge-outcome-drawer` / `details.bridge-outcome-drawer` / `[data-bridge-outcome-drawer]`)
- Default collapsed language present: button/summary text matching /no usable list|log tracker outcome|City replied/i
- Five radio values preserved exactly:
  needs_clarification | no | other_source | they_charge | approved_bad_data
- `name="bridge-city-outcome"` preserved
- `#bridge-outcome-type` still has `code_violation` and `water_shutoff` (NOT water_shut_off)
- saveCityOutcome still POSTs `/api/bridge/city-outcome` with body fields:
  cityId, response_status, request_type, notes, response_raw

<!-- As-built locks (must stay GREEN even before UI) -->
- Five radio values exist in html today
- saveCityOutcome POST body fields exist in js today
- GET history path `/api/bridge/history/` still in js

<!-- Handler tests (GREEN in this plan — backend already exists) -->
Reuse callBridge harness from bridge-api-handlers.test.js:
- POST valid needs_clarification + request_type code_violation → 200/ok
- POST other_source without notes → 400 MISSING_NOTES
- POST invalid status → 400 INVALID_STATUS
- POST water_shutoff request_type accepted
- Missing cityId → 400 MISSING_CITY
Do not invent new routes. Mock Forge as existing harness does.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 static contracts for CITY-01/02 in bridge-city-dossier.test.js</name>
  <files>tests/bridge-city-dossier.test.js</files>
  <read_first>
    - .planning/phases/62-city-dossier/62-RESEARCH.md (Patterns 5–6, DOM contract table, Validation Architecture)
    - .planning/phases/62-city-dossier/62-CONTEXT.md (locked dossier + exception path)
    - public/bridge.html (city step ~96–171)
    - public/js/bridge.js (saveCityOutcome ~1547–1592, onCityChange ~1594–1619, loadHistory ~2219)
    - tests/bridge-list-factory-ux.test.js or tests/bridge-efficiency-path.test.js (static scan style)
  </read_first>
  <behavior>
    - GREEN today: radio values set, saveCityOutcome POST fields, history API path string present
    - RED until Plan 02: #bridge-city-dossier present; dossier facet hooks; renderCityDossier/buildDossierModel; lists filtered by cityId; history fetch from city-change path; outcome drawer wrapper + collapsed entry control
    - Always: forbidden rename of radio values; request_type water_shutoff not water_shut_off; no new /api/bridge/dossier route required in product code
  </behavior>
  <action>
    Create `tests/bridge-city-dossier.test.js` with node:test + assert/strict, reading public/bridge.html, public/js/bridge.js, and public/css/bridge.css.

    Group tests with titles containing `CITY-01` or `CITY-02`:

    **CITY-01 (expect RED until Plan 02):**
    1. HTML contains `id="bridge-city-dossier"` (or `id='bridge-city-dossier'`).
    2. HTML contains last-scrub + staged-lists surface hooks (e.g. `bridge-dossier-last-scrub` and `bridge-dossier-lists` as id or class).
    3. JS contains `buildDossierModel` OR `renderCityDossier` AND filters with `cityId` (pattern like `l.cityId` / `cityId ===` / `listsForCity`).
    4. JS city-select path loads history for dossier: assert presence of `loadCityDossierHistory` **or** that `onCityChange` function body (slice between `function onCityChange` and next top-level `function`) calls `loadHistory(` or a named dossier history helper — not only `openHistoryDialog`.

    **CITY-02 (expect RED until Plan 02 for drawer; GREEN for payload):**
    5. HTML has secondary drawer/scrap wrapper around outcomes (`bridge-outcome-drawer` id/class or `data-bridge-outcome-drawer`).
    6. Collapsed entry copy matches `/no usable list|Log tracker outcome|City replied/i`.
    7. All five radio values present with `name="bridge-city-outcome"`.
    8. `#bridge-outcome-type` options include `code_violation` and `water_shutoff`; assert HTML does **not** use `water_shut_off` as outcome request_type option value.
    9. JS `saveCityOutcome` still includes `cityId`, `response_status`, `request_type`, `notes`, `response_raw` and path `/api/bridge/city-outcome`.

    **Regression locks (GREEN now):**
    10. JS still has `function saveCityOutcome` and `function loadHistory`.
    11. No production requirement for `GET /api/bridge/dossier` — assert product files need not introduce it; optional negative: if someone adds it later that's out of scope, do not require it.

    Do **not** modify public/ or lib/ in this task.
  </action>
  <acceptance_criteria>
    - File `tests/bridge-city-dossier.test.js` exists
    - At least 2 test titles contain `CITY-01` and 2 contain `CITY-02`
    - Running the file reports some failures (RED) until Plan 02 AND some passes for as-built payload locks
    - No production files modified
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridge-city-dossier.test.js; $code=$LASTEXITCODE; if ($code -eq 0) { Write-Host 'NOTE: all green — dossier UI may already exist'; exit 0 } else { Write-Host 'EXPECTED RED until Plan 02 for new dossier contracts'; exit 0 }</automated>
  </verify>
  <done>Wave 0 static CITY-01/02 contracts committed; payload/radio locks green; new dossier/drawer contracts red until Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close city-outcome handler soft gap in bridge-api-handlers.test.js</name>
  <files>tests/bridge-api-handlers.test.js</files>
  <read_first>
    - lib/bridge-api.js (handleCityOutcome, CITY_OUTCOME_STATUSES, CITY_OUTCOME_REQUEST_TYPES)
    - tests/bridge-api-handlers.test.js (callBridge harness + history GET tests ~458+)
    - .planning/phases/62-city-dossier/62-RESEARCH.md (Pattern 4 City outcome API)
  </read_first>
  <behavior>
    - POST /api/bridge/city-outcome with valid body succeeds (ok/event or 200)
    - other_source without notes → 400 + MISSING_NOTES
    - invalid response_status → 400 + INVALID_STATUS
    - invalid/missing cityId → 400 + MISSING_CITY (or equivalent)
    - request_type water_shutoff accepted
    - Does not delete filter lists (if response documents ok only — assert handler does not call list-store delete; optional comment assertion)
  </behavior>
  <action>
    Append tests to `tests/bridge-api-handlers.test.js` titled with `city-outcome` or `CITY-02`:

    1. Happy path: POST `/api/bridge/city-outcome` JSON `{ cityId: known fixture city if harness has one, response_status: 'needs_clarification', request_type: 'code_violation', notes: '', response_raw: '' }` — follow existing Forge mock patterns; assert status 200 and json.ok or response_status echo.
    2. `other_source` with empty notes → 400, code `MISSING_NOTES`.
    3. Bad status e.g. `nope` → 400, code `INVALID_STATUS`.
    4. Missing cityId → 400, code `MISSING_CITY`.
    5. `request_type: 'water_shutoff'` with status `no` → accepted (200).

    If harness cannot hit Forge for outcome without extra mock, mirror how other portal-proxy POSTs are mocked in this file — **do not** invent a new Bridge route.

    These tests must be **GREEN** in this plan (handler already exists).
  </action>
  <acceptance_criteria>
    - `city-outcome` appears in at least 3 test titles or bodies in bridge-api-handlers.test.js
    - Automated command below exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="city-outcome|CITY-02|history" tests/bridge-api-handlers.test.js</automated>
  </verify>
  <done>City-outcome POST handler soft gap closed; history GET regressions still green.</done>
</task>

</tasks>

<verification>
- bridge-city-dossier.test.js exists with CITY-01/02 titles (partial RED OK)
- city-outcome handler tests green
- No public/ or lib/ production edits in this plan
</verification>

<success_criteria>
- Wave 0 contracts drive Plan 02 dossier + drawer work
- Outcome POST payload and five statuses locked before UI moves
- Handler soft gap for city-outcome closed
</success_criteria>

<output>
After completion, create `.planning/phases/62-city-dossier/62-01-SUMMARY.md`
</output>
