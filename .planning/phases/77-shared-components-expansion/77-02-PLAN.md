---
phase: 77-shared-components-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - public/css/phuglee-components.css
  - public/bridge.html
autonomous: true
requirements:
  - FORMS-02
  - FORMS-03
  - CARDS-01
  - STATES-01
  - STATES-03
must_haves:
  truths:
    - "System chip components support radio semantics (hidden radio + face) for list type selection"
    - "Selected chips show auth-tab gold/orange gradient face; unselected chips stay calm stone/glass"
    - "Shared glass panel language includes desk-safe static/dense variants without inventing kill-report theater"
    - "Shared empty + error/success status patterns exist using semantic status tokens"
  artifacts:
    - path: "public/css/phuglee-components.css"
      provides: "phuglee-chip + selected auth-tab energy"
      contains: "phuglee-chip"
    - path: "public/css/phuglee-components.css"
      provides: "panel desk variants (static/dense)"
      contains: "phuglee-panel--static"
    - path: "public/css/phuglee-components.css"
      provides: "empty + error/success status system"
      contains: "phuglee-success"
    - path: "public/bridge.html"
      provides: "type chips dual-class wired to system chip"
      contains: "phuglee-chip"
  key_links:
    - from: "bridge.html type labels"
      to: ".phuglee-chip + .phuglee-chip-face"
      via: "dual-class on existing radio labels (IDs/name/values frozen)"
      pattern: "bridge-type-chip phuglee-chip"
    - from: ".phuglee-chip input:checked + .phuglee-chip-face"
      to: "auth-tab gradient face"
      via: "gold→orange→terracotta selected energy"
      pattern: "phuglee-chip input:checked \\+ \\.phuglee-chip-face"
    - from: ".phuglee-error / .phuglee-success"
      to: "--phuglee-danger / --phuglee-success"
      via: "semantic token colors only"
      pattern: "--phuglee-danger|--phuglee-success"
---

<objective>
Expand shared chips, panels, and empty/error/success patterns in `phuglee-components.css`, and dual-class wire Filter type chips so selected state can use auth-tab energy without renaming radio contracts.

Purpose: Complete Phase 77 shared control surface (FORMS-02/03, CARDS-01, STATES-01/03) so Phase 78 can cascade-hook the rest of the desk. Kill-report / Train theater stay out of the shared file.

Output: `.phuglee-chip*`, panel desk variants, status/empty expansions, and minimal `bridge.html` dual-class on type chips only.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-shared-components-expansion/77-CONTEXT.md
@.planning/phases/77-shared-components-expansion/77-01-SUMMARY.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@public/css/phuglee-components.css
@public/css/auth.css
@public/css/bridge.css
@public/bridge.html
@public/css/tokens.css
@AGENTS.md

<interfaces>
<!-- Type chips today (FREEZE name/value/structure; only add classes). -->

From public/bridge.html:
```html
<div class="bridge-type-chips" role="radiogroup" aria-label="Upload type">
  <label class="bridge-type-chip">
    <input type="radio" name="bridge-upload-type" value="code_violation" checked />
    <span class="bridge-type-chip-face">Code violation</span>
  </label>
  <label class="bridge-type-chip">
    <input type="radio" name="bridge-upload-type" value="water_shut_off" />
    <span class="bridge-type-chip-face">Water shut-off</span>
  </label>
</div>
```

Target dual-class (agent discretion locked for this plan):
```html
<div class="bridge-type-chips phuglee-chip-group" role="radiogroup" aria-label="Upload type">
  <label class="bridge-type-chip phuglee-chip">
    <input type="radio" name="bridge-upload-type" value="code_violation" checked />
    <span class="bridge-type-chip-face phuglee-chip-face">Code violation</span>
  </label>
  <!-- same for water; name/values/checked unchanged -->
</div>
```

Auth-tab selected energy (north star — mirror values, not `.auth-tab` selectors in shared):
```css
/* auth.css .auth-tab.is-active */
color: var(--phuglee-black);
background: linear-gradient(
  135deg,
  var(--phuglee-gold) 0%,
  var(--phuglee-orange) 55%,
  var(--phuglee-terracotta) 100%
);
border-color: rgba(238, 183, 70, 0.35);
box-shadow: 0 2px 12px rgba(229, 132, 53, 0.28);
```

Existing panels/states to extend (do not delete):
```css
.phuglee-panel, .phuglee-panel-featured, .phuglee-panel-exclusive, .phuglee-panel-vault
.phuglee-panel.distress-glass--float, .phuglee-panel--float
.phuglee-empty-state, .phuglee-empty, .phuglee-error-wrap, .phuglee-error, .phuglee-error-retry
.phuglee-loading-state /* leave loading deep-wire to Phase 78 STATES-02 */
```

Error markup on Filter (IDs frozen — dual-class only if adding shared status hooks):
```html
<div class="bridge-error-wrap" id="bridge-error-wrap" hidden role="alert">
  <p class="bridge-error" id="bridge-error"></p>
</div>
```
Optional dual-class: `bridge-error-wrap phuglee-error-wrap` + `bridge-error phuglee-error` — IDs untouched. Full status-line wire across desk can wait for Phase 78 if dual-class alone risks cascade fights; at minimum shared CSS must define the classes.

Hard constraints:
- Expand `phuglee-components.css` only for shared look — no parallel theme sheet
- Markup: dual-class only; never rename `bridge-*` IDs, `name="bridge-upload-type"`, values, or `data-action`
- Do NOT put kill-report / mission board / Train theater styles into shared components
- No `public/js/**` or `lib/**` edits
- Do not wipe data stores
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared chips with auth-tab selected energy + dual-class wire</name>
  <files>public/css/phuglee-components.css, public/bridge.html</files>
  <action>
**FORMS-02 + FORMS-03**

1. In `phuglee-components.css`, add a shared chip system (new section after buttons or near forms):

   - `.phuglee-chip-group` — flex wrap + gap (radiogroup container utility)
   - `.phuglee-chip` — position relative, cursor pointer, margin 0 (label shell)
   - `.phuglee-chip input` — visually hidden (absolute, opacity 0, pointer-events none) — **preserve radio semantics**; do not restyle as checkbox
   - `.phuglee-chip-face` — calm unselected: stone/taupe text, dark glass fill, subtle border, pill or md radius, short transitions
   - `.phuglee-chip:hover .phuglee-chip-face` — cream text, warmer border (not full selected energy)
   - **Selected (auth-tab energy):**
     ```css
     .phuglee-chip input:checked + .phuglee-chip-face {
       color: var(--phuglee-black);
       background: linear-gradient(135deg, var(--phuglee-gold) 0%, var(--phuglee-orange) 55%, var(--phuglee-terracotta) 100%);
       border-color: rgba(238, 183, 70, 0.35);
       box-shadow: 0 2px 12px rgba(229, 132, 53, 0.28);
     }
     ```
   - `.phuglee-chip input:focus-visible + .phuglee-chip-face` — 2px orange outline / focus ring
   - Reduced-motion: disable chip face transform if any lift is used

2. In `public/bridge.html`, dual-class only the type chip block:
   - Container: `bridge-type-chips phuglee-chip-group`
   - Each label: `bridge-type-chip phuglee-chip`
   - Each face span: `bridge-type-chip-face phuglee-chip-face`
   - **Do not change** `name`, `value`, `checked`, `role="radiogroup"`, surrounding panel IDs, or step structure

3. Do not delete `.bridge-type-chip*` rules in `bridge.css` this phase (Phase 78 cascade may demote/remove duplicates). Shared selected energy must live on `.phuglee-chip*` so it is reusable outside Filter.

4. Do not invent non-radio chip variants beyond what's needed for type selection unless a tiny `.phuglee-chip--static` (non-input) is trivial — prefer radio structure only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('public/css/phuglee-components.css','utf8');const html=fs.readFileSync('public/bridge.html','utf8');for (const n of ['.phuglee-chip','.phuglee-chip-face','.phuglee-chip-group']){if(!css.includes(n)){console.error('CSS missing',n);process.exit(1);}}if(!/phuglee-chip input:checked\s*\+\s*\.phuglee-chip-face/.test(css)){console.error('missing selected chip selector');process.exit(1);}if(!css.includes('var(--phuglee-gold)')||!css.includes('var(--phuglee-orange)')){console.error('selected chip must use gold/orange tokens');process.exit(1);}if(!html.includes('bridge-type-chip phuglee-chip')){console.error('html dual-class label missing');process.exit(1);}if(!html.includes('bridge-type-chip-face phuglee-chip-face')){console.error('html dual-class face missing');process.exit(1);}if(!html.includes('name=\"bridge-upload-type\"')||!html.includes('value=\"code_violation\"')||!html.includes('value=\"water_shut_off\"')){console.error('radio contracts broken');process.exit(1);}console.log('FORMS-02/03 chips OK');"</automated>
  </verify>
  <done>Shared chip CSS with auth-tab selected gradient exists; type chips dual-classed; radio name/values/IDs frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Panel desk variants + empty/error/success status patterns</name>
  <files>public/css/phuglee-components.css, public/bridge.html</files>
  <action>
**CARDS-01 + STATES-01 + STATES-03**

1. **Panels (CARDS-01)** — extend existing `.phuglee-panel` family (do not replace glass tokens):
   - Keep current glass fill/border/shadow/grain hairline behavior
   - Add `.phuglee-panel--static` (and/or `.phuglee-panel.phuglee-panel--static`): **no hover lift/transform** (mirror `.auth-panel.phuglee-panel:hover { transform: none }`). Desk sections that already use `phuglee-panel` must be able to opt into static so operators are not thrash-lifted all day
   - Add `.phuglee-panel--dense`: tighter padding (e.g. ~1rem / 1.1rem) for ops density while same glass DNA
   - Keep featured / exclusive / vault / float as-is (elevation map application is Phase 79 CARDS-03)
   - **Do not** add kill-report, mission KPI, or Train theater panel types to shared CSS

2. **Empty (STATES-01)** — polish existing `.phuglee-empty-state` / `.phuglee-empty`:
   - Ensure title/lead utilities remain (`.phuglee-empty-title`, `.phuglee-empty-lead`)
   - Add `.phuglee-empty-state--compact` (less vertical padding) for dense desk empty boxes
   - Pattern watermark may stay; no new image assets required

3. **Error + success (STATES-03)** — expand status language on semantic tokens only:
   - Keep/refine `.phuglee-error-wrap`, `.phuglee-error`, `.phuglee-error-retry` using `var(--phuglee-danger)` (and warm terracotta danger-glass fill if already present — no random reds)
   - Add `.phuglee-success-wrap` + `.phuglee-success` (and optional `.phuglee-status` / `.phuglee-status--success` / `.phuglee-status--error` / `.phuglee-status--warn`) for status lines:
     - success → `var(--phuglee-success)` text + calm green-tint glass border/fill
     - warn → `var(--phuglee-warn)` if status utility is added
     - error → danger tokens
   - Support `[hidden] { display: none }` on wrap utilities (match error-wrap)
   - **Optional markup:** dual-class Filter error wrap: `class="bridge-error-wrap phuglee-error-wrap"` and `class="bridge-error phuglee-error"` — IDs `bridge-error-wrap` / `bridge-error` must remain. Skip dual-class if it would require JS classList rewrites; CSS-only dual-class on static HTML is fine.

4. **Out of scope here:** loading/scrub-feed (STATES-02 → Phase 78), dialogs (CARDS-02 → Phase 78), full desk dual-class of every panel (Phases 78–79).

5. After CSS/HTML edits, run:
   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1
   ```
   If verify fails, restart via `scripts\restart.ps1` and re-verify. Do not claim done without exit 0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('public/css/phuglee-components.css','utf8');const need=['.phuglee-panel--static','.phuglee-panel--dense','.phuglee-empty-state','.phuglee-error-wrap','.phuglee-success'];for(const n of need){if(!css.includes(n)){console.error('missing',n);process.exit(1);}}if(!css.includes('--phuglee-danger')||!css.includes('--phuglee-success')){console.error('status patterns must use semantic tokens');process.exit(1);}if(css.includes('.bridge-kill')||css.includes('kill-report-shared')){console.error('kill theater must stay out of shared');process.exit(1);}console.log('CARDS-01 STATES-01/03 OK');"</automated>
  </verify>
  <done>Panel static/dense variants, empty polish, and error/success status patterns exist on semantic tokens; no kill-theater in shared CSS; verify-live run after edits exits 0.</done>
</task>

</tasks>

<verification>
- Chip dual-class present; radio contracts greppable unchanged
- Selected chip selector uses gold/orange gradient tokens (auth-tab energy)
- Panel `--static` / `--dense` present; empty + success/error present
- `scripts/verify-live.ps1` exit 0 after site edits
- No parallel CSS theme file created
</verification>

<success_criteria>
- FORMS-02: system chips with radio structure ready and dual-classed on type chips
- FORMS-03: selected = auth-tab gold/orange face; unselected calm
- CARDS-01: shared glass panel language + desk static/dense hooks
- STATES-01: shared empty patterns available
- STATES-03: shared error + success (semantic tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/77-shared-components-expansion/77-02-SUMMARY.md`
</output>
