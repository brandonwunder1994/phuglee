---
phase: 77-shared-components-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/phuglee-components.css
autonomous: true
requirements:
  - BUTTONS-01
  - BUTTONS-02
  - BUTTONS-03
must_haves:
  truths:
    - "Primary, secondary, ghost, and danger system button variants exist under .phuglee-btn* with home-grade gem/glass energy"
    - "Hover, focus-visible, active, and disabled states are defined consistently for all four button variants"
    - "Primary CTA shimmer is hover-triggered only (not perpetual), with desk-safe lift/scale caps and reduced-motion kill"
  artifacts:
    - path: "public/css/phuglee-components.css"
      provides: "System button variants + state matrix + capped primary shimmer"
      contains: "phuglee-btn-primary"
    - path: "public/css/phuglee-components.css"
      provides: "Explicit focus-visible on system buttons"
      contains: "phuglee-btn:focus-visible"
    - path: "public/css/phuglee-components.css"
      provides: "Reduced-motion kill for primary shimmer pseudo"
      contains: "phuglee-btn-primary::after"
  key_links:
    - from: ".phuglee-btn-primary::after"
      to: "hover-only translateX shimmer"
      via: "transform transition on :hover:not(:disabled)::after"
      pattern: "phuglee-btn-primary:hover:not\\(:disabled\\)::after"
    - from: ".phuglee-btn:disabled"
      to: "no lift / no shimmer / not-allowed"
      via: "opacity + transform/filter none"
      pattern: "phuglee-btn:disabled|phuglee-btn\\[disabled\\]"
    - from: "@media (prefers-reduced-motion: reduce)"
      to: ".phuglee-btn-primary::after"
      via: "display:none or equivalent shimmer kill"
      pattern: "prefers-reduced-motion[\\s\\S]*phuglee-btn-primary::after"
---

<objective>
Expand the shared Phuglee button system in `phuglee-components.css` to home-grade primary/secondary/ghost/danger with a full interactive state matrix and contained (hover-only, desk-capped) primary CTA shimmer.

Purpose: Filter and later pages consume one button language instead of one-off CTAs. This plan ships the shared API ready for desk wire (Phase 78–79); no JS, no bridge ID renames, no parallel theme sheet.

Output: Polished `.phuglee-btn*` block in `public/css/phuglee-components.css` satisfying BUTTONS-01..03.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-shared-components-expansion/77-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@public/css/phuglee-components.css
@public/css/auth.css
@public/css/tokens.css
@public/css/phuglee-a11y.css
@AGENTS.md

<interfaces>
<!-- Existing button contracts — expand/polish in place; do not rename classes. -->

From public/css/phuglee-components.css (current API):
```css
.phuglee-btn
.phuglee-btn:disabled, .phuglee-btn[disabled]
.phuglee-btn-primary          /* gold→orange gem + ::before sheen + ::after hover shimmer */
.phuglee-btn-secondary        /* glass border CTA */
.phuglee-btn-ghost            /* text-only */
.phuglee-btn-danger           /* danger glass */
```

Auth north-star primary (mirror energy, not copy selectors):
```css
/* auth.css .auth-btn-primary */
background: linear-gradient(135deg, var(--phuglee-gold), var(--phuglee-orange), var(--phuglee-terracotta));
/* hover shimmer via ::after translateX; hover lift translateY(-2px) */
```

Tokens to consume (never hardcode new hex islands when a token exists):
```css
--phuglee-gold, --phuglee-orange, --phuglee-terracotta, --phuglee-black, --phuglee-cream
--phuglee-danger, --shadow-cta, --shadow-cta-hover, --shadow-glow-orange
--card-duration, --card-ease, --font-display
```

Global focus already in phuglee-a11y.css (`button:focus-visible` → `--phuglee-focus-ring`). Still add explicit `.phuglee-btn:focus-visible` so system buttons stay correct if a11y load order shifts.

Hard constraints:
- Edit ONLY `public/css/phuglee-components.css` in this plan
- No parallel theme sheet; no `bridge.css` / `bridge.html` / `public/js/**` / `lib/**` edits
- Do not invent kill-report or Train theater components here
- CSS only — zero functional change
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete button variant + state matrix</name>
  <files>public/css/phuglee-components.css</files>
  <action>
Polish the existing `.phuglee-btn` family so all four variants are home-grade and state-complete:

1. **Base `.phuglee-btn`** — keep inline-flex, display font, uppercase, letter-spacing, shared transitions. Ensure disabled rules cover both `:disabled` and `[disabled]` (opacity ~0.45, `cursor: not-allowed`, force `transform: none`, `filter: none`, muted shadow).

2. **Variants (BUTTONS-01)** — keep/refine:
   - `.phuglee-btn-primary` — gold→orange→terracotta gem gradient (auth/home DNA), dark text on gem face, CTA shadow tokens
   - `.phuglee-btn-secondary` — glass fill + gold-tint border + top hairline (existing pattern OK; align hover border to `--glass-border-hover` / orange family tokens where possible)
   - `.phuglee-btn-ghost` — transparent, cream text → orange hover; add a calm active press state (no random hex)
   - `.phuglee-btn-danger` — use `var(--phuglee-danger)` for border/glow/text tints instead of bare `#ffc9c9` where practical; keep glass danger treatment

3. **State matrix (BUTTONS-02)** — for EACH of primary/secondary/ghost/danger, ensure greppable rules for:
   - `:hover:not(:disabled)` — visual feedback (border/color/lift as appropriate)
   - `:focus-visible` — outline/box-shadow using `var(--phuglee-focus-ring)` or equivalent 3px orange ring (explicit on `.phuglee-btn:focus-visible` at minimum; variant overrides only if needed)
   - `:active:not(:disabled)` — pressed (slightly less lift than hover)
   - `:disabled` / `[disabled]` — inherited from base; verify no variant hover wins over disabled

4. **Optional dense size** — add `.phuglee-btn-sm` (tighter padding, slightly smaller type) for desk density later. Do NOT remove default sizes used on home/auth.

5. **Do not** rename existing classes, add a second button system, or touch Filter markup.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('public/css/phuglee-components.css','utf8');const req=['.phuglee-btn-primary','.phuglee-btn-secondary','.phuglee-btn-ghost','.phuglee-btn-danger','phuglee-btn:focus-visible','phuglee-btn:disabled'];for(const r of req){if(!c.includes(r)){console.error('MISSING',r);process.exit(1);}}if(!c.includes('phuglee-btn-primary:hover:not(:disabled)')){console.error('MISSING primary hover');process.exit(1);}if(!c.includes('phuglee-btn-primary:active:not(:disabled)')){console.error('MISSING primary active');process.exit(1);}console.log('BUTTONS-01/02 matrix OK');"</automated>
  </verify>
  <done>All four `.phuglee-btn-*` variants exist with hover, focus-visible, active, and disabled coverage in `phuglee-components.css`.</done>
</task>

<task type="auto">
  <name>Task 2: Cap primary CTA shimmer for all-day desk use</name>
  <files>public/css/phuglee-components.css</files>
  <action>
Implement BUTTONS-03 — contained gem energy, not perpetual motion:

1. **Shimmer stays hover-only** — keep `.phuglee-btn-primary::after` as a diagonal sheen that `translateX`s only on `:hover:not(:disabled)`. Do NOT add infinite `animation` on the primary face.

2. **Desk-safe caps** (reduce fatigue vs marketing hero CTAs):
   - Hover lift: target ~`translateY(-2px)` and scale ≤ `1.01` (auth-like; not `-3px` / `1.02` thrash)
   - Active: settle near `translateY(0)` / slight scale down
   - Sheen opacity: keep subtle (~0.22–0.28 white band); do not brighten beyond home auth

3. **Reduced-motion** — inside existing `@media (prefers-reduced-motion: reduce)` block(s), ensure:
   - `.phuglee-btn-primary::after { display: none; }` (already present — keep/extend)
   - Hover transforms on all button variants forced to `none`
   - No new motion without a reduced-motion twin

4. **Comment** — one short CSS comment above primary shimmer: e.g. `/* BUTTONS-03: hover-only sheen; capped lift for desk */` so later phases do not reintroduce perpetual shimmer.

5. Still only touch `phuglee-components.css`. Pure CSS file edit does not require server restart; do not touch server scripts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('public/css/phuglee-components.css','utf8');if(!c.includes('phuglee-btn-primary::after')){console.error('no shimmer pseudo');process.exit(1);}if(!c.includes('phuglee-btn-primary:hover:not(:disabled)::after')){console.error('shimmer not hover-bound');process.exit(1);}if(/phuglee-btn-primary::after\s*\{[^}]*animation\s*:/.test(c)){console.error('perpetual animation on primary::after forbidden');process.exit(1);}if(!/prefers-reduced-motion:\s*reduce[\s\S]*phuglee-btn-primary::after/.test(c)){console.error('reduced-motion must kill primary::after');process.exit(1);}console.log('BUTTONS-03 shimmer caps OK');"</automated>
  </verify>
  <done>Primary shimmer is hover-only, lift is desk-capped, reduced-motion disables sheen; no perpetual primary animation.</done>
</task>

</tasks>

<verification>
- Grep confirms four variants + focus-visible + disabled + hover-bound `::after`
- No new CSS file created (sole expansion surface remains `phuglee-components.css`)
- No edits under `public/js/`, `lib/`, or bridge IDs
</verification>

<success_criteria>
- BUTTONS-01: primary / secondary / ghost / danger present and usable as system classes
- BUTTONS-02: hover, focus-visible, active, disabled defined consistently
- BUTTONS-03: contained hover shimmer + capped lift + reduced-motion kill
</success_criteria>

<output>
After completion, create `.planning/phases/77-shared-components-expansion/77-01-SUMMARY.md`
</output>
