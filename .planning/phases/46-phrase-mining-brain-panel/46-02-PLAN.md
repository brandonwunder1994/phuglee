---
phase: 46-phrase-mining-brain-panel
plan: 02
type: execute
wave: 2
depends_on:
  - "46-01"
files_modified:
  - lib/bridge-api.js
  - tests/bridge-brain-api.test.js
  - public/bridge.html
  - public/js/bridge.js
  - public/css
autonomous: true
requirements:
  - PHRASE-03
must_haves:
  truths:
    - "Admin GET /api/bridge/brain returns version, typeRules, phraseRules, metrics"
    - "Non-admin GET brain and POST rule status return 403 ADMIN_REQUIRED"
    - "Admin can set rule status to active, rejected, or disabled via POST .../rules/:id/status"
    - "Admin Filter brain panel lists active type rules, proposed phrases, and active phrases"
    - "Panel Activate / Reject / Disable calls status API and refreshes lists"
  artifacts:
    - path: "lib/bridge-api.js"
      provides: "GET /brain + POST /brain/rules/:id/status with requireAdmin"
      contains: "brain/rules"
    - path: "tests/bridge-brain-api.test.js"
      provides: "403 + status transition coverage"
      min_lines: 40
    - path: "public/bridge.html"
      provides: "Filter brain panel markup sections"
      contains: "brain"
    - path: "public/js/bridge.js"
      provides: "load/render/act on brain panel"
      contains: "/api/bridge/brain"
  key_links:
    - from: "public/js/bridge.js"
      to: "/api/bridge/brain"
      via: "fetch with phugleeSessionHeaders"
      pattern: "/api/bridge/brain"
    - from: "lib/bridge-api.js"
      to: "requireAdmin"
      via: "gate GET brain + POST status"
      pattern: "requireAdmin|ADMIN_REQUIRED"
    - from: "POST rules/:id/status"
      to: "lib/bridge-brain-store.js"
      via: "loadBrain → mutate rule.status → saveBrain"
      pattern: "saveBrain"
---

<objective>
Expose admin-only brain read + rule status APIs and ship the Filter brain panel so admins can view, activate, reject, or disable type and phrase rules (PHRASE-03).

Purpose: HITL gate for proposed phrases and operational control of type rules without editing JSON.
Output: Brain API routes + tests, panel markup/JS (and minimal CSS matching bridge design system).
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-phrase-mining-brain-panel/46-CONTEXT.md
@.planning/phases/46-phrase-mining-brain-panel/46-RESEARCH.md
@.planning/phases/46-phrase-mining-brain-panel/46-01-SUMMARY.md
@lib/bridge-api.js
@lib/bridge-brain-store.js
@lib/bridge-phrase-miner.js
@public/bridge.html
@public/js/bridge.js
@public/js/phuglee-session-headers.js
@tests/bridge-brain-api.test.js

<interfaces>
<!-- From phase 45 + 46-01 -->

Admin gate (phase 45):
```js
// requireAdmin(req) → username or throws/returns 403 { code: 'ADMIN_REQUIRED' }
// Uses readPhugleeUser(req) === 'admin' via X-Phuglee-User
```

GET /api/bridge/brain (admin):
```http
GET /api/bridge/brain
Headers: x-phuglee-user: admin
→ 200 {
  version, updatedAt,
  typeRules, phraseRules,
  metrics,
  // optional: events tail last 20 OR omit events for payload size
}
```

POST /api/bridge/brain/rules/:id/status (admin):
```http
POST /api/bridge/brain/rules/:id/status
Headers: x-phuglee-user: admin
Body: { "status": "active" | "rejected" | "disabled" }
→ 200 { ok: true, rule, brainSummary: { version, metrics } }
→ 404 { code: 'RULE_NOT_FOUND' }
→ 400 { code: 'INVALID_STATUS' }
→ 403 { code: 'ADMIN_REQUIRED' }
```

Status machine:
| From | Allowed to |
|------|------------|
| proposed | active, rejected |
| active | disabled |
| disabled | active |
| rejected | (optional no re-open in v1 — reject only from proposed) |

Find rule by id in typeRules OR phraseRules. Set reviewedAt ISO + reviewedBy username on status change. Optional audit event: approve_phrase_rule / reject_phrase_rule / disable_rule.

Panel sections (locked):
1. Active type rules — kind + label + Disable
2. Proposed phrase rules — pattern + kind + Activate / Reject
3. Active phrase rules — pattern + kind + Disable
4. Light metrics counts if available
Empty states for each list. Entry: admin-only “Filter brain” control on results/train toolbar. Match existing bridge cards/toolbar — no new visual language. After activate, optional note: “Applies on next file process.”

Client: all fetches use phugleeSessionHeaders(); hide panel entry for non-admin (server still enforces 403).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brain GET + rule status API with tests</name>
  <files>lib/bridge-api.js, tests/bridge-brain-api.test.js</files>
  <read_first>lib/bridge-api.js, tests/bridge-brain-api.test.js, lib/bridge-brain-store.js, .planning/phases/45-decisions-type-rules/45-RESEARCH.md</read_first>
  <behavior>
    - Non-admin GET /api/bridge/brain → 403 ADMIN_REQUIRED
    - Admin GET → 200 with typeRules, phraseRules, version, metrics arrays/objects
    - Non-admin POST .../rules/:id/status → 403
    - Admin activate proposed phrase → rule.status active; saveBrain persists
    - Admin reject proposed → rejected
    - Admin disable active type or phrase → disabled
    - Unknown id → 404 RULE_NOT_FOUND
    - Invalid status body → 400 INVALID_STATUS
  </behavior>
  <action>
    1. Extend or create `tests/bridge-brain-api.test.js` using project pattern (temp BRIDGE_BRAIN_ROOT, invoke handle or HTTP helper used by existing brain decision tests).

    2. In `lib/bridge-api.js`:
    - Route `GET` pathname `/api/bridge/brain` → requireAdmin → loadBrain → sendJson 200 summary (omit full huge events or return tail of 20 max)
    - Route `POST` match `/api/bridge/brain/rules/:id/status` → requireAdmin → parse body → validate status ∈ {active,rejected,disabled} → find rule in typeRules or phraseRules → mutate status, reviewedAt, reviewedBy → optional append audit event → saveBrain → 200
    - Wire routes in existing `handle()` switch/if chain next to decisions route
    - Do NOT implement undo or metrics-only endpoint (phase 47)

    3. Run API tests until green.
  </action>
  <verify>
    <automated>node --test tests/bridge-brain-api.test.js</automated>
  </verify>
  <done>Admin brain read + status transitions work; non-admin always 403; 404/400 handled</done>
</task>

<task type="auto">
  <name>Task 2: Filter brain panel UI (type + proposed + active phrases)</name>
  <files>public/bridge.html, public/js/bridge.js, public/css</files>
  <read_first>public/bridge.html, public/js/bridge.js, public/js/phuglee-session-headers.js, .planning/phases/46-phrase-mining-brain-panel/46-RESEARCH.md</read_first>
  <action>
    1. **public/bridge.html** — Add admin-only panel structure (drawer or inline section matching bridge design):
    - Container id e.g. `bridge-brain-panel` with three list regions:
      - `brain-type-rules` (active type rules)
      - `brain-phrase-proposed`
      - `brain-phrase-active`
    - Toolbar button “Filter brain” (visible only when session user is admin — same gate as Train brain)
    - Empty-state copy in each list
    - Optional metrics strip (counts)

    2. **public/js/bridge.js**:
    - `loadBrainPanel()` → GET `/api/bridge/brain` with session headers
    - `renderBrainPanel(data)` — split typeRules active; phraseRules proposed vs active; render Disable / Activate / Reject buttons with data-rule-id
    - `setRuleStatus(id, status)` → POST `/api/bridge/brain/rules/${id}/status` then reload panel
    - On Activate success, show brief note that rule applies on next process
    - Failures: toast/inline error; 403 should not open panel for non-admin

    3. **CSS** — Reuse existing bridge card/toolbar classes; minimal new rules only if needed under existing bridge stylesheet (find current bridge CSS path and match tokens). No new visual language.

    4. Do NOT add undo stack, caps UI, or public non-admin metrics (phase 47 / deferred).

    5. After public/ edits, run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` (restart via scripts\restart.ps1 if needed) — must exit 0 before claiming live.
  </action>
  <verify>
    <automated>node --test tests/bridge-brain-api.test.js; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1</automated>
  </verify>
  <done>Panel shows three rule lists; admin actions hit status API; non-admin has no panel entry; verify-live green</done>
</task>

</tasks>

<verification>
- API tests green for 403 matrix + status transitions
- bridge.html contains brain panel section markers
- bridge.js fetches `/api/bridge/brain` and posts status
- verify-live.ps1 exit 0 after UI edits
</verification>

<success_criteria>
- PHRASE-03: Admin can view, activate, reject, or disable type and phrase rules in Filter brain panel
- Server gates all brain read/status writes with requireAdmin
- Proposed phrases remain non-live until Activate (enforced by phase 42 apply + 46-01 miner)
</success_criteria>

<output>
After completion, create `.planning/phases/46-phrase-mining-brain-panel/46-02-SUMMARY.md`
</output>
