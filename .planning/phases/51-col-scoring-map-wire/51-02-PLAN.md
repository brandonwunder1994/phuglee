---
phase: 51-col-scoring-map-wire
plan: 02
type: tdd
wave: 2
depends_on:
  - 51-01
files_modified:
  - lib/bridge-type-column-score.js
  - tests/bridge-type-column-score.test.js
autonomous: true
requirements:
  - COL-01
  - COL-02
  - COL-04
must_haves:
  truths:
    - "Every header is scored with header-alias features AND value-shape features from a row sample"
    - "pickTypeColumn returns exactly one winner header or null — never a blend of columns"
    - "Trap sheets pick Vio Cat / Issue Type / Category over Status/Violation/Code/Ordinance Description"
    - "Classic Violation Type still wins when present with category-like values"
    - "No candidacy → resolve returns header null / source unresolved (COL-02 pure)"
    - "Aliases are scoring points only — description aliases do not auto-win without categorical value shape"
  artifacts:
    - path: "lib/bridge-type-column-score.js"
      provides: "Pure scoreTypeColumns + pickTypeColumn + resolveTypeColumnHeader"
      exports:
        - scoreTypeColumns
        - pickTypeColumn
        - resolveTypeColumnHeader
      min_lines: 80
    - path: "tests/bridge-type-column-score.test.js"
      provides: "Green trap matrix for pure scorer"
  key_links:
    - from: "lib/bridge-type-column-score.js"
      to: "lib/bridge-intake-schema.js"
      via: "normalizeHeader + INTAKE_FIELD_ALIASES.violationIssueType as features only"
      pattern: "INTAKE_FIELD_ALIASES|normalizeHeader"
    - from: "lib/bridge-type-column-score.js"
      to: "lib/bridge-category-promote.js"
      via: "isCategoryLikeHeader / NARRATIVE_HEADER_RE / TIMESTAMP_ONLY_RE"
      pattern: "isCategoryLikeHeader|NARRATIVE_HEADER_RE|TIMESTAMP_ONLY_RE"
    - from: "pickTypeColumn"
      to: "single header or null"
      via: "minScore + margin/tie rules; never concatenate"
      pattern: "pickTypeColumn"
---

<objective>
Implement pure `lib/bridge-type-column-score.js` until the Wave 0 unit trap matrix is green.

Purpose: Deterministic value-aware Type candidacy so process can force a single best Type column (wired in Plan 03).
Output: Green pure unit suite; zero process/normalizer changes in this plan.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-col-scoring-map-wire/51-RESEARCH.md
@.planning/phases/51-col-scoring-map-wire/51-01-SUMMARY.md
@lib/bridge-intake-schema.js
@lib/bridge-category-promote.js
@tests/bridge-type-column-score.test.js
@AGENTS.md

**Discretion (locked outcomes > exact numbers):**
- Starting weights from 51-RESEARCH.md (exact alias +40, partial +22, category-like +18, narrative −45, etc.)
- Defaults: `sampleRows` first N=80, up to 40 non-empty samples/col, `minScore=45`, `minMargin=8`
- Tie: prefer stronger header-alias tier; else shorter median length; else earlier header order
- Tune weights only if trap matrix fails — outcomes are the contract
- Export `DEFAULTS` for tests/debug

**Do not:**
- Wire normalizer (Plan 03)
- Change `detectIntakeColumnMap` / grow intake-schema into the scorer
- Add npm packages
- Implement confirm/format/short labels
- Call alias map as fallback winner when scorer null

<interfaces>
From lib/bridge-intake-schema.js (reuse — do not change Type alias list in this plan unless a pure bug blocks fixtures):
```js
function normalizeHeader(value) { /* lower trim collapse */ }
const INTAKE_FIELD_ALIASES = {
  violationIssueType: [ /* includes toxic description labels — score features only */ ],
  violationDate: [ /* demote date headers */ ],
  // ...
};
// STREET_HINT_RE available for address demotion if exported; else local light digit+street heuristic
```

From lib/bridge-category-promote.js (reuse):
```js
function isCategoryLikeHeader(header) { /* false if NARRATIVE_HEADER_RE */ }
const NARRATIVE_HEADER_RE = /\b(description|notes|comments|...)\b/i;
const TIMESTAMP_ONLY_RE = /^\d{1,4}[\/\-]\d{1,2}[\/\-]\d{1,4}...$/;
```

Target module API:
```js
// lib/bridge-type-column-score.js
function scoreTypeColumns(headers, sampleRows, opts = {}) { /* ranked desc */ }
function pickTypeColumn(ranked, opts = {}) { /* one or null */ }
function resolveTypeColumnHeader(headers, sampleRows, opts = {}) {
  return { header, score, ranked, source: header ? 'scorer' : 'unresolved' };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure type-column scorer module</name>
  <files>lib/bridge-type-column-score.js</files>
  <behavior>
    - scoreTypeColumns ranks all real headers (skip empty, skip `_meta`)
    - Header features: exact alias, partial alias, isCategoryLikeHeader, narrative demotion, date demotion, status demotion, claimed geo/date skip when opts.claimedHeaders provided
    - Value features on sample: median length bands, date-like fraction, address-like fraction, distinct ratio categorical band, status enum demotion, long-prose demotion
    - Toxic description aliases on Type list get capped alias credit unless value shape is categorical (prevent Status/Violation Description auto-win)
    - pickTypeColumn: if top score &lt; minScore → null; else single ranked[0]; never blend
    - resolveTypeColumnHeader composes score + pick
  </behavior>
  <action>
    Create CommonJS pure module `lib/bridge-type-column-score.js` (zero new packages).

    1. **Imports:** `normalizeHeader`, `INTAKE_FIELD_ALIASES` from `./bridge-intake-schema`; `isCategoryLikeHeader`, `NARRATIVE_HEADER_RE`, `TIMESTAMP_ONLY_RE` from `./bridge-category-promote`. Prefer importing `STREET_HINT_RE` from intake-schema if exported; if not exported, use a local equivalent of the same idea (digit + street token) without mutating intake-schema unless a one-line export is clearly safer.

    2. **Sampling:** For each header, collect up to 40 non-empty trimmed strings from first `opts.sampleSize || 80` rows. Zero samples → header features only.

    3. **scoreTypeColumns(headers, sampleRows, opts):**
       - Build per-header `{ header, score, reasons: string[], samples: string[] }`
       - Apply research feature table (header + value-shape)
       - Sort desc by score; stable secondary key = original header index
       - Optional `opts.claimedHeaders` Set (address/date/geo) → hard skip or −100 so Type never equals address/date

    4. **pickTypeColumn(ranked, opts):**
       - Defaults `minScore=45`, `minMargin=8` (export via `DEFAULTS`)
       - If empty or top.score &lt; minScore → `null`
       - If #1 and #2 both ≥ minScore and margin &lt; minMargin: prefer #1 when its header-alias tier is strictly better; else if still ambiguous return null **or** take #1 for process stability — document choice in module comment. Prefer: take #1 when alias tier better; else take #1 if margin ≥ minMargin; else null.
       - Return single ranked entry object — never string-concat two headers

    5. **resolveTypeColumnHeader:** score → pick → `{ header, score, ranked, source }`

    6. **Export:** `scoreTypeColumns`, `pickTypeColumn`, `resolveTypeColumnHeader`, `DEFAULTS`, and any pure helpers tests need.

    7. Do **not** modify normalizer, processUpload, promote, or engine tests in this task.
    8. Do **not** touch user data directories.
  </action>
  <verify>
    <automated>node --test tests/bridge-type-column-score.test.js</automated>
  </verify>
  <done>All pure trap matrix tests pass; module exports resolve/score/pick; no blend; null on no candidacy.</done>
</task>

<task type="auto">
  <name>Task 2: Tune weights only if needed + keep promote suite green</name>
  <files>lib/bridge-type-column-score.js, tests/bridge-type-column-score.test.js</files>
  <action>
    If Task 1 left any trap red, adjust **only** scorer weights/thresholds/tie rules inside `lib/bridge-type-column-score.js` (or add a narrowly targeted unit case if a fixture was unrealistic). Do not weaken trap expected winners.

    1. Re-run pure suite until fully green.
    2. Confirm promote unit suite still green (scorer must not require promote changes):
       `node --test tests/bridge-category-promote.test.js`
    3. Optionally add 1–2 unit asserts documenting DEFAULTS and “never blend” if not already covered.
    4. Do not wire normalizer yet.
  </action>
  <verify>
    <automated>node --test tests/bridge-type-column-score.test.js tests/bridge-category-promote.test.js</automated>
  </verify>
  <done>Pure COL matrix green; promote tests unchanged and green; scorer remains pure with no process wire.</done>
</task>

</tasks>

<verification>
- `node --test tests/bridge-type-column-score.test.js` exits 0
- No normalizer force yet (engine COL wire tests may still RED — expected until Plan 03)
- Zero new dependencies in package.json
</verification>

<success_criteria>
1. `lib/bridge-type-column-score.js` exists with score/pick/resolve exports
2. All Wave 0 pure traps green (COL-01 single winner, COL-02 null, description aliases not auto-win)
3. Promote unit tests still green without code changes
4. Process path still unforced until Plan 03 (document in SUMMARY if engine COL tests still red)
</success_criteria>

<output>
After completion, create `.planning/phases/51-col-scoring-map-wire/51-02-SUMMARY.md`
</output>
