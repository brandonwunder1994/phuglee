---
phase: 51-col-scoring-map-wire
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/bridge-type-column-score.test.js
  - tests/bridge-engine.test.js
autonomous: true
requirements:
  - COL-01
  - COL-02
  - COL-04
must_haves:
  truths:
    - "Wave 0 pure trap matrix exists and fails until scorer is implemented"
    - "Wave 0 processUpload/normalizer wire expectations exist and fail until normalizer forces scorer Type"
    - "Trap sheets lock Status/Violation/Code/Ordinance Description losers vs Vio Cat / Issue Type / Category winners"
    - "No-candidacy case expects null Type header with no new discard reason"
    - "Classic Violation Type still expected to win"
  artifacts:
    - path: "tests/bridge-type-column-score.test.js"
      provides: "COL-01/02/04 pure scorer trap matrix (RED until Plan 02)"
      min_lines: 120
      contains: "resolveTypeColumnHeader"
    - path: "tests/bridge-engine.test.js"
      provides: "COL-01/04 process columnMap force + COL-02 no silent drop wire contracts (RED until Plan 03)"
      contains: "COL-01"
  key_links:
    - from: "tests/bridge-type-column-score.test.js"
      to: "lib/bridge-type-column-score.js"
      via: "require resolveTypeColumnHeader / scoreTypeColumns / pickTypeColumn"
      pattern: "bridge-type-column-score"
    - from: "tests/bridge-engine.test.js COL wire cases"
      to: "processUpload processingMeta.columnMap.violationIssueType"
      via: "trap CSV fixtures assert scorer winner not alias-first"
      pattern: "columnMap\\.violationIssueType"
---

<objective>
Write failing Wave 0 tests that lock Type-column scoring outcomes and process-path columnMap force — before any production scorer or normalizer wire exists.

Purpose: Prevent “hint-only scorer” (unit green while process still alias-first) and freeze trap winners as the implementation contract.
Output: RED `tests/bridge-type-column-score.test.js` + RED engine wire cases in `tests/bridge-engine.test.js`.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-col-scoring-map-wire/51-RESEARCH.md
@.planning/phases/51-col-scoring-map-wire/51-VALIDATION.md
@lib/bridge-intake-schema.js
@lib/bridge-category-promote.js
@lib/bridge-engine/normalizer.js
@lib/bridge-engine/index.js
@tests/bridge-engine.test.js
@tests/bridge-category-promote.test.js
@AGENTS.md

**Locked (non-negotiable):**
- Exactly one Type winner or null — never blend/concatenate
- Always force map from scorer (including null) — no alias-only fallback for Type on process path (COL-04)
- No silent drop solely for “no type column” (COL-02)
- Zero new npm packages
- Do NOT implement format store, confirm gate, short labels (Phases 52–53)
- Do not wipe `data/filter-lists/` or `data/bridge-brain/`

**Alias-first traps (must lock as expected winners):**

| Headers | Alias-first today | Scorer must pick |
|---------|-------------------|------------------|
| Status Description, Vio Cat, Description, Date | Status Description | Vio Cat |
| Violation Description, Issue Type, Notes | Violation Description | Issue Type |
| Code Description, Category, Comments | Code Description | Category |
| Ordinance Description, Vio Cat | Ordinance Description | Vio Cat |
| Category, Violation Description | Violation Description | Category |
| Violation Type, Description | Violation Type | Violation Type |
| Address + Notes + Open Date only | null / notes | null |

**Note:** `detectIntakeColumnMap` unit tests may keep asserting alias-first on the pure function — that is OK. Process path must not treat that Type as final.

<interfaces>
<!-- Contracts tests must require. Module does not exist yet — tests fail on require or on assertions until Plan 02. -->

Target API for lib/bridge-type-column-score.js (Plan 02 implements):
```js
function scoreTypeColumns(headers, sampleRows, opts = {}) {
  // returns ranked array: [{ header, score, reasons[], samples[] }, ...] desc by score
}
function pickTypeColumn(ranked, { minScore = 45, minMargin = 8 } = {}) {
  // single winner object or null — never blend
}
function resolveTypeColumnHeader(headers, sampleRows, opts) {
  // { header: string|null, score: number|null, ranked, source: 'scorer'|'unresolved' }
}
module.exports = {
  scoreTypeColumns,
  pickTypeColumn,
  resolveTypeColumnHeader,
  // optional pure helpers: headerFeatureScore, valueShapeScore, DEFAULTS
};
```

From lib/bridge-engine (wire tests — Plan 03 implements force):
```js
const { processUpload } = require('../lib/bridge-engine');
// result.processingMeta.columnMap.violationIssueType === scorer winner header or null/undefined empty
// Existing harness: CITY, BRIDGE_BRAIN_ROOT temp isolation, FIXTURES
```

From tests/bridge-engine.test.js existing MAP baseline (must stay green later):
```js
// 'processUpload promotes unmapped Vio Cat into type and FN/distressed labels (MAP-01/02, TEST-02)'
// Headers: Property Address,Vio Cat,Notes — scorer may pick Vio Cat OR leave null+promote; type cells still High Grass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED pure scorer trap matrix</name>
  <files>tests/bridge-type-column-score.test.js</files>
  <behavior>
    - Trap Status Description + Vio Cat + Description + Open Date → resolveTypeColumnHeader.header === 'Vio Cat'
    - Trap Violation Description + Issue Type + Notes → header === 'Issue Type'
    - Trap Code Description + Category + Comments → header === 'Category'
    - Trap Ordinance Description + Vio Cat → header === 'Vio Cat'
    - Trap Category + Violation Description (categorical Category values, narrative Violation Description) → header === 'Category'
    - Classic Violation Type + Description with short type values → header === 'Violation Type'
    - Address + Notes + Open Date only → header === null, source === 'unresolved'
    - pickTypeColumn never returns blended/concatenated multi-header string; at most one header
    - scoreTypeColumns ranks all non-empty headers; top of ranked for Status trap is Vio Cat (or pick returns Vio Cat)
    - Optional: empty samples with classic "Violation Type" header still candidacy-eligible via header features alone
  </behavior>
  <action>
    Create `tests/bridge-type-column-score.test.js` using Node built-in `node:test` + `node:assert/strict`.

    1. Require the not-yet-implemented module:
       `const { scoreTypeColumns, pickTypeColumn, resolveTypeColumnHeader } = require('../lib/bridge-type-column-score');`
       (If require throws MODULE_NOT_FOUND, that is acceptable RED — prefer writing asserts that will fail meaningfully once stub exists; do **not** create a stub module in this plan.)

    2. Build inline row fixtures (≥2 rows each) with realistic value shapes:
       - Category winners: short repeating labels (`High Grass`, `Trash`, `Fence Permit`)
       - Narrative losers: long sentences with timestamps
       - Status losers: `Open` / `Closed`
       - Date losers: `01/15/2024` style

    3. Assert each trap via `resolveTypeColumnHeader(headers, rows)` (or score+pick).
       Message strings should name the trap so failures are readable.

    4. Assert single-winner invariant: `pick` result is null or one object with `.header` string that is one of the input headers — never includes `,` concatenation of two headers.

    5. Do **not** implement `lib/bridge-type-column-score.js` here.
    6. Do **not** touch normalizer, intake-schema, promote, or user data dirs.
  </action>
  <verify>
    <automated>node --test tests/bridge-type-column-score.test.js</automated>
  </verify>
  <done>File exists with trap matrix; run exits non-zero (RED) because scorer module missing or assertions fail. No production scorer code committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED process wire contracts for forced Type map</name>
  <files>tests/bridge-engine.test.js</files>
  <behavior>
    - COL-01: processUpload CSV with Status Description + Vio Cat + Description + Open Date → processingMeta.columnMap.violationIssueType === 'Vio Cat'; kept row type is High Grass (not Open)
    - COL-04: same fixture proves alias-first toxic header is NOT left as columnMap.violationIssueType (not 'Status Description')
    - COL-01 alt: Violation Description + Issue Type → columnMap.violationIssueType === 'Issue Type'
    - COL-02: Address + Notes (weeds) + Open Date only → columnMap.violationIssueType is null/undefined/empty; process still ok; distressed weeds row kept (no new discard reason no_type_column)
    - Existing MAP TEST-02 Vio Cat promote test remains present (must not be deleted)
  </behavior>
  <action>
    Append new tests to `tests/bridge-engine.test.js` (do not rewrite the file). Reuse existing `processUpload`, `CITY`, brain-root isolation harness.

    1. **Trap force (COL-01/04)** — CSV:
       ```
       Property Address,Status Description,Vio Cat,Description,Open Date
       100 Main St,Open,High Grass,Weeds exceeding 12 inches as of 01/15/2024 10:30,01/15/2024
       200 Oak Ave,Closed,Trash,Junk in yard observed 02/01/2024 09:00,02/01/2024
       ```
       Assert:
       - `result.ok === true`
       - `result.processingMeta.columnMap.violationIssueType === 'Vio Cat'`
       - NOT equal to `'Status Description'`
       - Distressed/kept grass row `violationIssueType` includes `High Grass` (not `Open`)

    2. **Second trap** — headers `Property Address,Violation Description,Issue Type,Notes` with short Issue Type values and long Violation Description → `columnMap.violationIssueType === 'Issue Type'`.

    3. **No candidacy (COL-02)** — headers `Property Address,Notes,Open Date` only; Notes contain overgrown weeds language so row can stay distressed:
       - `columnMap.violationIssueType` is falsy (null/undefined/'')
       - `result.ok === true`
       - No discard reason string containing `no_type` / `no_type_column`
       - At least one kept distressed row still present (or FN if tagger classifies that way — either way, not total silent drop of the weeds address)

    4. Tag test names with `COL-01`, `COL-02`, `COL-04` for traceability.

    5. Do **not** modify `lib/bridge-engine/normalizer.js` in this plan — tests must RED against current alias-first behavior.
  </action>
  <verify>
    <automated>node --test tests/bridge-engine.test.js</automated>
  </verify>
  <done>New COL wire tests exist and fail under current alias-first normalizer; existing suite tests still run; MAP TEST-02 not removed.</done>
</task>

</tasks>

<verification>
- Pure score test file present and fails
- Engine COL wire tests present and fail (alias-first still maps Status Description)
- No production scorer/normalizer changes in this plan
- No new npm packages
</verification>

<success_criteria>
1. `tests/bridge-type-column-score.test.js` commits with trap matrix covering COL-01/02/04 pure outcomes
2. `tests/bridge-engine.test.js` has COL-01/02/04 process expectations that fail until Plan 03 wire
3. Both test runs are RED for the new cases (expected Wave 0 state)
4. Out of scope (confirm/format/labels) not introduced
</success_criteria>

<output>
After completion, create `.planning/phases/51-col-scoring-map-wire/51-01-SUMMARY.md`
</output>
