---
quick: 260710-k9c
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/bridge-stable-text.js
  - lib/bridge-review-groups.js
  - tests/bridge-stable-text.test.js
  - tests/bridge-review-groups.test.js
autonomous: true
requirements:
  - STACK-01
  - STACK-02
  - STACK-03
  - STACK-04
must_haves:
  truths:
    - "Leading municipal codes (HGW, HGW - notes, HGW*meta*) stack into one group"
    - "Combo codes (HGW/TD) stay separate from pure HGW"
    - "O/S normalizes to a single code token (not o+s split)"
    - "English free-text without leading codes (fence vs pool) stays separate"
    - "Phase 49 timestamp stacking still green"
    - "OTHER / STOP / TEST-style English tokens are not treated as municipal codes"
    - "isSingleton remains pure count === 1"
  artifacts:
    - path: "lib/bridge-stable-text.js"
      provides: "stripIncidentalNoise + extractLeadingTypeCodes + upgraded stable keys"
      exports: ["stripIncidentalTimestamps", "stripIncidentalNoise", "extractLeadingTypeCodes", "stableTypeKey", "stableDescriptionKey"]
    - path: "lib/bridge-review-groups.js"
      provides: "buildReviewGroups uses upgraded stable keys (no API break)"
    - path: "tests/bridge-stable-text.test.js"
      provides: "code extract + noise strip unit matrix"
    - path: "tests/bridge-review-groups.test.js"
      provides: "Irving-like HGW stack + combo split + regressions"
  key_links:
    - from: "stableTypeKey / stableDescriptionKey"
      to: "extractLeadingTypeCodes"
      via: "if codes → sorted join key; else noise strip + existing normalize"
    - from: "buildReviewGroups"
      to: "stableTypeKey / stableDescriptionKey"
      via: "unchanged call sites; behavior upgraded in helpers"
---

<objective>
Collapse confident same-category Train groups (municipal type codes + incidental noise) without fuzzy over-merge, so operators review stacked categories instead of hundreds of false singletons.

Purpose: Irving-class exports key `HGW` + free-text tails as N singletons today.  
Output: Pure helper upgrades + TDD matrix green + full suite + verify-live.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260710-k9c-confident-category-stacking-for-train-re/260710-k9c-CONTEXT.md
@.planning/quick/260710-k9c-confident-category-stacking-for-train-re/260710-k9c-RESEARCH.md
@lib/bridge-stable-text.js
@lib/bridge-review-groups.js
@tests/bridge-stable-text.test.js
@tests/bridge-review-groups.test.js
@AGENTS.md
</context>

---

## Tasks

### Task 1: RED — unit tests for confident keys + grouping

**Files:**
- `tests/bridge-stable-text.test.js`
- `tests/bridge-review-groups.test.js`

**Action:**
Add failing tests that lock:

1. **Noise strip:** case IDs (`PS-2026-04-3104`), `*CALL BACK*`, `X2` multipliers removed; timestamps still stripped; ordinance-like `12-3457` kept.
2. **Leading codes:** `HGW`, `HGW - OVERGROWN GRASS`, `HGW\n*CALL BACK*` → same key; `HGW/TD` / `HGW, TD` → `hgw+td` (sorted); pure `HGW` ≠ combo.
3. **O/S:** `O/S - washer` → single `os` (not `o+s`).
4. **Denylist:** `OTHER - gas meter…` does **not** extract as code `other` (falls through free-text path; different OTHER tails may remain separate after full normalize).
5. **Group build:** 5 Irving-like HGW variants (typed) → 1 group count 5; HGW + HGW/TD → 2 groups; empty-type fence vs pool still 2; Phase 49 timestamp empty-type still 1 group.

**Verify:** `node --test tests/bridge-stable-text.test.js tests/bridge-review-groups.test.js` shows new tests RED.

**Done:** New assertions committed as failing tests only (no product code yet if pure TDD; product may already partially pass Phase 49 cases).

### Task 2: GREEN — implement noise strip + code extract + wire stable keys

**Files:**
- `lib/bridge-stable-text.js`
- `lib/bridge-review-groups.js` (only if call-site changes needed; prefer helpers-only)

**Action:**
1. Add `stripIncidentalNoise` (timestamps + case IDs + `*…*` + `xN` multipliers + separator cleanup). Keep `stripIncidentalTimestamps` as thin wrapper or alias so existing exports/tests hold.
2. Add `export extractLeadingTypeCodes(text)` pure helper with O/S token rule + denylist.
3. Upgrade `stableTypeKey` / `stableDescriptionKey`:
   - if leading codes → key = sorted unique codes joined by `+` (lowercase)
   - for `stableTypeKey`, empty still → `__unknown__`
   - for `stableDescriptionKey`, empty still → `''`
   - else noise-strip then existing normalize path
4. Display labels in `buildReviewGroups` already use stripped text — prefer noise-stripped first-seen label when setting `violationTypeLabel` if easy without behavior regression.

**Verify:** Focused tests GREEN.

**Done:** Helpers exported; all new + Phase 49 grouping tests pass.

### Task 3: Suite + live gate + summary

**Files:**
- none required (docs summary only)

**Action:**
1. Run full `npm test` — fix any accidental regressions (gold fixtures, shortLabel, independence).
2. `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-live.ps1` exit 0.
3. Write `${quick_dir}/260710-k9c-SUMMARY.md`.
4. Remove or keep `scripts/_irving-group-sim.js` as research-only (do not ship as product path); prefer leave untracked or delete if not needed.

**Verify:** suite green + verify-live 200.

**Done:** SUMMARY written; ready to push/deploy.

---

## Requirement map

| ID | Statement |
|----|-----------|
| STACK-01 | Leading municipal codes with free-text tails stack when codes match |
| STACK-02 | Combo code sets remain distinct groups |
| STACK-03 | No fuzzy free-text merge; fence/pool stay separate; denylist English pseudo-codes |
| STACK-04 | Phase 49 timestamp behavior + isSingleton formula preserved |

## Out of scope

- Fuzzy clustering
- City-specific dictionaries
- Train UI redesign
- Distress tagger keep/kill changes
- Wiping lists/brain data
